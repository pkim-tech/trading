# Research Log

Permanent, append-only record of experiments run against this system — a lab notebook,
not a to-do list. Distinct from `docs/backlog_cache.md`/`docs/deep_backlog.md` (open
action items) and `docs/conversation_summary.md` (session narrative): an entry here
exists whenever a real hypothesis was tested, whether or not it produced code changes or
a new backlog item. Oldest first, new entries appended at the end. Committed (not
gitignored, not capped) — old findings should stay greppable indefinitely.

Infrastructure findings that change how future checks should be run (e.g. a new
watchlist-candidate-checklist check) get logged here too, but the actual reusable
procedure/script documentation lives in `docs/watchlist_candidate_checklist.md` (or the
relevant script's docstring) — don't duplicate it here, just note that it changed and
why.

Each entry:
```
## YYYY-MM-DD — <ticker/topic> — <one-line finding>
**Hypothesis/question:**
**Method:**
**Result:**
**Verdict:** Confirmed / Refuted / Inconclusive / Resolved-no-action
**Follow-up:** backlog item name, or "none"
```

---

## 2026-07-14 — KORU/same-day re-entry — trailing-buy re-entry timing after a same-day exit needs no fix
**Hypothesis/question:** If a same-day re-entry trigger hits (ticker sold, then dislocates
again same day), does the live trailing-buy order need to be placed relative to the 9:30
open or the 10:30 normal bar time?
**Method:** Read `_simulate_trail_both`'s actual loop (`backtester.py:538-643`) to check
whether new-signal detection has any calendar-day reset or first-entry-of-day special-casing.
**Result:** New-signal detection is already restricted to the same two configured hours
every day (`target_h0`/`target_h1`, matching the two live Slack windows), with no
day-boundary logic at all. A same-day re-entry is scanned for using the exact same two
windows as any other entry, walk-forward — nothing day-of special about it. The original
"10:30" framing in the question was itself a misunderstanding: 10:30 was never a
signal-check hour, backtest or live.
**Verdict:** Resolved-no-action — no bug, no fix needed.
**Follow-up:** none.

---

## 2026-07-15 — SOXL/KORU — Phase 3 (full parameter mesh) adds no value over Phase 1/2/2.5
**Hypothesis/question:** Does the v4 sweep's Phase 3 (full mesh refinement) ever actually
improve on the best node Phase 1 (coarse) + Phase 2 (island search) + Phase 2.5
(cliff-box) already found?
**Method:** Added `phase`/`generation`-level tracking and a `--max-phase` CLI cap
(`run_optimization_sweep.py`), then compared each phase's own best
`MIN(possible,pessimistic,certain)` alpha node across all 30 tagged SOXL+KORU SL-sweep
campaigns run so far.
**Result:** Phase 3 won 0/30 campaigns — Phase 1 or Phase 2 always held the best robust-alpha
node; Phase 2.5 won a few. Separately confirmed island/cliff-safety selection (Checkpoint 2)
already only depends on Phase1+2+2.5 data, so Phase 3 was never part of that calculation
anyway.
**Verdict:** Confirmed (Phase 3 is dead weight for this sweep's actual selection criterion).
**Follow-up:** `--max-phase {1,2,2.5,3}` added (default 3, unchanged pipeline behavior) so
future runs can skip it for compute/disk savings; `generation` column added to similarly
test whether island search's extra generational passes earn their cost (not yet analyzed).

---

## 2026-07-17 — watchlist-wide — same-day buy→sell block explored and deliberately not built
**Hypothesis/question:** Should a same-day buy-then-sell round trip on the same security be
blocked, to avoid being flagged/classified as a day trader (a compliance-optics concern, not
a broker-side rule)?
**Method:** (1) Confirmed the live account is a limited margin account (no PDT-style
broker-side count limit, no cash-account good-faith-violation risk). (2) Live FINRA lookup:
the classic PDT $25k-minimum/4-trades-in-5-days rule was eliminated entirely effective
2026-06-04 (Regulatory Notice 26-10), replaced by an intraday-margin-deficit framework that
doesn't restrict day trading by count at all. (3) Quantified the real cost of avoiding
same-day round trips anyway, via a new gap-aware deferred-sell simulator
(`scripts/sim_delayed_sell.py`/`export_trades.simulate_trail_both_deferred_sell`) on GDXD's
real production node.
**Result:** No broker-side or (post-2026-06-04) regulatory reason to block same-day round
trips remains. Employer-side: no official written rule, only an unstated soft preference.
Real cost of blocking anyway is high — GDXD's SL=1% node retains only ~47% of its edge
(+22,402% → +10,473%) if forced to defer same-day exits to the next day (first-pass number
of +25,793% was a bug: deferred stop-loss fills were pinned to the nominal `stop_price`
regardless of overnight gap risk; fixed to charge the worse of `stop_price` or the resolving
day's `Open`).
**Verdict:** Resolved-no-action — proceed without a same-day buy→sell block. Worst case if
compliance ever objects is being told to stop, a reversible outcome not worth giving up real
edge for a rule that may not even apply. `schwab_safety.py`'s existing `same_day_block`
(blocks same-day *re-buy* after an exit — the original cash-account-era behavior, a
different direction than this question) was left untouched, still enforced live.
**Follow-up:** none on this question. Revisit only if compliance actually raises it.

---

## 2026-07-18 — watchlist candidates — checklist checks 4/9/10 run for 19 tickers
**Hypothesis/question:** Of the 19 tickers screened by the fast checklist pass (1/2/3/6/7/8)
this session and last, which ones show real win-rate decay (check 4) or real exposure to
the live, still-enforced `schwab_safety.py` same-day-block (checks 9/10) that the fast
screen doesn't catch?
**Method:** New `scripts/checklist_deep_checks.py` — reuses `run_backtest_v110`
(`same_day_block=True` vs `False`), `_summarize_trades`, and `compute_bh_returns` directly
(no reimplemented kernel/alpha math). For each of AGQ/DPST/EDC/GDXD/GDXU/HIBL/KORU/LABU/
NUGT/SOXL/TQQQ/YANG/UDOW/USD/UVIX/ZSL/NAIL/DUST/RETL: pulled the real v4 SL=1%/open_check
winning node, ran it blocked and unblocked, computed robust alpha
(`MIN(possible,pessimistic,certain)`) for each, and split both trade lists chronologically
70/30 to check stability of the retention ratio over time.
**Result:** Check 4: GDXU (25.6%→5.6% win rate) and RETL (15.7%→7.7%) show real late-window
decay; everyone else holds flat or improves late. Check 9: same-day-block retention of
robust alpha ranges from severe (ZSL 3.5%, YANG 3.7%, GDXD 6.9%, UDOW 14.7%) to fine-to-improving
(DPST 111.9%, UVIX 162.7%). Check 10: several tickers' late-window retention ratio goes
negative or blows up (TQQQ, YANG, UDOW, USD, UVIX, ZSL) — traced to a thin/near-zero late-window
baseline robust alpha making the ratio unstable, a methodology artifact of small late samples
rather than a real reversal; treat those specific late-window numbers as inconclusive, not as
"gets worse." Full numbers in `logs/checklist_deep_checks.csv`.
**Verdict:** Confirmed real flags for GDXU/RETL (check 4) and ZSL/YANG/GDXD/UDOW (check 9);
inconclusive for check 10's negative-ratio cases specifically.
**Follow-up:** feeds directly into the open "promote other 11 watchlist tickers' v4 nodes"
backlog decision — none of this is a hard blocker (same-day-block isn't being newly
enforced beyond what already exists) but it's real information about how concentrated each
node's edge is in trades a live constraint could someday clip.

---

## 2026-07-18 — DPST/SOXL — chaos-monkey "improves under misses" divergence explained
**Hypothesis/question:** Session 18's chaos-monkey run found DPST/SOXL's mean compounded
return holds up or improves as signal miss rate increases, unlike the rest of the
watchlist (which decays, e.g. KORU -31% at 20% miss). Is this a real, understood
mechanism, or noise/small-sample artifact?
**Method:** New `scripts/investigate_chaos_divergence.py` — reuses
`export_trades.simulate_trail_both_chaos` directly, decomposing the combined miss_rate
into entry-only and exit-only runs (2000 trials each, `drop` mode, 20% miss) for
DPST/SOXL/KORU/HIBL (KORU/HIBL as tickers that decay normally, for contrast). Also pulled
each ticker's real live node params and baseline per-trade return distribution
(win rate, mean/median return per trade).
**Result:** Two separate effects, not one: (1) **Exit-side misses are structurally
benign-to-beneficial for every ticker tested** (ratio 0.98–1.22, never a real drag) — the
SL check (`export_trades.py:341`, `if low <= stop_price`) is re-evaluated fresh every bar
with no memory of a prior touch, so a missed check on a spike-down bar that recovers by
the next bar simply never re-fires; the position survives a stop-out the
perfect-adherence baseline would have taken. This is a property of the current
kernel/live-monitoring design, not sampling luck. (2) **The real ticker-specific
divergence is on the entry side**: KORU (ratio 0.70) and HIBL (0.72) lose heavily to
missed entries because their winning trades are infrequent and high-quality (KORU: 68.8%
win rate, 11.0% median per-trade return — missing one is a real lost opportunity), while
DPST (1.06, improves) and SOXL (0.95) have much thinner, closer-to-coinflip edge per trade
(DPST: 52.9% win rate, 0.9% median per-trade return) — missing some of DPST's many
marginal-quality entries costs little and occasionally dodges a loser, netting positive.
SOXL's exit-side ratio (1.22) is the largest of the four and not fully explained —
plausibly tied to its already-known high intrabar volatility relative to `trail_sell_pct`
(fill-drift ratio 3.47, an earlier checklist finding) but not confirmed.
**Verdict:** Confirmed — a real, understood structural mechanism (exit-side SL-check
statelessness) plus a real per-ticker entry-quality effect, not noise or a small-sample
artifact.
**Follow-up:** SOXL's unusually large exit-side ratio is still open, worth a closer look
if SOXL comes up again. No code changes needed — this explains existing behavior, doesn't
require a fix (the SL-check statelessness matches live behavior already, not a
backtest/live divergence).

---

## 2026-07-18 — SOXL/AGQ/KORU/watchlist-wide — v3-vs-v4 trade overlap, watchlist-wide max drawdown, live selloff comparison
**Hypothesis/question:** Prompted by a real, live semiconductor selloff hitting SOXL/KORU:
(1) is v4 (SL=1%, open_check) really "the same trades as v3 with a tighter stop," or a
different strategy? (2) What's the worst-case historical drawdown for every watchlist
ticker's v4 node, and does any of it come close to a stated ~50% risk tolerance? (3) Right
now, mid-selloff, how does v4's live drawdown compare to what the actual live v3.x
`watch_list` params would be showing for the same tickers?
**Method:** New `scripts/v4_max_drawdown.py` (true equity-curve peak-to-trough max
drawdown, not just consecutive-loss-streak length) run across all 12 watchlist tickers'
v4 nodes. Separately compared each v4 node's trades against its real live v3.x
`watch_list` node's trades (same underlying kernel, different params) by matching entry
times within ±1 day. Also computed each node's *current* (as-of-now) drawdown from its
own running peak, for both v4 and the real live v3.x params, using latest cached data
mid-selloff.
**Result:** (1) v3/v4 are meaningfully different strategies, not the same trades with a
different stop — v4's `trail_buy_pct=1%` catches 3-5x more (smaller, more frequent)
entries than v3's wider thresholds; only 10-33% of v4's trades correspond to a real v3
trade around the same time (SOXL 33%, AGQ 22%, KORU 10%). (2) Every ticker's v4 worst-case
historical drawdown falls between -5.9% (YANG) and -23.8% (SOXL) — all well under a stated
~50% tolerance (which itself would take ~69 consecutive -1% losses to reach; SOXL's actual
worst streak was 27, i.e. -23.8%). SOXL/DPST/HIBL share the same real Aug-Oct 2023
drawdown window — a shared macro event across tickers, not independent risk. (3) Mid the
real, live 2026-07 semiconductor selloff: v4's current drawdown is minor everywhere (SOXL
-3.0%, KORU -2.0%, worst is GDXU -6.8%) while the real live v3.x params are taking much
larger current hits on the same tickers right now (SOXL -33.8%, KORU -38.6%, GDXU -55.7% —
GDXU's live v3.x node is at its all-time-worst drawdown point at this exact moment).
**Verdict:** Confirmed — v4's tight stop meaningfully changes real-time risk exposure, not
just backtest cosmetics; live-validated during an actual adverse event, not just
historical replay. v3-vs-v4 trade-overlap finding is a real caveat for the promotion
decision: v4 isn't a strict safety upgrade to the same strategy, it's a different
(busier, lower per-trade win-rate) strategy that also happens to have a much tighter stop.
**Follow-up:** Added checks 11 (max drawdown) and 12 (current-drawdown-vs-worst-case
calibration) to `docs/watchlist_candidate_checklist.md`. Feeds directly into the still-open
v4-promotion backlog decision.

---

## 2026-07-18 — 19 tickers (watchlist + screened candidates) — train/test split + walk-forward out-of-sample validation, resolving the "Train/test split" deferred backlog item
**Hypothesis/question:** Every v4 node on file (island search, cliff-safety, robust-alpha
ranking) is selected against the full historical range with no held-out data — is any of
it overfit to the in-sample period? This was the stated real blocker on the
watchlist-wide v4-promotion decision (see backlog).
**Method:** Two scripts built, both reusing the existing backtest kernel/trade-list rather
than reimplementing anything, and both avoiding a costly re-sweep by exploiting that the
strategy's SMA/std indicators are already backward-looking (no lookahead) — running the
backtest once over full history and slicing the resulting trade list chronologically is
numerically equivalent to running separate backtests on split date ranges:
1. `scripts/train_test_split_check.py` — single 70/30 chronological split per ticker
   (per-ticker 70th-percentile entry-time cutoff), robust-alpha (`MIN` of
   possible/pessimistic/certain) computed separately for each half, each against SPY's
   *own* return over that half's specific date range (`period_spy_bh`) — not one blended
   full-history SPY number, which was a real bug caught and fixed mid-session (the first
   version used `compute_bh_returns()`'s single full-history SPY return for both halves,
   which understated test-period alpha and even showed spurious negative retention for
   RETL/UVIX before the fix).
2. `scripts/walk_forward_check.py` — generalizes the single split into N=5 equal
   chronological calendar windows, each independently evaluated (not cumulative
   compounding across windows) against its own SPY benchmark. Built specifically because
   the single-split method showed a real interpretability weakness on KORU (see below).
Run across all 19 tickers cleared by the 2026-07-18 checklist screen (12 live watchlist +
UDOW/USD/UVIX/ZSL/NAIL/DUST/RETL).
**Result:** Single 70/30 split: median out-of-sample retention well under 50% (e.g. AGQ
28.5%, HIBL 23.1%, YANG 7.7%, TQQQ 4.9%), with only LABU/DPST/ZSL/DUST/KORU holding up
strongly. GDXD and SOXL landed at a near-exact tie on absolute out-of-sample numbers
(test robust-alpha 818.9 vs 815.1, test compounded 832.1% vs 844.9%) despite very
different retention ratios (43.0% vs 29.5%) — the ratio difference was mostly an artifact
of SOXL's training number starting higher, not a real difference in OOS quality. KORU's
70/30 result (test retention 518%, an apparent *improvement*) was traced to a single
outlier trade (+91.7% over a ~28-calendar-day hold, legitimately within the node's own
`max_hold_hours=126` trading-hour cap, not a bug) that alone accounted for roughly half
the entire test-period compounded return — a red flag that a single split's result can be
dominated by one trade and isn't trustworthy in isolation, in either direction.
Walk-forward (5 folds) gave a materially more useful picture: **14/19 tickers had zero
negative-alpha folds across all 5 windows** (AGQ, DUST, EDC, GDXD, GDXU, HIBL, KORU, LABU,
NAIL, SOXL, TQQQ, USD, YANG, ZSL); 5 had exactly one negative fold (DPST, NUGT, RETL,
UDOW, UVIX). Critically, KORU's walk-forward folds showed positive, real alpha (51-238%)
across all four windows *before* the standout recent-selloff window (900%) — the edge
isn't purely a fluke confined to one lucky test period, contrary to what the single split
alone suggested. In one fold (fold 3, 2024-10 to 2025-05) SPY itself returned -1.3% while
KORU/GDXD/SOXL all still posted large positive absolute returns (50-430%), real evidence
these aren't just riding a rising SPY tide.
**Verdict:** Confirmed with a real caveat — a single 70/30 split is not reliable enough to
trust alone (demonstrated concretely by KORU's outlier-driven, direction-ambiguous
result); the walk-forward version is the more trustworthy check going forward. Most
watchlist tickers show real, broadly consistent out-of-sample edge (14/19 zero-negative-
fold), though absolute out-of-sample magnitude is much smaller than the in-sample
headline numbers suggest across the board — none of this should be read as "the in-sample
numbers are right," only as "the edge is real, just smaller and noisier than in-sample
suggested."
**Follow-up:** Added as checklist check 13 (`docs/watchlist_candidate_checklist.md`).
DPST/NUGT/RETL/UDOW/UVIX's single negative fold each needs a closer look (which window,
how large) before treating them as validated. Directly informs, but does not fully
resolve, the still-open watchlist-wide v4-promotion decision (see backlog) — this
resolves the "is any of this in-sample-only" concern with real evidence, but the
promotion decision itself is still open.

## 2026-07-20 — Immediate-entry (TrailingExit) vs trailing-buy (TrailingBoth) full 18-ticker v5 comparison
**Hypothesis:** does `trail_buy_pct` (waiting for a bounce off the running low before entering) actually earn its cost, given how often overnight gaps blow past the trigger — and separately, is it worth dropping since trailing-buy orders are operationally hard to execute manually right now (the reason all 18 tickers are paused in `research` mode)?
**Method:** `scripts/run_sweep_queue.sh` (both strategies, `entry_timing=open_check`, `fixed_sl ∈ {1,2,3}`, all 18 watchlist tickers) run to completion on the corrected (gap-through-trigger-fixed) `v5` kernel — 230 `backtest_cache` phase rows confirmed present across all 36 (ticker × strategy) combos × up to 3 SLs. All numbers below are `ROBUST_ALPHA_SQL` (`MIN(alpha_vs_spy, pessimistic, certain)`, alpha vs SPY, not raw strategy return) — the same convention the live sweep engine's own Checkpoint2 cliff check uses.
**First pass was wrong**: initially compared raw best-alpha per ticker per strategy without checking cliff-safety (`worst_neighbor` in the real Phase2.5 cliff-box log), and reported TB winning 11/18 vs TE 7/18. The user caught this by pointing at the actual `sweep_queue_v5_20260720_092857.log` cliff-check lines, which show many of those "TB wins" nodes are CLIFF (unstable neighbors), not legitimate candidates.
**Corrected result (filtered to `worst_neighbor >= 0`, i.e. cliff-safe only, per ticker/strategy/fixed_sl combo from the real log)**:
- TrailingExit has a cliff-safe node on 12/18 tickers; TrailingBoth on only 7/18.
- On 5 tickers — DPST, KORU, LABU, TQQQ, YANG — TrailingBoth has **no cliff-safe node at all** across `fixed_sl ∈ {1,2,3}`, while TrailingExit does. No ticker shows the reverse (TB safe, TE not).
- Where both have a safe candidate: TB wins GDXU (267.2 vs 245.8), HIBL (466.2 vs 440.8), SOXL (1212.1 vs 947.0), USD (198.9 vs 195.6); TE wins AGQ (1114.9 vs 923.7), NUGT (384.8 vs 216.8), UDOW (300.4 vs 214.0).
- Neither strategy has a safe node at all for DUST, GDXD, NAIL, RETL, UVIX, ZSL (in this SL range).
**Verdict:** Mixed, corrected a second time — an earlier draft of this entry claimed TE "is never worse where both are viable," which is wrong: TB beats TE on 4 of the 7 tickers where both have a safe node (GDXU, HIBL, SOXL, USD). The only one-sided part of the result is *existence*: TB has zero safe candidates on 5 tickers where TE has one, never the reverse. So TE is the safer default where you need *a* viable node at all, but where both are viable, it's a real per-ticker split (TB wins 4, TE wins 3), not a blanket TE advantage. Combined with the real motivation (trailing-buy is hard to execute manually right now), TE is still the more practical default, but not because it backtests strictly better.
**Follow-up:** This used the log's per-combo Phase2.5 cliff-box output directly, not a fresh SQL cliff query against `backtest_cache` — worth a proper `CLIFF_RADIUS`-neighborhood SQL cliff check across the full grid (not just the best-per-combo candidate already promoted to Phase2.5) before treating this as final. No node changes made; this is a research finding only.

## 2026-07-20 — Last-window market-on-close vs trailing-buy comparison
**Hypothesis:** for a signal firing in the last daily signal window (the 14:30-labeled bar, checked in the 15:25-15:40 window — no bars left same-day before the overnight gap), does waiting for a trailing-buy bounce (TB) actually earn its cost, or would a plain market-on-close (MOC) entry at that signal bar's own Close do as well or better?
**Method:** new `scripts/sim_close_vs_trail_buy.py` + `export_trades.collect_last_window_comparisons`/`simulate_trail_both_signal_tracked`/`_simulate_exit_from_entry` — pure-Python mirrors of `backtester._simulate_trail_both`, extended to track `signal_i` and share the SL/TP/trailing/TIME exit state machine between the real TB fill and an MOC counterfactual entered at the same signal bar's Close. Run against the real Live v5 watchlist (id=65)'s 4 `TrailingBothZScoreBreakout` tickers (GDXU, HIBL, SOXL, USD), all 3 fill resolutions (possible/pessimistic/certain).
**Result:** TB beat MOC on every ticker and every resolution, not close — compounded return e.g. SOXL 1001-9603% (TB) vs 168-465% (MOC), GDXU 434% vs 81-118%, HIBL 339-430% vs 2.5-83%, USD 122-132% vs 25-42%. MOC actually "wins" more individual trades head-to-head on 3 of 4 tickers (higher count where moc_ret > tb_ret), but TB's rarer wins are much larger (fat right tail), so it dominates on mean/compounded return regardless. Hand-verified 2 SOXL trades (a simple 1-bar SL loss, and the largest TWIN winner, held to the 70h time limit) entry-to-exit against real OHLC bars in `SOXL_1h.csv` — every fill price traced exactly, to full float precision, to a real Open/High/Low/Close, confirming the result isn't a computation artifact.
**Verdict:** Resolved — MOC does not beat TB for last-window signals on the current watchlist; no ticker came close enough to justify an 18-ticker backfill or a live entry-timing change. Waiting for the trailing-buy bounce is still worth its cost even on the operationally-hardest signal window.
**Follow-up:** None planned, treated as closed. `scripts/sim_close_vs_trail_buy.py` is reusable if the question resurfaces on a different watchlist/ticker set.

## 2026-07-20 (cont.) — Watchlist 65 candidate testing: chaos-monkey, Stage 5 compliance gap found+fixed, watchlists.id gap root-caused+fixed
**Chaos-monkey execution-adherence, repointed at watchlist 65**: `sim_chaos_monkey.py` previously only supported `TrailingBothZScoreBreakout` and `entry_timing='close'` — extended with a new `export_trades.simulate_trail_exit_chaos` mirror (for the 6 `TrailingExitZScoreBreakout` tickers) and `open_check` support in both chaos mirrors (every node in watchlist 65 runs `open_check` live). Baseline switched to the real production kernel (`run_backtest_v110`/`run_backtest_v18`) instead of a pure-Python mirror. Verified all 10 nodes reproduce the real kernel exactly at 0% miss rate. Result: no node collapses even at a 20%-per-check miss rate (`drop` mode) — retention ranges 57.8% (HIBL) to 90.1% (DPST) of baseline compounded return; USD's thin sample (12 trades, previously flagged) held up fine (87.2%). Full detail: `output/chaos_monkey_summary.csv`.
**Stage 5 compliance recheck found a real, unfixed gap**: `strategies.py`'s `check_exit` (used by both real live `signals_compute.check_sell_condition` and `paper_trading.check_paper_sells`) never received the 2026-07-20 exit-side gap-through-trigger fix that's in `backtester.py`'s kernels — it only checked `low <= stop_price`/`low <= trail_stop`, no Open-first check, and the call site (`active_signals.py`) never even extracted the bar's Open to pass through. Practical effect: a live position gapping through its SL/trailing-stop overnight would report the stale theoretical price in the Slack SELL alert and in paper-trading's simulated exit, not the real gapped fill — same bug class as the kernel fix, never propagated to live-facing code.
**Fixed**: threaded a new `open_price` param through `check_sell_condition` from both call sites (`active_signals.py`, `paper_trading.py`, both now extract `bar['Open']` at bar-close); `strategies.py::check_exit` for `TrailingBothZScoreBreakout`, `TrailingExitZScoreBreakout`, and `LimitOrderTrailingExit` (identical duplicated block, fixed for consistency even though not currently live) now checks Open before falling to Low, mirroring the kernel exactly.
**Verified**: full `pytest tests/` (98 passed, unchanged — no existing test exercised the missing param, which is exactly the gap that was found). 3 synthetic cases (SL gap, trailing-stop gap, no-gap sanity check) confirmed correct behavior. Full bar-by-bar parity check against real historical data: forward-simulated `check_exit` from every real entry against the corrected kernel's actual trades — 151/151 SOXL (TB) trades matched exactly (exit bar, price, reason); 128/129 AGQ (TE) trades matched exactly, the 1 "mismatch" being an artifact of the test harness (a still-open position at the very end of the dataset, not a real divergence).
**Separately found and fixed a real `watchlists.id` bug while investigating the 57->65 id jump**: `signals_db.create_watchlist` used `INSERT OR IGNORE` keyed on the UNIQUE `name` column — reproduced directly in an isolated test DB that SQLite silently burns an AUTOINCREMENT id on a name conflict even though no row is written (5 duplicate-name attempts advanced the sequence by 5 with zero new rows). This explains the unexplained 57->65 gap (6 ids burned, zero trace in `watch_list_audit` since no row/error was ever produced). **Fixed**: check for an existing name first, only INSERT when genuinely new — verified duplicate calls now return the existing id with zero ids consumed, while genuinely new names still increment normally (1-by-1, no gaps).
**Verdict**: watchlist 65's 10 nodes are now fully candidate-tested (execution-adherence + live/paper compliance) with no blockers found; the compliance gap found along the way was real and is now fixed, verified against real trade history. `create_watchlist` bug fixed as a bonus finding, unrelated to the node params themselves.

## 2026-07-20 (cont.) — Full 13-check candidate checklist run on watchlist 65
**Method**: `scripts/checklist_v65.py` (new) runs checks 1/4/8/9/10/11/12/13 against all 10 watchlist-65 nodes' real live params (not a re-derived "best" node); checks 2/3/6 via existing scripts (`verify_trailing_buy/sell_resolution.py`, `check_stock_splits.py`), which already default to the active watchlist/all cached tickers.
**Findings**:
- Check 9 (same-day-block sensitivity): GDXU/SOXL showed severe alpha loss under the block (12.2%/5.2% retention) — **reinterpreted same session, not a real concern**: the guardrail (`signals_db.closed_today`, docstring: "IRA/SEP cash accounts can't reuse that capital until T+1 settlement") only applies to cash accounts; user confirmed watchlist 65 tickers will trade in limited margin accounts, which don't have this settlement restriction. GDXU/SOXL's real relevant number is their unblocked robust alpha (267.0%/1209.0%), not the same-day-blocked figure. TE strategy couldn't be checked at all here — `_simulate_trail`'s kernel has no `same_day_block` parameter.
- Check 13 (walk-forward, 5-fold): only UDOW/USD have zero negative folds; DPST/KORU/NUGT/SOXL/YANG have 2/5 negative — a real step down from the earlier v4 cohort's 14/19 clean record (2026-07-18).
- Check 11 (max drawdown): DPST -65.5% exceeds the previously-stated ~50% risk tolerance; YANG/SOXL/KORU (-54.3%/-47.9%/-46.0%) sit at the line. Deeper than v4's cohort (-5.9% to -23.8%), expected given v5's wider fixed_sl (2-3% vs v4's 1%).
- Check 12 (current drawdown): AGQ/KORU/SOXL sitting in a real drawdown right now (-21.1%/-34.7%/-33.6%), not at peak.
- Check 4: GDXU and NUGT show real win-rate decay early-to-late (22.7%→10.5%, 34.8%→20.0%).
- Check 6 (splits): 5 real splits found in-range (YANG, KORU×2, UDOW, USD) — spot-checked all against raw price series (±3-day max single-bar move, all under 10%), no unadjusted-jump artifacts, cache is correctly adjusted.
- Check 7 (fill-optimism): 90-94% of TB entries are "certain" fills, not dependent on the Low/High-ordering guess. The compounded-return-delta side of this check is stale — `simulate_trail_both_ohlc_aware` predates the 2026-07-19/20 gap-through-trigger fixes, so a direct comparison against the current kernel's return would be misleading; not reported.
- Check 8: no pure trade-count fluke on any node — all stay solidly profitable even excluding the single best trade.
- Check 2/3 (5-min resolution): SOXL shows the most drift (mean -0.53% entry-side, ratio 3.9 exit-side, consistent with its historical flag); check 3's sample is thin (3 exits total across the watchlist).
**Verdict**: no node is blocked from anything today (all still `mode='research'`). Real, structural findings worth remembering: DPST's drawdown exceeds stated risk tolerance; SOXL/KORU/YANG close behind; walk-forward consistency is noticeably weaker than v4's cohort across half the tickers. Full detail: `output/checklist_v65_summary.csv`.
**Follow-up**: `schwab_safety.py`'s `same_day_block` needs account-type awareness before it's ever wired into the live loop — logged to `docs/backlog_cache.md`.

## 2026-07-21 — Trailing-buy budget-adherence design (Part 3): four empirical checks that each changed the design mid-session
**Hypothesis/question:** designing the live order-placement infra for the "trailing-buy needs re-sizing"/"gap-through-trigger" backlog items (`docs/backlog_cache.md`) — several sub-questions came up that had real, checkable answers instead of being assumed.
**Method/Result, one per question:**
- *Does a trailing-buy order even need to rest overnight, or could placement be deferred to a single next-morning check?* Checked real signal-to-fill delay (`simulate_trail_both_signal_tracked`, `possible` resolution) across watchlist-65's 4 `TrailingBothZScoreBreakout` tickers: same-day fill rate is GDXU 40.6%, HIBL 82.0%, SOXL 53.0%, USD 100.0% — same-day fills are the majority, not the exception, so any design has to handle same-day sizing drift, not just overnight.
- *Is a flat sizing-pad percentage enough to bound overnight gap-through risk?* Computed the real overnight upward-gap distribution (close→next-open) per ticker against each node's real `trail_buy_pct`: p99 gaps run 8-19%, max observed 20.6% (GDXU). Even a +5-point pad still leaves 4-15% of gap days exceeding it — no finite fixed pad closes the risk, only bounds it probabilistically. Conclusion: overnight handling needs an active pre-open cancel/resize checkpoint, not a bigger static pad.
- *How stale is the pre-market quote available at a 9:15-9:29 pre-open check?* Pulled real 1-minute yfinance bars (small sample, yfinance caps 1m history at ~30 days): last pre-market print averages 16 minutes before the 9:30 open for GDXU/SOXL/USD, but **41 minutes for HIBL**, whose thin pre-market feed produced up to 13.4% drift to the real open in-sample. Led to checking Schwab's own quote endpoint (`Client.get_quote`) directly — confirmed live (`realtime: true`, `extended.lastPrice`/`quoteTime` fields populated) via a real call, materially fresher than yfinance's feed. Adopted as primary price source for the gap-check, with yfinance kept as fallback only.
- *What's the actual regulatory consequence of a limited-margin-account fill exceeding its budgeted notional?* Initial read (good-faith-violation/90-day-restriction rules) was the wrong mechanism — those govern selling before settlement, not a single oversized buy, and a true limited-margin (non-debit) account likely rejects/partially-fills an order it can't cover rather than completing it and creating a violation. Found the actually-relevant, very recent mechanism instead: FINRA Regulatory Notice 26-10 (Rule 4210 amendments, effective 2026-06-04, replacing the PDT rule) — an "intraday margin deficit" must be cured by close of business on the 5th business day before repeated-failure risk accrues, with a 90-day freeze only after a *pattern* of failures, not one incident. Whether this framework covers a limited-margin IRA (which doesn't support real debit) specifically is **not resolved** — FINRA's own notice says interpretive guidance was still forthcoming as of publication.
**Verdict:** Design finalized on the strength of these checks, not assumption — see the full design at `docs/backlog_cache.md`'s pointer entry and the session's plan file. Real conclusion of the regulatory check: stay conservative (pad + fast reconciliation) regardless, since account-type applicability of the cure window is still genuinely unconfirmed.
**Follow-up:** Confirm with Schwab directly whether the limited-margin IRA is in scope for the Rule 4210 intraday-margin cure mechanism. Once `schwab_client.get_current_price` is live, rerun the pre-market-to-open drift check against Schwab's own quote feed (not yfinance) to confirm the flat 5% branch-B pad is still well-calibrated with a live source instead of yfinance's staler one.

## 2026-07-21 — Real intraday drift across the four live signal-reaction windows, for automated bar-close BUY order sizing
**Hypothesis/question:** raised while discussing automating bar-close order placement for `TrailingExitZScoreBreakout` (currently a fully manual "Executed" Slack flow, `signals_notify.notify_buy_signal`'s non-trailing branch, `signals_handlers.py:193`'s `entry_price_submit` handler) — what sizing pad does an automated market order need, given real intraday price movement rather than an assumed near-instant fill? A live observation (watching HIBL fill across several partial orders at the open, some legs 0.25%+ apart) contradicted the initial "market order fills in ~5 seconds, pad can be tiny" assumption.
**Correction found mid-discussion, before any data was pulled:** the live daemon's `entry_timing='open_check'` mechanism (`active_signals._OPEN_CHECK_WINDOWS = [(9,31,9,40),(14,31,14,40)]`) doesn't read a literal exchange Open tick — `signals_compute.compute_buy_signal` always fetches the current real-time price and compares it to `lower_band` (from strictly-prior daily data), regardless of which window is polling. So "open_check" live means "poll earlier in the day, using whatever the live price is at that moment" — an approximation of the backtest kernel's literal `Open` column check (`backtester._simulate_trail`/`_simulate_trail_both`, `open_check_entry_timing`), which does read the true OHLC Open. Confirmed the two live windows correctly mirror the backtest, which evaluates `open_check_entry_timing` at both `target_h0=9` and `target_h1=14` (`backtester.py:806-822`) — the midday 14:31-14:40 window is intentional, not a bug.
**Also found:** `_OPEN_CHECK_WINDOWS`' ~9-10 minute width isn't arbitrary — `signals_config.POLL_SECS=300` (5-minute default polling cadence) means the daemon isn't phase-aligned to the market clock (`active_signals.py:274-275` comment: "POLL_SECS=300 means we rarely land on the exact opening minute"); the window needs to be wide enough that at least one poll reliably lands inside it regardless of phase.
**Method:** built `scripts/sim_open_window_volatility.py` (real yfinance 1-minute bars, ~7-8 trading days available) measuring, per ticker per day, how far price drifts from the specific print each of the 4 live windows reacts to: `morning_open` (drift from the 9:30:00 print, over 9:30-9:40), `midday_open` (drift from the 14:30:00 print, over 14:30-14:40), `morning_close` (drift from the 9:30-10:30 bar's Close/~10:30:00 print, over 10:25-10:40), `afternoon_close` (drift from the 14:30-15:30 bar's Close/~15:30:00 print, over 15:25-15:40). Run across all 10 real watchlist-65 nodes.
**Result — window volatility (mean of `max_dev_pct`, averaged across the 6 `TrailingExitZScoreBreakout` tickers: AGQ, DPST, KORU, NUGT, UDOW, YANG):**
| window | avg mean_dev | worst single day (avg across tickers) |
|---|---|---|
| morning_open | 1.78% | up to 5.1% (KORU) |
| morning_close | 1.16% | up to 4.6% (KORU) |
| afternoon_close | 0.51% | up to 2.3% (SOXL) |
| midday_open | 0.44% | up to 1.8% (SOXL) |

The 9:30 open is 3-4x more volatile than the midday/afternoon windows across every ticker checked — expected (opening-auction price discovery vs. a routine mid-session hourly boundary), but not previously quantified. Per-ticker spread is large: KORU's morning-open (3.49% mean dev) is ~8x YANG's midday-open (0.16%), so a single flat pad across tickers/windows is the wrong shape.
**Cross-checked against real entry-resolution mix** (separate quick analysis, real production kernel via `run_backtest_dispatch`, all 6 `TrailingExit` tickers' actual live nodes): 90.6% of entries resolve via the open-check branch (451/498), only 9.4% fall through to the close-window fallback. Of the open-check fires, 72% (324/451) are the *afternoon* (14:30) open-check — which is also the calmest window measured (0.44% mean dev). So the dominant real execution path is also the safest one; the volatile 9:30-morning-open path is both the minority (127/451 of open-check fires) and needs the biggest pad.
**Drift-accumulation profile** (added after the question "is the 1.78% morning-open drift instantaneous or does it build up?" — extended the same script to sample dev_pct at fixed minute-offsets from the 9:30:00 print): averaged across the 6 tickers, dev grows 0.94% (1 min) → 1.05% (2m) → 1.22% (3m) → 1.49% (5m) → 1.78% (10m). Roughly half the eventual 10-minute drift is present within the first minute (opening-print noise), but it keeps building meaningfully afterward, especially for the choppier tickers (KORU 2.59%→3.49% just between minute 5 and 10; SOXL 2.85%→3.92%). Not purely instantaneous noise — faster polling during the open_check windows specifically (vs. the default 5-minute `POLL_SECS`) would materially cut typical exposure, roughly in half if catching the signal at minute 1-2 instead of minute 8-9, though not eliminate it.
**Verdict:** a single flat sizing pad for automated bar-close BUY orders is the wrong design — needs to be per-ticker and per-window at minimum (morning-open vs. everything-else is the sharpest split). Poll frequency during the open_check windows is a real, separate lever against the same problem, not just pad size. Full data: `output/open_window_volatility_summary.csv`, `output/open_drift_profile.csv`.
**Follow-up:** (1) design the actual per-ticker/per-window buy/fill sizing budget (pad table + top-up) for the bar-close automation, using this data as the calibration input — not started. (2) Check whether the 5-minute poll cadence can cause a real signal to be missed at an open_check window's own boundary (e.g. a signal only true starting ~9:38 might not be caught until a poll lands at 9:41+, just past the window's 9:40 close) — raised, not yet checked.

## 2026-07-21 — Part 4 implementation: backtest-replay verification (Deliverable 1) surfaced a real `prev_close` bug
**Hypothesis/question:** while implementing Part 4 (pinned entry-check automation for the 6 `TrailingExitZScoreBreakout` watchlist-65 tickers), built `scripts/verify_pinned_entry_vs_backtest.py` to check that `signals_compute.compute_buy_signal(node, as_of=..., price_override=...)` reproduces the real backtest kernel's own BUY decisions when fed the exact historical Open/Close price the kernel used for each real trade entry.
**Method:** for each ticker's real watchlist node, ran the real backtest kernel (`backtester.run_backtest_dispatch`) to get every trade's Entry Time/Entry Price, determined which branch fired (Open at the 9:30/14:30 target hour, or Close otherwise) by matching Entry Price against the bar's real Open/Close, then replayed `compute_buy_signal` at that historical date/price and asserted `signal == 'BUY'`.
**Result — first run, before any fix:** every ticker failed on the large majority of trades (e.g. AGQ 109/129, DPST 104/126) — `⚠️ Possible corporate action ... freezing new signals` fired on almost every replayed entry. Root cause: `compute_buy_signal`'s `prev_close` was computed from the unsliced `df_daily['Close'].iloc[-1]` — the most recent row in the *entire* cached daily frame — instead of `df_daily_prior` (already correctly sliced to `< as_of`/today, the same frame the SMA/Std indicators are computed from). Historical replay was comparing e.g. a 2026-02-02 entry price against 2026-07-21's real close, years of drift away, spuriously tripping the corporate-action discontinuity guard (`detect_price_discontinuity`) on nearly every trade.
**This isn't replay-only — a real latent live-mode bug too:** `df_daily = df.resample('D').last().dropna()` includes today's own partial/in-progress bar if any intraday data already exists for today by the time `compute_buy_signal` runs. So in live use (`as_of=None`), `df_daily.iloc[-1]` could silently be *today's own latest print* mislabeled as "previous close," not yesterday's real close — feeding both the corporate-action guard and `build_reference_table`'s `Overnight %` display a wrong reference on any day this triggers. Not confirmed to have caused a real false-positive freeze live (the corporate-action guard is coarse — needs to land near a known split ratio — so this may have gone unnoticed), but the mechanism is real and was silently present before this fix.
**Fix:** switched `prev_close` to derive from `df_daily_prior['Close']` (correctly sliced) instead of the unsliced `df_daily['Close']`. Also affects two other existing `as_of`-based scripts that were silently exposed to the same bug: `scripts/verify_live_parity.py` and `scripts/live_sim.py`.
**Re-run after fix:** 5/6 tickers 100% clean (DPST 126/126, KORU 77/77, NUGT 34/34, UDOW 72/72, YANG 60/60). AGQ: 128/129 — the one remaining mismatch is a real ~2:1-ratio price discontinuity on 2026-02-02 (prev_close=$285.99 → current=$145.00), almost certainly a genuine AGQ stock split. This is *expected*, not a defect: the backtest kernel has no corporate-action awareness at all (operates on raw, presumably split-adjusted-at-collection-time cached prices), while the live-side guard correctly does — a live/backtest disagreement on a real split day is the guard working as designed, not something to chase further.
**Verdict:** the pinned-entry replay mechanism is verified correct on 5/6 tickers and the 1 remaining "mismatch" is explained, not a defect. Full `pytest tests/` (131 passed) confirms no regression from the `prev_close` fix.
**Follow-up:** none required for Part 4 to proceed to paper-trading/Deliverable 2. If a future session investigates a live corporate-action false-positive/negative, check whether this `prev_close` mechanism (today's partial bar leaking in) played a role.

## 2026-07-22 — Reconciling the 2026-07-16 `auto_adjust=True`/split-guard open question, and a data-traceability discussion
**Hypothesis/question:** raised while discussing whether Yahoo Finance historical data can silently change out from under a past backtest. The 2026-07-16 GDXD entry above found `yf.download()` actually defaults to `auto_adjust=True` (confirmed via `inspect.signature`) and neither `data_manager.py` call site overrides it — contradicting the in-code comment claiming hourly bars aren't retroactively split-adjusted — but left open why the split-guard rescue (built for the KORU incident) was ever needed if Yahoo already adjusts everything.
**Method:** re-confirmed the installed version (`yfinance==1.4.1`) and default (`auto_adjust=True`) directly; reasoned through the fetch/merge flow in `data_manager.py` (`STEP 3`: only the recent `safe_days_to_fetch`-day delta window is re-fetched from Yahoo on each incremental update, then merged with the existing local CSV).
**Result:** `auto_adjust=True` only adjusts *the window being fetched right now* for corporate actions known as of today — it does not reach back and re-adjust rows already sitting in the local cache from a prior fetch. So when a new split happens, the next incremental delta fetch comes back on the new adjusted scale while the old cached rows are still on the prior scale, producing exactly the discontinuity the split-guard (`detect_price_discontinuity`, wired into `data_manager.py`'s merge step) detects and rescales the whole local file to fix.
**Verdict:** the split-guard is not dead code / not solving a fake problem — it's correctly compensating for fetch-boundary staleness, not for Yahoo failing to adjust at all. The 2026-07-16 item's "why didn't auto_adjust prevent KORU" question is resolved: auto_adjust and the split-guard are two different layers (per-fetch adjustment vs. cross-fetch reconciliation), both needed. Because the guard's rescale is a single multiplicative factor applied to the whole series, downstream signals (z-scores, %-based SL/TP/arm triggers, returns) are scale-invariant to it, so past `backtest_cache` alpha numbers should remain reproducible by re-running the same code.
**Real, distinct gap surfaced in the same discussion (not resolved, deliberately backlogged)**: there is no archived/immutable snapshot of the exact cache file bytes that fed any given past backtest — the on-disk CSV is mutated in place on every incremental merge and every split-guard rescale. So "re-run the same code, get the same number" should hold (per the scale-invariance argument above), but literal forensic reproducibility ("what data, byte-for-byte, produced this specific historical result") is not possible today. Discussed two shapes: (a) full immutable/versioned data linked to each `backtest_cache` row (real reproducibility, but a meaningfully bigger lift — schema change touching the whole sweep engine, plus real storage growth), vs. (b) a lightweight append-only mutation-event log (timestamp/ticker/factor/before-after checksum) giving an audit trail without redesigning the pipeline. User's read: doesn't want to over-build now, but flagged this as a "we'll pay for this later" concern rather than a solved one — see `docs/backlog_cache.md`.
**Follow-up:** none scheduled yet; backlogged at low priority pending the user deciding between (a)/(b) above, or a real incident that makes the tradeoff concrete.

## 2026-07-22 — "v6": does parking idle capital in SPY (or any other vehicle) between an EOD exit and the next entry actually beat sitting in cash?
**Hypothesis/question:** raised mid-session as a new strategy variant idea — when a position exits on the trading day's last bar, instead of letting the freed capital sit idle in cash until the next signal, buy SPY at the exit price, hold it continuously (however many days the gap lasts), and market-sell it the instant the next real trailing-buy signal fires (SPY's liquidity means this adds only seconds of friction, modeled as zero slippage). Generalized mid-discussion to: don't assume SPY is the right vehicle — extract every real such gap window across all 10 watchlist-65 tickers and scan the whole cached ticker universe for what would have actually made money parked there ("we had spare capital across ~2 years of real gaps — what would have made money with it?").
**Method:** `scripts/sim_v6_spy_parking.py` (single-ticker) and `scripts/sim_v6_parking_vehicle_sweep.py` (universe sweep). "EOD" = an exit landing on the genuinely last bar on file for its calendar date (not a fixed hour — the exit-check loop can still fire on the 15:30 bar even though new entries stop being checked at 14:30). Used `backtester.run_backtest_dispatch` (the same strategy-aware dispatcher `run_optimization_sweep.py` uses) against each ticker's real active watchlist-65 v5 node — correctly handling that 6 of the 10 nodes are actually `TrailingExitZScoreBreakout`, not `TrailingBothZScoreBreakout` (an assumption the first draft of the script got wrong and had to fix). Extracted 62 real EOD-exit→next-entry gap windows across all 10 tickers (AGQ 9, DPST 15, GDXU 3, HIBL 7, KORU 9, NUGT 3, SOXL 4, UDOW 10, USD 1, YANG 1), then scanned ~1,448 cached tickers (the full existing research universe, including broad-market and crypto-linked leveraged ETFs), pricing each window independently via nearest Close at-or-before the window's start/end timestamp, and compounding each candidate's per-window return across all 62 windows chronologically (a simplification — doesn't model overlapping windows across tickers sharing one real capital pool).
**Result — the core premise does NOT hold**: SPY itself would have *lost* money parked across these exact 62 windows (-5.4% compounded, mean per-window -0.07%, win rate exactly 50%). QQQ also loses (-11.2%). Broad-market alternatives (DIA, IWM, VOO) are all close to flat. A T-bill-like cash proxy (BIL) is also close to flat/slightly negative; SHY near-zero — i.e., real risk-free yield over these specific short windows is indistinguishable from doing nothing, which is itself informative (the current baseline's implicit 0%-during-the-gap assumption isn't obviously wrong for a cash-equivalent). On SOXL specifically (single-ticker check, 4 EOD windows), SPY-parking made SOXL's own compounded return slightly *worse* (614.5% baseline → 590.6% v6, ratio 0.9666).
**The "top 30 by return" leaderboard is very likely overfitting noise, not a real finding**: scanning ~600 tickers with sufficient data against only 62 (correlated, not independent — multiple tickers' windows can span the same real calendar dislocation days) return windows is a classic multiple-comparisons trap. The top hits (TSLQ +141.7%, SARK +131.7%, WEBS +115.0%, BERZ +93.6%, SCO +85.1%, ...) have win rates clustered at 50-61% — not a robust edge, just noise that happened to compound well over a small, correlated sample. None of this should be read as "buy TSLQ between trades."
**Verdict:** the v6 idea as originally framed (SPY beats cash) is not supported by the real extracted windows — if anything, SPY parking would have been a net drag on this exact sample. The generalized vehicle-sweep didn't surface a credible replacement either; its "winners" are a sampling artifact, not alpha. Recommend not pursuing this direction further without either (a) a much larger/independent sample of gap windows (would need many more tickers/years, and de-correlating overlapping windows), or (b) a principled reason to prefer one specific vehicle (e.g., a genuine inverse-hedge rationale) rather than mining the full universe for whatever happened to win.
**Out-of-sample split-check, same session** (`scripts/sim_v6_split_check.py`): split the 62 windows chronologically into two 31-window halves and re-scored SPY/QQQ and the 6 direct SPY/QQQ short-or-inverse candidates (SH/SDS/SPXU/SPXS/PSQ/SQQQ/QID) plus BIL independently in each half. **Every single candidate reverses sign between the two halves** — including SQQQ, which looked best in the full sample (+16.4% compounded) but was +47.4% in the first half and -21.0% in the second, a complete reversal, not a stable edge. SPY itself: -9.3% (h1) vs. +4.3% (h2). Only BIL is close to consistent across halves, and that's because it's near-zero in both (i.e., "doing nothing" is the only thing that held up). **This is a clean, direct demonstration that 62 correlated windows is not enough sample to trust any vehicle pick here** — not just the leaderboard noise flagged above, but the two economically-motivated candidates (SQQQ/QID as a "short the market on bad days" hedge) specifically.
**Verdict (final):** don't act on any vehicle choice from this analysis. The idea needs a much larger, less-correlated sample (many more real gap windows, likely requiring years more live data or a much broader ticker set feeding the gap-extraction step) before it's worth revisiting, and even then the "pick the best backtested vehicle" framing invites exactly this overfitting trap — a future attempt should pre-commit to a candidate with an independent rationale before looking at the numbers, not choose after.
**Follow-up:** none planned. Full per-window detail: `output/v6_gap_windows.csv`; per-ticker detail: `output/v6_spy_parking_SOXL.csv`; full universe sweep: `output/v6_vehicle_sweep_results.csv`; split-check: `scripts/sim_v6_split_check.py` (rerunnable if more gap windows accumulate later).

## 2026-07-22 — HIBL paper trade entered and stopped out in 31 seconds: stale-cache race at market open
**Hypothesis/question:** user noticed a HIBL paper-trading position enter at $104.09 and exit via SL 31 seconds later at $100.68 (-3.28%) — asked "why did it trade, and SL out in under a minute."
**Method:** reconstructed the sequence from `paper_trade_log`, `coverage_events`, and the raw cached CSV (`cache/research/HIBL_1h.csv`). Entry price ($104.09) matched exactly the *prior day's* 15:30 bar Close, not any value in the actual 09:30 session bar (Open=Low=$100.37, Close=$103.69, High=$105.16).
**Result:** `signals_compute._current_price(ticker)` reads `df['Close'].iloc[-1]` from the locally cached CSV with no staleness check at all. The pending trailing-buy's `running_low` was set to $100.37 (the real live price at the moment yesterday's 15:25-15:40 signal window fired, mid-bar, ahead of that bar's eventual $104.09 close). This morning at 09:30:12, `paper_trading.update_paper_buys()` called `_current_price()` before that ticker's first same-day data refresh had landed — the CSV's last row was still yesterday's stale $104.09 close, which cleared the $101.37 bounce trigger ($100.37 × 1.01) and "filled" the paper buy at a price that was never actually tradable at that moment. The next poll's fresh data (real $100.37 open) then immediately tripped the 1% SL.
**Verdict:** real bug, paper-trading-only in actual impact (real trailing-buy orders execute against Schwab's live order book, not this cache, so no real-money exposure existed) — but it also affects two **real (non-paper)** call sites sharing the same function: `active_signals._check_position_exit`'s mid-bar branch (a real position's SL/trailing-sell continuous check) and `_check_limit_fill` (live limit-order fill detection). Neither had ever hit the race live (every account still `dry_run=True`, and no real ticker signal has landed in that exact narrow open-market window yet), but the mechanism was real and reachable.
**Fix:** `_current_price()` now returns `(None, None)` if the cache's last row predates today's date, it's a weekday, and `now.time() >= 9:30` — every one of the 6 call sites already handled `None` gracefully. Independent Opus review confirmed the fix logic is correct and strictly safer than the prior behavior, but flagged a residual gap: on a live trading day, if a *real* position's data refresh genuinely fails (not just the narrow open-race), the guard now silently suppresses that position's intrabar exit check with only a `log_poll` trace, no Slack alert — worth a follow-up alert for that specific case. Full suite: 181 passed.
**Also built this session, prompted by "we need logs of what we're sending to Slack" (there was previously zero persistent record of _post_message content) and "track each price/bar the pollers are hitting":** `signals_db.slack_message_log`/`log_slack_message`/`get_slack_messages` (full text + mode, wired into `signals_blocks._post_message`), and a shared `log_poll()` helper (`signals_helpers.py`) writing `[poll]`-prefixed trace lines to the existing `VERBOSE_LOG_PATH`, wired into every price/bar-consuming decision point identified during this investigation (`_current_price`, `_check_position_exit`, `_scan_pinned_exit_arm`, `_scan_pinned_entry`, `_check_limit_fill`, `paper_trading.update_paper_buys`/`check_paper_sells`).
**Follow-up (not yet built):** (1) Slack alert for the real-position stale-guard suppression case Opus flagged. (2) A canary-node plan was designed and independently reviewed but found to have a real sizing bug (`starting_notional=500` sizes to 0 shares at SPY/QQQ's real price — canaries would silently never fill) — not yet fixed or deployed. (3) Separately, confirmed `active_signals._scan_pinned_exit_arm` only ever reads real (non-paper) `open_positions` — no paper canary, however designed, can exercise the pinned exit-arm path; this is either a real coverage gap worth closing or an accepted limitation, not yet decided. (4) User wants a scriptable extension of `scripts/live_sim.py` to drive `active_signals.py`'s real functions directly (pinned entry/exit, top-up, gap-resize, TIME exit, both entry paths) as a fast, repeatable coverage harness — proposed as the new standard verification step for any future `active_signals.py`/`signals_*.py` change, not yet built.

## 2026-07-23 — Morning Report investigation: a wrong initial diagnosis, two real bugs found, one root cause
**Hypothesis/question:** user reported the daemon restart didn't send a Morning Report to Slack ("no report"). Initial hypothesis floated by the user: maybe the report is gated to skip when no tickers are `mode='live'`.
**Method, and how the diagnosis went wrong before it went right:** checked `send_reference_report` (`signals_notify.py`) directly for mode-gating — found none, so ruled out the user's hypothesis. Found `slack_message_log` (built the prior session) showed a row for the missing report with no caught error, and manually re-invoking the same function from a standalone script succeeded and was independently confirmed via `chat.getPermalink` — leading to a wrong intermediate conclusion that delivery was fine and the issue was on the user's viewing side. This was wrong on two counts: (1) `slack_message_log` was logging *intent* (written before the send attempt), not delivery, so a row's existence never actually proved success; (2) `logs/active_signals.log` (the human-readable daemon log) had no explicit `.flush()` call and was block-buffered, so real evidence from the failed attempt (if any) was sitting unflushed and unobservable on disk the whole time — confirmed by the file's mtime being frozen for 10+ minutes while the daemon's heartbeat proved it was still actively looping.
**Fixed those two observability gaps first** (`buffering=1` on the human log; moved `log_slack_message` to fire after the attempt with a real `error` column) — which paid off immediately: the next real Slack send failed with a confirmable, visible error (`invalid_blocks`, see below) instead of another round of blind guessing.
**Real root cause, once observability actually worked:** `build_reference_table` (`signals_notify.py`) filtered its input to `mode == 'live'` nodes only. Every watchlist node has been `mode='research'` since the 2026-07-20 v5 promotion — so the report had been posting successfully (header, kill-switch status, "No open positions", "Buy Candidates" header) with **zero candidate rows** underneath, for weeks, with no error at any layer. This was exactly the user's original hypothesis, which got wrongly ruled out earlier in the same conversation by checking the wrong function.
**Second bug, exposed immediately by fixing the first:** once all 16 nodes rendered (10 real + 6 newly-added canaries), the message hit 53 Slack blocks — over the hard 50-block-per-message API limit — and was rejected outright (`invalid_blocks`, confirmed live via the now-working error logging). Fixed by collapsing each row's up-to-3 separate `actions` blocks into one (Slack allows up to 5 elements per `actions` block).
**Verdict:** the empty-report bug was real, silent, and had been live for the entire life of the all-research watchlist (2026-07-20 onward) — a genuinely useful report was returning structurally-valid-but-content-free messages the whole time, and nothing in the system would have caught it without this investigation. The block-limit bug would have blocked the fix from working at all had the observability gaps not been fixed first — worth noting as a case where fixing "boring" logging infrastructure was a hard prerequisite for diagnosing the "real" bug, not a side quest.
**Follow-up:** none required — both bugs fixed and independently verified via a real permalink lookup, not just trusting `_post_message`'s return value. Full suite: 181 passed throughout. See `docs/backlog_cache.md`'s 2026-07-23 entries for the mechanical fix detail.

## 2026-08-01 — Does the buy-sizing safety pad create a real compounding drag over many trades? No — already solved by an existing same-day top-up.
**Hypothesis/question:** while sizing 3 new small "real-world coverage pilot" live nodes (HIBL/USD/YANG, `soxl_ira`), noticed `signals_helpers.buy_order_sizing` deliberately under-deploys capital every buy (a 1% safety pad, plus `trail_buy_pct` itself for trailing-buy strategies) to cover fill-price uncertainty, and that real/dry_run sizing compounds off its own realized proceeds (`_last_sale_recovery`). Since a compounding loop multiplicatively reapplies that same ~1-2%/trade shortfall, does this silently erode real returns far below what the backtest assumes, for tickers that trade often?
**Method:** built `scripts/sim_pilot_notional_drag.py`, replaying each of the 10 real v5 nodes' actual historical trade sequence (`backtester.run_backtest_dispatch` against cached OHLC) through the exact production sizing formula (`int(notional // (price × (1 + pad%)))`, next-cycle notional = `shares × exit_price`), compared against the backtest's own idealized full-reinvestment compounded return (`((Return+1).prod()-1)`).
**Initial result (WRONG, see correction below):** at real $50k sizing, several tickers appeared to flip from large backtest winners to real losses purely from this compounding pad — SOXL +794.2% idealized vs a simulated -99.8% (near-total wipeout), HIBL +673.7% vs -58.6%, GDXU +436.3% vs -62.5%, DPST +295.8% vs -5.7%. Two real script bugs were also found and fixed en route (using `trail_buy_pct` as the swept axis for `TrailingExitZScoreBreakout`, which actually sweeps `trail_sell_pct` — silently zeroed YANG's real 17% trailing-exit width; and a hardcoded `z_score_threshold=2.0` that didn't match HIBL's real node value of 1.0) — both real bugs, correctly fixed, unrelated to the eventual wrong conclusion below.
**Correction — the simulation omitted an already-built production mechanism:** `signals_notify._reconcile_fill` (built 2026-07-21, called unconditionally by `_reconcile_buy_fill` on every real fill) already does a same-day post-fill top-up — buys the shortfall between `target_notional` and the real fill notional immediately, at the now-known real fill price, with no pad needed (the pad only exists to cover price *uncertainty*, which is gone once the fill is confirmed). This was missed entirely in the original simulation, which modeled every trade as if the pad's shortfall were never recovered.
**Result (corrected, with top-up modeled):** the "drag" nearly vanishes for every v5 ticker — SOXL 794.2% vs 781.4% (12.8pp gap), HIBL 673.7% vs 649.6% (24.1pp), GDXU 436.3% vs 413.1% (23.2pp), DPST 295.8% vs 260.6% (35.2pp), the rest within 1-15pp. The residual is mathematically bounded to under one share's worth of dollars per trade (the top-up's own floor-division remainder) — consistent with ordinary rounding noise, not a systemic compounding failure.
**Verdict:** no real gap exists; no code change needed. The design change that was about to be built in response (persist an unused-notional "reserve" and fold it into the next trade, or replay full trade history from `starting_notional` each time to derive it statelessly) is unnecessary — `_reconcile_fill`'s existing same-day top-up already achieves the same effect, and does it better (same-day at the known real price, vs. next-cycle at an estimate).
**Process note:** this cost real, avoidable investigation time — the mistake was modeling "what does the current sizing formula do" from first principles (`signals_helpers.py`) without first checking whether a downstream reconciliation step (`signals_notify.py`) already closes the gap. Before building a simulation of current production behavior, grep the full real code path (entry sizing *and* post-fill reconciliation), not just the sizing function in isolation.
**Follow-up:** none needed. `scripts/sim_pilot_notional_drag.py` is committed and reusable (`--watch-id ID... [--top-up]`) if this question ever needs revisiting for a new node.

## 2026-08-01 — Do "gap/no-dip" trailing-buy entries (paying near-worst-case padded price, little/no benefit from a running-low dip) differ in quality from entries that got a discount from waiting? Explored, no consistent pattern, not pursued further.
**Hypothesis/question:** raised while investigating the sizing-pad compounding question above — since `running_low` only ever falls before a trailing-buy bounce-fill, some entries end up paying close to the padded worst-case price (little/no dip captured) while others get a real discount. Does "wait ≤1 bar before fill" (a proxy for no meaningful dip) correlate with worse trade quality, and would a mechanical rule ("after 3 consecutive losses, only re-enter on a gap trade until a win") improve returns?
**Method:** `scripts/export_trades.py::simulate_trail_both_signal_tracked` (extended pure-Python mirror of `_simulate_trail_both` that also tracks `signal_i` per trade, not exposed by the real numba kernel) run against the 4 real v5 tickers that actually use `TrailingBothZScoreBreakout` (GDXU, HIBL, SOXL, USD — the other 6 v5 tickers run `TrailingExitZScoreBreakout`, a direct market-buy strategy with no waiting/bounce phase at all, so this question doesn't apply to them). Computed win rate and return magnitude (mean/compounded) split by gap (wait≤1 bar) vs. normal (dipped first) buckets, a Wald-Wolfowitz runs test on each ticker's full win/loss sequence to check for streak clustering beyond chance, and a direct simulation of the "3-strikes" rule (skip non-gap trades once 3 consecutive losses have occurred, reset on a win).
**Result:** no consistent cross-ticker pattern. Win rate alone was often misleading — HIBL's gap trades win slightly *more* (21.6% vs 20.0%) and have comparable/better mean return (+1.87% vs +1.48%), while GDXU's gap trades win less (15.4% vs 23.7%) *and* are much weaker in magnitude (+0.62% mean vs +5.07%, contributing only +7.6% of GDXU's +436.3%+7.6% total despite being 41% of trades). SOXL is a milder version of GDXU's pattern; USD has no comparison group (100% gap trades). The runs test found no ticker with statistically significant streak clustering (all |z| < 1.96) — the long losing streaks that look dramatic in isolation (DPST 23 straight, KORU 16, HIBL 14) are consistent with plain random ordering given each ticker's low win rate (12-32%), not evidence of a distinct "bad regime" by trade-sequence alone (calendar-time clustering was spot-checked for DPST's 23-loss streak — dense, 2025-02-20 to 2025-04-11 — but not investigated further). The 3-strikes rule, simulated directly against real trade sequences, was net harmful on 2 of 4 tickers (GDXU +436.3%→+48.8%, SOXL +794.2%→+35.8%) and only helped HIBL (+673.7%→+732.6%) — and only because HIBL happens to be the one ticker where gap-trade quality isn't worse to begin with, not because the rule itself is sound.
**Verdict:** not pursued further. The rule doesn't generalize across even this small 4-ticker sample, and picking it based on which ticker it happened to help would be overfitting on n=4. No code change.
**Follow-up:** none planned now ("another research permutation" for later, per the user). If revisited: calendar-time clustering of long streaks (does DPST's dense 2025-02-20/04-11 streak line up with a real SPY/VIX regime?) is the one thread that was flagged but not run.
**Correction, 2026-08-01 (Opus challenge review):** "no consistent pattern" overreaches — n=4 tickers (really n=3, USD has no comparison group) can't distinguish "no pattern" from "underpowered to detect one." The Wald-Wolfowitz runs test's independence assumption is violated by serially-correlated leveraged-ETF trades sharing an underlying price path, so its non-rejection is weak evidence. DPST's calendar-dense 23-loss streak (2025-02-20→04-11, noted above but not investigated) directly cuts against "consistent with plain random ordering." Verdict softened to: underpowered, not resolved.

## 2026-08-01 — SPY-trend/VIX regime correlation across 669 real v5 trades — retracted as originally framed, multiple-comparisons trap
**This entry was never written at the time** (only `docs/session_cache.md`'s 2026-08-01 evening entry described it, which is gitignored and capped at 10 — a real process gap, caught the next session). Backfilled here from that summary, then immediately corrected by an independent Opus challenge review.
**Original claim:** tagged all 669 real v5 trades by SPY-vs-200d-SMA and VIX level at entry; found SPY-downtrend entries are the *best* trades (inverting the original hypothesis), held up "in weakened but consistent form" when re-tested at 50d/100d SMA, with 11 of 12 regime-cells (4 regimes × 3 SMA windows) net positive. Caveat given at the time: only 3 distinct SPY-below-200SMA episodes exist in the cached history, all quick V-shapes.
**Correction (Opus challenge, 2026-08-01):** 669 trades across 10 correlated leveraged-ETF tickers, all entering on the same handful of real historical dislocation days, is nowhere near 669 independent observations — effective n is ~3 (the episode count). Re-slicing the *same three episodes* at 50d/100d/200d SMA is one finding examined three ways, not three independent confirmations (the SMA windows are highly correlated over this span). "11 of 12 cells net positive" is closer to 1-3 real evidence points, and since the strategy is net-positive overall in this sample, most cells being positive is close to guaranteed regardless of any real regime effect. This is the exact multiple-comparisons trap already named in this log's 2026-07-22 entry ("scanning ~600 tickers against 62 correlated windows") — reproduced one investigation later without being caught at the time. The committed script (`scripts/sim_regime_correlation.py`) only implements the 200d case and has no significance test; the 50d/100d re-test and cell table described above are uncommitted and not reproducible from what's checked in.
**Verdict:** not a validated finding — retracted as originally framed. Real effective sample size is ~3 non-independent episodes; no conclusion about SPY-downtrend entry quality should be drawn from this analysis. If revisited: needs either genuinely independent episodes (more market history — see the daily-bar-variant and 1-min-recording backlog ideas) or a method that doesn't multiply-count the same 3 episodes.
**Follow-up:** none planned; flagged as a caution against re-deriving the same "SPY-downtrend is fine" conclusion from this data without addressing the sample-size problem first.

## 2026-08-01 — Time-reversal as a sustained-bear-market proxy — retracted, doesn't test what it claims to
**Original claim:** reversed real OHLC bars for 5 tickers as a stand-in for a sustained bear market; all 5 flipped from large backtest winners to real losses, presented as "a concrete (if imperfect) signal the observed edge is tied to this data's specific brief-V-shaped-dip character."
**Correction (Opus challenge, 2026-08-01):** time-reversal is not a valid bear-market proxy. Reversing the bar sequence inverts the sign of drift while preserving volatility, turning every mean-reversion setup into its mirror image — a "dip then rebound" becomes "rally then decline," so the strategy's entry trigger now systematically buys local *tops* instead of dips. That the reversed series loses money is a near-mathematical consequence of the reversal itself, not evidence about how the strategy would perform in a real sustained decline. It also breaks path-dependence specific to daily-reset leveraged ETFs (decay is not time-symmetric) and any real volatility-clustering asymmetry. The experiment actually tests "does this strategy require the arrow of time to work" (trivially yes, for a mean-reversion strategy), not "does it survive a bear market."
**Verdict:** retracted, not merely caveated. No real evidence currently exists — positive or negative — about how the live strategy would perform in a genuine sustained bear market (2020, 2022); the cached hourly data doesn't cover either period (see the 2026-08-01 data-retention backlog entry) and no valid proxy has been run. A real test needs either actual bear-market-period data (paid vendor) or a resampling method with injected negative drift, not reversal.
**Follow-up:** none run yet. This is the answer to "do we have a research item for bear-market testing" — no, this was the closest thing and it doesn't count as one.

## 2026-08-01 — Compounding-drag correction (from the earlier 2026-08-01 entry above) — redone against the actual pilot nodes; the $50k-scale "no drag" conclusion holds, but at pilot-scale notional most trades are skipped outright, a distinct finding
**Round 1 (Opus challenge, 2026-08-01):** flagged that the "corrected, with top-up modeled" simulation in the entry above is an algebraic identity — `shares += int((notional - shares*price)//price)` always sums to exactly `floor(notional/price)` when the top-up gate fires, regardless of pad size — and that the headline table ran against the 10 $50k `research`-mode v5 nodes (86-95) instead of the 3 small pilot nodes (154 HIBL/155 USD/156 YANG, $60-200 notional) that actually motivated the question.
**Round 2 (rerun against the actual pilot nodes, `scripts/sim_pilot_notional_drag.py`'s own default watch-ids, `--top-up`):** real, severe drag: HIBL +673.7% idealized vs -89.4% realistic (763.1pp, 120-123/141 trades skipped for lack of an affordable share), USD +193.0% vs -58.3% (251.4pp, 1-4/14 skipped), YANG +354.6% vs -61.1% (415.7pp, 42/59 skipped). Top-up on/off barely moves the numbers — the driver isn't the top-up mechanism, it's that these are high-historical-price-range leveraged decay ETFs (HIBL $14.73-$137.15, USD $35-$115.59, YANG $20.22-$379.60 over the cached window) where a $60-200 notional frequently can't afford even 1 share after the sizing pad, so the trade is skipped entirely (not padded, not topped up).
**Verdict, corrected once more (per user, 2026-08-01):** this doesn't reopen the original $50k-scale question — nobody is realistically deploying real capital at $200 notional; these 3 nodes exist purely to generate coverage-test order flow. Reframed: the "identity" from round 1 is not a null result at realistic notional, it's a *proof* — whenever `delta > fill_price` fires (which it will on nearly every trade once notional is large relative to one share's price, e.g. $50k), `shares_final` is mathematically forced to equal `floor(target_notional/fill_price)`, the same value an idealized full-notional buy would round down to. **No real compounding drag exists at real-money-scale notional — this was correctly concluded the first time, on sound math, and didn't need live re-proof.** The genuinely new finding from round 2 is narrower and different in kind: the 3 pilot nodes' tiny notional may be undermining their own stated purpose (generating real broker order flow for coverage gaps) by skipping 71-87% of signal-window trades outright. That's a coverage-flow question, not a returns/drag question — see `docs/backlog_cache.md`.
**Residual, still not modeled:** real market-buy slippage on the top-up leg itself, and the `SafetyViolation`/exception early-return paths — both could, in principle, reintroduce a small amount of drag even at $50k scale, but neither has been quantified or found to matter in practice. Low priority given the math above already bounds the effect to "rare failure of an already-rare-to-need top-up," not a structural gap.

## 2026-08-03 — FFT cycle detection (v5 watchlist) — no significant periodicity in returns; the "significant" price-level cycle is a null-test artifact
**Hypothesis/question:** Does decomposing a ticker's price series into dominant FFT frequencies explain why some tickers respond better than others to the current `window` sweep (10/20, chosen empirically), or reveal exploitable cyclical structure?
**Method:** `scripts/fft_cycle_analysis.py` (new). For each of the 10 real v5 watchlist tickers (AGQ/DPST/GDXU/HIBL/KORU/NUGT/SOXL/UDOW/USD/YANG), ran `scipy.fft.rfft` on (a) the linearly-detrended log-price series and (b) the return series, extracted the strongest peak in the 3-500 bar band, and tested significance against a 1000-trial random-permutation null (shuffling destroys all autocorrelation, not just periodicity — the correct baseline for "is this peak more than what noise with this variance would produce"). Cross-referenced each ticker's actual best-performing `window` (10 or 20 bars) live from `backtest_cache` via `campaign_comparison_table.best_node` (v5, `open_check`, robust alpha).
**Result:** Return-series peaks (the economically relevant series — price levels are non-stationary) cluster at 3.5-6.5 bars (half a day to a day) across all 10 tickers, but none are significant: p-values 0.076-0.837, all well above the 0.05 bar. No correlation with the winning `window` (10 vs 20) either. Price-level peaks all landed near 350-480 bars and all showed p<0.05 — but this is a known artifact, not real cyclicality: linear detrending doesn't remove multi-month drift/regime structure, and a full-shuffle null destroys *all* autocorrelation, so any series with real (non-periodic) autocorrelation beats it trivially. It is not evidence of a genuine cycle.
**Verdict:** Refuted (for the naive form tested) — hourly leveraged-ETF returns show no significant deterministic spectral cycle on any of the 10 v5 tickers; FFT in this form doesn't explain or improve on the empirical window choice. Consistent with the broader efficient-markets expectation for return series at this timescale.
**Follow-up:** none planned as a strategy input. If FFT is revisited, it would need to be as part of the already-backlogged separate paradigm (live spectral filtering + trend-following, a distinct strategy class — see `docs/backlog_cache.md`'s 2026-08-02 entry), not as a window-selection input for the existing z-score kernel — this result closes that narrower question.

## 2026-08-03 — Entry-timing seasonality (v5 watchlist) — no significant win-rate/return difference between the two daily signal windows; but a real, unexplained 3x volume imbalance between them
**Hypothesis/question:** Live trading checks two fixed daily signal windows (10:25-10:40 ET catching the 9:30 bar close, 15:25-15:40 ET catching the 14:30 bar close) with equal trust/weight — never tested whether one window's signals are actually more reliable than the other's.
**Method:** `scripts/entry_timing_seasonality.py` (new). Reused the existing trade-simulation code the live sweep engine already trusts (`scripts/export_trades.py`'s `simulate_trail_both_annotated`/`simulate_trail_exit_chaos` mirrors of the real numba kernel, zero miss-rate) against each of the 10 real v5 watchlist tickers' actual node config (`watch_list`, `watchlist_id=65`, `mode='research'`). Tagged every historical trade by which bar (9:30 or 14:30) fired its entry signal, then compared win rate and mean return between the two groups per ticker (Mann-Whitney U on returns, Fisher exact on win/loss counts) and pooled across all 10.
**Result:** No ticker showed a significant difference (Mann-Whitney p=0.056-0.951, Fisher p=0.054-1.0, all above 0.05 — HIBL and GDXU closest at ~0.05-0.06 but not below it). Pooled across all 10 tickers (877 trades): 9:30-bar trades win 16.5% (mean return 1.35%) vs 14:30-bar trades win 18.8% (mean return 2.44%) — directionally favors the afternoon window on every ticker but AGQ/KORU, but pooled Mann-Whitney p=0.199, Fisher p=0.536, not significant.
**Side finding, not the original question but real:** the 14:30 window fires roughly 3x more signals than the 9:30 window across nearly every ticker (pooled 671 vs 206; e.g. HIBL 113 vs 27, SOXL 122 vs 31). Not explained or investigated here — could be a real structural effect (afternoon volatility, cumulative reversion building through the day) or an artifact of the two-window signal-check design itself.
**Verdict:** Inconclusive-leaning-refuted for the original question (no proof one window is more reliable than the other; current equal-trust treatment isn't contradicted by this data) — but underpowered on several tickers (USD n=6/8, NUGT n=7/36) so this isn't a strong confirmation either, just "no evidence found."
**Follow-up:** the volume-imbalance side finding is a real, distinct, unscoped question — added to `docs/backlog_cache.md`. Not pursued further this session per the user's original ask (time-of-day *outcome* quality, not volume).

## 2026-08-03 — Multi-day cycle entry-timing test (v5 watchlist), re-run with the Opus-reviewed methodology fixes — still no real cycle found
**Hypothesis/question:** Does any v5 watchlist ticker have a genuine, repeating multi-day price wave (as opposed to the already-confirmed-real intraday 3.5-bar volatility seasonality, or a discrete peak anywhere in [3,500] bars, already refuted for signed returns in this file's earlier 2026-08-03 FFT entry)? If so, does entering near a specific phase of that wave (e.g. the trough) improve trade outcomes?
**Method:** `scripts/multiday_cycle_entry_test.py` (new). Addressed the same-day Opus review's critique of the original FFT script directly: used `scipy.signal.welch` instead of a raw periodogram (cuts estimator variance), restricted the search to a targeted multi-day band (21-200 bars, ~3-28 trading days) instead of a broad global argmax, and replaced the iid-shuffle null (flagged as anti-conservative for heteroskedastic returns) with a moving-block bootstrap (7-bar blocks, well below the tested band — preserves real short-range autocorrelation/volatility clustering within a block while destroying anything periodic across blocks, the correct null for "is there a cycle longer than local structure alone would produce").
**Result:** No ticker cleared significance. p-values: AGQ 0.306, DPST 0.262, GDXU 0.098, HIBL 0.546, KORU 0.050 (exactly at the boundary — with 500 bootstrap reps that's 25/500, and across 10 tickers tested one borderline value is expected by chance alone; doesn't survive even an uncorrected read, let alone multiple-comparisons correction), NUGT 0.106, SOXL 0.260, UDOW 0.462, USD 0.818, YANG 0.332. None triggered the planned phase-vs-outcome follow-up (Hilbert-transform phase extraction + Spearman correlation against trade returns), since none cleared the p<0.05 gate to justify it.
**Verdict:** Refuted, more confidently than the original attempt — this used a properly-specified, more conservative null (the review's own analysis showed the original iid-shuffle null was biased toward *finding* false cycles, not missing real ones, so a negative result under a corrected, harder-to-pass null is a stronger negative, not a weaker one). No v5 watchlist ticker shows evidence of a real multi-day repeating price wave; "enter near a favorable cycle phase" isn't a viable entry-timing signal for any of these 10 tickers.
**Follow-up:** none planned for the fixed-cycle framing — answers the user's direct "can rhythm help decide entries" question (no, for both the global-argmax framing and this targeted-band/better-null framing). Does NOT close the separate re-opened 2026-08-03 backlog item on *rolling/time-varying* spectral power as a regime signal — this test still assumes one fixed dominant period across the whole ~2-year series, not a time-varying one; that item is a genuinely different question and stays open. The distinct entry-timing-seasonality side finding (14:30 window firing ~3x more signals than 9:30) is unrelated and still open.

## 2026-08-03 — Intraday drift check (9:30-to-14:30 bar) — refutes "prices fall through the day" as the explanation for the 14:30-vs-9:30 signal-volume imbalance
**Hypothesis/question:** The entry-timing-seasonality check (this file's earlier 2026-08-03 entry) found the 14:30 daily signal window fires ~3x more BUY dislocation signals than the 9:30 window. Does that happen because prices systematically drift down over the trading day (a real directional bias), or is it just more elapsed time for any large move to occur by the later check?
**Method:** `scripts/intraday_drift_check.py` (new). For each of the 10 v5 watchlist tickers, computed the 9:30-bar-Close to 14:30-bar-Close percent change for every trading day with both bars present, and tested whether the mean/median is really nonzero (Wilcoxon signed-rank test) rather than noise.
**Result:** No ticker showed a significant directional drift individually (all p>0.14). Pooled across all 10 tickers (6,035 trading days): mean change +0.048%, median +0.077%, 48.3% of days down / 51.7% up — technically significant at the pooled level (p=0.0235, driven by sample size) but the direction is slightly *positive*, not negative, and the effect size is economically negligible.
**Verdict:** Refuted — prices do not systematically fall from 9:30 to 14:30. The 14:30-vs-9:30 BUY-signal volume imbalance is not explained by directional intraday drift. Leaves the alternative explanation (more elapsed trading time by the 6th bar vs. the 1st bar gives more opportunity for *any* large-enough move, in either direction, to breach the lower band) as the more likely mechanism, though that alternative wasn't directly tested here (would need e.g. comparing intraday volatility/range by bar number, not just net drift).
**Follow-up:** none planned — this was a check on a side finding, not the main line of research. The signal-volume-imbalance item in `docs/backlog_cache.md` should be updated to note directional drift is ruled out, cumulative-volatility-by-bar-number remains the leading unverified explanation.

## 2026-08-03 — Inverse-pair idle-capital fill: static hold rejected (leveraged-decay blowup), secondary-trade framing inconclusive (no good base node exists for the candidate inverses)
**Hypothesis/question:** User's idea — when a primary v5 node's position is closed, use the idle time productively by trading its real, matched leveraged inverse (e.g. SOXL/SOXS, QID/QLD, GDXU/GDXD, NUGT/DUST, AGQ/ZSL), on the premise that a buy signal on one side implies the inverse is far from its own trigger, so they shouldn't fire simultaneously.
**Method (pass 1 — static hold):** `scripts/sim_v6_inverse_parking.py` (new). Extracted each primary's real exit-to-next-entry idle windows (reusing `sim_v6_parking_vehicle_sweep.py`'s windowing/pricing logic) for the 3 pairs currently on the corrected v5 kernel with both legs backtested and a real watchlist_id=65 node to source windows from — GDXU/GDXD, NUGT/DUST, AGQ/ZSL — then scored a buy-and-hold of the inverse across each window, vs. SPY and cash baselines.
**Result (pass 1):** Compounded returns were wildly inconsistent with per-window means (GDXD -87.3% compounded despite 65% win rate/median +1.4%; ZSL -17.5% compounded despite median +0.44%; DUST +48.1%). Root cause confirmed directly: the worst single windows were long holds (GDXU's worst window was 124 days, AGQ's worst 60 days) — 2x/3x leveraged ETFs decay from daily rebalancing regardless of direction, so a handful of month-plus idle windows wipe out an otherwise-fine median. **Verdict: static holding rejected** — not what the user intended anyway (clarified: they want the inverse *traded on its own signal rules*, not held).
**Method (pass 2 — secondary trade):** `scripts/sim_v6_inverse_secondary_trade.py` (new). Pulled each inverse ticker's own best cliff-safe (fallback best-any) v5 node config via `campaign_comparison_table.best_node`, ran it through the same `run_backtest_dispatch` the sweep engine uses to get a real trade list, then counted only trades whose full entry-to-exit span fit inside one of the primary's idle windows (a trade still open when the primary re-enters is excluded, not overlap-handled).
**Result (pass 2):** GDXD showed 135 qualifying secondary trades (compounded +8.9%, but only 8.1% win rate), DUST 22 trades (compounded -35.0%, 0% win rate), ZSL 82 trades (compounded +55.9%, 37.8% win rate) — but checking each inverse's *own* full-sweep numbers across all 3 fixed_sl values showed **none of the three ever had a cliff-safe node, and win rates ranged 0.5%-14% across the board** (GDXD: 5.3%/7.2%/9.3%; DUST: 2.1%/5.4%/13.6%; ZSL: 4.3%/0.5%/3.6%), with alpha mostly negative. DUST's 0% is consistent with (not an artifact of) its uniformly bad standalone numbers. The apparent positive compounded returns for GDXD/ZSL are being driven by a small number of outlier winning trades inside an already non-cliff-safe, low-win-rate config — exactly the noise pattern the cliff-safety check exists to screen out, not a validated edge.
**Verdict:** Inconclusive by construction — testing "trade the inverse during idle time" on top of a base strategy that has no good node for that inverse ticker at all doesn't validate or refute the underlying hypothesis. GDXD/DUST/ZSL were only ever swept once (`TrailingExitZScoreBreakout`, `open_check`, `fixed_sl∈{1,2,3}`), a narrower grid than the primary tickers got (missing `TrailingBothZScoreBreakout` and wider `fixed_sl` values).
**Follow-up:** backlogged — a proper resweep of GDXD/DUST/ZSL (and eventually SOXS/QLD/YINN/SQQQ/FAZ/FNGD/SCO/TMV for the other matched pairs) under the full grid, matching what the primary tickers received, before the secondary-trade idea can be judged fairly. See `docs/backlog_cache.md`.

## 2026-08-03 — Idle-capital/joint-capital pairing framework, refuted with real evidence: no genuine edge, mechanism is trade-timing-overlap noise
**Hypothesis/question:** Picking up from the earlier idle-capital-reuse pass (this file's prior 2026-08-03 entry) — is there a *real, predictable* benefit to pairing two tickers via the joint-capital merge (filler's non-overlapping trades merged into primary's idle windows), beyond noise? Raised specifically after AGQ/ZSL showed a 1.56x multiplier and SOXL/SOXS traced to a real ~11.6% weekend gap with no concurrent offset (session_cache 2026-08-03 later-still entry) — user challenged whether "true inverse pair" was doing anything a random pair wouldn't.
**Method:** `/tmp/all_v5_pairs.py` (ad hoc, not committed) computed the joint-capital multiplier for all 90 directional pairs among the 10 real v5 watchlist tickers (AGQ/DPST/GDXU/HIBL/KORU/NUGT/SOXL/UDOW/USD/YANG — full real backtest history, no inverse relationship required), using each ticker's actual live-node config and real trade list.
**Result:** Mean multiplier 1.02, median 0.96, spread 0.27x-3.45x, 40/90 help vs 38/90 hurt — centered almost exactly at "no effect," with more variance than any of the "special" inverse pairs showed. Top hit (GDXU-primary/USD-filler, 3.45x) and worst (HIBL-primary/DPST-filler, 0.27x) both involve totally unrelated tickers, no inverse/correlation story. Checking "mutually beneficial regardless of role" (both directions >1.0x): only 12/45 unordered pairs qualify, and 7 of those 12 involve GDXU specifically — traced to GDXU having the smallest standalone return (57%, next-lowest KORU 40%, everyone else 113%+), which mechanically amplifies ratio noise via a small denominator, not a real pairing property.
**Follow-up checks, same session:** (1) SOXS finally got real v5 backtest data this session (user ran the committed sweep; final cliff-safe node: `TrailingExitZScoreBreakout` fixed_sl=2, 73 trades, 11.0% win rate, +113.0% alpha vs SPY). Re-ran SOXS against all 10 watchlist tickers — multipliers ranged 0.27x-1.85x, same noise pattern. (2) Time-sliced SOXL/GDXU/AGQ/NUGT into 5 equal calendar periods (2023-08 to 2026-08) to test luck-vs-consistency directly: most "1.0x" cells were literally zero-fit periods (no filler trades landed in idle windows at all, not a real neutral result); the one pair with real fits across all 5 periods on both sides (SOXL/AGQ) was **consistently a mild drag** (0.72-1.09x, below 1.0 in 4/5 periods each direction) — the only genuinely time-consistent result found all session, and it's negative. (3) Same fifths test on SOXL/SOXS: SOXS only has real data starting 2025-07-31, so 3 of 5 periods are completely empty; the 2 real periods disagree with each other (SOXL-primary/SOXS-filler: 1.16x then 0.86x; SOXS-primary/SOXL-filler: 2.10x then 0.74x) — confirmed the headline "1.85x" SOXS number was built from an n=2 sample that doesn't even agree with itself, though the two sub-period multipliers do compose multiplicatively into the full-period figure (verified algebraically: `mult_full = mult_1 × mult_2 × ...`, so a lucky first period can carry an unlucky second period above 1.0 in the product without that implying real skill).
**Also tested:** SOXL paired against all 16 stable candidates from the same-session 1x-ticker universe screen (see next entry) — same noise pattern (0.25x-2.51x spread), no consistent lift from any 1x pairing either.
**Verdict:** Refuted, with real cross-checked evidence (not just the earlier single-pair anecdotes). The joint-capital/idle-window-filling mechanism has no predictable edge, inverse-pair or otherwise — multiplier outcomes are explained entirely by (a) which specific trade timestamps happen to overlap idle windows (pure scheduling luck), (b) small-standalone-return denominators mechanically amplifying the ratio, and (c) short real history for newer tickers (SOXS, GDXU/NUGT pre-2025) making "consistency" unobservable. Where there was enough real history to actually test consistency (SOXL/AGQ), the result was a small, real, *negative* drag, not a benefit. The individual v5 nodes' own backtested edges are unaffected by this finding — this refutes only the *combination* mechanism, not the underlying strategies.
**Follow-up:** none planned — the pairing/concurrent-additive strategy paradigm idea (raised same night, see prior entry) is closed. Running nodes independently (as the live watchlist already does) is the practical conclusion; no coordination mechanism between tickers showed a reliable payoff. Also raised, not pursued: whether a genuinely different strategy paradigm (not another mean-reversion node) would be worth running during idle capital instead — parked for a future session.

## 2026-08-03 — 1x (non-leveraged) ticker universe screen: USO is a real standout, everything else screens negative alpha
**Hypothesis/question:** Since DUST/ZSL/SOXS/GDXD all showed the same failure mode (2x/3x leveraged daily-reset decay against a strong secular trend in the underlying — DUST -94.2%, ZSL -95.5%, SOXS -96.5% buy-and-hold over the backtest window, vs. their bullish twins NUGT +65.0%/AGQ +111.0%/SOXL +366.9%), does the mean-reversion strategy work better on 1x (non-leveraged) tickers, which don't have that decay confound?
**Method:** `/tmp/universe_screen.py` (ad hoc, not committed) — fetched missing hourly data (yfinance, 730-day max) for 30 candidates (16 sector/index ETFs, 10 commodity/thematic, 5 international), filtered to 16 that passed a buy-and-hold stability check (|bh| < 60%, vs. the ~95%+ decay the leveraged inverses showed), then ran a lightweight backtest grid (2 windows × 3 z-thresholds × 3 fixed_sl × 3 trail_pct × 3 take_profit, `TrailingExitZScoreBreakout`/`open_check`) per survivor, tracking best-alpha combo.
**Result:** Only **USO** (1x crude oil, WTI) cleared positive alpha: **+105.9% alpha vs SPY, 52.2% win rate, 67 trades** (window=10, z=1.5, fixed_sl=3, trail_pct=1, tp=27). Everything else negative, closest to breakeven: XLE (-10.4%), XLV (-11.2%), FXI (-16.9%), DIA (-17.4%). Follow-up window/z sensitivity test (window∈{3,5,7,10,20}) confirmed USO's edge is robust across window 5-10 (not a lucky single-window pick, alpha 105-108% at both), collapses at window=20 (3.1%) — but the same smaller-window idea made XLE/XLV *worse*, not better, so it's ticker-specific, not a general 1x-ticker rule.
**Also tested:** SOXL paired with USO via the joint-capital mechanism — 1.28x (SOXL-primary) vs 0.38x (USO-primary), same role-order instability as every other pair tested this session (see prior entry).
**Verdict:** USO is a genuine candidate worth a real committed v5 sweep (not yet run — see `docs/backlog_cache.md`). The broader "1x tickers avoid the leveraged-decay trap" hypothesis is confirmed directionally (none of the 16 stable 1x candidates showed anything like DUST/ZSL/SOXS's near-total wipeout), but stability alone didn't translate into strategy edge for 15 of 16 — low-volatility, well-arbitraged 1x index/sector ETFs mostly don't move enough to produce a tradeable z-score dislocation.
**Follow-up:** run USO through the real committed sweep (`run_sweep_queue.sh`, user-run); optionally widen the grid on XLE/XLV before fully writing them off (this was only a 2-window, 3-trail_pct, 3-tp lightweight grid, narrower than SOXS's fuller check).

## 2026-08-04 — TQQQ tax structure confirmed clean (no K-1), reopening IRA as a real placement option alongside the existing brokerage position
**Hypothesis/question:** Following the AGQ/USO K-1 discovery earlier this session, is TQQQ (ProShares UltraPro QQQ) also at risk of K-1/UBTI exposure, and separately — does the standing "TQQQ held elsewhere" watchlist note still block trading it in the IRA given wash-sale concerns?
**Method:** Checked ProShares' own tax/filing documentation (web search) for TQQQ's fund structure, same verification pattern used for AGQ/USO.
**Result:** TQQQ is a standard '40 Act equity leveraged ETF (RIC, 1099 distributions) tracking the Nasdaq-100 — not a commodity/currency/volatility ProShares product, which is the specific category that gets K-1/partnership treatment. Confirmed clean, same category as SOXL/NUGT, not AGQ/USO. Separately, user pointed out the cross-account wash-sale risk is conditional, not absolute: it only triggers if the brokerage TQQQ position is sold at a loss within 30 days of an IRA purchase — with no near-term plan to sell brokerage TQQQ at a loss, that risk is currently moot regardless of IRA activity.
**Verdict:** TQQQ is a real, currently-viable candidate for IRA trading (best v5 node: `TrailingBothZScoreBreakout`, fixed_sl=2, open_check, 120 trades, 20.8% win rate, +242.8% robust alpha vs SPY — see this session's node pull) — not blocked by tax structure, and not blocked by wash-sale as long as the brokerage leg stays unsold-at-a-loss. That "as long as" is a standing condition on the brokerage position, not a one-time check.
**Follow-up:** not yet acted on — no IRA node added for TQQQ yet. See `docs/backlog_cache.md`.

## 2026-08-04 — AGQ + USO reserve pairing tested: USO fails as a crash reserve (negative during 2020 COVID), SPY stays the better reserve vehicle
**Hypothesis/question:** Given AGQ and USO are both K-1/UBTI-flagged commodity instruments and get discussed together, does USO make a good skim-and-reserve vehicle for AGQ specifically — better than SPY, since it's at least somewhat correlated to AGQ's own commodity nature?
**Method:** (1) `sim_real_skim_reserve.py AGQ --reserve-ticker USO` vs `--reserve-ticker SPY` over AGQ's real 2023-07/2026-08 trade history. (2) Generalized `sim_downturn_reinvest_vs_hold.py` (was SOXL/SPY/SSO-hardcoded) to take `--strategy-ticker`/arbitrary `--reserve-ticker`, fetching USO's own real long-history data directly (1x, no leverage synthesis needed, real yfinance coverage since 2006) rather than a synthesized proxy. Ran AGQ-strategy against both USO and SPY reserves across the 3 synthetic crash windows (2008 GFC, 2020 COVID, 2022 bear).
**Result:** Real full-history test: USO reserve overlay actually beat SPY (+468.4% vs +361.3%) — but that's USO's own 2023-2026 bull run, not a defensive property. Crash-window test reversed this: SPY reserve outperformed USO in 2 of 3 crashes (2008 GFC: SPY hold +75.9% vs USO hold +19.6%; 2020 COVID: SPY hold +18.5% vs USO hold **-14.3%**, negative), and only lost narrowly to USO in the 2022 bear (inflation/energy-driven divergence that year). USO going negative during 2020 COVID confirms the predicted failure mode — oil demand collapsed alongside equities that crash (WTI briefly went negative), so USO doesn't provide the "safe parking" a reserve vehicle needs during exactly the drawdown it's meant to protect through.
**Verdict:** Refuted — USO is not a good reserve vehicle for AGQ (or presumably any other skim-reserve node). SPY remains the better default reserve, consistent with the earlier SPY-vs-QQQ/SSO/UPRO/QLD/TQQQ sweep. USO's real-full-history "win" was a distinct effect (its own price appreciation, unrelated to crash protection) — the two shouldn't be conflated.
**Follow-up:** none planned — closes the AGQ/USO reserve-pairing question. `sim_downturn_reinvest_vs_hold.py`'s generalization (any strategy ticker with a `PROXIES` entry x any reserve ticker) is reusable for future reserve-vehicle candidates.

## 2026-08-04 (evening) — z-score entry velocity/overshoot audit (AGQ) — no correlation with trade outcome, first pass
**Hypothesis/question:** Raised while reasoning about SOXL's weak uptrend performance vs. AGQ's strength in downturns — does the *shape* of a trade's approach into the z-score trigger (a sharp, fast single-bar breach vs. a gradual multi-bar grind across it) predict outcome, distinct from whether it crossed the threshold at all? Motivated by the idea that a violent breach looks more like the start of a real crash leg (keeps falling / slow to bounce) than normal chop finding its floor.
**Method:** `scripts/z_entry_velocity_audit.py` (new). For AGQ's real v5 research node (`TrailingExitZScoreBreakout`, window=10, z=1.0, watchlist_id=65, id=86), reused `scripts/export_trades.py`'s `simulate_trail_exit_chaos` (zero miss rate, same trusted mirror `entry_timing_seasonality.py` uses) to get the real trade list, reconstructed the full z-score series, and computed per-trade `overshoot` (how far past -z_thresh the signal fired) and `velocity` (z[signal] - z[signal-3 bars]). Correlated both against return (Spearman) and split into sharp/gradual terciles by velocity (Mann-Whitney on returns, Fisher exact on win/loss).
**Result:** n=118 real trades. velocity vs. ret: rho=-0.002, p=0.984. overshoot vs. ret: rho=-0.084, p=0.365. Sharp tercile (n=39): 20.5% win rate, mean ret +0.09%. Gradual tercile (n=40): 20.0% win rate, mean ret +1.64% — directionally favors gradual but Mann-Whitney p=0.611, Fisher p=1.0, not significant.
**Verdict:** No evidence, first pass — breach steepness/overshoot alone doesn't predict AGQ trade outcome at n=118. Doesn't yet address the volatility-vs-direction framing that prompted this (SOXL uptrend weakness vs. AGQ downturn strength) — this only tested one narrow proxy (3-bar velocity into a single ticker's signal), not the broader vol/slope/churn-rate regime-characterization discussed the same evening.
**Follow-up:** explicitly a first pass meant to be iterated on (user's framing). Candidates: run across more/all v5 tickers pooled for power; try different velocity lags (1-bar, 5-bar) since 3 may not be the right window; restrict specifically to down-trending regimes rather than all trades (the original question was about crash/downturn behavior, not the general case); and this doesn't yet connect to the still-unbuilt structural regime script (volatility, trend slope, z-crossing rate, z-saturation vs. win rate/alpha) discussed the same evening — see `docs/backlog_cache.md`.

## 2026-08-04 (evening, later) — chop-cluster persistence scan (all 10 v5 tickers) — pooled "significant" result is a cross-ticker pooling artifact; no robust per-ticker signal
**Hypothesis/question:** Follow-on to the z-entry-velocity audit — instead of asking whether one trade's breach shape predicts its own outcome, does a ticker's choppy state *persist*? After two quick round-trip entries/exits close together in time, is a third trade more likely to also win (chop clustering has momentum), or is each trade independent?
**Method:** `scripts/chop_cluster_scan.py` (new). Signal-frequency problem: the live daemon only checks 2 fixed daily windows, which starves this question of examples (most real trades are days/weeks apart). Deliberately decoupled from that live constraint for research purposes only: built every-bar-signal-checking mirrors of `simulate_trail_exit_chaos`/`simulate_trail_both_annotated` (same SL/TP/TIME/gap-through-trigger exit mechanics, only the `h != target_h0 and h != target_h1` signal-window gate removed) — not a live proposal, purely to see how choppy each ticker's own price action is at full hourly resolution. Grouped consecutive trades into clusters wherever the gap between one trade's exit and the next's entry signal was <=8 bars (~1 trading day), then tested win rate/return by position-within-cluster.
**Result:** Every-bar checking increased signal count far less than expected (AGQ: 130 trades vs. 118 under the 2-window version — ~10%, not a multiplier) — the entry condition (price crossing the z-band) is the real bottleneck, not check frequency; more frequent checking mostly changes *which* bar catches an already-rare crossing, not how often one occurs. Pooled across all 10 tickers: 1,263 trades, 203 clusters (39 singletons, up to a 32-trade HIBL cluster) — chop clustering clearly happens. Pooled Spearman (position vs. return, clusters len>=3, n=1004): rho=0.192, p<0.001, looked like a real finding. **Split per-ticker, this doesn't hold**: only SOXL individually significant (n=207, rho=0.189, p=0.006); the other 8 tickers with enough data are weak/non-significant (p=0.10-0.68), UDOW is directionally negative (rho=-0.068). The pooled p<0.001 is a cross-ticker pooling artifact (different base win rates/cluster lengths per ticker mixing into a spurious trend), not a real effect.
**Verdict:** Refuted as a general/portfolio-wide effect. SOXL's individual result (p=0.006) is the one candidate worth a second look, but doesn't survive an implicit multiple-comparisons read (1 of 10 tickers tested, and cluster position is correlated with ticker identity here since cluster lengths differ by ticker) — not strong enough to act on alone.
**Follow-up:** if SOXL's result is worth chasing, retest it in isolation with a pre-registered gap threshold (8 bars was a round-number guess, not tuned) rather than the current post-hoc multi-ticker scan. The original vol/slope/churn-rate structural-regime idea from earlier the same evening is still unbuilt and is a different, still-open angle on the same underlying SOXL-uptrend-weak/AGQ-downturn-strong question.

## 2026-08-04 (evening, later still) — structural-regime regression (vol/slope/z-crossing-rate/z-saturation vs. return) — no signal, cleanly controlled
**Hypothesis/question:** The direct test of the evening's original framing (SOXL weak in its own uptrend vs. AGQ strong in its downturn) — does realized volatility, trend slope, z-crossing frequency, or z-saturation (time pinned past the trigger) predict trade return, once properly controlled for collinearity between the four and for cross-ticker pooling artifacts (the chop-cluster scan's failure mode earlier this evening)?
**Method:** `scripts/regime_structural_scan.py` (new). Real v5 two-daily-signal-window trades (same trusted mirrors as `entry_timing_seasonality.py`, not the chop-cluster scan's every-bar variant). For each trade, computed 4 features using only bars strictly *before* the signal (no leakage): `vol_20`/`slope_20` (realized vol / OLS trend slope over the trailing 20 bars), `cross_rate_60`/`sat_frac_60` (z-threshold crossing frequency / time-pinned-past-threshold fraction over the trailing 60 bars). Checked collinearity via VIF before trusting any regression coefficients, then ran a pooled OLS (`ret ~ features + ticker fixed effects`) — fixed effects specifically to prevent the cross-ticker pooling artifact found in the chop-cluster scan.
**Result:** n=854 trades, 10 tickers. VIF all <1.6 (no meaningful collinearity, all 4 features independently interpretable) — vol_20/slope_20 do correlate with each other at the raw level (rho=-0.503, expected: higher vol tends to come with less-clean directional slope) but not enough to distort the regression. Per-ticker univariate Spearman: no ticker shows any feature significant at p<0.05 (all p=0.12-0.97 except USD's n=14, too thin to trust). Pooled OLS with ticker fixed effects: none of the 4 regime features significant (p=0.10-0.88), R²=0.014 (essentially no explanatory power). The only significant regression term was USD's own ticker-level intercept dummy (p=0.004) — a base-rate difference, not a regime effect.
**Verdict:** Refuted, cleanly this time (collinearity checked, cross-ticker pooling controlled via fixed effects, per-ticker checked individually) — none of realized volatility, trend slope, z-crossing rate, or z-saturation predicts trade return in this v5 trade set. Combined with tonight's two earlier negative/non-robust passes (breach velocity/overshoot, chop-cluster position), the "strategy edge tracks volatility/churn more than direction" hypothesis has now had three independent, reasonably well-controlled tests and found nothing at real trade counts (~850-1250 trades depending on the pass).
**Follow-up:** the original SOXL-uptrend-weak/AGQ-downturn-strong observation that prompted this whole evening thread is still unexplained — none of these three angles (breach shape, chop persistence, regime regression) accounts for it. Possible remaining explanations not yet tested: it could be a ticker-specific effect unrelated to regime (e.g. AGQ/SOXL have structurally different node configs — different z-thresholds, hold times — not just different underlying assets), or simply noise given each ticker's own trade count is only 100-230. Not immediately obvious what the next test should be; needs fresh thinking before continuing this thread further.

## 2026-08-04 (evening, later still) — config cross-apply test (AGQ <-> SOXL) — edges survive a config swap; but SOXL isn't actually weak in the full v5 backtest, undercutting the thread's premise
**Hypothesis/question:** 4th pass on the evening's SOXL-weak/AGQ-strong thread. Checking node configs directly surfaced something the first 3 passes never controlled for: AGQ and SOXL don't just differ by underlying asset -- AGQ runs `TrailingExitZScoreBreakout` (immediate market-buy), SOXL runs `TrailingBothZScoreBreakout` (trailing-buy-wait), with different exit params. Is the ticker-level performance difference actually config-driven rather than asset/regime-driven?
**Method:** `scripts/cross_apply_config_test.py` (new). Used `run_backtest_dispatch` (the real trusted dispatcher, not a pure-Python mirror) to run each ticker's real v5 config against BOTH tickers' own price data -- home cell (own config/own data) plus 2 cross-applied cells.
**Result:** AGQ/AGQ (home): 113 trades, 23.9% win rate, +589.9% compounded. AGQ data / SOXL config: 113 trades, 22.1% win rate, +260.7% -- still strongly positive, similar win rate. SOXL/SOXL (home): 157 trades, 16.6% win rate, **+1195.9% compounded**. SOXL data / AGQ config: 178 trades, 12.4% win rate, +829.8% -- also still strongly positive. Both tickers' edges survive a full config swap (same order of magnitude, not destroyed) -- refutes "it's config, not asset."
**But:** SOXL's own home-config result (+1195.9%) is the *best* of all four cells, not weak -- directly contradicts the "SOXL poor performance when the stock is going up" premise that started this entire evening thread (this entry, plus the z-entry-velocity, chop-cluster, and structural-regime entries above).
**Verdict:** Config-driven explanation refuted (edges are asset-driven, not config-driven) -- but more importantly, the original observation may not be a backtest-observable phenomenon at all. Flagged to the user: the "SOXL weak" observation needs to be pinned down to its actual source (full v5 backtest vs. real/paper live trading, a much smaller, execution-constrained, possibly recent-period-only sample) before a 5th pass, since all 4 tests tonight ran against the idealized full-history backtest -- if the real observation was from live execution, none of tonight's methodology could ever have found it.
**Follow-up:** awaiting user clarification on where the original SOXL-weak observation came from before continuing.

## 2026-08-04 (late) — two real bugs found and fixed in tonight's 5 research scripts (+ a 6th, already-committed script); corrected numbers confirm the original AGQ/SOXL observation, but reveal 2026 is an outlier year for both tickers relative to their own historical pattern
**Context:** User challenged the "backtest doesn't support your observation" conclusion from the cross-apply-config pass, correctly recalling real prior numbers (SOXL -40.8% YTD 2026, AGQ +68.3% YTD 2026 after-tax) that contradicted it.
**Bug 1 (take_profit/arm_sell_pct column):** `watch_list` stores the arm-sell threshold in `arm_sell_pct` for `TrailingBothZScoreBreakout` nodes but in `take_profit` for `TrailingExitZScoreBreakout` nodes (documented convention, `signals_db.py:983-988`, never both populated on one row). All 5 of tonight's scripts only selected `arm_sell_pct` and hardcoded a disabling value (1.0/100.0) for TrailingExit nodes -- silently wrong for AGQ, DPST, KORU, NUGT, UDOW, YANG (every TrailingExit-strategy ticker on watchlist 65). Verified directly: AGQ's real best-swept node uses `take_profit=8`, not disabled; rerunning with the correct value flipped AGQ's 2026 YTD compounded return from -22.2% to +68.2%, matching the previously-reported +68.3% almost exactly.
**Bug 2 (open_check entry timing), found independently by both a direct trade-count/return comparison and a cold Opus review of the fix:** every real v5 research node uses `entry_timing='open_check'`, but none of tonight's 5 scripts (nor the pre-existing committed `scripts/entry_timing_seasonality.py`, nor `scripts/multiday_cycle_entry_test.py`) ever read `entry_timing` from the DB or passed `open_check=True` to the trade-simulation mirrors, which default to `open_check=False`. Verified directly: passing `open_check=True` explicitly for AGQ made the pure-Python mirror match the trusted numba kernel exactly (68.2% both ways); without it, results silently used close-based entry timing instead of the real open-based timing every v5 node actually runs.
**Fix applied:** all 5 of tonight's scripts, plus `scripts/entry_timing_seasonality.py` (committed, has a published 2026-08-03 finding -- see caveat below), now read `entry_timing`/`take_profit` correctly and unify into one `arm_pct` field per the documented strategy-conditional convention. Also fixed (found by the same Opus review): 3 of the 5 scripts were counting the trade-simulation mirrors' final still-open (`result=OPEN`) mark-to-market row as a completed trade -- most consequential for `period_trend_vs_strategy_return.py`, since that row always falls in the most recent (2026 YTD) period, exactly the window this whole thread cares about.
**Corrected result (period_trend_vs_strategy_return.py, all fixes applied):** AGQ 2026 YTD compounded +52.3% (42 trades), SOXL 2026 YTD compounded -30.8% (33 trades) -- confirms the user's original observation in direction and rough magnitude (the earlier, pre-bug-fix run had shown AGQ *negative* for 2026, which was wrong). Pooled Spearman across all 10 tickers/105 periods is STILL positive (rho=+0.539, p<0.0001, survives ticker fixed effects: price_ret coefficient +0.095, p=0.006) -- strategy return generally tracks WITH a ticker's own price trend, not against it, confirmed even after both bugs were fixed. AGQ's and SOXL's own per-ticker historical correlations are also both positive (AGQ rho=+0.462 p=0.112 n.s.; SOXL rho=+0.714 p=0.006) -- meaning **2026 itself is the anomaly for both tickers relative to their own normal pattern**, not an example of it: AGQ's usual positive price-trend/strategy-return relationship inverted this year (price fell, strategy still won big), and SOXL's usual positive relationship also inverted (price soared, strategy still lost).
**Verdict:** The original SOXL-weak/AGQ-strong 2026 observation is real and now confirmed under corrected data -- but it's not explained by the general cross-ticker/cross-time relationship between price trend and strategy return (which runs the opposite direction, and did even before the bugs were fixed). 2026 is a genuine outlier year for both tickers specifically, still unexplained.
**Follow-up:** `scripts/multiday_cycle_entry_test.py` had the same two-bug pattern in its `get_trades`/`load_nodes` helper (fixed tonight) but on inspection, that helper is only ever called as a phase-vs-outcome follow-up when a ticker's core cycle test clears p<0.05 -- the core test itself runs directly on the raw price-return series, independent of node config. Since no ticker cleared that bar in the original 2026-08-03 run, the buggy path was never actually exercised -- **that published finding (no real multi-day cycle in any v5 ticker) is unaffected**, unlike initially feared. `entry_timing_seasonality.py`'s 2026-08-03 finding (14:30 window fires ~3x more signals than 9:30, no significant win-rate difference) DOES use the buggy path for its entire result -- code is now fixed but the finding itself was not rerun tonight, still needs re-verification.

**All 5 of tonight's scripts rerun with both fixes applied, full results:**
- `period_trend_vs_strategy_return.py`: AGQ 2026 YTD +52.3% (42 trades), SOXL 2026 YTD -30.8% (33 trades) -- see main entry above.
- `cross_apply_config_test.py` (already had open_check correct, only needed the TP fix): AGQ home +1242.6% (129 trades, 31.8% win rate, up from the pre-fix 589.9%); SOXL data / AGQ config dropped from the pre-fix (wrong) +829.8% to a corrected +44.4% (210 trades, 21.9% win rate) -- AGQ's real (TP=8) config is actually much less robust on SOXL's data than the old, wrong (TP=disabled) config suggested. AGQ data / SOXL config +236.5% (112 trades, 21.4% win rate) and SOXL home (+1195.9%) were already directionally correct; all four cells corrected once more 2026-08-04 (very late, session-wrap review) after an independent Opus review found `run_backtest_dispatch`'s raw output was never filtered for the trailing `Result='OPEN'` mark-to-market row (the same bug class 3 from earlier, a different door) -- AGQ's two cells shifted modestly (1382.7%->1242.6%, 260.7%->236.5%; SOXL's two cells were unaffected, no OPEN row for that node in this window). Conclusions unchanged: AGQ's edge is real and large, still much less portable onto SOXL's data than SOXL's own edge is.
- `chop_cluster_scan.py`: per-ticker chop-cluster-position-vs-return Spearman, clusters len>=3 -- AGQ n=144 rho=+0.159 p=0.058, DPST n=137 rho=-0.079 p=0.356, GDXU n=79 rho=-0.018 p=0.872, HIBL n=182 rho=+0.090 p=0.225, KORU n=76 rho=+0.242 p=0.035, NUGT n=31 rho=+0.237 p=0.199, SOXL n=202 rho=+0.046 p=0.514, UDOW n=67 rho=+0.051 p=0.680, YANG n=45 rho=+0.063 p=0.680. **Notable reversal**: pre-fix, SOXL was the one ticker that looked individually significant (p=0.006); post-fix, SOXL is no longer significant (p=0.514) and KORU is now the closest (p=0.035, still not Bonferroni-robust across 9 tickers). Net conclusion unchanged (no robust chop-cluster-persistence effect), but which ticker looked closest to significant was itself a bug artifact.
- `regime_structural_scan.py`: still no signal after the fix (R²=0.013, all 4 features p>0.17, pooled OLS with ticker fixed effects) -- conclusion unchanged.
- `z_entry_velocity_audit.py` (AGQ): still no significant correlation (n=128, velocity rho=0.136 p=0.125, overshoot rho=-0.058 p=0.515) -- conclusion unchanged, though AGQ's win rate is now visibly higher throughout (consistent with the TP fix generally helping AGQ's numbers).

## 2026-08-04 (late) — entry_timing_seasonality.py re-verified after the take_profit/open_check fix; original finding confirmed, more solidly null
**Method:** Reran `scripts/entry_timing_seasonality.py` (now fixed -- see prior entry) to re-verify its 2026-08-03 finding, since that finding used the buggy code path for every ticker.
**Result:** Pooled 9:30-bar (n=232) vs. 14:30-bar (n=645) -- ~2.8x more signals in the afternoon window, matching the original ~3x imbalance finding. Win rates now nearly identical (23.3% vs 22.9%, was 16.5%/18.8% pre-fix), mean returns still lean slightly toward the afternoon (2.15% vs 2.99%). Pooled Mann-Whitney p=0.9407, Fisher p=0.9277 -- both *less* significant than the original run (p=0.199/0.536).
**Verdict:** Original 2026-08-03 finding confirmed under corrected data, if anything more solidly null. No re-verification concern remains for this script.

## 2026-08-04 (late) — near-miss retracement test (AGQ/SOXL) — refuted, wrong direction and not 2026-specific
**Hypothesis/question:** Speculative hunch on the still-open 2026 AGQ/SOXL divergence -- do SOXL's 2026 losing trades never get close to reverting (consistent with a low-noise grinding rally), while AGQ's 2026 losses nearly recover before the exit (consistent with two-sided chop even in decline)?
**Method:** `scripts/near_miss_retracement_test.py` (new). For every LOSS/TLOSS trade (real v5 trade lists, both bugs from earlier tonight already fixed), computed `peak_unrealized_ret` = the best price reached (max High) between entry and exit relative to entry price -- how close the position got to profitable before being closed. Compared AGQ vs SOXL 2026, and each ticker's 2026 vs its own pre-2026 baseline (the within-ticker comparison controls for any fixed per-ticker difference).
**Result:** AGQ 2026 losses: mean peak_unrealized_ret +1.67% (n=28). SOXL 2026 losses: mean +5.12% (n=29). Mann-Whitney p=0.0007 -- highly significant, but SOXL's losses got *closer* to recovering, the opposite of the hypothesized direction. Within-ticker 2026-vs-pre-2026: AGQ p=0.239, SOXL p=0.259 -- neither ticker's own 2026 differs from its own historical pattern on this metric. SOXL's baseline (pre-2026) already runs higher than AGQ's baseline (4.4% vs 2.3%), consistent with SOXL simply being a higher-volatility 3x fund vs. AGQ's 2x, a standing structural difference, not something new in 2026.
**Verdict:** Refuted -- both in direction (opposite of hypothesized) and in relevance (the AGQ/SOXL gap on this metric predates 2026 and isn't what changed this year). Does not explain the 2026 anomaly.
**Follow-up:** none planned -- sixth negative/non-explanatory test on this specific question tonight (velocity, chop-cluster, regime regression, config cross-apply, and now near-miss retracement). The 2026 AGQ/SOXL divergence remains a real, confirmed, unexplained anomaly.

## 2026-08-04 (late) — data-integrity check + regression-residual reframe: SOXL is a massive singular 2026 outlier, AGQ's 2026 is not particularly anomalous once controlled
**Context:** User pushed back on treating the AGQ/SOXL anomaly as closed after 5 negative tests -- two new angles tried: (1) is the extreme AGQ 2026 price action a data artifact (reverse split, bad print)? (2) is 2026 a broad regime shift affecting many tickers, or specific to AGQ/SOXL?
**Data-integrity check:** AGQ's daily bars show a violent decline 2026-01-29 to 2026-02-05 ($399 -> $122, several >15%/day moves including a -48.2% single day). Checked for a reverse-split signature: real, substantial volume throughout (8-16M shares/day, no gaps or zero-volume bars), no clean split ratio, no `data_mutation_log` entry for AGQ. **Conclusion: real, extraordinarily volatile price action, not a data bug** -- rules out the data-integrity hypothesis, confirms the underlying move was genuine.
**Regime-breadth check:** Computed residuals (actual strat_ret - predicted) from the existing pooled OLS (`price_ret + ticker fixed effects`, from the period-trend regression). Mean |residual| by year: 2023=0.203, 2024=0.176, 2025=0.244, 2026=0.314 -- 2026 does show elevated unpredictability overall. But broken out by ticker, **2026 residuals are dominated entirely by SOXL** (mean -0.521, n=3 periods) -- every other ticker's 2026 residual is small and within the normal range seen in other years (-0.08 to +0.11, comparable to 2025's spread). AGQ's 2026 residual is +0.014, essentially zero.
**Verdict:** Reframes the whole question. This is NOT a symmetric "AGQ good / SOXL bad" story -- AGQ's 2026 performance is largely *explained* by the general price-trend model plus its own (already weak/non-significant) typical relationship; it isn't the anomaly it first appeared to be. **SOXL alone is the real, massive, singular outlier** -- a residual roughly 5-10x larger than any other ticker/year combination in the dataset. The search should narrow to "what's different about SOXL specifically in 2026," not a general AGQ-vs-SOXL divergence story.
**Follow-up:** open -- next natural step would be a SOXL-specific investigation (news/sector catalyst check for 2026's semiconductor rally character, or a closer look at whether SOXL's specific node config/leverage interacts unusually with this particular rally's shape) rather than further AGQ/SOXL comparative tests.

## 2026-08-04 (very late) — the mechanical vol-vs-return finding decisively debunked as a frequency artifact; drought-detection test (twice-daily vol percentile) gives no advance warning, coin-flip exit warning
**Context:** User correctly challenged the period_volatility_regression.py "finding" -- strat_ret is forced to exactly 0.0 in zero-trade weeks, so any positive-average-edge strategy will mechanically show higher period returns in weeks with more trades, independent of whether volatility makes each trade *better*.
**Decomposition test:** Regressed (a) n_trades ~ vol (ticker FE): coef=0.459, p<0.0001, R²=0.155 -- vol strongly predicts trade frequency, as expected mechanically (the z-band entry condition requires enough price movement to breach it). (b) avg per-trade return (geometric mean, conditional on >=1 trade that week) ~ vol (ticker FE): coef=0.0041, p=0.554, not significant. **Verdict: the period-level "vol predicts return" result is entirely a frequency artifact, not evidence volatility improves trade quality** -- this actually confirms (rather than overturns) the original trade-level `regime_structural_scan.py` null result. The real mechanism: the strategy can't make money without opportunities to trade, and opportunities require volatility by construction of the z-threshold entry rule -- a near-tautology, not a deep regime insight.
**Drought-detection test (`scripts/drought_detection_test.py`, new):** User proposed testing whether a low-latency, twice-daily (9:30/14:30 checkpoint) realized-volatility read -- decoupled from whether a trade fires -- could give useful advance warning of a drought (extended no-trade stretch) starting or ending, as a basis for a lower-capital options overlay during suspected smooth regimes (explicitly NOT a z-threshold retune, to avoid single-episode curve-fitting). Tested pooled across all 185 real historical droughts (>=10 trading days, all 10 v5 tickers), using trailing 5-day realized vol and an expanding-window (no-lookahead) percentile against each ticker's own history.
**Result:** Detection lead time (advance warning before a drought starts): mean 0.2 days, median 0.0 -- only 10/185 droughts had any advance warning at all. Recovery lead time (advance warning before a drought ends): 93/185 (50%) droughts showed the vol percentile crossing back above 0.50 before the drought's actual end, with mean lead time 8.1 days when it did fire; the other 50% gave no advance recovery signal.
**Verdict:** This specific detection mechanism cannot predict when a drought will start (would need a reactive trigger like "N days since last trade" instead), and is only moderately useful (about half the time, ~8 days lead when it works) for signaling when to exit before normal trading resumes. Not strong enough on its own to build a low-risk options overlay around, but not nothing either -- a real, quantified answer to "when will the smoothness give out and how fast can we pick it up," rather than an untested assumption.
**Follow-up:** open. If the options-overlay idea is pursued further, the entry trigger would need to be reactive (days-since-last-trade based) rather than predictive, and the exit-timing benefit of this vol-percentile signal (50% hit rate, 8-day lead) would need to be weighed against option premium cost/decay during both the successful and failed detection cases.

## 2026-08-04 (very late) — drought-overlay full thread: mechanics validated, gap-risk quantified and shown to be underlying-specific, put-hedge identified as the structural fix
**Context:** Continuation of the SOXL-weak/AGQ-strong 2026 investigation. Confirmed mechanism (drought_detection_test.py, prior entry): SOXL's weak 2026 comes from ~zero trades during a smooth 6-week rally, not bad trades during it. This thread tests whether a real, deployable overlay could capture that missed opportunity.
**Design evolution (per conversation, each step a real correction, not a restart):** (1) naive buy-and-hold through the whole drought to the next real signal -- rejected, open-ended risk. (2) SL/trailing-stop applied immediately from entry, reusing the core strategy's own fixed_sl/trail_sell_pct -- tested, pooled compounded -76.8%, mean win only +4.31% (trail engaging immediately caps winners too early). (3) **Correct version**: reuse the core strategy's actual arm-then-trail exit state machine (tight SL protects immediately; trailing stop only engages after price clears an arm threshold, letting real winners run) -- this is not a new design, it's literally `TrailingBothZScoreBreakout`'s own mechanic, just triggered by a drought-confirmation entry (10 trading days with no signal) instead of a z-band signal.
**Result on the 10 v5 watchlist tickers:** win/loss ratio improved to 5.41x (mean win +18.79%, mean loss -3.47%) but pooled win rate only 10% (below the ~15.6% breakeven this ratio needs) -- pooled compounded still -76.9%. **SOXL specifically: +30.2% compounded (13 trades, 15.4% win rate)** -- but inspecting the trade-by-trade list, this is driven by exactly 2 of 13 trades (the two TRAIL exits, +34.9% and +34.1%); pull either out and it flips negative like everything else. Fragile, not a broad-based edge.
**Real risk found, not initially modeled:** several tickers show SL losses far exceeding the nominal stop width due to gap-through-trigger fills -- KORU -33.64% (nominal 3%), AGQ -14.93% (nominal 2%), HIBL -8.26%, YANG -9% to -12%. Buying into a suspected-stable run has less cushion against a sudden violent reversal (e.g. AGQ's real -48% single day) than the core strategy's dip-entries.
**Tested against the "big 6" (SPY, SSO, UPRO, QQQ, QLD, TQQQ), using each ticker's real best-swept v5 config from `backtest_cache`:** gap-through excess (actual SL loss minus nominal SL%) mean=0.23%, max=2.03% across 17 SL exits -- vs. the original 10 leveraged sector/commodity tickers' mean=1.26%, max=30.64% across 74 SL exits. **The fat gap-risk tail is concentrated in the volatile, thinner, idiosyncratic leveraged sector/commodity names (silver, Korea, China, semis), not the broad, deep, liquid index-tracking leveraged funds.** TQQQ specifically: core strategy alone compounds 497.7% (185 trades); adding its 8 overlay trades (+0.14% alone) moves the combined total to only 498.5% -- a +0.81 percentage point delta, essentially negligible, since TQQQ's core strategy already captures almost everything (few long droughts to begin with).
**Put-hedge idea (not backtested -- no historical options data available; yfinance only serves live option chains, no historical time series):** a long put converts the gap-through tail risk into a defined, capped maximum loss, unlike a stop order which can always be gapped through -- a structural fix for exactly the risk just quantified, not a probabilistic improvement. Real, useful side insight: since droughts are low-realized-vol periods, and implied vol tends to track realized vol with a lag (volatility clustering/mean-reversion), buying puts specifically during a detected drought should tend to buy protection near its cheapest point of the year -- before the market has repriced risk higher, which typically only happens after the reversal begins. Caveat: realized/implied vol are correlated but not identical (IV can jump ahead of known events even during a realized-vol lull).
**Verdict, overall:** the drought-overlay concept is mechanistically sound (reuses proven exit mechanics) and has real evidence of ticker-dependent value (SOXL positive though fragile; TQQQ negligible; broad-index names show much lower gap-risk than sector/commodity names), but is NOT validated as a deployable strategy -- small samples throughout (13 SOXL trades, 8 TQQQ trades), no historical options data to price the put-hedge fix, and the core payoff-ratio-vs-win-rate economics are still marginal even in the corrected design.
**Follow-up, all open:** (1) put-hedge economics need either a rough Black-Scholes approximation (assumed IV) or waiting for real options-data infrastructure. (2) if pursued further, needs a much larger sample of droughts (more tickers/history) before trusting SOXL's 2-trade-driven result as real. (3) the broad-index vs. sector/commodity gap-risk split suggests this whole idea might be better suited to SPY/QQQ-family tickers specifically, even though their core strategy edge is smaller than the leveraged sector names -- an explicit tradeoff (smaller edge, much safer tail) not yet weighed.
Scripts: `scripts/drought_detection_test.py`, `scripts/drought_buy_hold_test.py` (superseded, naive version), `scripts/drought_overlay_test.py` (current, correct version).

## 2026-08-04 (very late) — paper-vs-backtest reconciliation investigation: root cause found (unsynchronized starting boundary + legitimate price-source difference), leads to a real two-account paper-trading design
**Context:** `scripts/paper_vs_backtest_reconcile.py` (new tonight) found HIBL/USD/SOXL/YANG showing real trade-count/direction divergence between paper trading and a full-history backtest replay for the same node config. Investigated with `scripts/paper_signal_intrabar_check.py` (new tonight, uses paper's real captured `signal_price` as ground truth, and a new `trace=` parameter added directly to `scripts/export_trades.py`'s trusted `simulate_trail_exit_chaos` function -- not a hand-copied reimplementation, which had already produced one wrong conclusion earlier in the same investigation).
**Root causes found, in order of discovery:**
1. An ad hoc "last trade before window" search script (written mid-investigation) was itself wrong -- it claimed YANG's backtest was flat with zero signals after 2026-07-14, when the real trusted trace showed it was actually in an open, trailing position since 2026-07-20. A reimplementation disagreeing with the trusted function is not evidence the trusted function is wrong.
2. **The real root cause**: paper trading and the full-history backtest replay are two independently-sequenced single-position simulations that were never synchronized at a common starting boundary. Paper trading has a real, known start date (~2026-07-22, first `paper_trade_log` row); the backtest replay runs continuously from the start of cached history (2023). Whatever position the backtest happened to be holding on paper's actual start date is arbitrary relative to paper's real (flat) starting state -- once unsynchronized, trade-count comparisons over any calendar window are not meaningful, independent of any bug.
3. **A second, legitimate (not a bug) divergence source**: verified directly in `signals_compute.compute_buy_signal` -- SMA/Std indicators use the identical `generate_daily_indicators` method and "prior completed day" cutoff as the backtest (confirmed matching), but the live/paper price compared against the threshold is a real-time 1-minute tick (`yf.Ticker(ticker).history(period='1d', interval='1m', prepost=True)`), not the backtest's fixed hourly-bar Close. A real, structural difference in price source, not a bug.
**Design decision reached (not yet built)**: rather than choosing between "let paper diverge" and "periodically reset it," run two separate paper-tracking accounts per ticker: **Account A** (existing, unmodified) never resets, accumulates real trade history under real market conditions -- validates actual portfolio performance over time, divergence from backtest is expected and is the point. **Account B** (new) periodically resets to the backtest's real state and uses the backtest's hourly-bar-close price for its signal check instead of a live tick, so any divergence remaining is provably attributable to price-availability timing only, not accumulated drift from unsynchronized starting conditions or anything else -- a controlled comparison isolating exactly one variable.
**Not yet built**: Account B requires a real change to `paper_trading.py`/`signals_compute.py` (an hourly-bar-close price mode for the signal check, plus a periodic reset mechanism) -- a second research-mode `watch_list` node per ticker fits the existing `wl_id`-keyed paper-trading model. Also not yet rebuilt: `paper_vs_backtest_reconcile.py` itself needs the synchronized-starting-boundary fix (per-ticker: sync flat at today if currently flat, sync in-trade at the real entry if currently holding a position) before it produces a trustworthy comparison for any ticker.
**Process note**: this investigation is also why `scripts/export_trades.py`'s `simulate_trail_exit_chaos` gained a `trace=(start_ts, end_ts)` parameter tonight -- prints the real function's own bar-by-bar decisions instead of requiring a fresh, less-trustworthy reimplementation every time this kind of question comes up. Should get an independent review before being fully trusted, since it modifies a file every research script tonight depends on.
**Session-wrap review correction**: an independent Opus review of `export_trades.py`'s `trace` addition (safe, empirically verified byte-identical for existing callers) also found a real HIGH-severity bug in `paper_signal_intrabar_check.py` -- it compared paper's real signal against the hourly bar matching its wall-clock hour, but the daemon's ambient close-window checks (hours 10 and 15) actually evaluate the *prior* bar (hours 9 and 14 -- "hourly bars are labeled by start time"). This affected the intermediate "both agree, must be a reconcile-script bug" read live in conversation. Fixed (`_evaluated_bar_hour` helper) and rerun: all 3 YANG signals still show a correct crossing on the properly-evaluated bar, confirming the final root cause above (unsynchronized starting boundary, backtest in an open position since 2026-07-20) still holds -- the bar-mapping bug was real and is fixed, but didn't change the conclusion already reached. Also fixed in the same pass: `multiday_cycle_entry_test.py`'s `phase_vs_outcome` (bug class 3 recurrence, never actually exercised per the original run so no published finding affected), `cross_apply_config_test.py` (bug class 3 recurrence -- corrected AGQ home from +1382.7%/130 trades to +1242.6%/129 trades, see this file's earlier entry), and `paper_vs_backtest_reconcile.py`'s asymmetric OPEN-counting + date-truncation bugs (both scripts' final-day date-string comparisons also fixed).

## 2026-08-05 (late) — SOXL drought-overlay parameter sweep: real grid search finds a cliff-safe cell with much broader ticker participation than the original SOXL-only result, but reveals a real, unfixed blind spot -- cliff-safety doesn't catch ticker-concentration risk
**Hypothesis/question:** the 2026-08-04 manual sweep moved fixed_sl/trail_sell_pct together in lockstep and found "wider is directionally better" (pooled compounded -76.9% -> +4.3%), but SOXL's one positive result was 2-of-13-trades fragile -- a real grid search over independent axes, screened for grid-neighbor cliff-safety, was needed to tell a real edge from an overfit peak (per `docs/design.md`'s "SOXL drought-overlay parameter sweep" design).
**Method:** `scripts/drought_overlay_sweep.py` (new) -- 4 independent axes (fixed_sl% in {1,2,3,5,7,10,15,18}, arm_pct% in {5,10,15,20,30,40}, trail_sell_pct% in {1,2,3,5,7,10,15}, confirm_days in {5,10,15,20}), 1344 cells, pooled across all 10 v5 watchlist tickers (the "big 6" broad-index comparison group from the 2026-08-04 finding was explicitly out of scope -- not on any watchlist, would need a separate backtest_cache lookup path). Reuses `drought_overlay_test.py`'s real building blocks directly (`get_trades_and_bars`, `simulate_overlay`); `find_drought_windows` was extracted from that script's inline loop so both scripts share one implementation (refactor verified byte-identical: SOXL's confirm_days=10 default-params result reproduced exactly, n=13, +30.2% compounded). Cliff-safety implemented as grid-neighbor robustness (worst pooled compounded return among a cell's immediate neighbors along each of the 4 axes) -- NOT the core kernel's MIN(possible, pessimistic, certain) intrabar-fill-ambiguity resolution, a deliberate scope decision (that would mean building 3 parallel fill resolutions into `simulate_overlay` itself, a much bigger addition than this question needed). Full sweep runs in ~9 seconds (105,168 total trade-observations across all cells).
**Result:** best pooled cell is confirm_days=20, fixed_sl=15%, arm_pct=15%, trail_sell_pct=5% -- n=37, win_rate=46%, compounded=+448.8%, worst-neighbor still +24.1% (cliff-safe). Per-ticker breakdown at this cell: 7 of 9 tickers with any drought windows are net positive (KORU +188.5%/n=7, AGQ +46.2%/n=3, USD +42.7%/n=2, HIBL +21.8%/n=8, GDXU +15.5%/n=2, DPST +10.6%/n=3, SOXL +3.1%/n=4; only UDOW -11.0%/n=3 and YANG -36.1%/n=5 net negative) -- genuinely broader participation than the original SOXL-only 2-trade result. But KORU alone contributes the large majority of the pooled 448.8% figure, on its own small n=7 sample -- cliff-safety (grid-neighbor robustness) confirmed the *parameter choice* isn't a lucky spike, but says nothing about *ticker concentration*, which is a different failure mode and the real reason the original SOXL result was suspect in the first place. NUGT had only 1008 of 1344 cells populated (zero drought windows at one confirm_days value, not a bug -- just fewer qualifying droughts at that ticker's price history).
**Verdict:** the sweep machinery works and is fast/cheap to rerun, and surfaces a materially better-diversified candidate than the manual sweep did -- but the top result is NOT validated as deployable as-is, since ticker-concentration risk (one ticker driving most of the pooled figure) was never screened for and this cell reproduces exactly that failure mode with KORU in SOXL's old role.
**Follow-up, open:** (1) add a concentration screen alongside cliff-safety (e.g. max single-ticker share of pooled compounded return, or require some minimum number of net-positive tickers with n>=X each) before trusting any pooled cell as a real candidate. (2) the put-hedge option-selection methodology (`docs/design.md`) was explicitly gated on this sweep landing on real risk parameters -- still blocked pending the concentration-risk fix above, since picking a strike/expiry off an unvalidated parameter choice would just launder the same problem downstream. (3) `output/drought_overlay_sweep_pooled.csv`/`_per_ticker.csv` (gitignored) have the full 1344-cell grid for further slicing.

## 2026-08-05 (very late 3) -- Drought-overlay deep dive: intraday vol entry-gate found (real but ticker-dependent, unresolved overfitting risk), exit-side vol-spike rule found NET NEGATIVE, HANDOFF mechanism traced to a concrete failure mode, big-6/18-ticker sample-expansion both run
**Context:** direct continuation of the SOXL-only fragility question from the entry above. User wanted (1) a bigger sample (more tickers, more history) before trusting anything, then a deep manual trade-by-trade audit of SOXL's specific losses, which led to several real, distinct findings below. No production code touched -- this is entirely `scripts/drought_overlay_sweep.py` (research/analysis script) plus ad hoc queries; nothing here is live/deployed.

**1. Sample expansion.** Historical range is already maxed out per ticker (`load_hourly` reads the full cached CSV) -- but GDXU/NUGT/USD only have ~13 months of hourly history (since 2025-06-27) vs ~3 years for the other 7 v5 tickers, a real, currently-unfixable data-availability gap (yfinance hourly windows don't reach back far enough from today to backfill it; would need a paid vendor). Widening the ticker universe was more productive: reran the pooled sweep against watchlist_id=57 (the 18-ticker v4 universe, +8 tickers: DUST/GDXD/LABU/NAIL/RETL/TQQQ/UVIX/ZSL) -- sample at confirm_days=5 jumped to n=356/cell (vs n=37 at the old best cell), only 3 cliff-safe cells surfaced, best: confirm_days=5/sl=5/arm=15/trail=10, n=356, +1021.0% pooled, worst-neighbor +27.3%. Also built `--big6` mode (SPY/SSO/UPRO/QQQ/QLD/TQQQ, config pulled directly from `backtest_cache`'s best-swept v5/open_check row per ticker since none of these are on any watch_list -- hit and fixed the same `TrailingExitZScoreBreakout`-stores-arm-in-`take_profit`-not-`arm_sell_pct` bug found in the 2026-08-04 late entries) -- confirmed broad-index droughts are too rare to be useful standalone (best cliff-safe cell n=14 total across all 6 tickers combined).

**2. SOXL trade-by-trade audit (confirm_days=20/sl=15/arm=15/trail=5 cell, n=4).** Full detail preserved in conversation, not reproduced here in full -- summary: (a) 2025-05-22 loss (-9.38%) was a pure overnight gap (Close 16.50 -> Open 15.21 next session, no intraday warning that day, but the *prior* day, 05-21, had already shown a violent single-hour break at 12:30, 7.3% bar range -- daily-close 5-day realized vol misleadingly read as a trough (19.95%) because it averages away exactly this kind of intrabar move). (b) 2025-10-22 loss (-11.16%) was a ~5-hour intraday grind (09:30-13:30, cumulative ~13.3% off the prior close at the session low) whose reversal started *inside* the same bar (14:30) that both fired the core strategy's next signal and priced the HANDOFF exit at that bar's Open -- missing a +3.3% intrabar recovery that happened in the same hour.

**3. Intraday vs. daily-close realized vol -- the daily-close measure is a real methodological trap.** A 5-day close-to-close rolling vol (the obvious first choice) systematically masks intrabar/intraday volatility, since it only samples 5 closes. Rebuilding as rolling stdev of *hourly* log returns, with the 9:30 bar excluded (it's a near-instantaneous overnight repricing, not accumulated intraday trading activity -- confirmed by checking the 9:30 bars separately: SOXL's 05-23 gap is a real 3.55%-range/​-7.82%-gap single print, not something continuous intraday vol would or should absorb), sharpened the entry-side signal substantially. Broad check across all 26 real HANDOFF-exit trades (all 10 v5 tickers, node's own real risk params, all 4 confirm_days): daily-close version gave low-vol-entry win rate 57% vs high-vol-entry 33%; the intraday (9:30-excluded, 2-day/12-bar) version gave **60% vs 14%** -- a much cleaner split. Checked and ruled out an alternative "declining vs. rising drift during the drought" hypothesis on the same 26 trades -- correlation with outcome was ~0 (-0.076), and a 2x2 with vol showed vol doing essentially all the discriminating work, drift adding nothing (small-n caveat: only 5/26 droughts were even declining, underpowered to fully rule out).

**4. Exit-side vol-spike rule -- tested, NET NEGATIVE as designed.** Hypothesis: exit early if intraday vol crosses an elevated percentile while the position is underwater (mirrors the entry gate). On SOXL's 4 example trades this looked great (both real losses cut ~4x smaller, -9.38%->-1.69% and -11.16%->-2.71%, the win untouched). But run across all 313 real drought windows (all tickers, all confirm_days, node's own real risk params) at threshold=0.5, it also fired on **9 trades that were going to be big TRAIL wins** (+3.6% to +34.9%) and cut them to small losses (-0.15% to -2.5%) -- the rule can't distinguish a capitulation dip before a big recovery from the start of a real decline, since both look identical in the moment (underwater + elevated vol). Net effect: pooled compounded improved (-92.7% -> -78.8%) but **win rate got worse** (14.7% -> 11.5%, since 9 killed winners > 2 saved losers in count, and this pooled base sample isn't even the deployable cliff-safe cell, just a diagnostic). **Verdict: do not adopt as designed.** Would need real refinement (sustained multi-bar confirmation, a minimum-loss-magnitude gate, or suppression once price nears the arm threshold) before revisiting.

**5. HANDOFF mechanism -- traced, not yet redesigned.** `simulate_overlay`'s HANDOFF exit sells at the backstop bar's `Open` unconditionally once neither SL nor trail has fired by the core strategy's next real signal. Checked whether this systematically sells right before the core strategy's own trade rallies (the intuitive "worst possible timing" story): across 26 real HANDOFF exits, only 2/14 losses (14%) saw the core strategy's subsequent trade go on to win -- the dominant pattern is actually correlated continuation (12/14 losses also saw the core trade's own next signal lose, mean -2.91%), not "sold right before the turn." SOXL's 2025-10-22 trade is the outlier case where this WAS true (overlay exited at 36.78, core trade rallied +31.4% to 49.89). **Open, not started**: whether HANDOFF should instead transfer management of the already-held position directly into the core strategy's own arm/SL/trail state machine (no liquidate-then-implicitly-rebuy round trip) rather than force-closing at the backstop Open -- raised, deliberately deferred to next session per user's explicit call.

**6. Per-ticker 5-axis sweep (own SL/arm/trail/confirm_days/vol-gate per ticker, not pooled) -- real result, real unresolved overfitting concern.** `run_ticker_sweep_vol_gated`/`run_per_ticker_mode` (new, `scripts/drought_overlay_sweep.py --per-ticker`) adds the intraday-vol-gate threshold (None/0.3/0.4/0.5/0.6/0.7, percentile of the ticker's own historical intraday-vol distribution) as a 5th independent axis, with its own 5-axis grid-neighbor cliff-safety, no cross-ticker pooling -- 8064 cells/ticker, deliberately accepting overfitting risk (user's explicit call: "I'm ok with a little overfit here"). Result: 5 of 10 tickers (KORU, GDXU, AGQ, DPST, USD) didn't need any vol gate -- their unfiltered best cell was already cliff-safe and the gate only ever *hurt* them (removed real winners: KORU lost 3 trades of +126.3%/+85.7%/+23.2% at gate=0.6, cutting its own compounded 531.9%->105.4%). 3 tickers (HIBL, SOXL, UDOW) were NOT cliff-safe ungated and the gate genuinely rescued them at one specific threshold each (SOXL@0.6 confirmed clean: exactly cut 5 losers, no winners touched). YANG isn't rescued by any gate level. AGQ zeroes out entirely under any gate (all 3 of its windows score high-vol). **KORU's per-ticker overlay result (n=16, +531.9% compounded) now exceeds its own core strategy's real backtested return (378.7% compounded, 76 trades, 18.4% win rate)** -- flagged live by the user as suspicious on its face (fewer trades outperforming the parent strategy is exactly the shape of an overfit result, not a validated one). Multiplying core x overlay (valid only if genuinely non-overlapping, which is the design's premise) gives a combined +2925% -- inherits all of the above's unresolved fragility, not a separate confirmation of it.
**Verdict, overall:** two genuinely separable findings. The intraday (9:30-excluded) realized-vol measure is methodologically real and important on its own (daily-close vol actively misleads here) -- this generalizes past the drought-overlay design specifically. Whether the *vol-gated entry* itself is a real edge vs. an artifact of small-n per-ticker fitting is **not yet resolved** -- SOXL's and KORU's clean-looking (opposite-direction) results are each consistent with either explanation given current sample sizes.
**Follow-up, open (deferred to next session per user):** (1) out-of-sample validation of the per-ticker vol-gate choice -- fit on first half of each ticker's history, test on second half, see if the helps-here/hurts-there pattern survives or scrambles. (2) whether the 3 gate-helped tickers (HIBL/SOXL/UDOW) share a real structural property vs. the gate-hurt ones (KORU/DPST/GDXU) that would make this a real hypothesis rather than a coincidence. (3) per-ticker exit-vol-spike research (today's version 4 above was pooled/uniform and found net-negative -- an equivalent per-ticker treatment, mirroring the entry-gate's per-ticker approach, hasn't been tried). (4) HANDOFF/handover mechanics redesign (item 5 above) -- transfer-management vs. current sell-then-rebuy, not started. (5) put-hedge cost/payoff modeling (rough Black-Scholes off the same intraday realized-vol measure as an IV proxy) -- discussed, not built. (6) concentration-risk screen (carried over from the entry above) still open, now compounded by the per-ticker overfitting question -- both need resolving before this design is anywhere near deployable. Scripts: `scripts/drought_overlay_sweep.py` (`--big6`, `--per-ticker` both new this session). Outputs (gitignored): `output/drought_overlay_sweep_pooled_v4universe.csv`/`_per_ticker_v4universe.csv` (18-ticker run), `output/drought_overlay_sweep_per_ticker_5axis.csv` (per-ticker 5-axis run); the big-6 run overwrote the original `output/drought_overlay_sweep_pooled.csv`/`_per_ticker.csv`.

## 2026-08-06 -- Per-ticker exit-vol-spike tuning: real but small (3/10 tickers), a DIFFERENT set from the entry-gate winners -- SOXL is helped on entry, hurt on exit
**Context:** deferred item (3) from the entry above -- the pooled/uniform exit-vol-spike rule (exit early if intraday vol crosses an elevated percentile while underwater) was found net-negative at the pooled level; untried was whether a per-ticker treatment (mirroring the entry-gate's per-ticker approach) survives that trade-off, or scrambles the same way the entry gate did.
**Method:** `scripts/drought_overlay_sweep.py --exit-vol-tuning` (new) -- `simulate_overlay` gained optional `exit_vol_gate`/`bar_vol_pctile` params (default `None`, zero behavior change for existing callers): forces an early close at that bar's own Close, reason `'VOLSPIKE'`, the first bar where the position is underwater (`cp < entry_price`) AND that bar's intraday-vol percentile (same 9:30-excluded rolling-hourly-log-return measure as the entry gate) is >= the gate threshold. New `get_bar_vol_pctile_array` precomputes a per-bar percentile array (forward-filled from the entry gate's existing `get_ivol_series`) for O(1) lookup inside the exit loop rather than re-searching per bar. Independent 5-axis (confirm_days x exit_vol_gate x sl x arm x trail) sweep per ticker, same grid-neighbor cliff-safety convention and same `VOL_GATE_GRID` threshold values as the entry-gate per-ticker sweep -- 8064 cells/ticker, ~41s for all 10 tickers.
**Result (apples-to-apples: best CLIFF-SAFE cell restricted to ungated vs. best cliff-safe cell restricted to gated, not "best overall" which can mask cliff-safety differences):**
- **Genuinely helped (3/10):** DPST (ungated cliff-safe best +56.6%/n=4 -> gated +74.8%/n=4 at gate=0.7), HIBL (+164.8%/n=18 -> +176.4%/n=18 at gate=0.7), UDOW (**no cliff-safe cell exists ungated at all** -> gated makes it cliff-safe, +5.6%/n=14 at gate=0.6 -- the gate's real contribution here is robustness, not raw return).
- **Genuinely hurt (3/10):** GDXU (+93.8%/n=8 -> +77.2%/n=8), KORU (+362.3%/n=11 -> +145.5%/n=7 -- drops both return AND trade count, the same small-n-cherry-picking shape flagged as a concentration-risk concern for KORU already), SOXL (+83.8%/n=20 -> +77.1%/n=20, small but real decline).
- **Unaffected (4/10):** AGQ, YANG (no cliff-safe cell either way), USD (cliff-safe ungated only, gate destroys it), NUGT (tie at n=1, no signal).
**Real finding, distinct from the entry-gate result:** the set of tickers helped by the exit gate (DPST, HIBL, UDOW) does NOT match the set helped by the entry gate (HIBL, SOXL, UDOW) from the prior session -- HIBL and UDOW are common to both, but **SOXL is helped by the entry gate and actively hurt by the exit gate**, and DPST (unaffected/hurt by entry gate treatment) is helped by exit. This rules out a simple "this ticker is generally vol-gate-friendly" story -- entry-side and exit-side vol-gating are separable questions with separate answers per ticker, not two expressions of one underlying property.
**Verdict:** per-ticker exit-vol-gating is real for a minority of tickers (same shape as the per-ticker entry gate: helps some, hurts others, most unaffected) -- confirms deferred item (3)'s premise (a per-ticker treatment behaves differently from the rejected pooled/uniform version) but does NOT resolve the standing overfitting question: KORU's exit-gate result reproduces the identical small-n-improvement-at-the-cost-of-trade-count pattern already flagged as suspicious for its entry-gate and pooled-sweep results. Not adopted as a live design change -- purely a research finding pending the still-open out-of-sample validation (deferred item (1), now applies to both the entry gate and this exit gate).
**Not built:** combining entry-gate and exit-gate as one joint per-ticker search (would be a 6th axis, ~48k cells/ticker) -- each was tuned independently against the SAME ungated baseline, not against each other's gated result, so a ticker helped by both (e.g. HIBL) hasn't been checked for whether stacking them compounds the benefit or fights itself.
Outputs (gitignored): `output/drought_overlay_sweep_exit_vol_5axis.csv`.

## 2026-08-06 (later) -- KORU trade-by-trade deep dive on the per-ticker exit-vol-gate: confirms rejection at the price-action level, not just aggregate stats
**Context:** the per-ticker exit-vol-gate sweep's KORU "improvement" (362.3%->145.5% compounded) turned out to be cross-axis noise -- the gated winner cell also changed confirm_days (10->20, fewer/different drought windows), not an apples-to-apples read of the gate itself. Isolating the gate's true effect (same confirm_days=10/sl=15/arm=20/trail=10, only exit_vol_gate toggled 0.7 vs None) reproduces the exact same shape the pooled test found: of 11 windows, the gate changes 5, and 3 of those 5 are big TRAIL winners (+57.5%, +56.0%, +3.5%) chopped down to small losses; only 1 (-33.6%->-26.5%) is a real loss meaningfully softened; 1 is a toss-up (+1.4%->-1.9%).
**Price-action audit of the 5 changed trades:** 4 of 5 share the same shape -- a shallow pullback during an underlying rally, not the start of a real decline. #0: recovered from -10% back to essentially flat (-0.49%) on the very bar VOLSPIKE fired (vol spike was the bounce itself), ungated rode it to +3.5%. #6: rallied to +12%, pulled back to -1.7% on a normal swing low, ungated rode it to +57.5%. #9: vol percentile pinned at 0.93-0.94 for the *entire* window (elevated from bar one, not a reactive spike to a new shock) -- rallied to +4.3%, pulled back to -1.1%, cut; ungated rode it to +56%. #8: genuinely ambiguous chop, small win either way. Only #10 is a clean save: a real single-bar overnight gap (-26.5% on the gap bar alone), caught by VOLSPIKE at Close before SL's Open/Low-based exit would have let it fall further to -33.6%.
**Verdict: the exit-vol-gate is not predictive enough to adopt, even per-ticker.** Its one real success (#10, a genuine sudden breakdown) is outweighed by 3 real winners killed mid-pullback on the same ticker, at the same fixed risk params -- the rule cannot distinguish "danger arriving" from "this ticker's droughts are just generally choppy" (most starkly shown by #9's constant-elevated, non-reactive vol reading). Closes the per-ticker exit-vol-gate line of inquiry as a dead end, not just under-optimized -- would need a fundamentally different signal (e.g. a directional/momentum confirmation alongside vol, not vol alone) to be worth revisiting, not a different threshold.
**User's read**, unprompted: "don't feel like it's predictive enough" -- independent confirmation of the same conclusion from the price-action evidence.

## 2026-08-06 (later 2) -- Put-hedge feasibility check: GDXU has NO options market; only 5/9 remaining tickers support a weekly-cadence overnight hedge; daily snapshot collection started
**Context:** discussing put-hedge as the structural fix for gap-through-SL risk (VOLSPIKE just having been rejected as too imprecise a predictive rule). User proposed two designs: (1) buy a put and hold it for the whole overlay position (put backs/replaces the SL), (2) buy a put only over the overnight window, sell in the morning, rely on existing SL/TRAIL intraday.
**Real feasibility check (yfinance live options data, all 10 v5 watchlist tickers):** GDXU has ZERO options expirations -- no options market exists for it at all, structurally excluded from any put-hedge design. Of the remaining 9: AGQ, DPST, KORU, NUGT, SOXL have near-term WEEKLY expirations (first expiration 1-2 days out); HIBL, UDOW, USD, YANG's nearest expiration is 2+ weeks out. This specifically rules idea (2) -- the cheap overnight-only design -- out for those 4 tickers, since you'd be forced to buy (and pay time-decay on) multi-week protection every single night, defeating the entire cost premise. Idea (1) doesn't have this constraint (already paying for a longer hold) but was assessed as less well-targeted -- it pays full time-decay across the entire drought hold (which can run days to weeks) including all the ordinary daytime hours where the existing SL/TRAIL already works fine, not just the specific overnight-gap failure mode that's actually unsolved.
**Liquidity, same snapshot:** thin almost everywhere (wide bid/ask spreads, single-digit volume/open-interest) except KORU (OI 141-1109 near the money) and SOXL (OI 151-213) -- real execution slippage risk for the other 7 tickers even where an options market technically exists.
**Infrastructure started:** `scripts/collect_options_snapshot.py` (new) -- yfinance only exposes LIVE options data, no historical chains, so (same "record now, use later" rationale as the still-unbuilt 1-minute-bar backlog idea) a real premium history needs to start accumulating now rather than waiting until the put-hedge design is fully specified and finding there's nothing to backtest against. One run = one snapshot (puts only, nearest 3 expirations per ticker) written to a new `options_snapshot` table in `trading_universe.db`. First real run (2026-08-06): 1,652 put rows across 9 tickers (GDXU confirmed 0, matching the check above). Not yet scheduled/cron'd -- ad hoc for now.
**Not yet decided:** which design (1 vs 2, or a hybrid/ticker-specific split given the weekly-expiration constraint) to pursue, and still gated on the concentration-risk fix per the existing backlog note.

## 2026-08-07 — v5-stacked composable overlay framework built (drought/add-on/put-hedge/skim); SOXL core cliff-safety false-negative bug found and corrected; per-ticker drought overlay results across 21 tickers; USO reopened for taxable brokerage

**Framework built.** `scripts/stacked_model/` (`trade_schema.py`, `put_hedge.py`, `drought.py`, `add_on.py`, `skim_reserve.py`) + `scripts/v5_stacked_backtest.py`/`v5_stacked_crash_stress.py` -- a reusable, composable overlay library on top of the frozen v5 core config (deliberately not a re-opened hyperparameter search, per this session's explicit framing: "core config stays frozen"). Each module verified against a known-good prior source before being trusted: `put_hedge.hedge_pnl` reproduces `sim_bear_market_stress_hedged.py`'s SOXL/KORU crash numbers exactly; `drought.py`/`add_on.py` reproduce `three_layer_summary.py`'s underlying logic byte-identical. Found and fixed a real bug along the way: `apply_hedge()` silently returned unhedged trades (`hedge_priced=False`) when no IV source was available, instead of erroring -- would have produced a false "hedge applied" result.

**Real, corrected reasoning on add-on hedging**: initially assumed hedging core+add-on together would double-hedge the same shares; this was wrong -- add-on is a genuinely separate share block bought at the arm bar, not the same shares core already holds, so it needs (and now gets) its own put purchased at arm_i. Fixed in `v5_stacked_backtest.py`.

**SOXL core cliff-safety: found unsafe, then found the finding was itself a bug, corrected back to safe.** Checking core's own robustness (never previously re-verified this session, though AGQ/KORU's were) against the pre-prune archive (`cache/research/permanent_archive/trading_universe_pre_prune_20260801.db`, since SOXL's live `backtest_cache` was wiped by the 2026-08-02 prune bug) initially found `worst_neighbor_alpha=-136.3%` -- seemingly a real, serious fragility in the live SOXL node, contradicting the "core is already island-verified" assumption this whole framework depends on. Root cause found on a second pass: the neighbor query never filtered `trail_buy_pct`, so the "neighbor" search was comparing against wildly different trail_buy_pct configs (1-30%) instead of holding SOXL's real value (3%) fixed. Re-run correctly: **worst_neighbor_alpha=+653.1%, genuinely cliff-safe**, consistent with AGQ (+285.2%) and KORU (+48.0%), both independently re-verified with the same fix and unaffected (their trail_buy_pct=0 for TrailingExit strategies has no ambiguity to trigger this bug). The official `scripts/top_safe_nodes.py` tool has the identical missing-filter bug (confirmed by inspection) -- its "None found" result for SOXL across the full 838K-row search space is not trustworthy as stated; not yet fixed in that script.

**Drought-overlay real signal found for SOXL, none of the earlier default/tuned configs held up elsewhere.** Extensive robustness work (cliff-safety, fit/test out-of-sample split, a "maximize worst-neighbor not raw return" selection mode, and a fine-grained day-by-day confirm_days scan 1-30) repeatedly found SOXL's plain default (confirm_days=10) NOT cliff-safe (worst_neighbor=-15.4%), and every tuned/vol-gated variant at confirm_days=5-10 failed the fit/test split. The real signal that survived: **confirm_days=3, vol_gate=0.4** (SOXL's own core sl=2/arm=30/trail=1) -- +174.8% full-history, positive on BOTH halves independently (fit +23.7%, test +122.1%), and confirmed via included-vs-excluded challenge to be real differential selection (included trades: 45% win rate/+174.8%; excluded: 8% win rate/-14.6%), not noise. Real caveat found and accepted as a known risk: this config sits one step from a cliff on the confirm_days axis specifically (cd=2 is -25.5%), and the result leans heavily on SOXL's extraordinary 2025-12→2026-05 rally (+449% in ~5 months) -- genuine chop existed within that window (two real -24%/-32% interim drawdowns), but the magnitude of the result is inflated by riding one historically extreme move that may not recur.

**AGQ/KORU/DPST drought overlay: rejected, multiple ways.** AGQ's -14.6% default result traced to one real gap-through-SL loss (-14.93%, 2025-04-01); put-hedge cushions it (-11.98% hedged) but doesn't flip the ticker's overall result positive (still a 22% win-rate signal, hedging the tail doesn't fix a weak entry rate). KORU's much-cited 531.9% "winner" (confirm_days=5, sl=10/arm=40/trail=10 -- NOT KORU's own real core values) was found to be a pure overfitting artifact: fit-half is a near-total wipeout (-24.2%, ~14% win rate), the entire result manufactured by 4 trades clustered in one ~12-month window (2025-04→2026-04) that coincides with a real, confirmed 8x rally in KORU's own underlying ($2.00→$15.87). Trend filters (20/50/100/200-day SMA, tested on SOXL/AGQ) were also tried and rejected -- same failure mode as the already-rejected exit-vol-gate: can't distinguish "dip about to bounce" from "start of a real decline," cuts real winners along with real losers (SOXL: 2 of 4 dropped entries were its own biggest wins).

**18-ticker (v4 universe) drought-overlay batch scan** (`scripts/drought_candidate_scan_18.py`, plateau-then-vol-gate-then-fit/test-split pipeline): 4 of 18 initially "passed" (GDXU, HIBL, KORU, SOXL). Single-trade-dependency stress test (remove the biggest winner, recheck) then split this further: **KORU's pass was a single-trade artifact** (n=6, removing the one +30.47% trade flips +23.7%→-5.2%) -- rejected, consistent with the earlier 531.9%-node finding. **GDXU's pass is real** -- n=6, 3 independent wins of comparable size (+34.9%/+26.3%/+26.0%), still +52.2% with the biggest winner removed. HIBL not yet stress-tested this way (n=9/9, no gate needed, looked the most solid of the four on the batch scan alone).

**Real per-ticker vol/trend regime finding (large sample, not the tiny drought-window sample)**: generic vol-percentile-vs-forward-return test (~4,400 bars/ticker, all horizons 1-40 days) found AGQ and (at 20-30d horizons) SOXL weakly but genuinely support "low vol precedes better returns" (rho -0.06 to -0.18); KORU shows the opposite (high vol precedes better returns, rho +0.03 to +0.09) at real statistical significance. Traced to a real, checked mechanism for KORU specifically: low vol during an established DOWNTREND meant continuation of the decline (2023-2025), while the powerful 2025 recovery rally was itself accompanied by unusually LOW vol once established -- i.e. the relationship is closer to trend-CONTINUATION than a universal "calm=bullish" signal, confirmed by tracing KORU's real vol trajectory through its 2025 bottom (a real vol spike at the April 2025 low, then vol dropping to its calmest reading of the whole series during the smoothest part of the rally).

**KORU real-time state, checked 2026-08-07**: NOT currently on an upswing -- price ($18.43) is well below both its 50-day ($33.42) and 100-day ($29.86) SMA after a ~71% decline from its June 2026 peak (~$63), and currently at vol_pctile=0.82 (elevated, not calm). `scripts/watchlist_status.py` gained `SMA50`/`>SMA50`/`VolPctl` columns (informational only, no gating logic) so this state is visible at a glance going forward, per the "KORU upswing-watch" idea -- alert when price crosses above both 50d/100d SMA AND vol_pctile<0.3, matching the trend-continuation mechanism found above. Not yet wired to an actual Slack alert (design only) -- eventual home is the existing start-of-day readiness report given the slow-moving (50-day) trigger has no urgency.

**AGQ add-on validated with real stress-testing** (out-of-sample split: fit +105.3%/test +66.5%, both independently positive, n=17/24, no single-trade dependency; margin-cost re-check using the actual hold-time distribution -- median 6.2d, not the ~1.4d the original 0.04% flat-cost validation used -- found real cumulative exposure of ~276 days (0.76 years) across the full backtest, ~7.9% of notional at the ~10.4% annualized rate the flat assumption implies; time-scaled recompute: +242.0%→+222.3%, survives).

**Tax/compliance research, confirmed via real search (not assumed)**: of the 18 v4-universe tickers, only **AGQ, ZSL, UVIX** are K-1/commodity-pool filers (ZSL confirmed same ProShares silver-futures family as AGQ; UVIX confirmed VIX-futures-based). The other 15 are standard 1099 filers, confirmed directly from ProShares'/Direxion's own tax-document pages. **GDXU/GDXD are not Direxion products at all -- they're BMO-issued ETNs** (no K-1, but real unsecured issuer/counterparty credit risk, a different risk class than every other ticker in the universe). Sources: proshares.com/resources/tax-and-filing-documents/k-1s-form-1065, taxpackagesupport.com/volatility_uvix, direxion.com/frequently-asked-questions, bmoetns.com/ETN/GDXU.P.

**USO reopened for taxable brokerage, real workup done.** Per the standing 2026-08-04 rule ("don't revisit without confirming K-1 status, restrict to brokerage if still K-1"), confirmed via real search USO is still a publicly-traded-partnership K-1 filer (uscfinvestments.com/tax-information). Since AGQ (same tax bucket) has a real liquidity constraint limiting how much capital brokerage can deploy there, USO was evaluated as an additional same-bucket candidate. Real v5 data already existed from the 2026-08-03 1x-universe screen (835K+/79.8K rows). Core: genuinely solid -- cliff-safe (+107.2% worst neighbor), positive on both out-of-sample halves independently (fit +39.6%/test +208.2%, n=25/28, well-distributed winners across the whole period, not concentrated). Add-on: too thin to trust (n=4, all in one ~5-month 2026 window; splitting just that window shows both halves positive but only 2 trades each -- "not disqualified," not validated). Drought overlay: **rejected outright, uniformly** -- every confirm_days value 1-15 negative (-31.1% to -1.0%), despite USO's own price being UP +57.1% over the same span (ruling out "no positive drift" as the explanation -- the drought-specific losses are a real, distinct pattern from the ticker's overall trend, cause not yet identified). Options: real liquid market confirmed live via yfinance (weekly expirations, real volume/OI) but zero historical IV data existed (USO was never in the options-snapshot collector) -- added to `scripts/collect_options_snapshot.py`'s `DEFAULT_TICKERS`, first real snapshot collected same session (162 put rows, 3 expirations). SCO (USO's ProShares inverse, -2x, also confirmed K-1/same tax bucket) tested as a "buy the inverse during USO's own droughts" idea -- promising-looking in-sample (multiple positive-day plateaus, day 4-6 and 10-12), but failed real out-of-sample testing at every cell checked except one 2-trade cell -- rejected, same fragility pattern as everything else that didn't survive today. The mirror question (SCO's own drought → buy USO) is blocked entirely: **zero v5 backtest data exists for SCO** -- would need a full sweep first, not started this session (per standing convention, the agent doesn't launch sweep campaigns).

**Verdict, final per-ticker recommendation as of this session:**
- SOXL: core (now confirmed cliff-safe) + put-hedge (crash-validated, unchanged from prior sessions) + drought overlay (confirm_days=3, vol_gate=0.4 -- real but accepted as a known one-step-from-cliff risk, and dependent on one exceptional rally period)
- AGQ: core + add-on (both validated with real out-of-sample/stress-testing this session)
- KORU: core alone (drought rejected every way tested; add-on not feasible in this account; currently NOT in the trend-continuation state that would make any overlay relevant)
- USO (new, taxable brokerage only, K-1): core alone validated; add-on/drought/hedge all either rejected or not yet ready
- GDXU: drought overlay candidate survives single-trade-dependency stress test, not yet given the full out-of-sample/included-vs-excluded treatment SOXL got
- HIBL: drought overlay batch-scan pass, not yet stress-tested for single-trade dependency

**Not yet done, real open items**: HIBL/GDXU still need the full SOXL-grade rigor (out-of-sample split, included-vs-excluded challenge) before trusting them the way SOXL's config is now trusted. SOXL's cliff-safety fix should be applied to `scripts/top_safe_nodes.py` itself (same missing trail_buy_pct filter). USO's uniformly-negative drought result (despite a positive overall price trend) is unexplained. SCO needs a real v5 sweep before the cross-hedge mirror question can be answered.

**Follow-on same session, after the entry above was written**: real capital-sizing questions explored for the USO/AGQ taxable-brokerage bucket. **UCO** (ProShares Ultra Bloomberg Crude Oil, 2x, confirmed K-1/same ProShares commodity-pool family via the same tax-page source) identified as a real candidate -- real liquidity confirmed (~$137.6M/day dollar volume via yfinance, essentially tied with AGQ's own ~$135.6M/day, not a liquidity upgrade despite UCO's higher raw share volume at its lower price). Cached hourly price data already exists; user launched a real `run_sweep_queue.sh` backfill (`VERSION=v5 TICKERS="UCO" FIXED_SLS="1 2 3" STRATEGIES="TrailingBothZScoreBreakout TrailingExitZScoreBreakout"`) in their own shell, not yet complete as of this entry -- **USO/UCO/AGQ core+add-on battery still needs to be run for UCO once that finishes**. **3OIL** (WisdomTree WTI Crude Oil 3x Daily Leveraged ETN) is the real direct-3x-crude product but trades only on London/Milan exchanges -- not accessible via a US Schwab account, ruled out. **NRGU** (MicroSectors US Big Oil 3x, BMO ETN) is a US-accessible 3x alternative but tracks a basket of oil-major *stocks*, not the crude oil commodity directly -- a materially different exposure, not pursued further this session. **SCO** (USO's ProShares inverse, -2x) also confirmed K-1/same tax family.

**Add-on concurrent-capital accounting question, resolved.** Real concern raised: does `v5_stacked_backtest.py`'s multiplicative trade-chaining overstate add-on's contribution, since add-on capital is concurrent with core (borrowed at arm_i), not sequentially reusing the same capital the way core+drought legitimately does? Resolved once the real sizing plan was stated explicitly: **add-on borrows 100% of the position's current size at arm** (doubling exposure), gain/loss added as real dollars to the account. Verified by direct computation that this sizing rule makes the existing multiplicative stacking mathematically EXACT, not an approximation -- `total + total*ret == total*(1+ret)`, the same operation as another compounding factor, specifically because the borrowed amount scales with current account value rather than being a fixed dollar amount. AGQ's core+addon number (+4491.6%) confirmed correct under this real sizing rule, verified two independent ways.

**Real, unmodeled liquidity/margin constraint, flagged as a genuine open item (not solved this session, explicitly deferred as a "2027 problem" pending the models continuing to hold up)**: 100%-margin add-on sizing means the practical starting core position needs real headroom to unwind BOTH legs (potentially simultaneously, e.g. a forced stop-loss exit) without exceeding safe liquidity bounds -- roughly halving the effective safe starting notional versus a core-only sizing assumption. Separately, and not modeled at all: margin availability at the moment an add-on triggers is a shared, time-varying resource across whatever else is open in the same brokerage account (USO/UCO/AGQ potentially all at once), not a dedicated pool for any one ticker -- a real portfolio-level constraint no single ticker's backtest can answer. Real growth-timeline math checked (44.9x compounded return over ~2.92 real backtest years implies ~268%/year; reaching the 20x needed to go from $50k to a ~$1M account -- roughly where AGQ's real liquidity ceiling would start to bind -- would take ~2.3 real years at that same rate, assuming it holds, which the whole session's throughline of found-and-corrected fragility argues against taking for granted).

## 2026-08-08 — Paired Opus review of the v5-stacked framework (user's explicit ask, outside the mechanical trigger since these are research scripts, not `signals_*.py`/kernel): 1 CRITICAL + 3 HIGH + several MEDIUM/LOW real bugs found and fixed; one prior claim in this log corrected

**Correction to the entry immediately above ("Add-on concurrent-capital accounting question, resolved")**: that conclusion was wrong. The claim that 100%-margin-at-arm sizing makes `combine_sequential`'s pure multiplicative stacking `(1+r_core)*(1+r_addon)` mathematically exact does not hold across a full trade sequence — the independent verification run at the time (`total + total*ret == total*(1+ret)`) checked a simpler, different identity than what the function actually computes when combining a core trade with its own add-on leg. The real formula for 100%-of-arm-time-value borrowing is `1 + r_core + (arm_p/entry_p)*r_addon`, not `(1+r_core)*(1+r_addon)` — worked through by hand with the user via a concrete dollar example (enter $100, arm $110, exit $130: real ending equity is $1.50/+50%, not the $1.5364/+53.6% the old formula gives). The multiplicative version implicitly sizes the add-on off the core position's *eventual exit-time* value rather than the *arm-time* value actually borrowed against — a form of look-ahead bias (confirmed by solving for what borrow amount *would* make the multiplicative number correct: it requires knowing the future exit price at arm time, which isn't real). AGQ's previously-reported +4,491.6% add-on number was inflated ~1.43x by this; SOXL/KORU were inflated less (~1.03x/1.09x) since their per-trade core moves are smaller.

**Context**: user asked for a paired Opus review (contextual + independent/cold, the standing `session wrap` pattern) of yesterday's v5-stacked framework build, even though none of the changed files are `active_signals.py`/`signals_*.py`/`schwab_*.py`/a backtest kernel module — the mechanical trigger in `CLAUDE.md` didn't technically require it, but the user wanted it anyway given real capital allocation decisions ride on these numbers. Both agents ran real numbers against the live DB rather than reasoning abstractly, and their findings converged rather than conflicted — the cold review independently found the same weak spots (hedge pricing, liquidity gating) and went deeper with harder empirical checks, plus surfaced one CRITICAL bug the contextual review missed. No formal rebuttal exchange was needed since nothing contradicted; findings were synthesized directly.

### Findings and fixes, in the order worked through

**1. [CRITICAL] `scripts/stacked_model/skim_reserve.py::manual_redeploy_overlay` — the manual-redeploy trigger never required an actual decline first, and the 100% threshold was unreachable.** The trigger fired the instant `declining` flipped True (a 0.1% dip, equity still ~99.9% of peak — already clears the 80% threshold trivially), and the 100% threshold required exact float equality on a bar where `declining` had already been reset False by the peak-update block that runs first. Fixed by tracking `min_since_peak` per decline cycle and requiring a threshold to have actually been crossed on the way down before it can fire on the way back up; the 100% threshold is now checked at the exact moment equity crosses back above the old peak. Verified via an instrumented trace on a synthetic rise/decline/recovery curve: fires exactly twice (idx 107 for the 80% threshold, idx 124 for 100%), both inside the real decline→recovery window, zero false fires during the pure uptrend or the decline itself.

**2. Add-on compounding math** — see the correction above. Fixed in `scripts/stacked_model/add_on.py::generate_addon_trades`: stores a `compound_scale = weight/(1+r_core)` alongside a compounding-equivalent `ret` (so a plain `cumprod([core_trade, addon_trade])` reproduces the exact concurrent-capital total), while `raw_ret` preserves the add-on leg's own true return for win/loss classification. `put_hedge.apply_hedge` updated to scale hedge P&L and the loss floor by the same `compound_scale` factor for add-on trades (their `ret` is on a different basis than core/drought trades' raw return, so hedge P&L computed on the raw basis needs the same conversion before combining).

**3. Add-on account eligibility.** Add-on requires a real margin loan; only AGQ's live node runs in a genuine margin account (`brokerage`) — SOXL and KORU's live nodes both run in `ira`, which can't borrow regardless of "limited margin" status. Matches the original planned-node split (`docs/design.md`'s 2026-08-07 entry): SOXL was validated for drought overlay, AGQ for add-on — never meant to be offered on every ticker. Per the user's explicit instruction ("report it, but report it as not safe" — don't silently omit), `v5_stacked_backtest.py` still computes and reports add-on stacks for every ticker, tagged with a new `addon_executable` column (True only for AGQ).

**4. [HIGH] `put_hedge.check_liquidity` hardcoded `otm_pct=5.0`** regardless of the real strike being hedged (15/25/50% OTM in practice) — so a tight 5%-OTM spread passed the gate and that one Boolean got reused for every OTM level, including ones with zero real bid (confirmed: AGQ's 25%/50% OTM and KORU's 50% OTM literally have no bid). Fixed: `check_liquidity(conn, ticker, otm_pct)` now checks the real strike, and `apply_hedge` calls it as a genuine hard gate (previously only printed a warning while still hedging every trade anyway) — if a ticker/strike fails, every trade in that call is left unhedged and flagged `hedge_priced=False`. Verified: AGQ now correctly shows `hedged_frac=0.0` at every OTM level tested (its spreads are too wide at all three); KORU passes at 15%/25% and correctly fails at 50%.

**5. [HIGH] `hedge_pnl` charged zero transaction cost.** Added real bid-ask spread cost (today's quoted spread at the real strike, same documented-approximation status as the existing IV proxy — no historical spread data exists anywhere). SOXL's `core+puthedge15.0` dropped from the original (buggy, cost-free) 1288.1% to 1137.5% with just this fix.

**6. [HIGH, found by the user during a live discussion, not the reviews] The hedge P&L denominator was wrong — buying a put is real cash on top of the position, not funded by the position's own capital.** `hedge_pnl`'s original formula computed P&L as a fraction of the stock's `entry_p` alone, implicitly treating the premium as free. The user caught this live while discussing a real SOXL put purchase ("but that means we're investing more capital... yeah but it's 1-2% for every trade!") — correctly identifying that a per-trade error this size compounds across every hedged trade the same way the add-on bug did. Fixed: the true return is `((exit_p-entry_p)+(v_exit-v_entry)-spread_dollars)/(entry_p+v_entry)`, denominator inflated by the real premium paid. Combined with fix #5, SOXL's `core+puthedge15.0` dropped further to **682.2%** — well below unhedged `core`'s 1195.9%.

**7. [Found while investigating #6's real-world magnitude, a bug in fix #5 itself] Crash-stress scale mismatch.** Applying fix #5/#6 to `v5_stacked_crash_stress.py` initially produced a nonsensical SOXL 2008 GFC result: -98.3% hedged vs -19.6% unhedged, a near-total wipeout from "protective" insurance. Traced by hand-inspecting individual trades: the crash-stress script uses a *synthetic* historical price series (entry prices $2-5 in the 2008 reconstruction), but the new spread cost used TODAY's real quoted dollar spread (calibrated to a ~$135 stock) as a flat dollar figure — trivial against $135, enormous against $2-5. Fixed: normalize spread to a fraction of TODAY's price first (same principle as IV — spread cost is properly a percentage, not a fixed dollar amount), then rescale to each trade's own price level. After the fix, SOXL's crash-stress hedge numbers are sane and restore the original qualitative finding: SOXL's put-hedge helps in all 4 historical crashes at 15%/25% OTM (2008: -19.6%→-9.9%, 2020: -42.9%→-30.8%, 2022: -38.4%→-26.6%, dotcom: -51.2%→-19.4%), KORU's mostly hurts (extreme IV makes the premium too expensive), consistent with the original 2026-08-06 finding.

**8. [Real, substantial correction, found while addressing the cold review's "dead code" finding on `drought.check_cliff_safety`] `v5_stacked_backtest.py` was using the WRONG, unvalidated default drought config for SOXL.** `build_stacks()` called `generate_drought_trades(node)` with no arguments — defaulting to `confirm_days=10, vol_gate=None`, which is the plain default already shown NOT cliff-safe (worst_neighbor=-15.4%), not SOXL's real validated `confirm_days=3, vol_gate=0.4` signal. Every "core+drought" number reported the prior day used the wrong config. Fixed via a new `DROUGHT_VALIDATED_CONFIG` dict (currently `{"SOXL": {"confirm_days": 3, "vol_gate": 0.4}}`) — AGQ/KORU (both rejected outright for drought) now correctly get zero drought trades rather than a silent unvalidated fallback. SOXL's `core+drought` corrected from 1587.0% (wrong config) to **3461.5%** (real validated config, 168 trades).

**9. Verified: SOXL's marquee `confirm_days=3, vol_gate=0.4` finding survives the neighbor-search bug below, untouched.** Its cliff-safety check (worst_neighbor=-15.8%, 9 real neighbors) is byte-identical before and after fix #10 — none of its neighbor cells happened to be thin enough to have been affected. The "one step from a cliff" caveat already documented (confirm_days=2 is -25.5%) stands unchanged.

**10. [MEDIUM] `drought_out_of_sample_check.py`'s neighbor-search allowed thin single-window neighbor cells to single-handedly decide a candidate's safety verdict**, even though the `MIN_TRADES=5` bar excluded those same thin cells from being *selected* — a real asymmetry (`cliff_safety_5axis` in `drought_overlay_sweep.py` only required a neighbor cell be non-empty). Fixed via a new `min_n` parameter (default 1, unchanged for the two other existing callers in `drought_overlay_sweep.py`; `drought_out_of_sample_check.py` now passes `min_n=MIN_TRADES`). Also found in the same investigation: `CONFIRM_DAYS_GRID` (the shared grid `drought_out_of_sample_check.py` reuses from `drought_overlay_sweep.py`) was `[5, 10, 15, 20]` — silently missing `confirm_days=3` entirely, the value cited in every doc for SOXL's real signal. Widened to `range(1, 21)`, restoring the density `drought_candidate_scan_18.py`'s original 1-15 scan used before the script was refactored to share this grid.

**11. [MEDIUM] `drought_candidate_scan_18.py::find_plateau_candidates` — a neighbor day with genuinely no drought windows (real no-data) was indistinguishable from a neighbor day simply outside the scanned range (legitimately OK to skip)**, both surfacing as `results.get(cd±1) is None`. Fixed via an explicit off-grid sentinel so only true range-edge cases get the free pass. Reran the full 18-ticker batch scan after the fix: identical 4 tickers pass (GDXU, HIBL, KORU, SOXL) — the bug didn't corrupt this particular historical run, but the gap is now closed for future scans.

**12. [MEDIUM-HIGH] `scripts/top_safe_nodes.py`'s 2026-08-07 fix (trail_buy_pct/trail_sell_pct) was incomplete** — `entry_timing`, a real `backtest_cache` primary-key column, was still unfiltered (currently latent: GDXD is the only `entry_timing='close'` ticker and its v5 alpha is below the 200% candidate bar, but a resweep would change that). Also the returned dict omitted `trail_buy_pct`/`trail_sell_pct`/`entry_timing` (the very params just proven load-bearing), making the "best safe node" output unreproducible, and printed TrailingBoth's `arm_sell_pct` value under a `tp` column header (a reader configuring a live node from that output would wrongly set `take_profit` on a strategy that doesn't have one). Fixed: `entry_timing` added to both the SQL SELECT and the neighbor mask; output dict key renamed `tp`→`arm_pct` and now includes every real config param.

**13. [LOW] `three_layer_summary.py::best_v5_node` — an empty neighbor set read as "safe."** `MIN()` over zero rows is SQL NULL/Python None, and the code defaulted `worst_neighbor = 0.0` in that case, making `safe = worst_neighbor >= 0` trivially True. Fixed to explicitly require `worst is not None` before `safe` can be True (no live query currently returns an empty set for the tickers checked, but the whole point of a cliff-safety check is that a wrong "safe" verdict is expensive — kept explicit rather than relying on it never happening).

**14. [LOW] Two documentation/dead-code corrections.** `drought.check_cliff_safety`'s docstring claimed it "closes" the robustness gap, but it's never called anywhere in the pipeline — corrected to describe it as a manual validation tool (SOXL's config was validated this way once, then hardcoded as `DROUGHT_VALIDATED_CONFIG`, not re-derived at runtime). `scripts/stacked_model/skim_reserve.py` only imported cleanly by accident (relied on `scripts/` being on `sys.path[0]`, true only when run as a direct script) — fixed by explicitly adding `scripts/` to `sys.path` in the module itself.

**15. Max-drawdown calc omitted the starting 1.0 peak** in both `v5_stacked_backtest.py::compounded_and_dd` and `v5_stacked_crash_stress.py::compounded_and_dd` — the running peak started tracking from the first trade's own outcome rather than the real starting point, so a losing first trade's own drawdown was invisible. Most likely to bite in the crash-stress script specifically, since it filters to decline-only trades whose first entry is often already a loser. Fixed by prepending 1.0 to both equity curves before computing the peak.

### Real, deliberately deferred design questions (not bugs)

**When to hedge.** The full-history backtest and the crash-specific reconstruction point opposite directions for SOXL: hedging every trade always-on drags long-run returns down substantially (682.2% hedged vs 1195.9% unhedged core), but the same hedge clearly helps during real historical crashes (all 4 tested). A vol-percentile-gated hedge trigger (mirroring the drought overlay's existing entry-gate infrastructure, just inverted — hedge on HIGH vol percentile instead of buying on LOW) was proposed as the most plausible next step, reusing already-validated infrastructure rather than inventing something new, but would need the same fit/test/stress-test rigor drought's own gate went through before trusting a threshold. **Deferred per the user's explicit call**: "this is additive — we can skip for now, notionals for the next year will be low."

**Daily overnight-only put hedge (buy before close, sell at open) vs. hold-through-trade.** Surfaced mid-session when the user asked about a real SOXL put purchase and recalled discussing daily buy/sell — confirmed real: `docs/backlog_cache.md` already records two undecided variants (hold-through vs. overnight-only), and today's entire fix pass was on the hold-through variant. Not resolved; still genuinely undecided.

**Put roll-past-20-days.** `hedge_pnl` prices one 20-day put and never simulates a roll if a trade runs longer (KORU up to 24 days, AGQ up to ~22). Real, but low-priority given low expected notionals — deferred.

**IV source inconsistency between the two runner scripts.** `v5_stacked_backtest.py` defaults to a realized-vol proxy (appropriate for real multi-year historical trades — no real IV data exists that far back); `v5_stacked_crash_stress.py` defaults to today's real quoted IV (appropriate for synthetic scenarios not tied to real dates). Checked directly: the two sources disagree by 4-27% depending on ticker, and disagree in a *different direction* than the code's own docstring assumed ("real IV typically runs above realized vol" — today, real IV is actually lower for all three tickers checked). Concluded this isn't a bug to fix — both are legitimate estimates for data that doesn't exist, and there's no "correct" choice to code toward. Left as-is; just don't over-trust either number's precision.

### Full suite/verification note
All fixes were run against the real live DB after each change (not left as a claim) — `v5_stacked_backtest.py`, `v5_stacked_crash_stress.py`, `top_safe_nodes.py`, `three_layer_summary.py`, `drought_out_of_sample_check.py`, and `drought_candidate_scan_18.py` all confirmed running clean end-to-end with the fixes applied. No test suite exists for `scripts/stacked_model/` yet (research/backtest scripts, not live-trading code — outside the standing pytest suite's scope, consistent with prior sessions' pattern for this category of work).

## 2026-08-07 — SQQQ (liquidity-screen candidate) — drought/add-on overlay window sensitivity to small parameter perturbations
**Hypothesis/question:** Raised mid-session while building a candidate-overlay smoke-test pipeline for the liquidity-screen backlog item — do drought-overlay windows (and by extension add-on trades, which are re-slices of core trades) mostly overlap across nearby node configs for the same ticker, since droughts intuitively feel like a property of the ticker's own price action rather than fine parameter tuning? If true, drought/add-on could be evaluated once per ticker and reused across neighboring configs instead of recomputed per node.
**Method:** Used SQQQ's real v5 `TrailingBothZScoreBreakout` sl1 winning node (w10, z2.0, trail_sell=7.0, hold=91) and its two immediate cliff-safety neighbors on the `trail_buy_pct` axis (8.0/9.0/10.0, the real adjacent grid values) — same axis/step convention `prune_backtest_cache.py`/cliff-safety checks already use. For each, ran `get_trades_and_bars`/`find_drought_windows`(confirm_days=10) and `generate_addon_trades` directly (no reimplementation), then compared drought entry-time sets (Jaccard overlap) and add-on trade counts/returns across the three neighbors.
**Result:** Drought windows: only ~30% Jaccard overlap between adjacent trail_buy_pct values (tb8 vs tb9: 7/15 shared; tb9 vs tb10: 6/(15∪12) shared) — core trade count itself shifts (29/26/20) because `trail_buy_pct` changes whether/when a trailing-buy signal actually fills, cascading into different exit times and thus different "days since last exit" gaps. Add-on is worse: trade count collapsed to 1/3/1 across the same three neighbors (add-on only fires on core trades that arm, a rare subset of an already-thin trade list), making "win rate"/"compounded return" at n=1-3 statistically meaningless. One add-on trade (2024-08-02 09:30:00 entry) survived all three configs — a real signal, but a single anecdote, not validated robustness.
**Verdict:** Refuted the "mostly overlaps, could reuse across neighbors" hypothesis — both drought and add-on are more sensitive to small parameter changes than the core edge itself, and cannot be evaluated once per ticker and assumed to generalize. Add-on additionally suffers a compounding sample-size problem on top of the sensitivity problem.
**Follow-up:** A genuinely independent "drought as its own strategy" idea (own entry signal off vol_pctile + z-score behavior, own SL/arm/trail grid, swept across all v5 tickers "for giggles") was raised in the same conversation and explicitly parked — `docs/research_log.md`'s existing `scripts/drought_overlay_sweep.py` (2026-08-05/06) already covers a similar independent-grid design for the 10 real v5 tickers (entry-side vol gate real for a minority: HIBL/SOXL/UDOW, still needs out-of-sample validation) and would need to hold up to that same rigor before extending further, per the user's own framing ("would have to be very robust to do this outside of v5"). Not started this session. Add-on's own robustness-across-nodes question (this entry) is answered: it does NOT hold robustly, at least not for SQQQ.

## 2026-08-07 (follow-up, same session) — SQQQ — full-axis sensitivity sweep refines the single-axis finding above
**Hypothesis/question:** The prior entry tested only `trail_buy_pct` and concluded drought/add-on results are broadly fragile to small parameter changes. Does that hold across every real axis, or was `trail_buy_pct` an unrepresentative pick?
**Method:** New `scripts/candidate_node_sensitivity.py` -- for each of SQQQ's two strategies' winning nodes, walks every real axis (`window`, `z_score_threshold`, `fixed_sl`, `max_hold_hours`, `trail_buy_pct`/`trail_sell_pct`, `arm_sell_pct`/`take_profit`) one at a time, holding every other column fixed via exact equality (per `docs/cliff_safety_query_checklist.md`), against the real adjacent grid value(s) that exist in `backtest_cache`. Computes drought-window Jaccard overlap and add-on trade-count shift per axis step.
**Result:** Not uniform. For `TrailingBothZScoreBreakout` (SQQQ's real, cliff-flagged-but-positive winner): `max_hold_hours` (jaccard 0.87-1.00), `trail_sell_pct` (0.87), and `arm_sell_pct` (0.93-1.00) are all robust; only `trail_buy_pct` (0.30-0.32, the entry-fill mechanic) and `fixed_sl` (0.53) are fragile. For `TrailingExitZScoreBreakout` (SQQQ's negative-alpha strategy): `window` (0.27) and `z_score_threshold` (0.31) are the fragile axes instead -- both directly redefine which bars generate an entry signal at all.
**Verdict:** Refines, doesn't overturn, the prior entry. The real pattern: axes that change entry-signal *timing* (window, z, or trail_buy_pct's fill-conversion mechanic) are fragile for drought/add-on's window-overlap; axes that only shape exit/risk behavior once already in a trade (hold time, trail_sell_pct, arm/TP level) are reasonably robust. "Broadly fragile" was an overgeneralization from testing only the one axis that happened to be fragile.
**Follow-up:** Not yet checked on any other ticker -- SQQQ-specific until repeated elsewhere. If this pattern holds generally, it suggests drought/add-on evaluation could reasonably reuse a winner's exit-side params across small hold/trail_sell/arm perturbations, but must always be recomputed fresh whenever window/z/trail_buy_pct differ.

## 2026-08-07 (later) — `prune_backtest_cache.py`'s island-selection had a missing `trades > 0` filter; full-universe recovery + fixed-script revalidation
**Hypothesis/question:** Raised when tranche 2 of the liquidity-screen backfill (`run_liquidity_tranches.sh`) hit its own built-in prune-validation gate ("PRUNE VALIDATION FAILED — best node changed after pruning") on SPXU. Why did the pre-prune best node (arm22.0/sl2.0/tb17.0/h77, alpha=-14.8%, trades=2) get dropped, replaced post-prune by a worse node (alpha=-56.8%, trades=11)?
**Method:** Queried the tranche-2 pre-prune snapshot directly for SPXU's `(TrailingBothZScoreBreakout, w10, z2.0)` group's top-ranked row by the same `ROBUST_ALPHA_SQL ORDER BY ... LIMIT 1` query `_island_rowids_for_group` uses.
**Result:** The top row was a **trades=0** config (`robust_alpha = 0.0` by construction — flat, matches SPY). `_island_rowids_for_group` (`prune_backtest_cache.py:58-63`), unlike `locate_best_node.py` and every other cliff-safety query in the codebase, had no `trades > 0` filter on its per-group "winner" pick — so a trivial zero-trade row silently outranked SPXU's real (losing but genuine) trades=2 node, and the island got built around the wrong center, dropping the real node entirely. Same bug *shape* as the take_profit/NULL and entry_timing bugs found earlier this week (a silently-missing filter changes what counts as a group's "winner") — third instance in one week, despite `docs/cliff_safety_query_checklist.md` already existing for this exact bug shape.
**Blast radius:** Not scoped to tranche 2 or SPXU — `_compute_keep_rowids` reprunes the *entire* `backtest_cache` table on every invocation, so the original 2026-08-02 full prune (65GB→256MB) and every prune since inherited the same exposure for any ticker/group whose true best (trades>0) node had negative alpha.
**Fix:** Added `AND trades > 0` to `_island_rowids_for_group`'s winner query, matching `locate_best_node.py`.
**Recovery:** Built a merged, full-grid recovery DB from the permanent pre-any-pruning archive (`trading_universe.db.pre_prune_20260802_013426`, 53 tickers/167,502,634 rows) plus the 6 tickers added since (QQQ/SPY/SSO/UPRO/USO/SPXU, pulled from their own pre-first-prune snapshots — verified via ticker-set diff that none of the 6 existed in the 08-02 archive, so no primary-key collision risk). Verified merge: 59 tickers/172,964,844 rows.
**Validation (new standard, see `.claude/skills/prune-validation/SKILL.md`):** Built `scripts/full_db_prune_validate.py` — snapshots each ticker's best node (via `locate_best_node.py`) before and after running the fixed `--build` extraction, live DB untouched throughout. All 59 tickers matched. Independently cross-checked via `scripts/top_safe_nodes.py` (materially different selection logic — requires alpha≥200% AND cliff-safety, not just raw max alpha): SOXL's result was bit-for-bit identical pre/post (alpha=1212.1007668386683%, worst_neighbor=5.610444916372387%, same node params) and matches the number already on record from the 2026-08-08 recovery session — real, independent confirmation the recovered/repruned data agrees with prior validated results.
**Verdict:** Confirmed and fixed. `--swap` run after full validation passed; live DB now 1.8G/7,179,743 rows (was 67G/172,964,844 unpruned — previous buggy prune had kept only 1,853,453 rows, confirming it was under-retaining broadly, not just for SPXU).
**Disposition of backups:** The 65GB pre-any-pruning archive was deleted after this validation (user's call — live DB is now a verified superset, and this is the most thoroughly-validated prune the project has run). The 67GB pre-swap merged-recovery copy (`trading_universe.db.pre_prune_20260807_100407`) is being kept ~1 month (until ~2026-09-07) as an extra safety margin in case a *different* kind of prune bug (not caught by the best-node check) surfaces later — see `docs/backlog_cache.md`.
**Follow-up:** `scripts/full_db_prune_validate.py` currently only automates the `locate_best_node.py` side of the new standard; the independent-tool cross-check (`top_safe_nodes.py`) and the kept-row-count regression log (flagging a previously-pruned ticker whose kept count unexpectedly drops) are still manual/not yet folded in — open, see the skill file.

## 2026-08-07 (later still) — Paired Opus review of the prune fix/validator round 2: index loss, incomplete tiebreaker, and swap-sentinel live-DB binding gap
**Hypothesis/question:** After the `trades > 0` fix, the recovery merge, and the rewritten `full_db_prune_validate.py` all passed a comprehensive validation (11,183/11,183 real island groups matched exactly) and the swap was executed live, was that swap actually trustworthy, and did the rewritten validator genuinely close the gap the prior review found in v1?
**Method:** Paired independent-cold + contextual Opus review of the diff (`prune_backtest_cache.py`, `full_db_prune_validate.py`, new `top_safe_nodes_full_partial.py`), both verifying claims directly against the real live DB rather than reasoning from code alone.
**Result:** Both reviews converged, no refutations either direction:
- **Real, already-materialized**: `cmd_build()` only copied `sqlite_master type='table'` schema, silently dropping indexes on every swap. Verified directly: the 2026-08-07 swap's pre-swap archive had `idx_bc_ticker`/`idx_bc_version_ticker_strategy`; the post-swap live DB had zero. The validator was blind to this (row-content-only check). Fixed: `cmd_build()` now creates the standard indexes directly on the pruned file (mirrors `run_optimization_sweep.rebuild_indexes()`), and the current live DB's indexes were rebuilt for real.
- **Real, incomplete despite the fix**: the deterministic tiebreaker added in round 1 (`trades DESC, stop_loss, max_hold_hours`) was measured to leave 5,514 of 11,183 real groups (49%) with a non-unique winner, ties spanning up to 9 units on the island-center axis (`take_profit`/`arm_sell_pct`, which wasn't in the tiebreak). Didn't invalidate the already-executed swap (PRE/POST both ran against one static file in one process), but means island centers aren't yet reproducible across separate prune runs. Left open, documented, not fixed this round (scope decision, not urgent for today's data).
- **Real, fixed**: the validation sentinel (added round 1 to gate `cmd_swap`) bound only to the pruned file's own fingerprint, not the live DB's -- so a sweep writing new rows during a long validation run (real same-day exposure: 40+ minutes, spanning a lock-contention crash from a concurrent index build and a rerun) could have silently lost those rows on swap. Fixed: sentinel now also binds to `live_db_fingerprint()` (live DB mtime+size), `cmd_swap()` refuses if the live DB changed since validation. Confirmed the *normal* automated path (`run_liquidity_tranches.sh`'s tight sequential sweep-then-prune) doesn't hit this race in practice -- exposure was specific to today's ad-hoc manual validation runs.
- **Confirmed safe, not a bug**: the XOR-fold fingerprinting approach in the rewritten validator -- both reviews independently verified 0 duplicate identity tuples across the real table (the exact condition that would make XOR-fold cancellation possible), and that `GROUP_COLS + IDENTITY_COLS` exactly matches the table's real primary key, so a false match would need an actual hash collision, not a structural gap.
- **Standing, accepted limitation (not closed by this work)**: `scripts/top_safe_nodes.py` picks its own "best" candidate by raw `alpha_vs_spy` (ranked), a different metric than the prune's own island-center selection (`robust_alpha`). Confirmed directly on AGQ/DPST: the exact same node was selected as "best safe" both pre- and post-prune, but `worst_neighbor` read more optimistic post-prune (AGQ +224.2%→+231.5%, DPST +67.2%→+69.7%) purely because fewer real neighbor rows survived pruning around that specific (non-island-center) row. A `MIN()` over a subset can only rise, never fall, so this bias is structurally one-directional (false-safe only, never false-alarm) -- real but bounded, and a standing limitation of `top_safe_nodes.py` itself, not something this validation gate is meant to close.
**New tool, informational only**: `scripts/top_safe_nodes_full_partial.py` -- reports `worst_neighbor` at radius 2 and 3, tagged FULL vs PARTIAL (genuinely complete neighborhood vs. truncated by the swept grid's own real edge). Run against SOXL/AGQ/DPST pre-swap: all three's #1 candidates sit at or next to the grid's real maximum (`take_profit`/`arm_sell_pct`=30.0, the actual ceiling ever swept) -- none have ever had a genuinely full neighborhood at either radius, independent of any prune logic. All three remain comfortably positive/safe regardless (worst case AGQ, still +224.2% at the narrower reading). User's call: not worth a backlog item to extend the sweep grid past 30 -- "30 is doing something right so I suspect 31 is also going to generally work," and returns at this scale aren't the kind of small-stakes case where the distinction would matter.
**Verdict:** The 2026-08-07 swap itself is trustworthy on data content (both reviews independently confirmed this). Two real gaps found and fixed same day (index loss, sentinel live-DB binding); one real gap found and left open by deliberate scope decision (incomplete tiebreaker, 49% of groups still non-deterministic across separate runs); one standing limitation documented, not closed (top_safe_nodes.py's false-safe-only bias post-prune, structurally bounded).

## 2026-08-08 — Real fill-resolution accuracy check: "possible" is empirically closest to real 5-min fills, not "pessimistic"/"certain"
**Hypothesis/question:** The kernel computes three trailing-buy bounce-fill resolutions per node (possible/pessimistic/certain) and `robust_alpha` takes MIN() across all three as the conservative estimate. Which of the three is actually closest to what a real, continuously-tracking order would do?
**Method:** New `scripts/verify_fill_resolution_accuracy.py` extends the existing `verify_trailing_buy_resolution.py` (which only replayed "possible") to also replay "pessimistic" and "certain" in Python, mirroring `backtester.py`'s exact numba logic for each (lines ~884-905 pessimistic, ~984-1005 certain), then compares all three against real 5-minute-bar ground truth (yfinance, ~58 days back) for the same real historical signals. Ran against 6 tickers from today's liquidity-screen batch (TNA, DRIP, FNGU, UGL, SCO, QID — the ones with signals in the lookback window), 25 real matched signals total.
**Result:** "Possible" was closest to the real 5-min fill in 24 of 25 signals. Mean absolute error: possible=0.107%, pessimistic=0.871%, certain=1.374%. Both pessimistic and certain are systematically biased toward *understating* the achievable fill price (pessimistic mean signed error -0.718%, i.e., predicts a worse fill than reality), not just "more conservative" — they are measurably less accurate, not just more cautious.
**Verdict:** Real, if small-sample (25 signals, 6 tickers, 58-day lookback window — yfinance's 5-min history limit), finding. Taking `MIN(possible, pessimistic, certain)` as `robust_alpha` may be leaving real, empirically-achievable return on the table across the board, not protecting against a real risk — pessimistic/certain aren't validated lower bounds, they're differently-reasoned single guesses that happen to run worse in practice.
**Follow-up, not done:** broader sample (more tickers, once yfinance's 5-min window allows, or a rolling re-check over time) before treating this as settled enough to change the `robust_alpha` convention project-wide. Worth revisiting once more tranches complete and more tickers have real recent signals to check.

## 2026-08-08 (later) — Fill-resolution accuracy check broadened: 66 signals/17 tickers, same result, still not enough to change the convention
**Hypothesis/question:** Same as above — does "possible" hold up as the closest resolution to real fills once checked against a wider, more independent set of tickers (all `TrailingBothZScoreBreakout` candidates registered in `candidate_nodes` so far from the liquidity-screen sweep, not just the original 6)?
**Method:** Same `scripts/verify_fill_resolution_accuracy.py`, run against the best `candidate_nodes` row per ticker for all `TrailingBothZScoreBreakout` entries registered so far (19 tickers: BOIL, DRIP, ERX, ERY, FNGU, GLL, JNUG, KOLD, QID, SCO, SDS, SPCL, SPXU, TECS, TMF, TNA, UGL, URTY, YINN). `TrailingExitZScoreBreakout` candidates excluded — market-buy entry has no bounce-fill resolution to compare. SPXU and TMF had no resolved signals in the 58-day window; the other 17 did.
**Result:** 66 real matched signals (up from 25). "Possible" closest to the real 5-min fill in 62/66 (94%, was 24/25). Mean absolute error: possible=0.166%, pessimistic=0.697%, certain=0.895% (was 0.107%/0.871%/1.374%) — same ranking, same order of magnitude, pessimistic and certain both still biased toward understating the achievable fill (pessimistic mean signed -0.541%).
**Verdict:** Confirms the original finding at ~2.6x the sample size and roughly 3x the ticker count, fully independent tickers (only TNA/DRIP/FNGU/UGL/SCO/QID overlap with the original 6). Still bounded by yfinance's fixed 58-day 5-min-history window — can't get more elapsed time, only more tickers, so this is close to the ceiling of what a single snapshot check can show without a rolling re-check over time. Not yet a "change `robust_alpha` project-wide" decision — that's a cross-cutting convention change (touches every sweep/prune/safety-check query) and is the user's call, not something to act on unilaterally off two accuracy checks, however consistent.
**Follow-up, not done:** a rolling re-check (re-run this same script in a few weeks once yfinance's window has rolled forward, to add a second independent time period rather than just more tickers in the same window) would be the strongest next confirmation. If the user decides to act on this, the actual convention change is itself a `backtest-change-rollout`-scale exercise (touches `run_optimization_sweep.ROBUST_ALPHA_SQL`, `top_safe_nodes.py`, `prune_backtest_cache.py`'s island selection, etc.) — should go through that skill's staged process, not a quick edit.

## 2026-08-08 (later still) — SOXL is an outlier on BOTH the aggregate-return question and the per-signal fill-accuracy question
**Hypothesis/question:** Manually comparing SOXL's currently-selected v5 node's three `alpha_vs_spy` numbers found `possible` (+1212.1%) was actually the LOWEST of the three (pessimistic +1415.0%, certain +8871.5%) — the opposite of the pooled per-signal accuracy finding above, where `possible` usually wins. Is SOXL's own node also unusual on the per-signal fill-accuracy question, or just on the aggregate-return one?
**Method:** New `scripts/compare_fill_resolution_selection.py` (reuses `top_safe_nodes.best_safe_node()`, now `metric`-parameterized, instead of duplicating the cliff-safety selection logic) shows both questions side by side per ticker. Ran against SOXL/AGQ/DPST. Then ran `verify_fill_resolution_accuracy.py --adhoc "SOXL:10:1.0:3.0:70"` (SOXL's actual selected node params) for the per-signal check.
**Result:** Node-selection comparison: ranking by `possible` alone would pick the SAME node for SOXL (rank #1 either way — SOXL's `possible` also happens to be its own node's best rank under either metric), but a DIFFERENT, worse node for AGQ (possible-only robust_alpha +862.1% vs the robust-alpha-selected node's +923.7%) and DPST (possible-only robust_alpha +147.0% vs +286.0%, and fewer trades: 87 vs 101) — possible-only selection is not simply "the same picks, better labeled," it can genuinely pick a worse node. Per-signal accuracy check: SOXL's own 16 real signals show `possible` closest only 12/16 (75%, vs the pooled 66-signal sample's 62/66=94%), mean abs error 3.532% (vs pooled 0.166%) — roughly 20x noisier than the pooled average, with two signals (2026-07-06, 2026-07-24) off 11-16% in the wrong direction.
**Verdict:** SOXL is a real outlier on both axes, not representative of the pooled per-signal accuracy number — consistent with its known "hair-trigger"/high-volatility profile (see `deep_backlog.md`'s prior gap-through-trigger findings for volatile leveraged names). Reinforces the existing decision to keep `robust_alpha = MIN(...)` as the real selection criterion rather than switching to `possible` alone — for at least 2 of 3 tickers checked, possible-only selection would have picked a strictly worse node.
**Follow-up, not done:** run the per-signal 5-min check against more tickers' own actual selected nodes (not just the pooled candidate_nodes list) to see whether SOXL's noise level is itself the outlier or whether high-volatility leveraged tickers as a class run noisier than the pooled average suggests.

## 2026-08-08 (later still, 2) — 3-way candidate comparison (cliff-safe / best-robust-ignoring-cliff / best-possible) across all 19 liquidity-screen candidates: possible-only selection consistently sacrifices robustness for no accuracy gain
**Hypothesis/question:** User's framing: since `possible` is empirically the most accurate single resolution (per the two entries above), should node SELECTION lean toward it -- and if so, does running the 5-min accuracy check as part of candidate review (across multiple candidate types, not just the one currently selected) support that?
**Method:** New `scripts/candidate_5min_report.py` finds up to 3 candidates per ticker -- cliff-safe (current convention: robust_alpha, MIN of the three, required to pass the CLIFF_RADIUS=3 neighbor check), best robust_alpha ignoring cliff-safety (top MIN() row regardless of neighbor safety), and best possible (top raw alpha_vs_spy row) -- deduped when identical, then runs the 5-min replay against each candidate's own actual entry params. Run against all 19 TrailingBoth liquidity-screen candidates (BOIL, DRIP, ERX, ERY, FNGU, GLL, JNUG, KOLD, QID, SCO, SDS, SPCL, SPXU, TECS, TMF, TNA, UGL, URTY, YINN).
**Result:** Two real, distinct findings:
1. **Most of these 19 tickers (11 of 19) have NO cliff-safe candidate at all** at the 200%-alpha floor (BOIL, ERY, FNGU, GLL, QID, SCO, SDS, SPXU, TECS, YINN, and TMF at one of its two rows) -- only JNUG, KOLD, and TNA had a genuine cliff-safe pick in this batch. Consistent with the existing backlog finding that most of this liquidity-screen batch is fragile (`backlog_resolved_recent.md`'s 2026-08-08 candidate-analysis entry: only 6/16 tickers cliff-safe on the first pass).
2. **Where best-possible differs from the robust pick (8 of 19 tickers: ERX, JNUG, SPCL, TMF, TNA, UGL, URTY -- 7 tickers with a real divergence), the possible-only pick's own robust_alpha is LOWER in every single case**, often by a lot: ERX 105.3%→54.1%, JNUG 274.2%→142.5%, SPCL 184.7%→152.2%, UGL 49.6%→24.8%, URTY 304.6%→251.9%, TNA 292.7%→283.4%. Meanwhile the 5-min per-signal accuracy is essentially IDENTICAL or only marginally better on the possible-only pick (e.g. TNA: 0.333% mean abs error on the robust pick vs 0.011% on the possible pick -- both already excellent) -- `possible` already wins the per-signal accuracy race under the ROBUST pick's own params in nearly every ticker, since robust_alpha's own `possible` component is usually what's being measured against ground truth regardless of which node got selected.
**Verdict:** Running the 5-min check as part of candidate review does NOT support switching to possible-only selection -- it does the opposite: it shows that chasing the higher raw `possible` number picks a worse-robustness node while buying little to no real accuracy improvement, since `possible`'s accuracy is already being captured by the robust-alpha-selected node's own `possible` resolution. Real caveats: sample sizes are small (1-13 signals per ticker in the 58-day window) so per-ticker error percentages are noisy; several tickers had a candidate with 0 or 1 resolved 5-min signal (SDS, SPXU: zero; several others: 1). Confirms the standing decision to keep `robust_alpha = MIN(...)` as the real selection criterion.
**Real bug found and fixed while building this**: `candidate_5min_report.py`'s first run crashed on JNUG -- `top_safe_nodes.best_safe_node()`'s cliff-safe candidate dict is keyed `alpha`, not `robust_alpha` like the other two candidate types built directly from a DataFrame row. Normalized before the summary table is built. No prior tool combined these two candidate sources in one script, so this key mismatch had never surfaced before.
**Follow-up, not done:** fold this into `candidate_summary_report.py` as a column (agreed direction, not yet built) once the pattern above is trusted further; a genuine out-of-sample re-check once yfinance's window rolls forward would strengthen the small per-ticker sample sizes.

## 2026-08-08 (later still, 3) — Raw alpha_vs_spy isn't cross-ticker comparable: calendar windows range from 0.31y to 3.04y; annualizing re-ranks candidates significantly
**Hypothesis/question:** User's doubt: is `robust_alpha`'s real problem not the MIN()-robustness part but the fact that raw `alpha_vs_spy` numbers across tickers span very different real time horizons, making direct comparison unfair regardless of the resolution-selection question?
**Method:** Confirmed directly: `run_optimization_sweep.compute_bh_returns` computes `asset_bh`/`spy_bh` over each ticker's own full cached-hourly-data date range (`df.index.min():max()`), not a shared window. Checked real cached CSV date ranges: SOXL/AGQ/DPST ~3.0-3.04y, UGL/JNUG ~1.1y, SPCL ~0.31y (4 months). New `scripts/annualized_alpha_report.py` computes CAGR from `strategy_return`/`spy_bh` over that same per-ticker calendar span (recomputed fresh from the CSV, since compute_bh_returns doesn't persist day-count) -- deliberately annualizing over the FULL calendar span, not invested-only time (user's explicit call: cheaper, no trade-list replay needed, directly fixes the horizon-mismatch problem; doesn't account for the strategy only being in the market some of that time).
**Result:** Real, large re-ranking. Raw robust_alpha order: SOXL (+1212.1%) > AGQ (+923.7%) > TNA (+292.7%) > DPST (+286.0%) > JNUG (+274.2%). Annualized-excess order: **JNUG (+225.1%/yr) > SOXL (+119.3%/yr) > AGQ (+108.1%/yr) > DPST (+51.7%/yr) ~ TNA (+51.3%/yr)** -- JNUG jumps from raw-alpha rank 4 (of 5) to a clear #1 once its shorter 1.11y window is accounted for; SOXL/AGQ's raw dominance shrinks sharply (both still solidly ahead of DPST/TNA, but nowhere near the ~4x gap the raw numbers implied).
**Verdict:** This is a real, separate finding from the possible/pessimistic/certain resolution-selection question above -- it doesn't touch `ROBUST_ALPHA_SQL` or the real selection convention at all, purely a reporting-layer annualization using data already available (deliberately scoped this way, not a "backtest-change-rollout"-scale refactor). Real open question: should annualized_excess actually inform prioritization/promotion decisions going forward (e.g. which liquidity-screen candidate to validate next), not just robust_alpha's raw magnitude? Not decided.
**Follow-up, not done:** fold into `candidate_summary_report.py`/`candidate_5min_report.py` as a column; extend to the full 19-ticker liquidity-screen batch (only ran 9 so far); decide whether/how annualized_excess should factor into real candidate prioritization.

## 2026-08-08 (later still, 4) — Overlay robustness stress test on the 19-ticker batch's positive addon/drought results: several fragile, some hold up
**Hypothesis/question:** After restructuring `candidate_summary_report.py` to compute real per-node addon/drought overlay for all 3 candidate rows per ticker, which of the resulting positive overlay numbers are real signal vs. one-lucky-trade artifacts (the same failure mode `docs/overlay_parameter_robustness_process.md`/`candidate_overlay_robustness_check.py` was built to catch, first found on TNA's drought overlay 2026-08-08 earlier).
**Method:** Ran `candidate_overlay_robustness_check.py` (chronological split + single-trade-removal stress test) against every (ticker, mechanism, candidate_node_id) combo from the batch with addon/drought compounded>5% and n>=5 real trades: BOIL/GLL/JNUG/KOLD/SCO/TNA/UGL/URTY, 15 checks total (several tickers had multiple registered candidate_nodes rows across today's re-runs -- disambiguated by matching each row's own robust_alpha to the exact candidate row being checked).
**Result:** Sign FLIPS negative with the biggest winner removed (fragile, single-trade-driven, same failure shape as TNA's earlier-found drought result): KOLD addon (+29.8%->-2.1%), TNA drought (all 3 candidate variants: +8.6%->-18.3%, +8.5%->-16.6%, +6.2%->-20.1%), UGL addon best-unsafe-node variant (+25.0%->-4.1%), URTY drought (both variants: +12.6%->-15.4%, +6.1%->-20.3%). Sign HOLDS (passes this check, not proof of a real edge on its own): BOIL addon (both variants), JNUG addon (both variants), UGL addon best-possible variant (+42.3%->+8.9%), URTY addon (both variants).
**Verdict:** Real, consequential result -- roughly half the newly-registered positive overlay numbers in this batch are fragile. TNA's drought overlay fragility reconfirms the earlier 2026-08-08 finding (same conclusion, now checked against a slightly different node set from today's re-registration). URTY's drought result is a new fragile finding. Passing candidates (BOIL/JNUG/UGL-possible/URTY-addon) still need the remaining robustness_process.md steps (out-of-sample split, included-vs-excluded challenge) before being trusted as real -- this check is necessary, not sufficient.
**Follow-up, not done:** run the remaining `overlay_parameter_robustness_process.md` steps on the candidates that passed this first filter; the multiple-candidate_nodes-rows-per-ticker situation (several tickers now have 2-4 registered ids from different sessions' re-runs) is itself a minor cleanup item -- not deduped/pruned, just growing.

## 2026-08-09 — Quarterly-rebalance overlay vs skim-and-redeploy vs 100%-strategy baseline, portfolio-level (all 10 real v5 watchlist tickers, equal-weighted)
**Hypothesis/question:** A separate conversation floated calendar-based quarterly rebalancing (reset strategy/SPY-reserve weights to a fixed target every ~63 trading days) as a simpler, "safer" alternative to the skim-and-reserve design (event-triggered: skim on a new-high threshold, redeploy on a drawdown threshold) — does it hold up against the same real crash-window stress test, and does it need less live-trading code (no new trigger logic to build/test)?
**Method:** New `scripts/sim_quarterly_rebalance.py`, reusing `sim_bear_market_stress.py`'s synthetic daily-bar leveraged-ETF harness and the same 4 real crash windows (2008 GFC, 2020 COVID, 2022 bear, 2000 dotcom). Built a portfolio-level combiner (not in the existing per-ticker `sim_skim_redeploy.py`): each of the 10 real watchlist tickers' (AGQ, DPST, GDXU, HIBL, KORU, NUGT, SOXL, UDOW, USD, YANG) own daily-bar strategy equity curve is reindexed onto SPY's trading-day calendar and equal-weight-averaged into one portfolio curve, then both overlays are applied to that combined curve vs SPY. Ran the head-to-head (`--target-weight 0.80 --rebalance-days 63`, matching skim's default `--skim-step 0.10 --skim-frac 0.20 --redeploy-step 0.15 --redeploy-frac 0.25`) plus a 3x3 sweep (target_weight {0.70,0.80,0.90} x rebalance_days {21,63,126}).
**Result:** 2008/2020/2022 all show **skim_max_reserve = 0.000** — skim never fired a single skim event in those 3 windows. Root cause, confirmed by reading the trigger logic: skim only fires on a new equity HIGH >= skim_step above the window's own starting reference; the portfolio's daily-bar strategy approximation never closed 10% above its window-start level in any of those 3 windows (baseline compounded return was net negative — -12.4%/-10.2%/-5.3% — across the full decline+recovery span), so skim structurally had nothing to work with. Quarterly rebalance, by contrast, moves capital on a fixed schedule regardless of price level, reaching 22-23% mean reserve fraction in those same windows and beating both baseline and skim on return in all 4 crash windows (portfolio means: baseline -26.3%, skim -23.7%, quarterly-rebalance -20.4%) — though NOT on max drawdown, which was roughly flat-to-slightly-worse than baseline/skim (-39.4% vs -40.4%/-38.9%), since even the 80/20 default keeps most capital in the (in this window) underperforming strategy through the crash leg itself; the return edge comes from the recovery leg, where periodic forced trims/rebuys into SPY captured more of SPY's own recovery than staying 100% in a strategy that underperformed SPY over the full window. Sweep: return improves monotonically as target_weight drops (more SPY, less strategy) — 70% target beats 80% beats 90% at every rebalance_days tested; rebalance_days itself (21 vs 63 vs 126) has a much smaller effect than target_weight. Only 2 tickers (HIBL, UDOW) have proxy history reaching back to 2000_dotcom — thin sample, that row should be weighted less than the other 3.
**Verdict:** Real, useful finding but with an important caveat on interpretation: quarterly rebalance's apparent edge here is largely a byproduct of this specific daily-bar synthetic approximation of the mean-reversion strategy underperforming SPY over these particular long crash+recovery windows — it is NOT evidence quarterly rebalance is a generically better risk-reducer than skim. What IS a robust, mechanism-level finding: skim's event-triggered design is structurally reserve-inactive in a scenario with no prior real gains to skim (a real risk for the "safer" framing — skim gives zero protection exactly when a fresh position immediately hits a crash before ever making a new high), while calendar rebalancing acts unconditionally. That's the actual argument for "feels safer" being right, distinct from the return-comparison numbers, which are window-specific and shouldn't be over-read. Same caveats as every `sim_bear_market_stress.py`-based result apply (daily-bar, not the live hourly kernel; no expense-ratio/financing drag).
**Follow-up, not done:** this doesn't touch the already-decided-in-principle skim-and-reserve design (`deep_backlog.md`'s 2026-08-04 entry) — that stays on file as-is; this is exploratory, run at the user's request as a second option to weigh, not yet a decision. Would need: (1) a scenario where the strategy DOES have prior gains before the crash starts (the real live case — the portfolio won't start fresh at a crash's exact onset), to give skim a fairer test; (2) real starting_notional-weighted portfolio combination instead of equal-weight, since real node sizing varies $500-$50k across nodes; (3) resolving which of quarterly-rebalance vs skim vs a hybrid is the actual real-money design, which is the user's call, not this script's.

## 2026-08-09 (later) — Daily-bar portfolio sim badly undersold the algo legs vs. real hourly-kernel numbers; real KORU/SOXL/AGQ beat SSO by 2-4x CAGR over the actual validated window
**Hypothesis/question:** The 30-year daily-bar 4-bucket portfolio sim (this session, `scripts/sim_1m_four_bucket_portfolio.py`) found Portfolio A (3 accounts, core-strategy-only KORU/SOXL/AGQ + SSO/TQQQ/Cash) beat plain 100% SSO in only 29% of cohort-horizon pairs, and doubling the algo weight (50/50 SSO/ticker) made it worse (14%). User's live reaction: "this is daily bar... it's not actually the algo" — is the daily-bar approximation (core-strategy-only, no drought/add-on, once-a-day signal check) losing the real strategy's edge, which lives in intraday hourly z-score signals?
**Method:** Pulled the actual real live `watch_list` nodes for KORU/SOXL/AGQ (watchlist_id=65) and their exact `backtest_cache` v5 rows (real hourly kernel, not the daily-bar approximation), over the real cached data window (2023-07-24 to 2026-08-07, 3.04 years — yfinance's hourly retention ceiling, as far back as the real kernel can be validated at all). Compared `strategy_return` against SSO's own real price return over the identical window (fetched directly).
**Result:** SSO real: +141.3% total / 33.6%/yr CAGR. KORU (real hourly-kernel strategy): +378.7% / 67.4%/yr (2x SSO's CAGR). AGQ: +1,178.6% / 131.2%/yr (~4x SSO). SOXL: +1,275.7% / 136.9%/yr (~4x SSO). All 3 real algo tickers dominate SSO by 2-4x CAGR over the window they're actually validated on.
**Verdict:** Confirms the daily-bar sim's "algo underperforms SSO" finding was a real artifact of the approximation, not evidence about the actual live strategy — the daily-bar `run_strategy_daily` (one signal check per day) structurally can't see the intraday z-score triggers the real hourly kernel trades on. **Real, unresolved limitation**: there is no way to get a true apples-to-apples 30-year (multi-regime, incl. dot-com/2008) comparison of the REAL hourly kernel vs. SSO — real hourly data only reaches back to 2023-07 (yfinance retention), which is why the 30-year sim had to use the daily-bar approximation in the first place. The 2-4x CAGR edge is real but only demonstrated over a ~3-year, mostly-favorable-for-leveraged-ETFs window; whether it holds through a real prolonged bear market (dot-com/2008-style) is still genuinely open, and the daily-bar sim's crash-window behavior (drawdown, recovery) can't be assumed to transfer to the real kernel's actual behavior in a future crash.
**Follow-up, not done:** no clean way to close this gap without either (a) a from-scratch synthetic-hourly-bar reconstruction back to 1995 (far more speculative than the existing daily-bar reconstruction, not attempted), or (b) accepting the daily-bar sim as a crash-behavior proxy only, while trusting the real hourly kernel's numbers for return magnitude over its own validated window — these two would need to be combined carefully, not read as contradicting each other.

## 2026-08-09 (later still) — Real hourly-kernel replay through the only real drawdown in the cached window (SPY -18.8%, 2025-02-19 to 2025-06-26): SOXL lost money for real, KORU/AGQ profited, blended algo bucket net positive
**Hypothesis/question:** Following the daily-bar-vs-real-kernel discrepancy found earlier this session, user asked directly: how do we know how the real strategy performs during an actual downturn? Real hourly data only reaches back to 2023-07, so no real 2008/dot-com-scale test is possible -- but is there ANY real drawdown within the cached window to test the real kernel against, rather than a synthetic/approximated one?
**Method:** Found the worst real SPY drawdown within the cached hourly window directly from real price data: -18.8%, peak 2025-02-19 ($602.61) to trough 2025-04-08 ($489.59), recovered by 2025-06-26. Ran the REAL hourly kernel (`backtester.run_backtest_dispatch`, not the daily-bar approximation) for KORU/SOXL/AGQ's actual live node params over the full cached history, then filtered to trades entered within that window.
**Result:** KORU: 7 trades, 42.9% win rate, +45.0% compounded return over the window. SOXL: 29 trades, 37.9% win rate, **-15.3% compounded return** -- real losses, driven by a long stretch of consecutive -2.0% SL stop-outs as SOXL's own price collapsed ~$26->$7.75 in early April. AGQ: 10 trades, 40.0% win rate, +16.3%. Equal-blend of the 3: net positive (~+15%) through a real ~19% market drawdown.
**Verdict:** First real (non-synthetic, non-approximated) evidence of how the actual live strategy behaves under real market stress. SOXL proves the strategy is NOT immune to drawdowns -- real losses did occur. But SL discipline capped each loss individually rather than compounding into a blowup (worst single trade in this stretch: -15.5% return on one trade, not a portfolio-wiping event), and cross-ticker diversification worked empirically, not just in theory -- KORU/AGQ both profited through the identical macro stress that hurt SOXL. Real, important caveat: this is one -18.8% event, nowhere near 2008/dot-com scale (-50%+), so it says nothing about behavior in a true crash -- only that the mechanism (tight SL + diversification) behaved as designed at this magnitude.
**Follow-up:** build a real (non-synthetic) daily-bar backtest using actual SOXL/AGQ/KORU historical daily OHLC (real ETF inception: SOXL 2010-03, AGQ 2008-12, KORU 2010-01 -- covers the real 2020 COVID crash and 2022 bear market, though not 2008 GFC/dot-com which predate these tickers) instead of the synthetic underlying-proxy reconstruction used in sim_bear_market_stress.py -- removes the reconstruction-uncertainty layer while keeping the (already-understood) daily-bar entry-signal-frequency undercount caveat.

## 2026-08-09 (later still, 2) — Real-ticker (not synthetic-proxy) daily-bar crash test: strategy beats buy-hold in 5/6 real cases, but KORU took a real -64.1% loss in the 2022 bear market
**Hypothesis/question:** Direct follow-up to the prior entry -- "redo the strategy for daily prices" using ACTUAL SOXL/AGQ/KORU historical daily OHLC (real ETF inception dates) instead of sim_bear_market_stress.py's synthetic underlying-proxy reconstruction (SOXX/EWY/SLV x leverage), to remove the reconstruction-uncertainty layer while still testing real historical down-market windows.
**Method:** New `scripts/sim_real_ticker_daily_crash_stress.py` -- fetches real daily OHLC directly for SOXL (inception 2010-03-11), AGQ (2008-12-04), KORU (2013-04-10), reuses the same `run_strategy_daily` daily-bar approximation and each ticker's real v5 node params, run through the 2 crash windows that postdate all 3 tickers' real inception (2020 COVID, 2022 bear -- 2008 GFC and 2000 dotcom predate all 3 real tickers, not computable this way).
**Result:** SOXL: 2020 COVID strategy +24.3% vs buy-hold -8.4%; 2022 bear strategy -7.0% vs buy-hold -54.8%. AGQ: 2020 COVID strategy +34.4% vs buy-hold +97.4% (strategy badly lagged the real silver rally); 2022 bear strategy +8.5% vs buy-hold -26.2%. KORU: 2020 COVID strategy -15.2% vs buy-hold -39.1%; 2022 bear strategy **-64.1%** vs buy-hold -72.1%.
**Verdict:** Strategy beat naive buy-and-hold in 5/6 real ticker-crash pairs, several by a wide margin (SOXL's 2022 bear especially: -7% vs a real -54.8% buy-hold loss). But it is NOT a universal downside-protection guarantee -- KORU's -64.1% real loss in the 2022 bear window is a genuine, severe drawdown, not a near-miss, and consistent with the separately-found overfitting concern on KORU's overlay config (see earlier 2026-08-07 research_log entry) -- reinforces treating KORU as the weakest of the 3 real algo tickers rather than a coincidence. AGQ's large opportunity-cost gap in 2020 COVID (missed ~63 points of the real silver rally) shows the strategy's exit discipline cuts real winners short too, a cost as well as a protection. Combined with the prior two entries (real hourly kernel through the 2025 drawdown; original synthetic-reconstruction daily-bar through 2000/2008/2020/2022), the honest triangulated picture: the strategy usually reduces real drawdown damage vs. raw buy-and-hold, but individual tickers can still take severe real losses -- cross-ticker diversification is doing real work here, not just theoretical work.
**Follow-up, not done:** same daily-bar entry-signal-frequency caveat as every other daily-bar test in this project applies here too (once-a-day check undercounts real intraday z-score triggers) -- these numbers likely understate the real strategy's actual crash-period performance the same way the earlier 30-year sim did for bull-market performance, just in an unknown direction for drawdown specifically. No way to close this without either extending real hourly data retention forward through a future real crash (waiting), or a materially more speculative synthetic-hourly-bar reconstruction (not attempted, flagged as high-risk-of-artifact in the prior entry).

---

## 2026-08-09 (later) — "certain" fill-resolution's credited entry price is unobtainable by any real execution mechanism; corrected price drops SOXL's example node ~10x
**Hypothesis/question:** Prompted by SOXL's `best certain` candidate row showing an "absolute ridiculous" `alpha_certain_pct` (+13,799.9% headline, +13,030.7% on a fresh rerun against current cached data) — is this number miscalculated, and separately, could a live execution mechanism be built to actually capture it?
**Method:** Manually re-ran the real, unmodified kernel (`run_backtest_v110`, `TrailingBothZScoreBreakout`, SOXL, window=10/z=1.0/trail_buy=5%/trail_sell=7%/arm=29%/sl=2%/hold=70/open_check) and traced individual trades bar-by-bar against real cached hourly OHLC, reproducing the kernel's exact `certain` logic (`backtester.py:984-1003`) in plain Python for inspection.
**Result:** The compounding arithmetic itself is exact and correct (manually multiplied all 101 trades' returns, matched `_summarize_trades`'s output to the decimal) — not a calculation bug. But the mechanism has a real flaw: `certain`'s Close-confirmed fill case credits the entry at the *theoretical trigger price* (`running_low × (1+trail_buy_pct)`), which by the time the bar's Close proves the fill happened, is a price that has already passed — not obtainable by any real order (neither a resting trailing-buy order, which would already have filled earlier via a `possible`-style same-bar High touch if one existed, nor a reactive order placed once certain, which could only transact at the Close price or later, not the earlier trigger level). Traced one real diverging trade in full: signal 2023-09-13 14:30, a spike-and-reverse bar (2023-09-18 12:30, High=20.180 clears trigger=20.160 but Close=20.037 falls back below it) causes `certain` to reject the fill and keep waiting 4 more calendar days while price kept falling, eventually filling 2023-09-22 11:30 at Close=18.290 crediting entry at trigger=18.259 — a price $0.031 (0.17%) *better* than the Close itself, i.e. impossible to actually obtain. Rebuilt the simulation with the one fix (Close-confirmed case credits entry at the bar's real Close, not the trigger; Open-confirmed case left unchanged, since Open is realistically actionable within seconds via the existing live `get_session_open_price` pinned-check mechanism) and reran full exit logic (same arm/trail_sell/SL/TIME state machine) end to end.
**Verdict:** Corrected result: **n=111 trades, compounded +1,340.5%, alpha vs SPY +1,270.3%, win_rate 10.8%** — down from the original +13,030.7%/18.8% by roughly 10x. Passed the standard robustness stress test (no single-trade sign flip: removing the biggest winner leaves +631.8%; win rate improving late vs early, not fading) but showed real new fragility the uncorrected number masked: first-half (through 2025-02) compounded return collapsed to +30.4% (vs the uncorrected version's +548.4%), with nearly all the corrected edge concentrated in the second half (+1,004.7%, closely tied to SOXL's already-flagged extraordinary 2025-12→2026-05 rally). Real, still-positive alpha survives the correction, but is far smaller and far more concentrated in one historical window than the uncorrected `certain` resolution implied.
**Follow-up, not done:** (1) whether this same correction meaningfully changes other tickers' `best certain` rows (only SOXL traced so far); (2) `robust_alpha`'s `MIN(possible,pessimistic,certain)` selection convention is NOT directly corrupted by this (certain is the *inflated outlier*, not the minimum, for this SOXL row specifically — `possible=537.3%` already wins the MIN), but `candidate_full_review.py`'s new "best certain" candidate-selection type (added 2026-08-09, ranks nodes directly by `alpha_certain_pct`) is directly exposed and should probably be reconsidered or fixed at the source (the kernel's `certain` price-crediting logic itself) rather than trusted as-is; (3) a genuinely new live-executable mechanism ("CertainEntryTrailingSell" — confirmed entry via bar-close proof, trailing-sell exit unchanged) was scoped in conversation but not built — real open design questions found: it would need new hourly polling infrastructure (only 53.5% of the 101 original-certain fills land in the 2 existing signal-check windows; the rest need all ~7 daily bars watched) and an explicit overnight/EOD exposure policy the backtest kernel never actually resolves (it just treats bar-data gaps as free, which live execution can't).

## 2026-08-09 (later still) — `certain` kernel fix applied (frozen-trigger case + real-Close crediting); per-ticker drop ratio ranges 2x-11x, not uniform (correction below: KORU does NOT flip to a loss)
**Follow-up to the entry above.** Two real, independent bugs in `_simulate_trail_both`'s certain branch (`backtester.py`), both now fixed: (1) the Close-crediting bug already described above; (2) a second, previously-unnoticed bug — a bar where the running low doesn't move (this bar's own Low is above the already-established running low) has its trigger price frozen for the entire bar regardless of intrabar High/Low ordering, so a High-touch on such a bar is exactly as certain as an Open-gap and should fire immediately — the old code only ever checked Close on these bars, needlessly deferring a provable fill (found by manually tracing SOXL's 2023-09-18 12:30 bar: trigger frozen at 20.16 since that bar's Low of 19.95 never undercuts the already-set 19.20 running low; High=20.18 clears it, but old `certain` deferred 4 more days to 2023-09-22 anyway). On SOXL's own node, 56 of `possible`'s 141 total fills (40%) were this frozen-trigger case — not a rare edge case.

**Fix applied**: both cases added to `_simulate_trail_both`'s certain branch (`backtester.py:984-1024`), plus a new standalone lean kernel `_simulate_certain_only` (no possible/pessimistic bookkeeping, for the cheaper full-cache recompute this enables — see backlog) and wrapper `run_backtest_certain_only`. Verified the lean kernel is byte-identical to the corrected bundled kernel's certain output (0 mismatches, SOXL n=119/119, HIBL, GDXD).

**Real methodology bug found and corrected in the same session**: a hand-written Python mirror (`certain_fixed_trades()`, used throughout this session's manual "true certain-fixed" analysis across ~80 tickers) was built with the Close-crediting fix only, *before* the frozen-trigger fix was discovered, and never updated afterward — confirmed diverging from the real corrected kernel on every ticker checked (SOXL 111 vs 119 trades, HIBL, AGQ, GDXD), mismatching from the very first trade. All conclusions drawn from that mirror during this session (the 82-ticker "certain-fixed core+drought" table, specific SOXL/HIBL/AGQ add-on/drought numbers) are unreliable and were superseded by a rerun against the real, verified kernel. Lesson: a hand-rolled Python mirror needs the same verification-against-the-real-kernel discipline as any other trusted mirror (`simulate_trail_both_annotated`, which *was* separately verified this session and found to match the kernel exactly, 0 mismatches) — don't trust a fresh mirror just because it "looks like" it implements the fix.

**Per-ticker impact of the certain fix, rerun against each ticker's own top `alpha_vs_spy_certain` node** (real kernel, not the buggy mirror): drop from the old buggy value is NOT a uniform ~10x — ranges roughly 2x (AGQ, NUGT) to SOXL's 10.8x. **Correction, found during session-wrap's required paired Opus review**: this entry originally claimed "KORU flips sign entirely... to a corrected small loss" — that number came from the discarded hand-mirror (missing the frozen-trigger fix, see above), not the real kernel. The real, kernel-verified value for KORU's own top-certain node is **+64.6%, still clearly positive** — no sign flip. Full per-ticker table in session scrollback / job tmp files, not yet committed anywhere permanent.

**Paired Opus review + isolation test (session wrap, same day)**: the paired independent+contextual review of this diff converged (after a rebuttal exchange) on a CONFIRMED HIGH finding: the Close-crediting fix is not actually a proven no-guess price the way its own comment claims — it's a *different* guess, and by construction (`cp >= buy_trigger_updated` is the branch's own entry condition) it is **provably always ≥ the old buggy code's credited price**, a one-directional bias, not a mixed-error correction. A direct isolation test (frozen-trigger fix alone, keeping the OLD Close-crediting via `buy_trigger_updated`, vs. the real shipped fix with both changes) confirms this is the dominant driver of the reported drop, not the frozen-trigger fix: frozen-trigger-only actually IMPROVES every ticker checked over the original buggy baseline (SOXL +3099.5%, KORU +914.6%, AGQ +1523.5%, HIBL +1381.4%, GDXD +926.1%), while adding the Close-crediting fix on top pulls all of them down substantially (SOXL +1439.7%, KORU +64.6%, AGQ +1061.7%, HIBL +687.4%, GDXD +340.4%). Both reviewers proposed `min(buy_trigger_prior, high)` as a genuinely tight, provable worst-case-price bound instead of `cp` — confirmed mathematically valid by both, but noted as a real semantic shift: it would make `certain` a *pessimistic-price* bound, contradicting its own docstring's claim that "certain can end up better than possible" (that property only holds for the frozen-trigger case, not the Close-confirm fallback). **Not fixed this session** — left as an open, documented question (see `docs/backlog_cache.md`) pending a decision on which definition of "certain" (provable fill vs. provable price) the project actually wants, rather than picking one under time pressure at session wrap. Also confirmed independently by both reviewers: two other scripts (`scripts/export_trades.py::simulate_trail_both_signal_tracked`, `scripts/verify_fill_resolution_accuracy.py`) still run the pre-fix certain logic and are now silently stale — `verify_fill_resolution_accuracy.py` specifically is the tool whose finding ("possible is 94% accurate") justified defaulting to possible, so it should be fixed before being trusted again. Neither was fixed this session (tabled alongside the rest of the certain-resolution work).

**Bigger architectural finding, not previously documented**: the entire candidate-reporting/overlay pipeline (`candidate_full_review.py`, `candidate_summary_report.py`, `run_overlay_shim.py`, `drought_overlay_test.py`) only ever generates and evaluates `possible`-resolution trades. `pessimistic`/`certain` are used *exclusively* as scalar inputs to `robust_alpha=MIN(possible,pessimistic,certain)` — a safety/ranking signal only. Even the "best certain" candidate type (added 2026-08-09) is selected by ranking on `alpha_vs_spy_certain`, but every downstream number for that row (`abs_return_pct`, CAGR, years, trades, drought/add-on overlay via `get_trades_and_bars`) is unconditionally computed from `possible`'s own trade sequence — traced directly in code (`candidate_5min_report.py::_node_from_row`: `'return': row['strategy_return']`, unconditional for all 4 candidate types). So "best certain" has never actually been overlay-tested or CAGR-reported against its own real trade behavior; the tooling structurally can't do that today. User's framing: `possible`, `pessimistic`, and `certain` (all three, once correctly computed) are viable real execution paths, not just a fill-ambiguity artifact to resolve down to one number — `possible` is the most-likely default (per the existing 94%-accuracy finding), but `certain` especially deserves its own real evaluation path since it requires no guessing at all. Backlogged as its own item, see `docs/backlog_cache.md`.

## 2026-08-12 (evening) — Paper-vs-backtest reconciliation investigated end to end: 2 real code bugs found and fixed (a stale comparison-tool node-selection heuristic, and a live-tick pricing gap in paper's own trailing-buy fill simulation), 2 more tickers' divergence explained by real operational history, not code
**Hypothesis/question:** User asked whether the paper-trading portfolios had actually traded, then how they were doing vs. their own backtest counterpart — `scripts/paper_vs_backtest_reconcile.py` showed large, alarming divergences (paper losing on tickers where the backtest showed gains) across HIBL/USD/SOXL/KORU/YANG, enough that the user considered halting live trading over it. Before that decision: is this a real signal-computation/live-execution problem, or something else?
**Method:** Direct replay of the real production signal path (`signals_compute.compute_buy_signal`) against real historical prices at each paper trade's actual entry moment, cross-checked against `watch_list_audit` (to rule out a stale/edited node config invalidating the comparison) and the kernel's own trace-capable mirror (`scripts/export_trades.py::simulate_trail_exit_chaos`'s `trace=` param) to see exactly what the backtest replay was doing bar-by-bar at the same moments. For SOXL specifically, cross-referenced the real verbose poll log (`logs/active_signals_verbose.log`) against the real hourly OHLC bar to find the exact mechanism.
**Result — 4 distinct causes found, no signal-computation or live-execution bug in any of them:**
1. **Comparison-tool bug #1 (fixed)**: `paper_vs_backtest_reconcile.py` selected each ticker's comparison node via `drought_detection_test.load_nodes()` (`MIN(id)` among currently-`state='paper'` nodes) — broke the moment a ticker's real paper-trading node got promoted to `state='live'` (SOXL) or whenever duplicate/candidate nodes existed for the ticker (true for every ticker checked: HIBL, USD, SOXL, KORU, YANG, AGQ, UDOW all had 5-14 duplicate nodes sharing the ticker). It was comparing paper's real trades against a completely different node's config — different strategy class, window, z-threshold, fixed_sl in every case. Fixed via `resolve_live_track_nodes_by_activity()`: resolve nodes by which `wl_id` actually has rows in `paper_trade_log`/`paper_positions`/`paper_pending_buys`, not a guess.
2. **Comparison-tool bug #2 (fixed)**: even after fix #1, the backtest replay ran over the *full* multi-year price history unconditionally, so it could be simulated as still holding a position "opened" weeks before the paper node even existed. Confirmed real for 2 of 5 tickers by checking whether the full-history backtest was actually mid-trade at each node's real `added_at` (2026-07-20 for all): KORU was mid-trade 07-17→07-21 (spanning its own creation), YANG was mid-trade in one continuous position from 07-20 all the way to 08-11 (22 days). HIBL/USD/SOXL were already flat at creation — confirming the fix is a no-op for those three, not a blanket assumption. Fixed via `get_trades_and_bars_since(node, sim_start)`: same kernel mirror, same full indicator lookback (SMA/std unsliced, since those are indexed per-calendar-day via `daily_idx`, not by array position), but the hourly OHLC/timestamp arrays fed to the bar-by-bar simulator are sliced to start at `sim_start` so the simulator starts flat like paper did.
3. **Real code bug in `paper_trading.py` (fixed)**: SOXL's real divergence traced to `update_paper_buys()` — the loop tracking a paper trailing-buy's `running_low`/trigger across polls — pricing itself entirely off `signals_compute._current_price()`, which reads the last row of a cached hourly-bar CSV refreshed only on the background data-collector's own schedule, not a live tick. Proved via the real verbose poll log: on 2026-07-27, SOXL gapped up to a real Open of $140.06 (the trailing-buy trigger was $140.5435 — missed by $0.46 on the one poll that caught the fresh price), then crashed to a Low of $120.73 within the same hourly bar. `_current_price` returned the identical frozen $140.08 on every ~34-second poll for 29 straight minutes (the poll loop itself never stalled), then jumped straight to $132.53 once the cache finally refreshed — well past the real gap. Paper's simulated fill never saw the trigger-crossing window and instead chased the already-crashed price down, eventually bounce-filling at $121.615 around 11:21am — a large, real divergence from the corrected kernel's actual $140.06 Open fill (which does apply the real 2026-07-19 gap-through-trigger fix correctly). Fixed by extracting `signals_compute._live_tick_price()` (the same yfinance 1-minute fetch `compute_buy_signal` already used for its own signal-check price, just factored out) and having `update_paper_buys()` upgrade to it after `_current_price()`'s existing None/staleness gate passes (which stays in place, unchanged — still needed for the entry-abandon-timeout logic's no-data case).
4. **Real operational history, not a bug**: HIBL/USD's remaining divergence traced to both nodes being flipped to `mode='live'` from 2026-07-23 12:56:53 to 2026-07-25 07:31:46 (the documented "real-account test day," confirmed via `watch_list_audit`) — paper trading only scans `mode='research'` nodes, so it simply wasn't running for ~42.5 hours, missing one real backtest-predicted signal entirely and catching a later one ~2 days late at a materially worse price once it resumed. No code change — this is accurate paper-trading history, not a defect. **Correction, found during session-wrap's required paired Opus review**: this was originally written as an HIBL/USD-specific event — `watch_list_audit` actually shows all 10 v5 live-track nodes (AGQ/DPST/GDXU/HIBL/KORU/NUGT/SOXL/UDOW/USD/YANG) flipped in the same single transaction and restored together, so the ~42.5h paper outage applies to every ticker in this investigation's default window, not just the two named. In particular, **part of SOXL's own divergence is this same outage, not only the fill-price bug in finding #3** — SOXL's backtest shows a real signal at 2026-07-24 14:30 (the same trade traced in finding #3) that falls inside the outage window, so both mechanisms (missed/delayed detection AND the stale-cache fill-price bug once paper did resume) contributed to that trade's divergence, not the fill-price bug alone as originally stated.
**Verdict:** Zero evidence of a live-execution or signal-computation bug anywhere in this investigation — every real divergence traced to either a comparison-tool artifact (now fixed) or paper trading's own simulation-specific gaps (one now fixed, one explained by real history). Cross-checked against the Trade-Flow Accountability Grid (`scripts/coverage_registry.py`) separately, which showed 26 `verified-live` real-order-execution scenarios firing correctly and recently — the actual live-trading mechanism (`schwab_client`/`schwab_safety`) is untouched by any of the 4 findings above, since paper trading never calls it. Did not stop live trading.
**Follow-up, not done**: HIBL's remaining ~2-hour-earlier gap (a 07-23 09:30 backtest signal, technically before the live-mode flip, still unaccounted for) — smaller and less certain than the other 3 findings, not traced further this session. USD's full trade-by-trade divergence (beyond the shared mode-flip explanation for its first trade) also not fully traced.

## 2026-08-13 (research pass) — JNUG v5-vs-v5.1 divergence explained: real ~2 extra years of backfilled history, not a bad-tick artifact
**Question:** JNUG's exact live-node param tuple showed v5 (56 trades, 148.8-395.4% strategy_return) vs v5.1 (154 trades, 993.8-2419.6% strategy_return) — same config, ~2.75x trade count, ~7-10x return. Flagged in `docs/backlog_cache.md` as having the same shape as the DFEN bad-tick artifact (fabricated trade count/return), unconfirmed either direction.
**Method:** Checked `sweep_runs` for both versions' run timestamps (v5: 2026-08-08 04:xx, run ids 640-657; v5.1: 2026-08-11 09:23-12:01, run ids 911-928, same `git_commit`). Checked `cache/research/JNUG_1h.csv` (5,086 rows, 2023-09-12 to 2026-08-12) against `logs/backfill_hourly_history_20260811.log`.
**Result:** `logs/backfill_hourly_history_20260811.log` shows `JNUG: 2025-06-27 -> 2023-09-12 (3127 new rows, 1945 -> 5072 total)` — a real history-extension backfill ran on 2026-08-11, between the v5 and v5.1 sweeps, extending JNUG's start date back from 2025-06-27 to 2023-09-12 (1,945 -> 5,072 rows, ~2.6x the data). The v5.1 sweep (same day, immediately after) ran against this newly-extended history.
**Verdict:** Not a bad-tick artifact — the trade-count jump (~2.75x) tracks closely with the data-row jump (~2.6x), consistent with a genuinely longer backtest window producing proportionally more trades and compounded return. `scan_bad_ticks.py` not needed for this case. Closed.

## 2026-08-13 (research pass) — 12 "suspect bad-tick round-trips" (2026-08-11 scan) are mostly real, confirmed stock splits the split-guard structurally cannot catch — a genuine gap, not vendor noise
**Question:** `docs/backlog_cache.md` carried 12 unexplained `scan_bad_ticks.py` hits (found 2026-08-11, full-universe scan, none in the current liquidity-tranche universe) as low-priority/no-action. Each hit shows the same shape: Open/High near the prior bar's Close, Close/Low collapsed 66-90%, next bar's Close recovers near the original level.
**Method:** Pulled `bad_tick_scan_log`'s `hits_json` for all 12 tickers directly (`ret_pct`/`next_ret_pct`/OHLC). Computed `prior_close/bar_close` ratios — found they cluster on clean numbers (2.0, 3.0, 4.0, 5.0, 10.0), the signature of a real split, not random noise. Web-searched each date/ticker pair for a real corporate action.
**Result — 11 of 12 confirmed or near-certain real stock splits, exact date+ratio match**:
- RXL 2024-11-06 (ratio 1.998): confirmed ProShares 2:1 forward split, record date 2024-11-05
- IHF/IYH/IHE 2024-03-06 (ratios 4.998/4.998/2.999): confirmed BlackRock iShares splits, payable 2024-03-06
- STHH/ASMH/ARMH 2026-07-13 (ratios 10.1/10.0/9.9): confirmed 10:1 forward splits (ASML/Arm/STMicro leveraged-ADR ETFs), effective 2026-07-13
- LTL 2025-11-19 (ratio 4.11): confirmed ProShares 4:1 forward split, effective 2025-11-20 (batch of 22 ETF splits)
- BFOR 2026-06-30 (ratio 4.0): confirmed 4:1 forward split, effective 2026-07-01
- CRDU 2026-07-20 (ratio 4.02): confirmed 4:1 forward split, effective after close 2026-07-20
- UPW 2025-11-19 (ratio 4.02): not individually confirmed by name, but same date + clean ratio as the same ProShares Nov-2025 batch that included LTL/USD/QLD/SSO/DDM/UDOW/TQQQ — near-certain
- **LCDL 2026-07-14 (ratio 4.56, not clean) is the one exception** — no matching split found; likely a genuine bad tick or a real (if extreme) LCID-driven price move, not a split. Needs separate handling.
**Root cause traced (`data_manager.py:119-144`)**: the existing split-guard only detects a split by comparing the local cache against a freshly re-fetched overlap window — it fires on a **cache-vs-incremental-delta mismatch**. It has no mechanism to catch an inconsistency **within a single bulk historical pull**, which is exactly this failure shape: Open/High land at pre-split scale, Close/Low at post-split scale, within the same bar, from one fetch — yfinance serving a partially-adjusted print for that one bar. Confirmed via direct query: all 12 tickers have zero `data_mutation_log` rows, meaning the corrupted bar sits raw and uncorrected in the cache.
**Verdict:** Not vendor noise, not "ignore unless it becomes relevant" — this is a real, confirmed structural gap in the split-guard. It's silent today only because none of these 12 tickers are in the current sweep universe, but the same blind spot (single-pull internal inconsistency around any real split) would silently corrupt any *future* ticker's data the same way, undetected, unless it happens to also hit the cache-vs-delta path. Not fixed this session — flagged for a decision on (1) whether to add a same-pull internal-consistency check to the split-guard (the actual fix), and (2) whether to manually rescale the 11 confirmed tickers' pre-split history now via the existing split-guard rescale pattern (logged to `data_mutation_log`, matching how every other detected split is handled).

## 2026-08-13 (research pass, continued) — split-guard gap fixed: same-pull one-bar-artifact detector built, 11 confirmed tickers' cached data corrected, LCDL disqualified as single-stock
**Fix built**: `signals_helpers.detect_one_bar_split_artifacts`/`fix_one_bar_split_artifacts` — detects a bar whose Close diverges from the prior bar's Close by a clean split-like ratio with the next bar recovering nearby (generous 15% recovery tolerance, since real recovery bars aren't exact clean multiples — found live on ARMH/CRDU, whose first-pass detector version with a strict double-neighbor match missed them). Handles both the "close-only" shape (Open/High stay consistent, only Close/Low collapsed -- RXL/IHF/IYH/IHE/STHH/ASMH/LTL/BFOR/CRDU/ARMH) and the "whole-bar" shape (every OHLC field collapses together -- UPW), correcting per-field only when doing so brings it closer to the prior bar's scale. Wired into `data_manager.py`'s bootstrap (`fetch_live_data_smart` Step 1) path — the actual gap, since the existing incremental split-guard only fires on cache-vs-fresh-fetch mismatch and never covers the initial pull. 7 new tests (`tests/test_one_bar_split_artifacts.py`), all pass.
**Applied to real cached data**: backed up all 11 tickers' CSVs first (`cache/research/pre_split_artifact_fix_20260813_030246/`). Fixed RXL/IHF/IYH/IHE/STHH/ASMH/ARMH/LTL/BFOR/CRDU/UPW in place, each logged to `data_mutation_log`. Reran `scan_bad_ticks.py` against all 12 original tickers post-fix: all 11 clean, only LCDL still flags (unchanged, expected).
**LCDL resolved separately**: web search found no matching split; it's a 2x leveraged single-stock ETF on LCID (Lucid Motors) — real, extreme underlier volatility is a plausible (if severe) explanation for the flagged bar without invoking a data bug. Marked `DISQUALIFIED` in `tickers` (`underlier_count=1`, `underlier_note`), same convention as SPCL/FNGU's single-stock/narrow-basket disqualifications — out of contention for any future campaign regardless of what the flagged bar turns out to be.
**Verdict**: closed. Split-guard's structural gap (single-pull internal inconsistency) is fixed going forward, not just patched for these 11; LCDL needs no further investigation since it's disqualified either way.

## 2026-08-13 (research pass, session-wrap review) — paired Opus review found real bugs in the split-artifact fix; redesigned and re-verified before commit
**What happened**: per this project's standing session-wrap rule, `signals_helpers.py` changed this session, triggering the required paired-review gate (independent-cold + contextual Opus, against the real diff, with a rebuttal exchange). Both reviewers independently found the same core issues, converged fully on rebuttal (no contradictions), with empirical repros:
- **HIGH**: the fix was wired only into `data_manager.py`'s bootstrap path (runs once per ticker, ever) -- the incremental-fetch path (Step 3, what every already-cached ticker's subsequent updates go through) never got the check, so a *future* split on an already-cached ticker would not have been caught. The "closes the gap for real, not just these 11 tickers" claim in the earlier research_log entry was wrong.
- **HIGH**: no cross-check against a real confirmed corporate action -- the fix fired purely on a price-ratio-and-recovery heuristic. Verified repro: a genuine large one-bar move that mostly reverts next bar (exactly LCDL's shape) would be silently "corrected." A plain bad-tick spike (10.0 -> 15.1 -> 10.05) also false-positived.
- **HIGH**: the per-field proportional-rescale correction could produce an internally inconsistent bar. Verified repro: a real, valid volatile bar (prev_close=100, O=100/H=101/L=60/C=50) got "fixed" to O=100/H=101/L=120/C=100 -- Low > High.
- **HIGH**: a NaN next-bar Close silently bypassed the recovery-check guard (`not nan` is falsy-adjacent in a way that let it through), letting a corrupt row pass as "recovered."
- **MEDIUM**: the `tolerance` parameter was accepted but never actually passed through to the underlying ratio check (dead parameter).
**Fix**: redesigned per both reviewers' converged findings. `detect_one_bar_split_artifacts`/`fix_one_bar_split_artifacts` now require a `splits` argument (real corporate-action data, `signals_helpers.get_real_splits(ticker)` -- reuses `scripts/check_stock_splits.py`'s same authoritative `yf.Ticker(ticker).splits` source) and only correct a bar when a real split is on file within 1 day at a matching ratio -- eliminates the false-positive risk structurally, not just narrows it. Correction strategy changed from per-field proportional rescaling to flattening the whole flagged bar to a single verified-trustworthy anchor (the bar's own Open if it's already on the prior bar's scale, else the prior bar's Close) -- trivially internally consistent by construction, can never produce Low>High. NaN/zero/negative prices now explicitly guarded via `math.isfinite`. Wired into both `data_manager.py`'s Step 1 (bootstrap) and Step 3 (incremental, on `df_combined` right before the CSV write) via a shared `_apply_split_artifact_fix` helper, closing the structural gap for real this time.
**Re-verification**: 13 new/rewritten tests (`tests/test_one_bar_split_artifacts.py`) including explicit regressions for every confirmed bug (no-real-split rejection, bad-tick-spike rejection, NaN handling, internal-consistency guarantee). All 11 tickers' cached CSVs re-fixed from the pre-fix backup using the corrected function, now cross-checked against real `yf.Ticker().splits` records directly (UPW's split is now directly confirmed, 4:1 2025-11-20 -- was only "near-certain" via web search before). `scan_bad_ticks.py` reconfirmed clean on all 11; LCDL still (correctly) flags, unaffected. Full test suite run before commit.
**Lesson**: the original entry marked this "closed" before the required session-wrap paired review actually ran -- premature. The review process caught real correctness bugs that would have shipped into the production data pipeline otherwise. Don't declare a fix closed until the mandatory review gate for the changed module type has actually run.

## 2026-08-14/15 (overnight Research sweep) — full candidate-universe sponsor/advance-notice mapping (82 tickers) + first-ever full `check_stock_splits.py` sweep (1,478 cached tickers)
**Hypothesis/question:** Extends the same night's NUGT/AGQ false-positive investigation (`signals_compute.detect_price_discontinuity`'s price-ratio heuristic mistook an ordinary 46% NUGT rally for a reverse split). The real fix in design keys off each sponsor's own advance-notice reverse-split announcements instead of guessing from price action, but that had only ever been confirmed for the 10 real-live tickers (checklist item #15). Two open questions: (1) which sponsor does every candidate ticker (not just the 10 live ones) actually belong to, and does that sponsor publish structured/dated advance notice; (2) has `scripts/check_stock_splits.py` (spot-checked only, UVIX/NBIZ) ever been run as a genuine full sweep, and what does it find.

**Method:** Queried `cache/research/trading_universe.db` — `candidate_nodes` has 82 distinct tickers (the real candidate/backtest universe scope for this task); the `tickers` table has 687 rows total, but the ~600 not in `candidate_nodes` are single-stock leveraged products (Leverage Shares/GraniteShares/Tradr/T-Rex/Direxion single-name 2x funds — AAPL, AMD, etc.) pulled in by a broader screening scan, never promoted to `candidate_nodes` and not part of this system's actual backtested/tradeable universe — scoped this sweep to the 82 real candidates, matching the task's own "82 tickers as of tonight" framing. Sponsor identified per ticker from `tickers.description` (already carries the issuer's own product name, e.g. "Direxion Daily Regional Banks Bull 3X Shares") cross-checked against `tickers.k1_status` (independently researched in an earlier session) for consistency — no disagreements found. For each distinct sponsor (10 total), ran WebSearch/WebFetch to confirm whether they publish structured, dated advance-notice reverse-split announcements. Ran `scripts/check_stock_splits.py` (no args — genuine full sweep, not spot-checked) across all 1,478 cached `*_1h.csv` files.

**Result — sponsor coverage (82/82 tickers, sponsor identified for all, 0 unknown):**

| Sponsor | # tickers | Advance-notice mechanism | Tickers |
|---|---|---|---|
| Direxion | 35 | **YES** — direxion.com/press-releases (already confirmed pre-session) | CURE, CWEB, DFEN, DPST, DRIP, DRN, DUST, EDC, ERX, ERY, FAS, FAZ, GUSH, HIBL, JDST, JNUG, KORU, LABD, LABU, NAIL, NUGT, RETL, SOXL, SOXS, SPXL, SPXS, TECL, TECS, TMF, TMV, TNA, TZA, WEBL, YANG, YINN |
| ProShares | 30 | **YES** — proshares.com/resources/tax-and-filing-documents + press releases (already confirmed pre-session) | AGQ, BITU, BOIL, DDM, DXD, GLL, KOLD, QID, QLD, ROM, SCO, SDOW, SDS, SPXU, SQQQ, SRTY, SSG, SSO, TBT, TQQQ, TWM, UCO, UDOW, UGL, UPRO, URTY, USD, UVXY, UWM, ZSL |
| BMO/MicroSectors | 6 | **YES** — microsectors.com/insights, newsroom.bmo.com, bmoetn.com (already confirmed pre-session) | BULZ, FNGD, FNGU, GDXD, GDXU, OILU |
| Volatility Shares | 4 | **YES** — volatilityshares.com/news/ (already confirmed pre-session) | BITX, ETHU, SOLT, UVIX |
| Defiance / Tidal Financial Group | 2 | **YES**, newly confirmed this sweep — distributed via GlobeNewswire/PRNewswire/Nasdaq/SeekingAlpha PR wire (not defianceetfs.com itself); real example: the Aug 2026 correction release announced Aug 6 for shareholders of record Aug 18-19 (~12 days notice); the original July 17 2026 release for the same batch gave even more lead time | QPUX, SPCL |
| REX Shares / Tuttle Capital Management | 1 | **YES**, newly confirmed this sweep — rexshares.com/... press-release pages, dated | BTCZ |
| Tradr (Investment Managers Series Trust II) | 1 | **YES**, newly confirmed this sweep — PRNewswire "Tradr ETFs" feed + SEC Form 497 prospectus supplements + Nasdaq Trader Corporate Actions Alerts (all dated, filed in advance of the effective date) | MQQQ |
| USCF Investments | 1 | **YES**, newly confirmed this sweep — PRNewswire/Nasdaq wire releases; real example: USO's 2020 1-for-8 reverse split announced ~6 days ahead of the effective date | USO |
| Invesco | 1 | **UNKNOWN / no mechanism found** — QQQ has exactly one stock action ever (a 2-for-1 *forward* split in 2000), no dedicated reverse-split notice page located. Practically negligible risk (unit investment trust, ~$300B+ AUM, structurally never needs a reverse split) but not a *confirmed* mechanism the way the other 9 sponsors are. | QQQ |
| State Street | 1 | **UNKNOWN / no mechanism found** — SPY has zero stock-split history since its 1993 inception. Same negligible-but-unconfirmed status as QQQ. | SPY |

**The only two real "zero confirmed advance-warning coverage" gaps are QQQ (Invesco) and SPY (State Street)** — both benchmark index trusts with no split history at all and effectively zero practical risk, but neither sponsor has a documented reverse-split announcement channel the way the other 9 do (there's simply never been one to find). Every one of the 10 real-live tickers (DFEN, DPST, GDXU, KORU, NUGT, SOXL, SOXS, AGQ, ETHU, JNUG) sits under a sponsor with a confirmed mechanism: 8 Direxion (DFEN, DPST, GDXU, JNUG, KORU, NUGT, SOXL, SOXS), 1 ProShares (AGQ), 1 Volatility Shares (ETHU). All 10 covered.

**Result — full `check_stock_splits.py` sweep (first time run in full, not spot-checked):** 266 real splits found within cached history across 1,478 tickers total. Filtered to the 82-candidate universe: **40 of 82 tickers (49%) have at least one real split landing inside their own cached data window** — BOIL, BULZ, DDM, DRIP, DUST, ETHU, FAZ, GDXD, GLL, JDST, KOLD, KORU, LABD, LABU, MQQQ, QID, QLD, QPUX, SDOW, SDS, SOLT, SOXS, SPXS, SPXU, SQQQ, SRTY, SSG, SSO, TECS, TMF, TQQQ, TWM, TZA, UDOW, UGL, USD, UVIX, UVXY, YANG, ZSL. Of the 10 real-live tickers, **3 are flagged: ETHU (2025-04-09, ratio=0.05 — this is exactly the known/confirmed 1-for-20 split cited in tonight's background), KORU (2025-02-10 ratio=0.1; 2026-07-15 ratio=20.0), SOXS (2024-04-15 ratio=0.1; 2026-03-05 ratio=0.05; 2026-07-15 ratio=0.1)**. The other 7 live tickers (DFEN, DPST, GDXU, NUGT, SOXL, AGQ, JNUG) are clean — no split lands inside their cached window.

**Important scoping note, per `check_stock_splits.py`'s own docstring**: a flag here means a real split's date falls inside the ticker's cached data range — it does **not** by itself prove the cached CSV is mis-scaled (a ticker whose entire history was bootstrapped fresh after the split, or whose cache happens to have been fully rebuilt since, would show a flag with no actual data corruption). This sweep is the mechanical "which tickers need a human/rebuild check" signal per the script's stated purpose, not a proof of live corruption. **No CSVs were touched, deleted, or rebuilt this session** — per standing convention, this is reported as a finding for the user to act on.

**Verdict:** Sponsor coverage is effectively complete (10/10 sponsors characterized, 82/82 tickers mapped) — the only real gaps (QQQ/SPY, Invesco/State Street) are both negligible-risk index trusts, not a live threat. The bigger, more actionable finding is the 40-ticker split-sweep result, especially the 3 flagged real-live tickers (ETHU/KORU/SOXS) — these warrant the human verify-and-rebuild-if-needed pass `check_stock_splits.py`'s docstring calls for, prioritized ahead of the other 37 non-live flagged tickers.

**Follow-up, not done this session:** (1) actually verify/rebuild the 3 flagged live tickers' (and ideally all 40 flagged candidate tickers') cached CSVs against the confirmed split dates/ratios — this sweep only identifies, doesn't fix; (2) no sponsor mapping was persisted to the DB (no new `tickers.issuer`/`tickers.sponsor_advance_notice` column added) — the mapping above lives only in this log entry; if it needs to be queryable going forward (e.g. wired into a future automated advance-notice-based discontinuity check), that's a small follow-up schema change, deliberately not done here to avoid an unreviewed schema migration on an unattended overnight run.

## 2026-08-15 — Execution price-drift audit: real historical `entry_drift_pct`/`exit_drift_pct` distribution pulled and quantified; HIBL's ~5% flagged drift traced to paper trading, not real execution
**Hypothesis/question:** User's manual review of past fills flagged HIBL losing ~5% on price on at least one side of a trade — is this a one-off outlier or a systemic execution-quality problem? Raised as the retrospective counterpart to the not-yet-built abnormal-drift alert item (`docs/backlog_cache.md`, provisional 0.5% adverse-drift threshold, explicitly "provisional until real per-ticker historical drift distributions are pulled and looked at").
**Method:** Queried `cache/live/trading_live.db` directly (raw sqlite3/pandas, read-only). `trade_log.is_dry_run_sim` distinguishes real broker fills (`=0`, since a dry_run/trading_enabled=False account never places a real order — its fills are always synthesized with `is_dry_run_sim=1`) from synthesized dry-run/canary fills (`=1`); `paper_trade_log` is a fully separate table for paper-trading simulation, kept separate throughout. All three populations have `entry_drift_pct`/`exit_drift_pct` already populated (some `exit_drift_pct` NULL for still-open positions, `entry_drift_pct` NULL only if pre-tracking — none found). Computed mean/median/std/min/max and |drift| threshold breach rates (>0.5%/1%/2%/5%) pooled, per ticker, per side, and cross-cut by strategy/account/exit_reason.
**Result — population sizes:** REAL (is_dry_run_sim=0) n=33 trades (28 with a closed exit); DRY_RUN_SIM n=85 (82 closed); PAPER n=46 (41 closed).

**Pooled |drift| threshold breach rates:**
| Population | Side | n | mean | median | std | min | max | >0.5% | >1% | >2% | >5% |
|---|---|---|---|---|---|---|---|---|---|---|---|
| REAL | entry | 33 | 0.63% | 0.00% | 4.68 | -4.09% | 22.29% | 51.5% | 42.4% | 21.2% | 6.1% |
| REAL | exit | 28 | 0.55% | 0.00% | 4.61 | -8.97% | 22.20% | 32.1% | 14.3% | 10.7% | 7.1% |
| DRY_RUN_SIM | entry | 85 | 0.06% | 0.10% | 1.22 | -7.97% | 4.23% | 21.2% | 15.3% | 3.5% | 1.2% |
| DRY_RUN_SIM | exit | 82 | -0.01% | 0.00% | 0.08 | -0.72% | 0.06% | 1.2% | 0% | 0% | 0% |
| PAPER | entry | 46 | 0.24% | 0.03% | 3.96 | -15.61% | 9.50% | 56.5% | 45.7% | 30.4% | 13.0% |
| PAPER | exit | 41 | -0.64% | 0.00% | 1.47 | -7.03% | 0.00% | 22.0% | 19.5% | 17.1% | 2.4% |

REAL's pooled numbers are dominated by 3 known-anomalous rows, all `entry_time` 2026-07-06: KORU entry_drift=22.29%, SOXL entry_drift=12.34%, AGQ exit_drift=22.20% — these are the documented KORU/SOXL 2026-07-06 catch-up entries off a missed 2026-07-02 signal (`docs/design.md`'s "Open positions" note in CLAUDE.md), manually backdated entries, not live-execution slippage on a real-time signal. Excluding that cluster (n=30 entry / n=25 exit remaining): entry mean=-0.46% median=0.00% std=1.40 (0% breach >5%); exit mean=-0.27% median=-0.02% std=1.91 (4.0%, i.e. 1 trade, still breaches >5% — see below). **This cleaned-up distribution is the one that should inform the alert threshold, not the raw pooled number.**

**HIBL specific check — verdict: HIBL's real (broker-executed) trades show no elevated drift; the ~5% the user flagged is from paper trading, not real execution.** HIBL has only 2 real (is_dry_run_sim=0) closed trades on file: entry_drift -0.22%/0.00%, exit_drift -0.03%/0.00% — both near-zero, well inside the rest of the real universe's distribution (real universe excl. HIBL: entry mean|d|=2.03%, exit mean|d|=1.51%). HIBL's 12 **paper** trades, however, do show a real outlier: entry_drift as low as **-9.01%** (2026-07-28 10:04:17, SL exit) and two more in the -3.5% to +2.9% range; paper HIBL's mean|entry_drift| (2.26%) and max (9.01%) are roughly in line with the rest of the paper universe (mean|d|=2.10%, max 15.61% on SOXL) — not a HIBL-specific defect, but genuinely large paper-trading drift generally. This matches HIBL's documented status as a toy-notional pilot node (`$60-200` sizing, `docs/backlog_cache.md`'s 2026-08-01 finding that HIBL/USD/YANG skip 71-87% of signal-window trades for lack of affordable share count) — HIBL has never actually traded live at meaningful notional; every large-drift instance the user could have observed is from the paper simulation.

**Real (non-catchup-cluster) drift outlier beyond HIBL, worth flagging on its own:** KORU real trade entry 2026-07-14 09:35:44 / exit 2026-07-15 16:11:40 (SL) shows exit_drift_pct=-8.97% — a genuine, non-backdated real SL exit with real execution slippage nearly 9%. This is the single largest real (non-catchup) drift event in the dataset and deserves its own look (was the SL a slow/gap-through fill, a manual-entry lag, or a data issue) before finalizing any threshold — not investigated further this session, flagged as follow-up.

**Systemic pattern check (real trades, small samples — flagged accordingly):**
- By strategy: `TrailingBothZScoreBreakout` (n=26 entry/23 exit) mean entry=0.53%/exit=0.68%; `TrailingExitZScoreBreakout` (n=7 entry/5 exit) mean entry=1.00%/exit=-0.04%. No clear separation given sample sizes this small.
- By exit_reason (exit_drift_pct only): SL (n=11) has by far the largest spread (mean=1.08%, std=7.49, driven by AGQ's 22.20% and KORU's -8.97%); TRAIL (n=6, mean=0.14%, std=0.23) and TIME (n=4, mean=0.20%, std=0.68) are both tight; MANUAL (n=5, mean=0.42%) is moderate. **SL exits appear to carry the most execution-price risk** — consistent with SL being the "unconfirmed intrabar breach" mechanism (per CLAUDE.md's stop-loss note) rather than a scheduled signal-window action — but n=11 with 2 outliers driving the mean means this isn't statistically strong, just directionally worth another look once more SL exits accumulate.
- By account: `soxl_ira` (n=21 entry) mean=-0.74% median=-0.34%, tighter than the overall pool; `ira`/`sep` samples too small (n=1-2) to read anything into.
- Paper vs real vs dry_run_sim: DRY_RUN_SIM's exit_drift is essentially zero-variance (std=0.08%, max|d|=0.72%) since these are synthesized fills against the same price series the exit check reads — this reflects synthesis fidelity, not real execution quality, and shouldn't be blended into a real-execution threshold discussion. PAPER shows materially worse drift than REAL on both sides even after excluding the catch-up cluster, plausibly because paper's entry fill simulation (per `docs/deep_backlog.md`'s 2026-08-12 paper-vs-backtest reconciliation finding) prices off cached hourly-bar data rather than a live tick — a known, already-documented gap, not new here.

**Verdict:** HIBL's flagged ~5% drift is not a real-execution problem — HIBL's actual real (broker-filled) trades show near-zero drift, in line with (better than) the rest of the real universe. The ~5-9% magnitude the user recalled is paper-trading drift, a different (already-partially-explained) phenomenon. Real-execution drift (excluding the one known backdated-entry cluster) is centered near 0% with std ~1.4-1.9%, but does have real tail events up to ~9% concentrated in SL exits — the provisional 0.5% adverse-drift alert threshold would fire on roughly a third to half of real trades at face value (32-52% breach rate depending on side/cluster-inclusion), which is far too tight to use as-is; a threshold in the 2-3% range would catch only the genuine tail (~10-21% of trades) while leaving routine noise unflagged, though this is a first-pass read on n=25-33, not a fitted recommendation.
**Follow-up, not done this session:** (1) trace the real KORU 2026-07-15 -8.97% SL exit specifically — was it a slow/gapped fill; (2) as more real trades accumulate, especially more SL exits, re-run this same query to see if the SL-exit-has-more-variance pattern holds up or was 2-outlier noise; (3) decide the actual alert threshold using this distribution (2-3% region suggested above) rather than the placeholder 0.5%; (4) this analysis used raw sqlite3/pandas queries, not a committed script — if this needs to be re-run periodically (e.g. alongside future threshold-tuning), it may be worth promoting to a small `scripts/` tool, per the project's script-inventory convention (not done here since this was framed as a one-off analysis).

## 2026-08-16 — CertainEntryTrailingSell: negative result once priced correctly at bar-close, idea closed
Follow-up to the 2026-08-09 finding above. When the "CertainEntryTrailingSell" mechanism idea was actually adjusted to price the confirmed entry off bar-close (rather than the best-possible-in-bar price the original scoping used), the result went **negative** — the "+1,270.3% alpha" figure carried in the original backlog entry was itself still using the best-possible-in-bar price, not a true bar-close-only execution price, so it overstated the mechanism's viability. User's own framing: "certain entry trailing sell died because we found out the certain entry uses the best possible price for the bar but when we adjusted to bar close it actually went negative." Closed as a dead end, not a deferred idea — no further design work planned. See `docs/deep_backlog.md`'s 2026-08-09 entry for the original scoping/open design gaps (hourly polling infra, overnight/EOD exposure policy), now moot given the negative result.
