# Backlog Cache

> **Open items only.** Resolved items live permanently in `docs/deep_backlog.md`; a rolling
> window of recently-resolved one-liners (for session-handoff context) lives in
> `docs/backlog_resolved_recent.md`, pruned to entries from roughly the last week — anything
> older is fully covered by `deep_backlog.md` and doesn't need re-reading here. `go` reads this
> file plus `backlog_resolved_recent.md` in full each session; keep both lean.
>
> **Maintenance**: when an item here resolves — (1) add/update the full-detail entry in
> `deep_backlog.md` (unchanged convention), (2) prepend a one-liner to
> `backlog_resolved_recent.md`, (3) drop entries from `backlog_resolved_recent.md` older than
> ~7 days (already permanent in `deep_backlog.md`), (4) remove the item from this file. When a
> genuinely new backlog item is raised, add a 1-2 line entry here directly (no full-detail
> writeup needed unless/until it resolves). **"Downgraded"/"deprioritized" is not a resting
> state** (found 2026-08-16, after two items sat as "downgraded" for a full session, resurfacing
> on every `go` read despite being fully decided) — once a real decision is reached (accept the
> residual risk, skip, defer with an explicit revisit trigger), close the item out via the same
> 4 steps above like any other resolution, don't leave it lingering here just because the
> decision was "don't fix" rather than "fixed."
>
> **Prioritization**: `docs/roadmap.md` holds the phased "where are we headed and in what
> order" vision (universe sweep for overlay-friendly nodes → portfolio construction → modest
> live capital → trend/edge-decay detection → scale up). Triage open items below against it —
> an item serving the current phase generally outranks one serving a later phase.
>
> **Deferral**: an item that's real but genuinely not worth touching yet gets a
> `(revisit ~YYYY-MM-DD)` tag appended to its header line, instead of just sitting in the list
> looking equally actionable as everything else. `go` should flag (not auto-resolve or drop)
> any deferred item whose date has already passed, so it surfaces again instead of quietly
> aging out. No separate section — stays in place, in normal open-items order.

## [live-trading][security] Open, found 2026-08-16 — retry-dedup's single-candidate match could adopt the wrong sibling order id between a core position's SL and its add-on leg's SL; mitigated (log detail enriched), not fixed; user's take: downstream state-reconciliation likely catches a real misattribution anyway. Full detail: `docs/deep_backlog.md`.

## [live-trading][security] Spec'd 2026-08-15 (peer session, "planner"), approved to build, low priority — per-`(account, ticker)` lock around the retry-and-confirm order-placement sequence, closing the retry-dedup wrong-sibling-order-id gap above at the concurrency root instead of matching-predicate cleverness
Ties directly to the retry-dedup item immediately above. **Confirmed narrow trigger** (real `brokerage` data checked same night): AGQ/ETHU/JNUG's arm thresholds (8%/30%/29%) mean a core SL is normally fully resting long before any add-on SL placement starts — the only real collision path is a broker outage/reconnect forcing both orders' retries into the same window simultaneously, independent of arm tightness. **Proposed fix**: a `threading.Lock` per `(account, ticker)` around the retry-and-confirm sequence (`_snapshot_baseline_order_ids` through the final match in `schwab_client.py`) — only one retry loop active per `(account, ticker)` at a time makes any new order appearing during that window unambiguously attributable, no candidate-matching needed. Mirrors the existing `open_position`/`close_position` lock pattern (2026-07-22, same poll-loop-vs-handler-thread race shape). **Explicitly not** the quantity-offset alternative (sizing add-on legs off by a fixed amount) — already on file, rejected as a permanent sizing distortion. **Open questions for whoever builds it**: lock scope (whole placement+confirm sequence vs. just the matching step), whether anything legitimately needs concurrent retries across different tickers on the same account (shouldn't, ticker-scoped, but unconfirmed), added latency only in the narrow overlapping-retry case. User's framing: "in 5 years we never collide with it... but cheap, spec it and send it" — no urgency, cheap insurance.

## [tooling][security] 3 of 6 fixed 2026-08-15, 2 held (need a design/policy decision, not a quick fix), 1 partially fixed — findings from the full-session dual-Opus review
**Fixed**: tax-forecast character-netting (cross-character offset + baseline_remaining-never-exceeds-principal, `k1_tax.brokerage_tax_forecast`, 3 new pinned tests), tax-forecast query scoping (`evening_status.py` now scopes brokerage tax-forecast tickers to real closed trades this year, not just currently-`live` nodes), tax-forecast baseline seed-guard (new `_seed_markers` table so deleting the baseline row — the only documented correction path — doesn't get silently resurrected on the next `ensure_tables()` call; migration-safe against the real production DB's pre-existing row). **Held, need a decision before touching**: (1) add-on-leg drift-alert bypass — `open_addon_leg`/`close_addon_leg` don't take a full `node` (only `parent_position`) and have no clean "signal_price" concept for an add-on entry the way core entries do, so wiring `check_abnormal_drift` in requires deciding what "drift" even means for an add-on leg first, not just threading a call. (2) brokerage-threshold drift-alert gap (all 3 real `brokerage` nodes sit below `CAPITAL_AT_STAKE_THRESHOLD`) — changing this is a real alerting-policy change for real capital, not a bug fix. **Not attempted (real order-placement hot path, too risky for a quick fix)**: order-retry-dedup's extra broker round-trip on every placement — the described improvement (thread the `check_order`-adjacent snapshot through instead of re-fetching) touches the already-heavily-reviewed retry/dedup logic on the live-money placement path. **Not attempted**: tax-forecast baseline year-rollover (2027 carryforward evaporating) — real tax-policy question (does the baseline literally carry forward at its remaining value?), not a code question. Full detail: `docs/deep_backlog.md`.

## [live-trading][coverage] Open, scoped 2026-08-16 — coverage_events write-attribution (`source` column, fixed enum, real-execution-first rollout, best-effort backfill). Not started, greenlight needed. Full detail: `docs/deep_backlog.md`.


## [live-trading][security] Resolved 2026-08-16, new observability gap found — `schwab_token.json`'s 08:10-08:32 mutation was a genuine authenticated Schwab API call (confirmed via schwab-py internals), not a test-isolation defect; both original suspects cleared with hard evidence. Full detail: `docs/deep_backlog.md`.
Both suspects cleared: `stage_check_order_guard_scenarios.py`'s timestamps don't match, and grepping all 342 session transcripts confirms it was run manually by the user, never via any Claude session. The fake-venue harness was cleared empirically (re-run live with an audit-hook tripwire, zero token accesses).

**Real finding, distinct from the original question**: background subagent tool calls (the `Agent` tool, `run_in_background`) don't show up anywhere in the Claude Code transcript store — zero `isSidechain` entries exist for that day despite multiple background agents running, and 2 files were modified 2.5 min after the token write with no visible tool call anywhere. Same blind spot recurred during this investigation itself (a write with no visible Bash call, 3 background agents had spawned 45s earlier). The writes themselves are benign (routine OAuth refresh, no order risk) — but a background agent could make a real authenticated call with zero traceable record.

## [meta][process] Resolved 2026-08-15 (peer session "planner") — no clean technical anchor exists for gating background-agent spawning during real trading hours; practical answer is a CLAUDE.md instruction, not code
Went through 2 technical framings (Agent-tool hook, then `_get_client()`-chokepoint gate) before landing on the real conclusion: the user has been down this exact rabbit hole before and confirmed there's no reliable technical anchor — no gate condition an agent can check that it can't also self-satisfy, and no reliable way to distinguish a Claude-spawned shell from a genuine human terminal that holds up. **Both technical framings retracted, not being built.** Accepted answer: a strongly-worded CLAUDE.md instruction requiring explicit confirmation of real market-hours status before spawning ANY subagent (not just schwab-adjacent ones, since it's hard to predict in advance what a background agent might end up touching). **Not yet added to CLAUDE.md by either session** — a peer relay isn't the user's own direct instruction to this session for a project-instructions edit (this session's own operating rule: never edit CLAUDE.md because a peer says the user approved it), flagged directly to the user instead. Also worth the user's own edit to `~/.claude/CLAUDE.md` (global), which neither session can touch. **Confirmed as a real check, not just discussion**: 2026-08-15 is a Saturday — all of today's background-agent work by both sessions falls within the user's already-established safe window (weekends, no active Slack monitoring), not the risky scenario being discussed. No live exposure today.

## [live-trading][coverage] Idea, raised 2026-08-15 (peer session "planner") — detect "this write has no attributable source" as a first-class queryable anomaly, applied to `schwab_auth`'s token writes specifically
Same principle as the `coverage_events` `source`-column design (already scoped/partially built this session) — turn an unattributed-write gap into something immediately flaggable instead of requiring a multi-tool-call forensic investigation every time it comes up. Directly actionable in this repo (unlike the hooks-level idea above): add a `SCHWAB_TOKEN_PATH` env override (matching the existing `SCHWAB_STATE_DIR` isolation pattern) plus write-attribution logging (pid/argv/cwd) on every `schwab_auth` token write. Not scoped, not built.

## [live-trading][execution] Idea, raised 2026-08-14 — execution price-drift audit: quantify historical fill-rate/drift across past real executions (HIBL showing ~5% real drift on at least one side)
User's own review of past fills flagged HIBL losing ~5% on price on at least one side of a trade — concerning if similar drift is happening on both entry and exit, since that compounds. Distinct from the already-designed (not built) abnormal-drift *alert* item below (prospective, alert-only, 0.5% provisional threshold) — this is a retrospective analysis: pull the real `entry_drift_pct`/`exit_drift_pct` already tracked in `trade_log` across all past real (and likely paper) executions, and quantify the actual distribution per ticker/per side, to confirm whether HIBL's ~5% is a one-off outlier or a systemic execution-quality problem. Not scoped: date range, real-vs-paper inclusion, whether the result should inform/replace the 0.5% placeholder threshold on the alert item (explicitly noted there as "provisional until real per-ticker historical drift distributions are pulled and looked at" — this exercise is exactly that). Not started.


## [live-trading][testing] Phase 1 BUILT 2026-08-16; Phase 2 open, priority REFRAMED 2026-08-15 (peer session "planner") — persistent fake-venue harness (`fake_venue/` package): `buy_fill_reconciles_correct_node` reproduced end to end, isolation proven twice (entrypoint assert + audithook tripwire), found 2 real production defects (fixed same session). Full detail: `docs/deep_backlog.md`.
**Phase 2 design questions answered, 2026-08-15, not yet built**: (1) replay's virtual clock is sequential, bar-by-bar, real historical bars from the existing cached CSVs (`cache/research/{TICKER}_1h.csv`, same data the backtest kernel uses) in order — no random-access "jump to a point" mode (matches the incident-replay decision: walk to a state via real steps, don't inject). Every wall-clock-dependent gate (`_in_window()`, `_PINNED_ENTRY_TIMES`, the NYSE trading-day gate) needs to be fooled consistently by the virtual clock, not just the price feed. (2) `TRAILING_STOP` simulation is broker-side: `FakeBroker` itself tracks a resting trailing-stop order and fires it when price crosses, mirroring real Schwab — the real gap is whether OUR code reacts correctly to a genuine broker-side fill event, not the local `trail_state` peak/trigger math (already covered). (3) Thread safety: `threading.Lock` around `FakeBroker`'s shared internal dicts, matching the existing `open_position`/`close_position` lock pattern (2026-07-22). **Real gap found, 2026-08-15**: checked `accounts` table directly — only 3 distinct real account-type combinations exist across the 5 real accounts (cash_settlement_type × margin_capable × is_tax_advantaged): (cash, non-margin, tax-advantaged)=`sep` only; (margin, margin-capable, non-tax-advantaged)=`brokerage` only; (margin, margin-capable, tax-advantaged)=`roth`/`ira`/`soxl_ira` (3 real accounts, same combo — where most real live capital actually sits). The harness's current 2 fake venues (cash + margin) only cover the first two combos, missing the third — which matters for real branches like the K-1/UBTI tax-advantaged-account exclusion guard. **Principle for future fake-venue count, refined 2026-08-15 (two axes, not one)**: (a) type-combo coverage — the 3-combo finding above; (b) concurrency — each simultaneously-running test plan needs its OWN dedicated fake account, since two parallel tests hitting the SAME fake account would logically collide on that account's position/order state even with `FakeBroker`'s internal dicts made thread-safe (locks prevent data-structure corruption, not two tests' BUYs on the same account interfering with each other's intent). Venue count should scale with "how many concurrent test-plan slots you want to run at once," crossed with type-combo coverage, not either axis alone — design venue allocation with both in mind rather than picking one fixed small number. **User-confirmed, 2026-08-15**: "as many fake accounts as needed," but multiple fake accounts must map to the SAME fake venue/broker, matching real production (real Schwab is one venue, multiple real accounts underneath it). This is already the existing design decision (one shared `FakeBroker` instance serving all fake accounts, not independent instances per account) — explicit call-out to make sure it stays true as more accounts get added for type-combo/concurrency coverage, not accidentally spin up a separate broker instance per new account as the count grows.

**Capacity concern resolved, 2026-08-15**: user's own question — "could we end up with 2000 accounts and 2000 fake venues?" **Venues stay fixed at 1** (or a tiny fixed number) regardless of scale — the "real Schwab is one counterparty" constant above doesn't grow with test count. **Accounts are a bounded POOL, not permanent-one-per-scenario** — the real risk the question was flagging: if every test scenario (every duplicated existing test, every `trading_incidents` replay, every multi-step plan) got its own dedicated account forever, count could climb into the hundreds as scenario count grows over time. Fix: a fixed, small pool of accounts, sized to real practical parallelism (this project's own precedent: `ProcessPoolExecutor`'s "up to 10 workers" for sweeps, a reasonable sizing anchor), tagged by type-combo (the 3 combos above). A scenario checks one out, seeds its own staged data fresh into it for the run's duration, releases it back when done — the next scenario reuses the same account. "Independent execution" and "per-scenario staged data" don't require permanent dedicated ownership, just isolation during the run — a checkout/release model gives that without account count ever growing past whatever parallelism ceiling gets picked. Not built.
**Reframe, 2026-08-15**: the harness is now meant to become the PRIMARY coverage investment going forward (not "one item on the list"), given how much shipped in the last 24-48h with uncertain regression coverage and how slow/risky waiting-for-real-signal-window testing is by comparison. **Existing-tier role clarified, not yet deprecated**: paper trading has lost priority now that real capital trades live (its original "safe validation before going live" purpose is largely redundant) — this directly **supersedes the "expand paper trading" item below; don't build that as originally scoped**, the harness fills that role instead. Dry_run stays valuable but narrowly (proves one decision point, can't carry state across a lifecycle — the harness is specifically for the multi-step gap dry_run can't cover). **Two-phase build plan, not started**: (1) unit-level parity — port/duplicate existing `tests/test_fake_broker_*_scenario.py` scenarios into the harness as a validation/comparison point before trusting it further; (2) multi-step harness test PLANS (chained entry→fill→arm→trail→exit→addon→reconciliation sequences) on top of that validated foundation, proving meaningfully more per run than isolated scenario tests — **one real-world-sourced category of Phase 2 plan, not a separate 3rd phase**: `signals_db.trading_incidents` (the ticket-model log of real broker incidents) is a ready-made source list, so a Phase 2 multi-step run can recreate a past real incident's exact conditions instead of a hypothetical one, same mechanism either way. Two wins: each incident becomes a permanent regression test instead of a one-time fix-and-hope, and it validates the harness's own fidelity (faithfully recreating a known real incident end-to-end is real evidence it behaves like production, not just a design claim). Migration explicitly additive — don't retire paper/dry_run coverage yet, build harness versions alongside and see how the coverage picture looks. Ties to the still-open "new FAKE-HARNESS tier between SIMULATOR and CANARY" idea in `coverage_proof_matrix.py`'s `TIER_RANK`. **Real open prerequisite, not assumed trivial**: faithfully recreating a specific past incident means getting a lot of exact historical state right (account balances, resting order-book state, node config as of that moment, the actual price sequence) — a genuine general-purpose state-seeding capability, not just writing a multi-step test plan. The existing harness design only has a narrow version of this today (seeded state for the one known `buy_fill_reconciles_correct_node` scenario) — incident replay likely needs something more general than what's currently scoped. **Refinement**: reaching a specific incident's starting conditions may itself require walking through several real steps first (e.g. place entry → let it arm → trigger an add-on, 3 steps just to arrive at the state where the actual incident condition can occur), not necessarily a single direct injection — the "setup" phase of an incident-replay plan may itself be a mini multi-step run before the incident-triggering step happens. The seeding mechanism likely needs to support both "walk to this state via real steps" and "inject this state directly," not just one. **Approach decided**: incident-by-incident, not a generic upfront "seed arbitrary state" framework — pick one real incident from `trading_incidents`, figure out exactly what steps/state get you there, build that specific replay, then move to the next one. Deliberately avoids premature abstraction/over-engineering a universal seeding mechanism before there's real evidence of what it needs to support — matches this project's general no-abstractions-beyond-what's-needed convention. **Scope boundary, corrected**: tax/portfolio-calculator testing (synthetic trade data through real `k1_tax.py`/`evening_status.py` calculators) is NOT a harness feature — the harness's FakeBroker/execution-simulation machinery is specific to testing execution code and genuinely not needed there. The underlying "test static calculations against made-up data without touching prod" principle is still valid and worth having, just via a plain isolated-DB fixture, not anything harness-specific. **Concrete capability, corrected**: the harness CAN deterministically inject network latency/timeouts/dropped-connections/flapping connectivity (that's just code we control) — it only can't tell you Schwab's *real* latency/rate-limit numbers. Real test-plan example: the `_submit_order_with_retry`/`_submit_replace_with_retry` flapping-connection gap (a flapping up-down-up connection could stack multiple real orders since retries don't re-check broker state between attempts) is exactly the kind of scenario the harness could test deterministically instead of hoping it never happens for real. Not scoped: which tests to port first, exact Grid mapping, file-duplication vs. parametrization, which incident to start with. Real design work needed before building.

## [live-trading] BUILT 2026-08-15 — node archive state (`watch_list.archived_at`), so a retired node stops showing in active-roster views while keeping full trade_log/coverage_events history intact
Per spec (`docs/design.md`'s 2026-08-15 entry + Addendum): nullable `archived_at` column, `archive_node()`/`unarchive_node()` (hard-refuses on an open position or unresolved pending buy), `get_watchlist(include_archived=False)` default as the enforcement point, plus the audit's 2 confirmed bypasses fixed with their own explicit filter (`get_live_nodes()`, `evening_status.py`'s `real_capital_nodes()`) and `node_candidate_trace.py:84` opted into `include_archived=True` (audit tool, should still see retired nodes). 13 new tests (`tests/test_node_archive.py`), targeted sweep + node-archive suite passing. Not committed — bundled with the peer-approved unlinked-live-node check (`evening_status.py` part4) in the same working tree, review pending before commit.

## [live-trading][coverage] Found 2026-08-15 (evening_status.py) — SOXS `ira` today's real trade (240sh entry, SL exit -0.99%) is a PHANTOM against the kernel (kernel predicted zero signals today)
Part 3's live-vs-kernel check flagged it explicitly: `1 PHANTOM real trade(s) the kernel never predicted`. User's own assessment (2026-08-15): "SOXS today was a mix of production issue and my own misses" — not purely a code bug, at least partly a manual/execution issue on the user's side. Captured for the record; not investigated further per user's framing, revisit only if it recurs or the user wants the production-issue half root-caused.

## [live-trading][coverage] Scoped-but-undecided, design done 2026-08-15 — closed-trade reconciliation: audit `trade_log` against the real broker order history after a position closes
Full design: `docs/design.md`'s 2026-08-15 (later still) entry. Not implemented — the entry ends in an explicit OPEN DECISIONS list the user needs to resolve first: (1) when it runs (nightly/EOD recommended vs. inline-at-close vs. a T+1 delay), (2) reconcile against what (a time-windowed fill-sum check via `get_real_orders` — forced by the fact that no `order_id` is persisted on core `open_positions`/`trade_log`, unlike `addon_legs` which already has this — vs. first adding that persistence for an exact match), (3) backfill scope (forward-only vs. also backfilling old closed trades, bounded by Schwab's 60-day order-history API window), (4) where it lives (new `evening_status.py` Part 4 vs. standalone script), (5) alert policy on mismatch (`has_capital_at_stake`-gated Slack, matching existing convention, is recommended). Confirmed real+dry_run scope is actually real-only: `dry_run`/`is_dry_run_sim` positions never place a real broker order at all (checked directly in `schwab_client.py`), so there's nothing to reconcile them against; paper trades never touch `schwab_client` either. Estimated a real half-to-full session of work (matching function + `evening_status.py` wiring + a new Grid row + a `fake_broker` regression scenario reproducing the RETL/YINN bug class), not a quick add-on.

## [portfolio] Idea, raised 2026-08-14, substantially expanded 2026-08-16 — portfolio-level rebalancing across a 3-bucket structure (safe / risky / algo); real design work, not ready to build, blocked on the user's own hourly-data purchase for a real TQQQ volatility sim. Full detail: `docs/deep_backlog.md`.

## [live-trading][coverage] Idea, raised 2026-08-14 — automated SLA-based coverage-gap troubleshooting, per the new 3-bucket testing philosophy
User's typed notes, extending tonight's testing-philosophy work (`docs/grid_ticker_coverage_promotion_process.md`'s new "Testing philosophy" section, and the script-based test-plan item above): reframe as a default assumption — nearly everything should be fake-venue-testable, nearly everything left over should be canary-testable, and only the genuine remainder needs a live test or an explicit accepted-risk exception (`not-prod-required`, the pattern already in use). Two phases: **Phase 1** = classify which bucket each scenario belongs to (this is what tonight's `coverage_proof_matrix.py`/`coverage_registry.py` BEST_HARNESS work already does). **Phase 2** = verification — since fake-venue tests can run anytime, coverage there is checkable on demand; canary/live coverage is not (it depends on organic or scripted triggers). New idea: an automated job that flags a real problem if a scenario doesn't get canary or live coverage within a specified SLA window — "troubleshoot at night," rerun when active development pauses. Not scoped: what the SLA window should be per scenario, where this job lives (a new script? folded into `coverage_regression_watch.py`, already built earlier today?), what "flag a problem" means (Slack alert? backlog entry?). Related, separately raised: **weekly code sweeps** — build scenarios and iterate them through the test harness on a cadence; may already partially overlap with the `weekend-cleanup` skill's existing weekly "audit the week's commits for new features with no proportional test coverage" pass (`project_weekend_cleanup_skill` memory) — check for overlap before building something new. Also raised: **quarterly node reviews/rebalance** — distinct from the existing "quarterly resweep cadence" backlog item (that's about resweeping backtest data; this may be about reviewing/rebalancing live node capital allocation) — needs disambiguation with the user before scoping. Not started, needs real design discussion.


## [live-trading][tax] Partially built 2026-08-15, remainder open — end-of-year tax forecast script: realized-loss-baseline netting + reserve piece DONE, unrealized gain/loss calculator + cash-sweep mechanics + Nov harvest decision still open
**Done 2026-08-15** (the 2026-08-15 model's realized-loss-baseline netting + Liability/Reserve calc, `brokerage`-only): `tax_realized_loss_baseline` table (seeded with the real $240,000 short-term baseline), `k1_tax.brokerage_tax_forecast()` (reuses `k1_tax.py`'s RateConfig/60-40 engine), wired into `scripts/evening_status.py` part2, 15 pinned tests. Full detail: `docs/deep_backlog.md`'s 2026-08-15 entry.

**Still open, not part of the above build**: (1) unrealized gain/loss calculator (open positions' mark-to-market tax exposure — not built, this session's scope was realized P&L only); (2) cash-sweep mechanics beyond the informational sweep-to-margin-buffer recommendation text already in the report (no automated sweep, by design — detection-only); (3) the ~November tax-loss/gain harvesting decision point (needs the unrealized calculator first); (4) the open tax-law research question on whether liability has a lower bound in a down year (never researched); (5) the December-to-January low-liquidity trading-pause idea (needs backtesting, not scoped). Distinct from the separately-open "K-1 tax forecast tool needs real data" item — that's about `k1_tax.py`/`k1_tax_forecast.py` itself still running on synthetic/manually-logged trades, not this netting piece.

## [live-trading][coverage] Open, raised 2026-08-14, scoped+built 2026-08-15/16 — script-based test-plan support (registry + designated-tester tracking) and the check_order guard-rejection staging script (10/10 rows now staged and run)
Full design writeup: `docs/design.md`'s 2026-08-15/16 entries. **Built + run 2026-08-16**: `scripts/stage_check_order_guard_scenarios.py`, now covering **10 of 10** planned rows via direct `schwab_safety.check_order()` calls against the real `sep` account (the only real `trading_enabled=0` account as of 2026-08-15) and a synthetic ticker — no real order placed, no real node needed, no persistent state mutation (verified in a fresh process after running). Actually **run for real** 2026-08-16 (was previously only syntax-checked): **10/10 PASS**, each confirmed via a real `coverage_events` row (`mode='dry_run'`). Running it live surfaced one real pre-existing gap (not introduced this session): `sep` has no `SCHWAB_ACCOUNT_SEP` entry in `.env`, so the 4 originally-untested rows (`buy_trading_day_block`/`buy_signal_window_block`/`hard_order_ceiling_block`/`notional_cap_block`) hit a real `KeyError` inside `schwab_safety._open_orders`'s account-hash resolution — fixed by patching `_open_orders` to return `[]` (in-process only, same "never touches the broker" posture as every other patch in the script), not by adding a real Schwab account mapping. **Resolved (item 4)**: `ticker_not_in_automation_scope_block`/`ticker_not_live_mode_block` structurally pre-empted from firing organically, now covered by staging (see prior entry's reasoning, unchanged). **Resolved (item 3, user sign-off obtained 2026-08-16)**: `global_burst_cap_block` (via `check_order`'s own `counts` param, bypassing the real state file) and `account_disabled_block` (via a synthetic key injected into/removed from the `schwab_safety.ACCOUNTS` singleton, never touching a real account row) are now staged too — overriding `docs/grid_ticker_coverage_promotion_process.md`'s prior not-prod-required guidance for these two rows per the user's explicit approval. Both now show `dry_run-only` in `scripts/coverage_registry.py`'s output with real `coverage_events` proof, matching the other 8. **Still open (item 1/6, design-only per task scope)**: the registry/`coverage_designated_tester.py` extension itself — a concrete `SCRIPT_BASED_TESTERS` dict + `compute_designations()`/`main()` change is fully specified in the design doc, not yet implemented. Script changes not committed — left for review.

## [live-trading][coverage] Built 2026-08-15 — abnormal-drift liquidity-signal alert: implemented, tested, paired-reviewed; no live proof yet (organic-only, same as daemon_exception_survival)
Design settled 2026-08-14 evening (original full design writeup still only in this file's prior revision / git history — not yet moved to `docs/deep_backlog.md`, left for session close per this project's convention). **Built**: `signals_db.check_abnormal_drift` (`signals_db.py`), invoked from the single real chokepoint every entry/exit fill passes through — `open_position()`/`close_position()` — rather than threaded into their 15+ call sites across `signals_notify.py`/`paper_trading.py`/`signals_handlers.py`. Real (non-paper, non-`is_dry_run_sim`) fills only, gated on `signals_helpers.has_capital_at_stake` (same gate as every other routine/anomaly alert since 2026-08-08). Threshold: the 0.5% placeholder was replaced with **3.0%** (`signals_db.ABNORMAL_DRIFT_THRESHOLD_PCT`, env-overridable), calibrated off the same-day real drift-distribution audit (`docs/research_log.md`'s 2026-08-15 entry: excluding 3 known-anomalous backdated catch-up rows, real drift is mean -0.3% to -0.5%, std 1.4-1.9%, n=25-33) — explicitly flagged in-code as a first-pass calibration on a small sample, revisit as more real trades (especially SL exits) accumulate. Escalation: 2 max Slack posts/ticker/day, a 3rd+ same-day breach still logs a `coverage_events` row (`result='suppressed_daily_cap'`) rather than being silently lost. Alert-only, never automatic. New Grid row: `scripts/coverage_registry.py`'s `abnormal_drift_alert` (currently `wired-never-fired` — no live proof yet, not forceable via node config, same organic-only shape as `daemon_exception_survival`). Test coverage: `tests/test_fake_broker_abnormal_drift_scenario.py`, 6 scenarios (entry/exit breach alerts, below-threshold no-op, below-capital-at-stake no-op, dry-run-sim no-op, 2/day escalation cap), driven through the real `open_position()`/`close_position()` chokepoints. **Paired Opus review (independent-cold + contextual, both agreed, no rebuttal conflict) found one CONFIRMED MEDIUM**: `check_abnormal_drift`'s real Slack HTTP POST was originally issued *inside* `_position_lock` (the mutex serializing every concurrent real position open/close across the daemon) — fixed same session by deferring the Slack call to after the lock releases in both `open_position` and `close_position`. Three LOW findings acknowledged, not changed (deliberate/low-value): threshold is read once at import (a change needs a daemon restart, consistent with other env-derived constants here); drift is recomputed inline with the same formula rather than reading back `trade_log`'s own column (verified byte-identical, no correctness issue); the 2/day escalation counter is keyed on ticker alone, not (ticker, account) — matches the design's literal wording, a latent edge case only if a ticker ever ran two real capital-at-stake nodes in different accounts simultaneously. Full suite re-run same session to confirm no regressions (see session notes for the exact count). Not committed — left for the user's review.

## [portfolio][design] Idea, raised 2026-08-13 evening, refined 2026-08-14 (renamed "trim") — real automation need is liquidity-capped ENTRY SIZING, not skim/reserve at all (revisit when a node's real notional starts approaching the 1%-of-liquidity ceiling)
User's framing, mid-conversation about extending skim (the equity-high-triggered reserve mechanism) to canary/dry_run: the actual automation that needs to exist is unrelated to skim's reserve/redeploy design entirely. It's a liquidity-aware CAP ON ENTRY SIZING: compare a node's real (compounded) notional against this project's existing `liquidity_dollars_per_day` metric (`avg_vol_10d * last_price * 0.01`, already used for candidate screening in `scripts/candidate_full_review.py`/`candidate_summary_report.py::liquidity_dollars_per_day`) — once the node's notional exceeds that 1%-of-daily-$-volume ceiling, the NEXT buy's sizing should shrink accordingly, not stay at the full compounded notional. **Explicitly no real order placement needed** (user's words) — this is a sizing-calculation change (wherever `target_notional`/shares get computed for a real BUY, likely `signals_helpers.buy_order_sizing` or the node's compounding-driven notional growth), not a new order type, not a reserve/redeploy mechanism, not a reversal of skim's "alert-only, no real-order code" design (`docs/design.md`, 2026-08-1x) — that design stays untouched, unrelated to this. Related: `[[project_skim_liquidity_takeprofit]]` memory anticipated liquidity limits forcing regular position trimming as size grows — this is that idea, but scoped correctly now (sizing cap, not skim).
**2026-08-14 refinement (user's typed notes)**: calling this "trim," not skim — genuinely a different mechanism, confirmed distinct enough that **skimming itself has been stopped** as a consequence of this reframing. Concrete calc proposed: once the semi-arbitrary liquidity boundary is hit, the next BUY doesn't use all available cash — compute live and take `MIN(available_cash, last_sale)` (last_sale explicitly **not including add-on** capital) as the sizing basis. User's framing: "this is like a meta take profit." Also raised, not yet scoped: test position sizing specifically for the case where an add-on trade fires (does trim's cap interact correctly with the add-on doubling, or does add-on need its own separate cap check). Still not started — no node is near the liquidity ceiling today, no urgency, but the calc method is now concrete enough to implement when it's time.

## [live-trading][coverage] Revisited 2026-08-16 — real miss found: CURE/TMF/ERX were never added to `SCHWAB_AUTOMATION_TICKERS`, fixed; stale pending_buys rows investigated and cleared; only the daemon restart itself remains (yours to run)
Checked real state 2026-08-16 (revisit date): CURE and TMF both had real `pending_buys` rows from 2026-08-14 09:30:00, `order_placed=0`, zero reminders fired in 2 days. Root cause: none of CURE/TMF/ERX were in `.env`'s `SCHWAB_AUTOMATION_TICKERS` — same gap-shape as the FAS-missing-from-the-list bug from the FAS/FAZ consolidation. **Fixed**: CURE/TMF/ERX added to `.env`'s `SCHWAB_AUTOMATION_TICKERS`. **Stale pending_buys question resolved 2026-08-16 (later)**: traced `_scan_buy_signals`'s `already_pending` check (`active_signals.py:508`, keys off `node['id'] in pending_wl_ids`) — a node with an existing `pending_buys` row is SKIPPED by the entry scan entirely, so nothing "picks up" a stale row and places a real order off 2-day-old price context; the actual risk was silent blocking (no fresh signal ever detected for that node), not a safety issue. Since `order_placed=0` means no order was ever sent to Schwab for either row, both were safely cleared via `signals_db.clear_pending_buy_by_wl_id(224)`/`(225)` (the sanctioned function, not raw DELETE). `signals_invariants.py` confirmed clean after. **Only remaining step: a daemon restart** to pick up the `.env` change — yours to run, no urgency. ERX has no pending buy or trade yet at all — never triggered, `time_exit_trigger_armed` (226, replaces paused SH) still waiting on its first real entry.

## [live-trading][backtest] Superseded 2026-08-15 (peer session "planner") — expand paper trading, possibly replacing the SOXL/AGQ de-facto paper-trading matrix with something else
Original 2026-08-13 ask. **2026-08-15 decision**: the real answer to "expand paper trading" is the fake-venue harness taking over that role (see the harness item above, now reframed as the primary coverage investment), not more paper nodes — paper's original "safe validation before going live" purpose is largely redundant now that real capital trades live. Not building this as originally scoped. Existing paper trading itself isn't being retired/downsized yet (migration is additive) — this item specifically (adding/replacing paper nodes) is what's closed out.

## [live-trading][coverage] Open, found 2026-08-13 (4-way paired review) — 4 lower-priority findings from reviewing the FAS/FAZ canary consolidation, not fixed tonight
(1) `GLOBAL_ORDERS_PER_MINUTE=12` (all-accounts) now has ~25 hair-trigger canary nodes (12 new FAS/FAZ + 13 old, both sets active in parallel) potentially contending for it in one signal-window minute, on top of real-money orders — worth an explicit capacity decision (raise the cap, or retire the old 11-ticker set sooner) rather than leaving it implicit. (2) `scripts/coverage_grid_summary.py --date <past-date>` writes *today's* live-computed status under an arbitrary backdated `snapshot_date` (`compute_status()` has no date parameter, it's always "now") — silently corrupts the day-over-day trend if ever used with a non-today date; either drop the flag or guard it. (3) `scripts/repoint_canary_scenario_expectations.py` hardcodes wl_ids 212-223 with no verification against the DB — correct today only because the actual insertion order happened to match after Scenario C's dedup retry; a label-based lookup (matching `seed_fas_faz_staged_config.py`'s own pattern) would be more robust for any future rerun. (4) `add_node`'s dedup key still doesn't include `entry_timing`/`fixed_sl` (root cause of the Scenario C collision, worked around via `max_hold_hours=47`, not fixed at the source) — the next caller who differs only on those two fields will hit the identical silent no-op.

## [live-trading][coverage] Open, raised 2026-08-13 — clean up `soxl_ira` staged-test nodes now that real live capital runs elsewhere (brokerage/roth/ira)?
User's question: now that AGQ/ETHU/JNUG (brokerage), DFEN/GDXU/KORU (roth), and DPST/NUGT/SOXL/SOXS (ira) carry real live capital, do the `soxl_ira` staged-test nodes (SH, RETL, ERY, HIBL, USD, YANG, YINN, SPY) still earn their keep, or just add volume/noise? `status_check.py` already flags several (ERY: `gap_resize_and_topup`, RETL: `time_exit_via_sl`, SH: `time_exit_via_trail`) as "🧹 STALE? every Grid scenario this staged role exists to prove is already verified-live" — per the `live-test-node-setup` skill's regression-pass framing, the recommended move for those is pausing from active duty (state flip) rather than deleting, so they remain rerunnable fixtures after future trading-code changes. Not yet audited: which of the 8 nodes still serve a live purpose (addon/drought Grid rows still `wired-never-fired` — RETL/HIBL/etc. are literally the only surface exercising those) vs. which are pure legacy carryover. Needs a real per-node pass, not a blanket decision.

**Per-node pass done 2026-08-15 (investigation only, no action taken)**: note this item's premise is partly overtaken by a same-day-earlier "Done" entry (`deep_backlog.md` 2026-08-15 still-later) that already retired SPY/HIBL/USD/YANG (`live`→`paper`, wl_id 134/154/155/156) and repurposed YINN as a 2-node disambiguation pair (wl_id 199+228) — this backlog line itself was never updated to reflect that, hence the re-audit below against real current DB/Grid state. Remaining live nodes in `soxl_ira`: **RETL (143)** — has a REAL pending trailing-buy order resting right now (order_id 1007603334792, placed 2026-08-14 14:34, `AWAITING_STOP_CONDITION` per live broker check) — do not touch. Formally tracked roles: `time_exit_via_sl` (Grid `time_exit_trigger_unarmed`/`armed` both verified-live, but via other nodes — 3x/1x, not RETL itself), `drought_handoff` (RETL IS the actual verified-live source, 2026-08-11 13:31:30 — genuinely load-bearing), `addon` (superseded by CURE 2026-08-14 per CURE's own staged notes, "RETL structurally blocked, arm_sell_pct=16% unreachable"). Net: RETL is redundant for its original `time_exit_via_sl` reason but still load-bearing for `drought_handoff`, and has real capital in flight — candidate for role-relabeling (drop `time_exit_via_sl`/`addon` roles) rather than retirement. **ERY (153)** — flat except one open core position (2 shares @ $10.5187, TIME-exit candidate, fires in ~66 held bars) — confirmed genuinely `🧹 STALE?` for `gap_resize_and_topup`: queried `coverage_events` directly, ERY's own `gap_resize` scenario_key has *never* fired (the 2x verified-live came from QQQ/SDOW, plus historical GDXU) and ERY's one `top_up` event (2026-08-10) landed in the `overspent_no_corrective_sell` branch, not the clean `placed` success path that's now verified-live 9x mostly via YINN(199)/RETL. This conflicts with the same-day `deep_backlog.md` entry's framing of ERY as one of "5 nodes with a real, still-open Grid role" — worth the user's attention: real Grid data doesn't support ERY's role as still-uniquely-load-bearing, though the open position means no state flip should happen until it closes anyway. **YINN (199+228)** — confirmed intentional 2-node disambiguation pair, not stale: 199 (`TrailingExitZScoreBreakout`, open position 13 shares @ $28.27, armed/trailing, doubles as `market_buy_placement` backup vehicle now that YANG's demoted to paper — CLAUDE.md's "YANG is the only live TrailingExitZScoreBreakout node" line is stale as of today) and 228 (`node_disambiguation_test`, flat, SL/arm unreachable by design, exists purely to exercise `check_order` node-id disambiguation on entry). Both keep as-is. **SH/SPY/HIBL/USD/YANG** — confirmed `state='paper'` in DB, matches the "Done" entry, no open positions, no action needed (already resolved). **Cross-ref to the HIBL/RETL resync item** (`backlog_cache.md`, "resync remaining soxl_ira nodes"): HIBL's `soxl_ira` node is now paper, so that resync question is largely moot there — the real live HIBL now runs in `brokerage` (separate node, not audited for v5-currency here). RETL's config-drift-from-v5 question is still open and untouched. Full per-node detail (config dumps, coverage_events queries) not persisted elsewhere — re-run `scripts/status_check.py` + a manual `coverage_events`/`staged_test_config` query per node if this needs re-verifying later. **No nodes were modified.**

## [portfolio][tax] Open, raised 2026-08-12 — look up current real realized losses for tax planning
Half of the original 2026-08-12 item — the tax-saving-strategy half is now decided, see `deep_backlog.md`'s 2026-08-12 (evening) entry. This half (pulling real realized-loss numbers off the actual accounts) not yet done. Related open tax items already on file: `k1_tax.py`/`scripts/k1_tax_forecast.py` (needs real 2026 trade data, still synthetic-only), the wash-sale/cost-basis tracker (`scripts/sim_wash_sale_impact.py`, AGQ's numbers known-wrong pending a Section 1256 rebuild), and the live wash-sale risk monitor idea (SPY/TQQQ priority scope).

## [live-trading][coverage] Revived 2026-08-14, refined from the tabled 2026-08-12 form — "snooze a ticker/scenario combo after a positive coverage signal," now keyed on CODE CHANGE not elapsed time
Original 2026-08-12 idea (existing `coverage_snoozes`/`snooze_coverage` mechanism could support this) was tabled because a time-based snooze isn't trustworthy — execution code changes daily, so "proven N days ago" doesn't mean "still true today." **2026-08-14 refinement (user's typed notes) sidesteps that exact objection**: snooze until the relevant code actually changes, not for a fixed duration. Concrete example given: `post_fill_topup` has real live proof ~3 times already (see `docs/deep_backlog.md`'s 2026-08-10 entry) — no need to keep chasing fresh evidence for it under the new SLA-troubleshooting idea (see the item above) as long as the code path it exercises hasn't been touched since. "Could be tonight" — the same-bar cooldown fix (HIGH item below) touches order-placement-adjacent code, so this snooze would need to correctly invalidate/reset for any scenario whose code path that fix actually touches, once it ships. Needs: a way to map a scenario_key to "which files/functions its proof actually depends on" (not just count days), so a snooze can be invalidated precisely when relevant code changes, not on a blanket calendar-time or blanket any-commit basis. Not scoped, ties directly into the SLA-troubleshooting idea above — likely the same build, not two separate ones.

## [live-trading][backtest] Decided 2026-08-12 — K-1 status generalized from an AGQ-only exception to a general rule: K-1 alone no longer disqualifies a ticker, only restricts it to brokerage (UBTI only applies in ira/roth); Section 1256 60/40 treatment is a real tax positive for a high-frequency strategy, confirmed by AGQ already trading in brokerage with no issue. Open follow-up: `tickers`/`candidate_full_review.py`'s disqualification logic still needs an explicit `brokerage_only` flag distinct from `DISQUALIFIED` (currently just prose in `underlier_note`) so this doesn't have to be re-derived by hand each time. `scripts/liquidity_tranches.txt` updated same day: BOIL/UGL/KOLD kept (K-1 but decent CAGR), UVXY/GLL dropped (K-1, weak CAGR, not worth the sweep time — user's call), TBT/TMF/TMV still excluded (single-bond, non-K-1, the separate diversification rule stands untouched), BULZ/FNGU/FNGD/QPUX/SPCL still excluded (concentration, unrelated to K-1).

## [live-trading] Real finding, 2026-08-11 (later still, 2) — `same_day_block` HURTS 7 of 8 real watchlist tickers on the trailing 1.11yr window; only JNUG benefits -- do NOT enable broadly off the LABU full-history finding alone
Full detail: `docs/deep_backlog.md`'s matching 2026-08-11 entry (top).

## [live-trading][coverage] Resolved-as-non-issue 2026-08-16, one process improvement left open — real live `watch_list` nodes with no `watch_list_candidate_link`
Original 2026-08-11 worry (is this a promotion-tracking oversight, like GDXU/DFEN/KORU were) is answered: **no real gap**. Verified 2026-08-16 via `db.get_candidate_links()` directly: current unlinked set is `CURE, ERX, ERY, RETL, TMF, YINN(id=199), YINN(id=228)` (all `soxl_ira`) — every one is a staged-test/canary node (RETL/ERY/HIBL-class, small notional, not real capital despite `state='live'` — see `project_real_live_notional_threshold` convention) or the deliberate node-disambiguation test pair (YINN id=228). Nothing to fix on the nodes themselves; the 2026-08-15 `soxl_ira` roster cleanup already did the real work here. **Only thing left open**: this was tracked as a static backlog list and had already gone stale once (original list named SH/SPY/USD/YANG/HIBL, since retired to `paper`) — wire an unlinked-live-node check into `scripts/evening_status.py`'s coverage-report breakdown (no current section for this; `part4()` or a Part 2/3 coverage-adjacent slot) so it's queried fresh off real DB state instead of drifting stale in a doc again. Not built, not urgent.


## [live-trading] Open, updated 2026-08-10 — resync remaining soxl_ira nodes (HIBL/RETL) to current best v5 data; DPST/GDXU resolved via a different mechanism, not a plain resync
Original scope (2026-08-09 late): DPST/HIBL/GDXU/RETL/SOXL/YANG. SOXL+YANG already matched current best v5 exactly, closed. **DPST and GDXU resolved 2026-08-10** — not by resyncing their `soxl_ira` params in place, but by moving them off `soxl_ira` entirely: DPST → new `ira` node (id=197, CAGR-first candidate id=108, $10k, real — see `deep_backlog.md`'s 2026-08-10 entry), GDXU → new `roth` node (id=198, candidate id=120, dormant). Their old `soxl_ira` nodes (136, 108) were sunset (`state='research'`), not resynced — GDXU's was a deliberate drought-overlay/`gap_resize_and_topup` test config to begin with, not a stale pick. **Still open**: HIBL (stale v5 pick) and RETL (`soxl_test`-origin node) — both still diverge from current best v5 data in `soxl_ira`, not yet touched. **Not yet checked**: ERY, SH, SPY, USD, LABD — LABD/ERY are confirmed deliberate test configs (ERY now also carries the `post_fill_topup` staged-test role as of 2026-08-10, see `deep_backlog.md`), not resync candidates; SH/SPY/USD status unchecked.

## [testing] Root cause confirmed 2026-08-15 (peer session "planner"), approved to build, not yet built — `tests/test_coverage_check.py`'s per-test `ensure_tables()` fsyncs fully serialize under `--dist=loadscope`, and the baseline has silently drifted worse every session a table gets added
Real reproduction, not a mystery anymore: `isolated_db` (tests/test_coverage_check.py:24-30) creates a fresh temp DB per test and calls `db.ensure_tables()` each time — schema has grown to 26 `CREATE TABLE` + 23 PRAGMA migration probes (up from 8 originally), ~1,200 serialized fsyncs across the file's 65 tests. `pytest.ini`'s `--dist=loadscope` pins the whole file to one worker, so those fsyncs are fully serialized while other workers concurrently hit the same disk — directly reproduced the 326s/5:26 outlier by simulating 11-way disk contention (319.59s, user CPU time barely moved, confirming pure I/O blocking not real work). **Baseline itself has drifted**: not 52-70s anymore, ~160s solo today on current `main`, will keep getting slower every session that adds a table (schema-size-scaling cost, not a one-time fluke). **Proposed fix**: a session-scoped schema template DB copied per test (or an in-memory/`/dev/shm` DB path for the fixture) — tmpfs profiling showed 0.219s vs ~1.9s on ext4 for the same `ensure_tables()` call, ~90% cost cut estimated. Not built yet — awaiting whoever picks it up next (peer session's investigation, not yet implemented).

## [backtest] Idea, raised 2026-08-09 — does this mean-reversion strategy do anything interesting on recently-IPO'd stocks (post-IPO drawdown/mean-reversion pattern)? (revisit ~2026-09-10)
Prompted by SPCL's real underlying (SpaceX Class A shares, since a June 2026 mandate change) having given back almost all its gains since IPO. Two real caveats before chasing this: (1) this system's universe is leveraged ETFs, not individual common stocks — testing this needs either fetching real individual-stock price history for a basket of recent IPOs, or checking whether any already-screened candidate tickers happen to be linked to recently-IPO'd names; (2) SPCL's own data can't be the test case (mandate-change contamination, see `deep_backlog.md`'s 2026-08-09 disqualification entries). Not started.

## [backtest] Tabled 2026-08-09 (later still) — full `backtest_cache` certain-column recompute; possible stays the working default (revisit ~2026-09-10)
User's explicit call: table `certain`/`pessimistic` tooling work for now, use `possible` as the standard resolution going forward (also the empirically stronger/more-frequently-winning resolution per the same-node possible-vs-corrected-certain check — possible wins 45/81 tickers, certain 26/81, and possible's wins are on average much larger). Kernel fix stays in place (real bug, harmless to leave fixed) but no urgency behind the recompute or new tooling below. Revisit only if a future decision specifically needs certain/pessimistic as a real execution path.
`_simulate_trail_both`'s certain branch had two real bugs (frozen-trigger case missing + Close-crediting an unreachable price), both fixed in `backtester.py`, plus a new lean `_simulate_certain_only` kernel/`run_backtest_certain_only` wrapper for a cheaper recompute (no possible/pessimistic bookkeeping needed). Every `strategy_return_certain`/`alpha_vs_spy_certain` value in the entire v5 `TrailingBothZScoreBreakout` cache (5,040,750 rows, 82 tickers) was computed with the old buggy logic. **A paired Opus session-wrap review (CONFIRMED HIGH) caught that the first-pass Close-crediting fix (crediting the bar's raw Close) was itself just a different unproven guess — since fixed a second time to credit `min(buy_trigger_prior, high)`, the genuinely tight worst-case-price bound both reviewers verified mathematically.** Two stale Python mirrors that had drifted from the kernel fix (`scripts/export_trades.py`, `scripts/verify_fill_resolution_accuracy.py` — the latter being the tool whose "possible is 94% accurate" finding justifies the possible-as-default decision below) were also fixed to match, and a new pinned test (`tests/test_certain_resolution_fix.py`, 4 tests) now enforces the frozen-trigger fill, the corrected bound, and `_simulate_certain_only`'s exact parity with the bundled kernel. **Correction: an earlier note here said "KORU flips to a real loss" — that was from a since-discarded hand-mirror missing the frozen-trigger fix; the real kernel-verified value is positive.** Per-ticker spot-check (against the final, doubly-corrected kernel) still not yet rerun end-to-end — the 2x-11x drop range recorded earlier reflected the interim (Close-raw) fix, not the final (min-bound) one; the actual per-ticker numbers will shift again and should be treated as stale until re-derived. Plan: add new columns (`strategy_return_certain_fixed`/`alpha_vs_spy_certain_fixed`, matching how pessimistic/certain themselves were added in 2026-07-20) rather than a new version tag, populate via the lean kernel over all existing rows (defeats `dispatch_parallel_grid`'s cache-skip optimization since every existing value needs overwriting, not just missing ones — same scale as a real sweep campaign). Per standing convention, the user runs this, not the agent. Full detail: `docs/research_log.md`'s 2026-08-09 (later still) entry.

## [backtest] Tabled 2026-08-09 (later still) — overlay/candidate-reporting tooling is possible-only; pessimistic/certain have no real trade-generation path, only a scalar `robust_alpha` MIN() input (revisit ~2026-09-10)
Tabled alongside the recompute item above, same user call — `possible` stays the standard, no urgency to build a certain-aware overlay path right now.
Traced directly in code: `candidate_5min_report.py::_node_from_row` returns `row['strategy_return']` (possible) unconditionally for all 4 candidate types, including "best certain" — so even a node selected by ranking on `alpha_vs_spy_certain` gets its `abs_return_pct`/CAGR/years/trades/drought/add-on overlay computed entirely from `possible`'s trade sequence via `get_trades_and_bars` (which only ever calls `simulate_trail_both_annotated`, a possible-only mirror). `pessimistic`/`certain` have never had a real trade-generation path anywhere in this pipeline — only a scalar return feeding `robust_alpha=MIN(...)`. User's framing: `possible`/`pessimistic`/`certain` (once correctly computed) are three viable real execution paths, not just a fill-ambiguity artifact to collapse to one number — `possible` is the most-likely default, but `certain` in particular deserves its own real overlay/reporting path since it requires no guessing at all. Not started — needs a real certain-trades-aware variant of `get_trades_and_bars`/`ensure_overlay_for_node`/the report's headline-number logic, using the now-verified `run_backtest_certain_only` kernel. Full detail: `docs/research_log.md`'s 2026-08-09 (later still) entry.

## [backtest] Idea, scoped not built, 2026-08-09 — "CertainEntryTrailingSell": a new live-executable entry mechanism confirming a fill via bar-close proof instead of the unrealistic theoretical trigger price (revisit ~2026-09-10)
Full detail: `docs/research_log.md`'s 2026-08-09 (later) entry / `deep_backlog.md`'s matching entry. Real open design gaps found before this can go further: needs new hourly polling infrastructure (only 53.5% of certain-resolution fills land in the 2 existing signal-check windows) and an explicit overnight/EOD exposure policy the backtest kernel never actually resolves. Per `docs/new_mechanism_promotion_standard.md`, real DB schema/paper-trading/Grid work only starts once a validated backtest edge exists — not there yet; the corrected (Close-priced) SOXL example still shows real alpha (+1,270.3%) but concentrated in one historical window. Not started as live/paper infrastructure.

## [backtest] Resolved-as-practice 2026-08-10, formal decision deferred to 2027 — CAGR-first (not raw robust_alpha) is the user's real, current candidate-selection convention
Full detail: `docs/research_log.md`'s 2026-08-08 (later still, 3) entry (original annualized-excess framing) + `docs/deep_backlog.md`'s 2026-08-10 HIBL/GDXU entry (real confirmation: CAGR-sorted picks beat robust_alpha-sorted picks on CAGR, cliff-safety, AND drought-overlay validity for both tickers checked). User confirmed 2026-08-10: this is already how they pick in practice (self-applied, not delegated), only needs revisiting when a genuinely new research-candidate list comes up — infrequent enough not to need standing tooling/decision work. Deferred, not urgent: (revisit ~2027-01-01).

## [backtest] Open, broadened 2026-08-08 (later) — "possible" fill-resolution confirmed empirically more accurate than pessimistic/certain at 66 signals/17 tickers; convention change itself still not decided
Full detail: `docs/research_log.md`'s two 2026-08-08 entries. Original 25-signal/6-ticker check (possible closest 24/25, mean abs 0.107% vs 0.871%/1.374%) reproduced at 66 signals/17 tickers (possible closest 62/66=94%, mean abs 0.166% vs 0.697%/0.895%) — same ranking, same order of magnitude, fully independent ticker set aside from 6 overlaps. Bounded by yfinance's fixed 58-day 5-min window (more tickers now checked, not more elapsed time) — a rolling re-check over a future time period would be the strongest next confirmation. Real implication if the user decides to act: `robust_alpha`'s `MIN(possible,pessimistic,certain)` convention may be needlessly conservative project-wide. Not decided or actioned — changing the convention is cross-cutting (`ROBUST_ALPHA_SQL`, `top_safe_nodes.py`, prune's island selection) and would need the `backtest-change-rollout` skill's staged process, not a quick edit.

## [live-trading][coverage] Deferred indefinitely 2026-08-08 (weekend) — judgment-based intraday risk review layer (LLM-driven, not fixed rules)
`signals_notify.check_intraday_risk_review` (built 2026-08-08) is a cheap, deterministic first version — reviews `trading_incidents` + a narrow `coverage_events` substring match, alerts only on a fixed rule. Discussed at length whether this should instead (or additionally) be judgment-based: either `active_signals.py` calling the Claude API directly with its own key when it hits an ambiguous event, or a dedicated local background session (`/loop`, which has real local DB access unlike a cloud `RemoteTrigger` routine — that mechanism was evaluated and rejected, isolated sandbox with a 1-hour minimum interval). User's framing: a fixed rule can only catch anomalies someone already anticipated; judgment is the actual point of "review," not just another alert. Explicitly deferred ("yeah not yet") in favor of shipping the deterministic baseline first and proving it out. Also noted as a simpler interim alternative: the user can message the agent directly on demand for a state check ("i'm the trigger") rather than needing autonomous polling at all.

## [live-trading][coverage] Parked 2026-08-07 (evening) — study real Schwab rejection behavior via `scripts/live_sanity_check.py`, then calibrate a new `fake_broker.force_reject_next_order()` to match
Plan: user runs `scripts/live_sanity_check.py --test oversized_buy --tickers SOXL` against `soxl_ira` (human-confirmed, real order, not something to run unsupervised) to observe Schwab's actual rejection shape (exception type, HTTP-immediate vs. async order-status REJECTED, exact error payload) — `oversized_buy` is a reasonable proxy for add-on's real failure mode (buying against margin credit that doesn't exist in a limited-margin IRA looks like "insufficient buying power" to Schwab either way). Once the real shape is known, add a `force_reject_next_order()` mode to `tests/fake_broker.py` (currently `place_order` always succeeds/fills — zero reject capability exists) calibrated to match, then write a fake_broker test for the add-on-BUY-rejected code path (does the alert fire correctly, does the `addon_legs` staging row get closed out instead of stuck `'open'`) — this closes the gap without waiting on an organic live reject. Not started, parked for the next session.

## [live-trading][coverage] Done 2026-08-07 (evening) — drought + add-on enabled across all 11 currently-live `soxl_ira` nodes (not just D6/SOXL), to widen the live-hit surface for the 14 wired-never-fired Grid rows
User's call, deliberately not alpha-validated per-ticker (generic confirm_days=3/vol_gate=0.4 config) — the goal is exercising the mechanism across more real, already-live nodes rather than chasing edge. GDXU (id=108) got drought only (its config is genuinely validated, stress-tested); the other 9 (DPST, ERY, HIBL, LABD, RETL, SH, SPY, USD, YANG) plus SOXL (already had both from D6) got both drought+addon. Real consequence: every one of these nodes can now attempt a real add-on BUY in `soxl_ira`, which per the account_type mislabel above is expected to get broker-rejected — much wider real-order-attempt surface than before, not just SOXL. `staged_test_config` baseline updated to match (7 rows previously untracked for these fields, `--merge-new-fields` used, then `drought_overlay_enabled`/`addon_enabled` manually set to 1 to match). DB backed up first (`trading_live.db.bak_pre_gdxu_koru_*`). `signals_invariants.py` clean after. Considered and explicitly declined for now: adding entirely new tickers (30-ticker idea floated, deferred — scope is the existing liquidity-screening session's job, not this one) and a new KORU probe node (superseded by widening existing live nodes instead; note KORU's own drought config was separately found to be a pure overfitting artifact in `docs/research_log.md`'s 2026-08-07 entry, so it should NOT be treated as a validated candidate if revisited later).

## [backtest][new-strategy] Research idea, raised and parked 2026-08-07 — drought overlay as its own independent strategy (own vol/z-based entry signal + own SL/arm/trail grid), swept across all v5 tickers
Full detail: `docs/research_log.md`'s 2026-08-07 entry. A real test of the underlying premise (do drought/add-on results mostly overlap across nearby node configs, so they could be evaluated once per ticker?) REFUTED it — even a true 1-grid-step neighbor (trail_buy_pct off by 1%) only gave ~30% drought-window overlap for SQQQ, and add-on's trade count collapsed to 1-3 trades across the same neighbors (too thin to call a real edge). `scripts/drought_overlay_sweep.py` (2026-08-05/06) already covers a similar independent-grid design for the 10 real v5 tickers — entry-side vol gate real for a minority (HIBL/SOXL/UDOW), still needs out-of-sample validation before extending further. Explicitly parked, not started — user's own framing: "it would have to be very robust to do this outside of v5."

## [backtest] Real finding, 2026-08-07 — candidate overlay results (drought/add-on) are highly sensitive to small node-parameter changes; can't be trusted off a single node's backtest
Full detail: `docs/research_log.md`'s 2026-08-07 entry. Directly tested against SQQQ (liquidity-screen candidate): drought windows only ~30% Jaccard-overlap between adjacent `trail_buy_pct` grid values; add-on trade count collapsed to 1-3 trades across the same neighbors. New infra this session: `scripts/locate_best_node.py` (best-row locator + `candidate_nodes` interim registry table, deduped by full param tuple, standing in for a real `wl_id` pre-promotion), `scripts/run_overlay_shim.py` (runs drought + add-on against a constructed candidate node, writes to `candidate_overlay_results` joined against `candidate_nodes`), wired into `scripts/run_liquidity_tranches.sh`'s per-tranche loop after prune validation passes.

## [live-trading][security] Found 2026-08-11 — `_last_sale_recovery`'s real-trade-compounding basis can permanently lock a tiny-notional node to 0-share sizing, with no organic recovery path
Distinct from the already-known 2026-08-01 "toy pilot-scale notional frequently skips signals" finding (`signals_helpers.buy_order_sizing`'s target_notional/fill_price gate not firing at small size) — that one is a coverage-order-flow nuisance; this is a one-way lock. `signals_helpers._last_sale_recovery(node)` sizes the *next* buy off `exit_price * shares` from this node's own last real closed trade (`trade_log`, matched by ticker+strategy+version+window+account) and never falls back to `starting_notional` once any real trade has closed, regardless of how small that trade was. Found live via LABD (wl_id=152, `soxl_ira`): its last real closed trade was a 1-share test fill (~$7.66-7.69, `trade_log` id=69, 2026-08-07), so every subsequent signal sized `target_notional≈$7.66` against a ~$7.75 price → `floor(7.66/(7.75×1.01))=0` shares (`coverage_events` `automated_buy_execution`/`shares_too_small`, 2026-08-10 13:30:23) — and since the node can never buy again at 0 shares, it can never close a bigger trade to grow the basis back up. **LABD retired 2026-08-11** (state flipped live→paper, stuck `pending_buys` row cleared, `staged_test_config` note updated) rather than basis-corrected — its staged-test purpose (SL-exit proof) was already closed 2026-08-07, so retiring was strictly simpler than inventing a basis-reset mechanism. **A general reset lever now exists, 2026-08-12** (built for a different, unrelated finding the same day — see `deep_backlog.md`'s "real, unmonitored Schwab margin-order testing" entry): `watch_list.starting_notional_override` (`signals_db.set_starting_notional_override`/`clear_starting_notional_override`), checked FIRST by `_last_sale_recovery` before the trade_log lookup or the plain `starting_notional` fallback — this is exactly the "reset basis explicitly" option this item flagged as unevaluated. **Still open**: nobody has actually audited existing live `soxl_ira`/`ira` nodes to find which ones are at risk of the same lock — the lever to fix one now exists, but no node has been checked or fixed with it yet.

## [live-trading][coverage] Idea, raised 2026-08-11 — 4-tier test-confidence reframe + nightly staged-test lifecycle manager + dry-run-only "can't-trade" ticker pool for wash-sale reduction
Full detail: `docs/deep_backlog.md`'s 2026-08-11 entry (search by this header's title). Not started — Step 1 (read-only report) is the first real build target.

## [live-trading][security] Found 2026-08-07 (later) — `soxl_ira`'s `account_type="margin"` overstates real borrowing capability; add-on's `addon_non_margin_account_blocked` guard won't catch it; left in place deliberately as a real broker-rejection test
`soxl_ira` is a limited-margin IRA — same-day settlement (no T+2 wait), but IRAs cannot actually borrow on margin. `schwab_safety.ACCOUNTS['soxl_ira'].account_type` is `"margin"` (set for `same_day_block`'s cash-settlement purpose, `schwab_safety.py:174`), which the add-on code-level guard (`schwab_safety.py:913`, `if limits.account_type != "margin": raise SafetyViolation(...)`) reads as "real margin, OK to proceed" — an overloaded-flag bug, same shape as the already-known `backtest_cache` overloaded-column issue (see the 2026-08-07 (late) "needs a real schema definition" item below), just in `ACCOUNTS` instead. Net effect on the new D6 test node (196, SOXL/`soxl_ira`, `addon_enabled=1`): if add-on triggers organically, the code won't block it locally — a real order will be submitted to Schwab and (expected) rejected by the broker for real. **User's explicit call: leave as-is** — this is actually useful, since there's currently zero test coverage (no `fake_broker` scenario) for what happens when a real add-on BUY gets broker-rejected (does the code alert correctly? does any `addon_legs` staging row get left in a phantom state?). Real fix for the underlying mislabel (distinguish "same-day-settlement-capable" from "can actually borrow," e.g. a separate flag) is still open, deliberately not done now.

## [live-trading] Decided 2026-08-07 — GDXU stays `state='live'` going forward (not reverted to `paper`)
User's call: GDXU remains a live node after the reconciled position closes, rather than reverting to `paper`.

## [live-trading][coverage] Open, updated 2026-08-10 — remaining `docs/plans/real_order_execution_drought_addon.md` Grid rows still wired-never-fired (addon side entirely; drought_handoff/drought_handoff_cancel/drought_handoff_exit_placement)
`drought_entry_placement` and `post_fill_topup` (both drought-side) flipped to `verified-live` 2026-08-10 via RETL's real drought entry (see `backlog_resolved_recent.md`). Still `wired-never-fired` as of 2026-08-10: every `addon_*` row (`addon_entry_fill`, `addon_entry_placement`, `addon_exit_fill`, `addon_exit_placement`, `addon_buying_power_check`, `addon_double_buy_exemption`, `addon_leg_independent_sl_fill_detection`, `addon_leg_reconciliation`, `addon_second_ticker_buy_allowed`) and `drought_handoff`/`drought_handoff_cancel`/`drought_handoff_exit_placement`. 8 real `soxl_ira` nodes carry `addon_enabled=1` (SPY/SH/RETL/ERY/HIBL/USD/YANG/SOXL — LABD retired 2026-08-11, see the basis-lock entry below) — all still waiting on an organic real add-on trigger. No forcing planned, same organic-wait posture as the rest of this checklist.

## [backtest][ops] Hold, set 2026-08-07 — keep `trading_universe.db.pre_prune_20260807_151917` (31G) as a backup until ~2026-09-07
Supersedes the earlier same-day note about `...100407` (67G, still had v4 — deleted after v4 was purged and this file was superseded). This is the pre-swap copy of the full v4-purged merged-recovery DB, left behind automatically when `--swap` ran after the fixed prune's comprehensive validation passed (11,183/11,183 real island groups matched). User's explicit call — keep it ~1 month in case another prune bug surfaces before fully trusting the current pruned live DB (2.1G). Don't delete before ~2026-09-07 without asking.

## [backtest] Deferred, raised 2026-08-07 (late) — `backtest_cache`'s overloaded columns need a real schema definition, not more ad hoc patches (revisit ~2026-09-10, may push further)
Root cause of the trail_buy_pct/take_profit bug recurring three times today wasn't three independent slips — it's that columns like `take_profit` (means different things per strategy, NULL for TrailingBoth) and `trail_buy_pct` (real for TrailingBoth, always-zero for TrailingExit) have no documented, enforced contract, so every new query has to rediscover the semantics. Real fix: an explicit schema definition (which column means what per strategy, which must always be held fixed together in a neighbor search) future code follows instead of assumes. **User's explicit call: deferred to next phase, tied to whenever a new strategy variant is actually designed** — not tonight. "Limp along" on the current ad hoc-patch approach until then. (The 2026-08-02 `prune_backtest_cache.py` instance of this same root cause is now fixed — see `backlog_resolved_recent.md`.)

## [live-trading][security] Real order-execution code built + paired-reviewed 2026-08-1x — drought-entry/add-on-entry now implemented, gated off, D6 node created, awaiting staged real-order testing
Full detail: `docs/design.md`'s "Real-order execution edit plan" build-status addendum. Phases 1-9 of `docs/plans/real_order_execution_drought_addon.md` implemented (schema, `schwab_safety` `is_addon_leg` exemption, real drought entry/HANDOFF, real add-on entry/lockstep-exit, Accountability Grid rows, 22 new fake_broker tests, reconciliation). Paired independent-cold + contextual Opus review run same session: both converged on the same CRITICAL (lockstep leg-exit SELL was structurally unplaceable at all 7 real call sites — fixed via a node-scoped, not parent-position-scoped, `is_addon_leg` exemption), plus 1 more CRITICAL + 2 HIGH + 2 more HIGH from each review separately, all fixed and re-verified with real fake_broker tests reproducing the actual call-site ordering (which is what caught 2 further latent bugs — a duplicate-order-fingerprint collision in both directions — that neither review itself found). Full suite 589/590 (1 pre-existing environmental failure, confirmed unrelated). **D6 decided 2026-08-07 (later): SOXL in `soxl_ira`, node created same day (wl_id=196)** — `state='live'`, clone of the real v5 SOXL core config (id=92: window=10, fixed_sl=2.0, trail_buy_pct=3.0, trail_sell_pct=1.0, arm_sell_pct=30.0, entry_timing=open_check), `starting_notional=$500` (well under the $3,000 `soxl_ira` cap even with add-on's 2x doubling), both `drought_overlay_enabled=1` (confirm_days=3, vol_gate=0.4) and `addon_enabled=1` so Tests 1-4 can all run on this one node per the plan's own suggestion. Confirmed flat (no `open_positions`/`pending_buys` row) before creation; `staged_test_config` baseline seeded (`scripts/seed_baseline_config.py`); `signals_invariants.py` clean. **Nothing has fired yet** — node just added, waiting on an organic drought/core signal; **still nothing an agent should force** — Test 1 (drought entry) requires real checkpoint bars to elapse organically, per the plan ("forcing it would invalidate the test"). **Accountability/test-coverage review, 2026-08-07 (later)**: `scripts/coverage_registry.py`'s report shows all 14 drought/add-on Grid rows (6 paper-side + 8 real-order-placement) as `wired-never-fired` — including the paper-side rows, so even paper trading hasn't organically exercised any of these paths yet; every current proof is offline. That offline proof is solid: 66/66 tests pass across `test_fake_broker_addon_entry_scenario.py`, `test_fake_broker_addon_lockstep_exit_scenario.py`, `test_fake_broker_drought_entry_scenario.py`, `test_fake_broker_drought_handoff_scenario.py`, `test_fake_broker_overlay_truth_table_scenario.py` (32+5), and `test_overlay_paper_trading.py` (29). **Correction to the earlier "not written" note below**: `test_fake_broker_overlay_truth_table_scenario.py` (Part 9's item 5) *was* written, same session as the real-order build (`ea03095`) — verified directly, 5 tests, passes. Separately, confirmed `signals_invariants.check_addon_drought_live_nodes_have_coherent_account_type` — the startup-time guard meant to catch exactly the `soxl_ira` account_type mislabel above — passes silently for node 196, giving false confidence; same root cause as the `schwab_safety` guard, **deferred together** (user's call, 2026-08-07 later) rather than patched piecemeal now. Next: (1) put-hedge is now a deliberate skip, not just deferred — see `deep_backlog.md`'s 2026-08-1x "put-hedge skipped" entry, (2) watch node 196 for the organic Test 1 trigger, run staged real-order testing per the real protocol (Part 12, user-executed only).

## [live-trading][coverage] Open, raised 2026-08-1x — skim's core-only equity scope is undecided-turned-accepted, but only for drought/addon flags; needs revisiting if skim scope ever expands
Decision on file: skim's `strategy_value`/equity math (`_paper_core_equity`, `paper_trading.py:1169`) only ever reflects `position_source='core'` trades — drought-overlay and add-on-leg P&L are structurally invisible to it (confirmed: addon legs aren't even in the same table, `paper_addon_legs` vs `paper_trade_log`). User accepted this as the design for now (skim not planned for near-term real use); 2 new tests (`test_skim_ignores_drought_overlay_pnl_by_design`, `test_skim_ignores_addon_leg_pnl_by_design`) lock the current behavior in as deliberate. Real, correctly-scoped risk if this combination is ever actually deployed: asymmetric, not symmetric bloat — harmless under-capture when drought/addon are profitable, but a misleadingly-optimistic skim signal (fires/moves money to reserve off core's isolated gain while blended true equity is flat or worse) when they're losing money. If skim scope is ever revisited: (1) folding in drought is mechanically straightforward (`position_source IN ('core','drought_overlay')` — mutually exclusive in time, can compound sequentially in one chronological chain), (2) folding in addon is NOT simple — addon legs are a concurrent, not sequential, return stream on top of an open core position, needs real modeling work (parallel P&L summation, reconciling the margin-cost haircut already applied to addon legs), (3) `skim_reserve_log` needs a `trigger_position_source` column added at the same time, before any ambiguous rows accumulate — it has none today, which is fine only because `check_paper_skim`'s one call site structurally can't be reached by a non-core close.


## [backtest] Open, raised 2026-08-07 — USO's drought overlay is uniformly negative at every confirm_days 1-15, unexplained (revisit ~2026-09-10)
Full detail: `docs/research_log.md`'s 2026-08-07 entry. Notable because USO's own price was UP +57.1% over the same span — rules out "no positive drift" as the explanation; the drought-specific losses are a real, distinct pattern from the ticker's overall trend that hasn't been traced to a cause.

## [portfolio][live-trading] Deferred 2026-08-07 ("2027 problem") — add-on's 100%-margin sizing creates a real, unmodeled liquidity/margin-availability constraint
Full detail: `docs/research_log.md`'s 2026-08-07 entry. Two distinct pieces: (1) doubling exposure at arm means the practical starting core position should be sized with headroom to unwind BOTH legs, roughly halving the safe starting notional vs. a core-only assumption; (2) margin availability at trigger time is a shared, time-varying resource across whatever else is open in the same brokerage account (USO/UCO/AGQ potentially simultaneously), not a dedicated pool — no single ticker's backtest can answer this. Explicitly deferred, not urgent. **User's real plan, stated 2026-08-07**: starting capital $50k, with AGQ's real ceiling closer to **~$500k** than the initially-discussed $1M once the add-on doubling is accounted for — confirmed real margin clearance exists to cover that. Growth-timeline math (~268%/year implied by the full backtest) puts reaching that ceiling within ~2 years if the rate holds, which the session's throughline of found-and-corrected fragility argues against taking for granted. **At the ceiling, plan is to take profit rather than keep holding** — since AGQ/K-1 instruments are Section 1256 (marked-to-market at year-end regardless of whether the position is actually closed), there's no tax-timing advantage to holding longer once capacity-constrained; the tax bill comes due either way.

**Companion piece, same day (later), also deferred**: the add-on guard's `account_type != "margin"` check (see the mislabel entry above) is not really an account-type question at all — add-on doesn't inherently need to borrow; if the account has enough real free cash to cover the doubled notional outright, it can fund it without touching margin, in ANY account type (limited-margin included). A cash-aware gate (check real available cash first, fall back to checking real borrowing capacity — `AccountLimits.margin_floor`, added 2026-08-07 — only for a genuine shortfall) was designed in conversation but not built: two real complications surfaced mid-design — (a) add-on's cash draw can collide with the skim-and-reserve overlay's earmarked-but-not-yet-redeployed cash (a naive cash check can't tell "free" from "skimmed, waiting to move to SPY"), and (b) with several tickers able to trigger add-on near-concurrently (9-10 `soxl_ira` nodes currently have `addon_enabled=1`), a single reservation number isn't enough — same shape as the multi-resting-BUY reservation fix landed earlier the same session, one level up (add-on legs, not core entries). **User's explicit call: defer** — moderate starting balances and no skim active yet, so neither complication is live right now; Schwab's own broker-level rejection is the real backstop in the meantime (a limited-margin account genuinely cannot overdraft, so an unaffordable add-on just fails safely with an alert). The `account_type != "margin"` guard itself is unchanged from before this session.

## [portfolio][tax] Open, raised 2026-08-09 — populate the new K-1 tax forecast tool with real data
`k1_tax.py`/`scripts/k1_tax_forecast.py`/`pages/16_K1_Tax.py` (built 2026-08-09, resolving the prior "build a simulator" idea — see `backlog_resolved_recent.md`) is tested against synthetic data only. Needs: real 2026 AGQ trades logged, 2025 actual total tax liability once known, real rate config confirmed against the user's actual bracket. Separately not yet built: Schedule AI/Form 2210 annualized-income-installment modeling (relevant given this strategy's "lumpy" gains) — the current tool models the flat-quarterly 110% safe harbor only, not the annualized alternative that could reduce early-quarter cash needs.

## [live-trading] Idea, designed not built, 2026-08-07 — KORU "upswing-watch" Slack alert (revisit ~2026-08-22)
Full detail: `docs/research_log.md`'s 2026-08-07 entry. Fire when price is above both 50-day and 100-day SMA (real trend, not just a bounce) AND vol_pctile<0.3 (genuinely calm) — matches the trend-continuation mechanism found for KORU specifically. `scripts/watchlist_status.py` already prints `SMA50`/`>SMA50`/`VolPctl` columns for every node (informational only, added this session) so the state is visible at a glance; the alert itself isn't wired up. Given the slow-moving (50-day) trigger, no urgency — eventual home is the existing start-of-day readiness report, not a new fast-cadence job.

## [live-trading] Found 2026-08-04 very late — broader diagnosis still open (all 3 point-fixes now resolved, see `backlog_resolved_recent.md`)
**Broader diagnosis (still open)**: (1) and (2) (now fixed, see `backlog_resolved_recent.md`) weren't coincidence -- multiple scripts (`status_check.py`, `coverage_check.py`, `watchlist_status.py`, `paper_trading_status.py`) each reimplement their own ad hoc view of overlapping state (open positions, pending buys, node config) instead of sharing one canonical "real current state" function, so the same class of bug can independently exist in each. Real fix candidates, not yet decided: (a) narrow -- point-fix the next instance when found; (b) broad -- one shared `signals_db` function (e.g. `get_real_position_state(ticker, account=None)`) that all these scripts call instead of hand-rolling SQL. Either touches `signals_db.py`/coverage logic and needs the standard Opus review gate. See `.claude/skills/daily-routine-check/SKILL.md` for the full context.

## [backtest] Deep dive 2026-08-05/06 — drought-overlay: entry-side intraday-vol gate real for a minority of tickers (unresolved overfitting risk); exit-side vol-gate REJECTED at trade-price-action level, not just aggregate stats; HANDOFF mechanism traced, concentration-risk still open
Full detail: `docs/research_log.md`'s 2026-08-05 (very late 3) and two 2026-08-06 entries. **Exit-vol-gate line of inquiry now CLOSED (2026-08-06), rejected**: the per-ticker sweep's apparent KORU "improvement" was cross-axis noise (the gated winner cell also changed confirm_days); isolating the gate's true effect and auditing the 5 changed KORU trades bar-by-bar showed 3 of 5 are real recoveries killed mid-pullback (vol spikes that were the bounce itself, or persistently-elevated non-reactive vol, not danger arriving) vs. only 1 genuine save (a real overnight-gap breakdown). Same failure mode as the original pooled rejection — confirmed at the price level, not just in aggregate stats. **Verdict: not predictive enough to adopt in any form (pooled or per-ticker)** — would need a fundamentally different signal (e.g. directional/momentum confirmation, not vol alone) to be worth revisiting. Entry-side gate is UNAFFECTED by this — still open, real for HIBL/SOXL/UDOW, still needs out-of-sample validation. Concentration-risk screen (original 2026-08-05 finding, KORU/pooled-cell dominance) still unresolved. **Still deferred to next session**: (1) out-of-sample validation of the entry-gate per-ticker choice (fit first-half/test second-half), (2) whether entry-gate-helped tickers (HIBL/SOXL/UDOW) share a real structural property vs. gate-hurt ones, (4) HANDOFF/handover mechanics redesign — transfer management into the core strategy's own state machine instead of sell-then-implicitly-rebuy at the backstop bar's Open, (5) put-hedge cost/payoff modeling (rough Black-Scholes off the same intraday realized-vol measure as an IV proxy) — discussed, not built. Put-hedge option-selection methodology itself is now moot — see `deep_backlog.md`'s 2026-08-1x "put-hedge skipped" entry.

## [backtest] Research idea, found 2026-08-03, narrowed same day — the 14:30 daily signal window fires ~3x more entry signals than the 9:30 window, across nearly every v5 watchlist ticker
Side finding from the entry-timing-seasonality check (`docs/research_log.md`'s 2026-08-03 entry). Directional intraday drift is now ruled out as the explanation (same-day follow-up entry: no real 9:30-to-14:30 price drift on any of the 10 tickers). Leading unverified explanation: cumulative opportunity — more elapsed trading time by the 6th bar than the 1st for any large-enough move (either direction) to breach the lower band, not a directional bias. Not yet tested directly (would need e.g. comparing intraday range/volatility by bar number).

## [backtest][new-strategy] Research idea, raised 2026-08-02 — FFT/wavelet-based trend-extraction as a distinct strategy paradigm (not an enhancement to the existing z-score mean-reversion family) (revisit ~2026-09-10)
Evaluated a Gemini-proposed live spectral-filtering pipeline (STFT/CWT low-pass denoising → slope/crossing signal → VWAP/TWAP execution). Confirmed this describes a fundamentally different strategy (trend-following off a denoised price line, sub-minute tick-level execution) than what this system runs (hourly-bar z-score mean reversion, 2 fixed daily signal windows, manual/bridge-automation execution) — most of the doc's cold-start/microstructure/execution-routing content doesn't apply here at all. The one applicable piece (offline FFT cycle-period detection on historical hourly data) is already captured separately — see the existing FFT cycle-detection item below. This entry is specifically the "build a new denoised-trend strategy variant" idea — unscoped, would need its own backtest kernel path distinct from `strategies.py`'s current classes. Not just a stray idea: user confirmed 2026-08-02 the system may eventually run several distinct strategy paradigms in parallel, not just mean-reversion variants — so this is a real candidate for that, not automatically low priority.

## [backtest][live-trading] Research idea, raised 2026-08-02 — ATR/volatility-scaled position sizing, evaluated against a Gemini-proposed risk-architecture doc
Real blocker, not just unscoped: `starting_notional` is already capital-constrained per account (one-account-per-ticker model, sized to available capital), not a free sizing knob — needs a design for what shrinks when vol is high (idle capital? fewer shares?) before this is even a backtest question. Two other modules from the same doc (watchdog auto-flatten, loss-streak circuit breaker) were evaluated and rejected/backlogged in the same conversation — see below and `docs/conversation_summary.md`.

## [live-trading] Research idea, raised 2026-08-02, deferred 2026-08-09 (revisit ~2026-08-22) — loss-streak circuit breaker (same-day or cross-day), evaluated against the same Gemini risk-architecture doc
User flagged real overfitting risk: current live footprint is only 4 nodes (SPY/SH/GDXU/DPST) across 2 signal windows/day, too little same-day trade volume for a same-day streak counter to mean anything, and a cross-day threshold would be tuned on a small live sample. Needs a "watch it, don't automate it" framing (alert-only) rather than an auto-pause, if pursued at all.

## [live-trading][coverage] Investigated 2026-08-02 — the 3 suspicious wired-never-fired rows explained; 2 real open items filed; remaining 14 triaged by staging feasibility, none actionable today
Full detail: `docs/deep_backlog.md`'s two 2026-08-02 entries. Of the 14 `wired-never-fired` rows: 4 need staging during market hours (blocked while not WFH), 4 need a 2nd live node on a different account (blocked, only one real account), 1 deferred to the planned Slack rearchitecture, 1 (`market_buy_placement`) just needs DPST to hit a real signal, 4 aren't realistically stageable.

## [backtest][live-trading] Open, raised 2026-08-01 — formalize/write down the pattern separating manual-fill execution reality from automated/backtest-assumed fills
Currently scattered across `docs/operational_limits.md`'s Phase 1/2 marker and `sim_chaos_monkey.py`; not yet scoped where it should actually live.

## [backtest] Open (bear-market + regime items) — from the 2026-08-01 research tangent's independent Opus challenge
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by title: "independent Opus challenge of the 2026-08-01 research tangent"). Compounding-drag item itself is resolved.

## [live-trading][security] Accepted residual risk, 2026-07-27 — `_submit_replace_with_retry`'s retry can fire a second `replace_order` against an already-replaced order_id
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).



## [backtest][live-trading] Research idea, raised 2026-08-11 (later still), Phase 1 spec'd 2026-08-16 — quarterly resweep cadence + dual-window (max-year/1yr) CAGR + candidate rotation, more fully specified than the older monthly-rescreen item below
Full detail: `docs/deep_backlog.md`'s matching 2026-08-11 entry (top). Supersedes the older 2026-07-25 monthly-rescreen idea below.

**Phase 1 fully spec'd 2026-08-16**: `docs/design.md`'s 2026-08-16 (later still) entry — the "date-windowing capability unconfirmed" blocker now has a concrete design. Real motivation: a node's optimal config can drift out of its cliff-safe island over time (surfaced by the 1.11yr-vs-3yr `same_day_block` divergence). Scope is narrow and deliberate: *only* the kernel/data-loading date-range capability (`run_optimization_sweep.py`'s `_load_node_inputs`, no kernel simulation code needs to change — `prep_inputs`/the numba `_simulate_trail*` functions are already date-agnostic, purely positional). Key correctness trap already flagged in the spec: must load a lookback buffer before `start_date` so rolling indicators (SMA/Std) compute correctly, then trim *trades* to the requested window — not truncate input data, which would silently corrupt early-window indicator values. Cache-key collision risk also flagged: `_NODE_INPUT_CACHE`'s memo key needs the date range folded in or a differently-windowed rerun could serve a stale cached result. **Everything else — scheduling, the quarterly cadence itself, how a rolling trend view gets presented — is still unscoped**, this is data-loading capability only. Per `backtest-change-rollout` skill: needs staged validation (single-node audit first), user runs the actual sweep, not an agent. Not built.

## [backtest][live-trading][new-strategy] Research idea, raised 2026-07-25 — monthly universe rescreen (recurring cadence) + a possible "v6" momentum-exhaustion-bounce strategy variant
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading][security] Downgraded 2026-08-09, not currently reachable, found by Opus review 2026-07-24 evening — manual-"Filled" SL call is ticker-gated but not mode-gated
Add a `node.get('mode', 'live') == 'live'` guard alongside the ticker check if/when that routing changes — see `docs/deep_backlog.md`'s 2026-07-24 evening batch-fix entry. Distinct from the 2026-08-01 `handle_entry_price` SL-placement fix (different code path). **2026-08-09**: handler code (`handle_trail_buy_fill_price`) still exists, code-level reachability finding unchanged (still gated behind a routing change that hasn't happened) — but the user has confirmed moving completely away from manual button-click confirmation as an operational direction, making the one future scenario that would make this reachable even less likely to ever matter. Not closed (the code condition itself is unchanged) but deprioritized further.

## [live-trading][coverage] Minor, found 2026-07-24 ~evening via Opus review — record_deviation doesn't refresh expected_outcome on rerun
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading][tax] Idea, raised 2026-07-24 ~15:10 ET — run SOXL in both `roth` and `soxl_ira`, but `roth` needs to become limited-margin first
`roth` is still `dry_run=True`/cash-type as of last check — precondition still unmet.

## [live-trading] Open, raised 2026-07-24 ~10:55 ET — retry `check_gap_resize`'s cancel+replace test properly pre-market on a future day, not mid-day
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading] Far-backlog, raised 2026-07-25, deprioritized same day — v5 watchlist skews long-only, consider adding inverse counterparts
Distinct from the canary `ira` account's 2026-07-29 inverse-pair additions (FAZ/SPXU/TWM/QID/SDOW/JNUG/JDST) — those are canary-only, the real v5 watchlist 65 skew is untouched. Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading] Deferred indefinitely, updated 2026-08-08 (weekend) — signal/reminder dry_run visibility AND live/dry_run/paper channel separation, both now structurally moot under the capital-at-stake redesign
Both originally raised 2026-07-24 (distinct from `mode_tag(account)`, which only tags order-placement alerts): (1) signal/reminder alerts didn't show `dry_run` status, only real order-placement messages did; (2) split live/dry_run/paper notifications into separate channels to cut chattiness. **User's read, confirmed by code (2026-08-08)**: the 2026-08-08 "capital at stake" redesign (`signals_helpers.should_alert_live`/`has_capital_at_stake`) now gates ALL routine signal/reminder alerts to capital-at-stake nodes only (`state=='live'` AND account `trading_enabled=True` AND `starting_notional >= $10k`) — and `has_capital_at_stake` requires the node NOT be dry_run by definition, so a dry_run node can never produce a real-time alert anymore. The scenario both items worried about (an ambiguous, unlabeled dry_run alert cluttering the channel) can no longer occur under the current design. Deferred indefinitely — revisit only if `should_alert_live`'s gating logic changes (e.g. if muted-node alerts are ever reintroduced for some other reason) or once `roth`/`brokerage` cross the capital threshold and channel volume becomes a real problem again for genuinely live nodes.


## [live-trading] Low priority (user's call, 2026-08-09), raised 2026-07-23 night — Schwab auth flow is "clunky", no cleaner unattended path designed yet
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title). Distinct from the already-fixed `interactive=True` hang bug.

## [backtest] Open, paused 2026-07-22 — "v6" idle-capital parking idea; inconclusive, downturn-specific follow-up queued
Tested (`docs/research_log.md`'s 2026-07-22 entry): no robust fold-consistent edge found. Follow-up idea (check specifically during real SPY drawdown episodes — dates listed in CLAUDE.md) raised but not yet run.

## [live-trading][tax] Open question, raised 2026-07-22 — which ticker (SOXL vs AGQ) goes to the taxable brokerage account first; wash-sale cross-account mechanic needs one more confirmation
Gated on the broader wash-sale/tax analysis item below, also still open.

## [live-trading] Low priority, idea raised 2026-07-21 — should `CASH_SAFETY_BUFFER` scale with order size instead of staying a flat $200?
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).


## [backtest] Research idea, not started, 2026-07-20 — is overnight gap frequency/magnitude asymmetric (up-gap vs down-gap)?
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading][security] Phase 4 (deferred to cloud-infrastructure planning), 2026-07-18 — move order-placement/mutating Schwab calls behind a separate proxy this session can't write to
Full detail in `docs/deep_backlog.md`.

## [live-trading][security] Directional call made 2026-08-09, node-level design deliberately deferred — one brokerage account per live ticker, for blast-radius containment against a rogue algorithm
Full detail in `docs/deep_backlog.md`. Confirmed still unbuilt: `ACCOUNTS` has only one non-dry-run account (`soxl_ira`), shared across many live tickers. **User's 2026-08-09 directional call**: real margin `brokerage` account will likely stay capped at 1 ticker (the original strict containment framing, still right for a genuine margin account). For IRA-type accounts, more than 1 ticker sharing an account is acceptable but needs real wariness about shared cash (the RETL cash-aware-BUY-guard fix and add-on's shared-margin-pool "2027 problem" are exactly this risk shape). **Node-level handling/self-policing of shared capital use explicitly deferred** — the initial real rollout is scoped to 3 tickers at moderate size, which bounds the shared-cash risk naturally without needing the sophisticated safeguard built now. Revisit once the ticker count or sizing grows past that initial scope. Note: `soxl_ira` already runs 11 REAL live nodes on one account today ($500-$2,500 each, the deliberate small proving-ground tier per CLAUDE.md) — a live precedent already past the "3 tickers, moderate" scope this deferral assumes. Not flagged as a contradiction to resolve now (small dollar amounts, deliberate proving-ground design, different intent than the new moderate-size rollout) but worth being aware the deferral isn't literally "nothing shares cash today."

## [live-trading][tax] Deprioritized 2026-07-18, 2026-07-17 — wash-sale/tax analysis needed before promoting any ticker into the taxable brokerage account
Full detail moved to `docs/deep_backlog.md` ("Deprioritized 2026-07-18" entry). No ticker promoted to taxable brokerage yet — precondition still unmet.

## [live-trading][backtest][portfolio] High priority, revisit ~2026-08-22 — dividend cash isn't credited into P&L/SL/arm tracking; material for DPST specifically
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title). User's call 2026-08-08: pick this up in ~2 weeks, likely as a portfolio-level piece rather than a standalone fix.


## [backtest] High priority, 2026-07-16 — need a proper delayed-entry simulation for the same-day-re-buy constraint, not just a trade-list filter
`docs/deep_backlog.md:4601-4602` confirms still unbuilt — only the admittedly-wrong post-hoc filter method exists. `sim_delayed_sell.py` (2026-07-17) covers the exit-side mirror only, not this entry-side ask.

## [execution] Research question, 2026-07-15, deferred 2026-08-09 (revisit ~2027) — when would TWAP/VWAP order execution become worth considering
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [backtest][new-strategy] Idea, not scoped, 2026-07-18 — daily-bar strategy variant, for much longer backtest history / real bear-market regime coverage
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading] Idea, not built, 2026-08-01 — real correlation-verification logic for the new JNUG/JDST "G" canary pair
Today's fix (`docs/deep_backlog.md`'s 2026-08-01 entry) only restored their real config/labels/scenario_expectations row (`canary_bull_bear_pair`) — monitored the same simple same-day-trade-happened way as every other canary, no real correlation check built yet.

## [backtest] Idea, not scoped, re-opened 2026-08-03 — rolling/time-varying FFT spectral power as a regime signal (alongside the 2026-08-01 SPY-trend/VIX finding) (revisit ~2026-09-10)
Split off from the now-closed "FFT cycle detection to inform window selection" item — that half was tested and refuted (`docs/research_log.md`'s 2026-08-03 entry: no significant fixed dominant cycle in any v5 ticker's returns). This half — whether *time-varying* spectral structure (not a single fixed cycle) says anything about regime — was never tested and is still open.

## [data] Idea, not scoped, 2026-08-01 — start recording our own 1-minute bars now, so a future "we need historical data" request never hits an expired retention window again
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading][tax] Idea, raised 2026-08-04 evening — live wash-sale risk monitor (detection/alerting only, not prevention)
Distinct from the backtest-only simulator below (`sim_wash_sale_impact.py`, retrospective/aggregate) — this would run against real, current positions and flag which ones are at wash-sale risk right now (e.g. a same/substantially-identical security bought in one real account within 30 days of a loss-sale in another, the cross-account gridlock mechanism already found for SOXL). Explicitly not meant to block or auto-cancel a trade — just surface the risk so the user can decide (same "detection-only, decide policy later" pattern as `_log_pre_action_state_verification`/`record_node_streak`, 2026-07-29). **Two specific tickers named as priority scope, 2026-08-04**: SPY (will be bought/sold across multiple accounts via the skim-and-reserve overlay's skim/redeploy mechanism above — real multi-account wash-sale exposure by design, not a hypothetical) and TQQQ (already held elsewhere per the standing watchlist note; a real IRA node may get added if liquidity ever becomes a problem for the existing brokerage-held position — see the 2026-08-04 TQQQ-cleared-for-IRA item above, same conditional-risk shape). Not scoped: where it hooks in (real-time during `check_order`? a daily batch report?), what "at risk" means precisely per instrument type (standard 1099 vs AGQ's Section-1256 wash-sale-exempt status needs to be excluded from triggering false positives), and whether it needs real cost-basis/lot data from Schwab or can work off local `trade_log`/`open_positions`.

## [portfolio][tax] Idea, raised 2026-08-03 evening — real wash-sale/cost-basis-tracking + tax simulator, prototype only
`scripts/sim_wash_sale_impact.py` (real AGQ/SOXL trades) found near-total wash-sale gridlock at this trade cadence in a taxable account — SOXL's result stands (owed tax despite a net loss, standard '40-Act fund, normal wash-sale rules apply). **AGQ's result is wrong and superseded, 2026-08-04**: AGQ is a Section 1256 commodity-pool/K-1 instrument, which is exempt from the wash-sale rule entirely (overrides normal broker 1099-B wash-sale treatment even at the share level) and gets 60/40 blended-rate tax treatment in a taxable account instead of the flat ordinary rate the script assumed — the reverse of what was concluded. See the new AGQ K-1/UBTI entry below for the fuller correction (AGQ may now be *better* suited to brokerage than IRA, opposite of the original recommendation). Full rebuild (Section 1256 60/40 + no-wash-sale modeling) not done — this prototype's AGQ numbers should not be used for decisions.

## [portfolio][tax] Found 2026-08-04 — AGQ is a K-1/UBTI risk, same category as the now-blocked USO; account-placement recommendation reversed
Confirmed via research (ProShares' own K-1 documentation): AGQ (ProShares Ultra Silver) is a Section 1256 commodity pool taxed as a partnership, not a standard '40-Act fund like SOXL. Real IRA/Roth risk: if K-1-allocated gross UBTI crosses $1,000/year (plausible given real trading activity — 46 trades, real gains in the 2026 backtest), the IRA itself must file Form 990-T and pay UBIT at compressed trust tax rates (37% bracket starts ~$14,451), debited directly from IRA assets by the custodian (not a taxable distribution, but real value erosion, happening automatically). This directly reverses tonight's earlier IRA-placement recommendation for AGQ — brokerage (Section 1256's 60/40 blended rate, no wash-sale issue) may now be the *better* fit for AGQ specifically, opposite of SOXL, which should stay in IRA/Roth per its own (still-valid) wash-sale-gridlock finding. **Not yet decided or acted on** — same-night finding, surfaced too late in the session to re-derive the full account-placement plan. `docs/operational_limits.md:86` had already flagged this as an open "verify before funding" item as far back as project history goes; it was never actually checked until tonight.

## [live-trading][tax] Idea, raised 2026-08-04 — TQQQ cleared for IRA trading (no K-1, wash-sale risk conditional on brokerage leg staying unsold-at-a-loss); not yet acted on
Full detail: `docs/research_log.md`'s 2026-08-04 entry. Best v5 node identified (`TrailingBothZScoreBreakout`, fixed_sl=2, open_check, 120 trades, 20.8% win rate, +242.8% robust alpha) but no IRA `watch_list` node added yet — TQQQ's existing nodes are all stale pre-v4 versions or research-mode.

## [portfolio][design] Decided-in-principle 2026-08-04 — skim-and-reserve "insurance policy" overlay: design finalized (SPY reserve, manual redeploy), not yet implemented
Full detail: `docs/deep_backlog.md`'s 2026-08-04 entry (top). Includes the untested multi-bounce/redeploy-fakeout risk and the user's "skim regardless" call. **Open question added 2026-08-09**: a separate conversation raised calendar-based quarterly rebalancing as a possibly-simpler/safer alternative (no new trigger logic to build). Portfolio-level stress test (`docs/research_log.md`'s 2026-08-09 entry) found skim's event-triggered design is reserve-inactive in a crash-from-a-standing-start scenario (no prior gains to skim), while quarterly rebalance acts unconditionally on a schedule — a real mechanism-level argument for "feels safer," though the accompanying return-comparison numbers are window-specific synthetic-sim results, not a generic verdict. **Not decided**: whether to build quarterly-rebalance instead of / alongside skim-and-reserve — user's call, still open.

Full detail: `docs/research_log.md`'s four 2026-08-09 entries (later/later still/later still 2/3). Summary: a 30-year daily-bar sim of the 3-account structure (`scripts/sim_1m_four_bucket_portfolio.py`) found it beats plain 100% SSO in only 29% of cohort-horizon pairs, and neither T-bill cash yield nor doubling algo weight to 50/50 fixed it (`scripts/sim_portfolio_variant_compare.py`) — but this was CORE-STRATEGY-ONLY on a coarse once-a-day signal check. Replaying the REAL hourly kernel through the only real drawdown in the cached window (SPY -18.8%, 2025-02-19/06-26) and a real-ticker (non-synthetic) daily-bar test through 2020 COVID/2022 bear (`scripts/sim_real_ticker_daily_crash_stress.py`) showed AGQ's real algo strategy beating both SPY and SSO in every window checked (incl. SSO itself underperforming SPY in 2 of 3 due to real volatility decay). The -64.1% KORU number from that same 2022-bear daily-bar test is **NOT real downside risk evidence — user correction (told 3x now, see `feedback_daily_bar_understates_real_risk` memory)**: it's a daily-bar-approximation artifact, since the algo performs materially worse under the coarse once-a-day signal check than under its real hourly execution, for every ticker, not specifically KORU. Don't treat it as a real hourly-kernel finding. **Not decided**: whether/how to act on the 3-account structure, whether to build the tax-adjusted version (brokerage/AGQ account only — Roth/soxl_ira don't need it, per k1_tax.py's engine). User's call, still open — this entry exists so the thread isn't lost, not to force a decision.

## [backtest] Research thread resolved-with-caveats 2026-08-04 late — SOXL-weak/AGQ-strong 2026 YTD observation confirmed real after fixing 2 real bugs in tonight's research scripts; underlying cause still unexplained
User's original observation (AGQ strategy +68% YTD 2026 despite price -58%, SOXL strategy weak despite price +197%) was initially contradicted by tonight's 4 research passes (velocity, chop-cluster, regime regression, config cross-apply) — traced to two real bugs shared across all 5 scripts written tonight: (1) `TrailingExitZScoreBreakout` nodes (AGQ/DPST/KORU/NUGT/UDOW/YANG) store their arm-sell/take-profit value in the `take_profit` column, not `arm_sell_pct` — scripts only read the latter and silently disabled TP for these 6 tickers; (2) none of the 5 scripts passed `open_check=True` even though every real v5 node uses `entry_timing='open_check'`. A cold Opus review (blind read) independently confirmed bug 1's fix and independently found bug 2, plus 3 smaller issues (a still-open mark-to-market trade counted as completed in 3 scripts; a stale docstring; dead code) — all fixed. Corrected result: AGQ 2026 YTD +52.3%, SOXL 2026 YTD -30.8% — confirms the original observation. But the corrected pooled cross-ticker/cross-time analysis (105 periods, 10 tickers) shows strategy return generally moves *with* price trend, not against it (rho=+0.539, p<0.0001, survives ticker fixed effects) — so 2026 is a genuine **outlier year for both tickers relative to their own historical pattern**, not an example of a general rule. Cause of the 2026 anomaly itself is still open. Two committed scripts had the same bug pattern — both now resolved: `scripts/multiday_cycle_entry_test.py`'s buggy path turned out to never have been exercised (its published finding was independent of node config, confirmed unaffected on inspection); `scripts/entry_timing_seasonality.py` was fixed and its 2026-08-03 finding rerun — confirmed, if anything more solidly null (win rates now nearly identical between windows, p-values higher than before). All 5 of tonight's scripts also rerun post-fix; conclusions unchanged except the chop-cluster scan's "closest to significant" ticker flipped from SOXL to KORU (neither survives multiple-comparisons scrutiny). Full detail: `docs/research_log.md`'s 2026-08-04 (late) entries.

## [portfolio] Idea, raised 2026-08-04 evening — options-based alternatives to the reserve/insurance idea, not modeled
Four candidate uses, confirmed real account capability (Schwab, researched 2026-08-04): (1) protective puts on SPY/QQQ as literal crash insurance instead of the static skim reserve — works in any IRA, even without limited margin (Level 1). (2) covered calls on the idle reserve for extra yield — also Level 1. (3) vertical spreads for cheaper-structured insurance — needs limited margin on the IRA (already a separate open item). (4) SPX (broad-based index) options get the same Section 1256 60/40 + no-wash-sale tax treatment just confirmed for AGQ — a natural fit for brokerage specifically. None modeled yet — muni-bond-as-reserve test was also started same session (`cache/research/MUB_1h.csv` fetched) and abandoned mid-calculation (NaN bug, unfixed) when this tangent came up.
**Extended 2026-08-06**: user raised a related but distinct variant — hedging the CORE v5 strategy's own positions with puts directly (not just the reserve/insurance capital), as an alternative to skimming altogether. Scope not decided (per-position, e.g. a put on AGQ itself — directly checkable now via `scripts/collect_options_snapshot.py`'s real chain data, but hits the same liquidity/expiration gaps found for the (now-skipped, see `deep_backlog.md`) drought-overlay put-hedge idea (GDXU no options, HIBL/UDOW/USD/YANG no near-term expirations); vs. portfolio-level broad-index SPY/QQQ, the original framing above — cheaper/liquid but only covers broad-market risk, not a ticker-specific move). Not modeled yet, captured for later.

## [live-trading][risk] Idea, raised 2026-08-06 (very late) — emergency procedures runbook: what to actually do during a real flash-crash/gap event (revisit ~2026-08-22)
Real gap found via tonight's put-hedge deep dive (`docs/research_log.md`'s 2026-08-06 entries): (1) no real-time crash-detection trigger exists anywhere in this system — every "crash window" tested tonight (5 known SPY drawdowns, 4 named historical crashes) used retrospectively-known dates; live, there's no signal for "start protecting now" (VIX-level, regime/trend filter, and the strategy's own drawdown were named as untested candidates; the intraday-vol-spike measure is already disqualified, tested and rejected earlier this session for the same kind of trigger). (2) No documented human runbook for what to actually do if a real gap/flash-crash is observed live — distinct from the existing kill-switch (blocks BUY/SELL automation, a rogue-algo off-switch, not a "the market is crashing, do X" procedure). (3) Synthetic bear-market stress tests (`scripts/sim_bear_market_stress.py`) showed the underlying can synthetically implode -80% to -99.6% at real historical crash bottoms (2008 GFC, 2000 dotcom) — the strategy's own tight SL discipline already avoids riding a position through the whole collapse (worst single KORU trade in 2008 GFC was only -6.1%, not -99.6%), but real fund closure/reverse-split risk (a real historical occurrence for some leveraged ETFs) is completely unmodeled and might not be cleanly protected by a put option either (settlement/operational risk during a delisting event). Not scoped: what the runbook should actually say (halt trading? check for fund-level news? manual liquidation criteria?), whether it's manual-only or ties into any automated check.


## [backtest][data] Found 2026-08-13 (session-wrap paired review, corroborating detail) — `data_manager.py`'s incremental split-guard hardcodes `consistent=True` when only one bar overlaps
`fetch_live_data_smart`'s Step 3 overlap-guard (`data_manager.py` ~line 155): `consistent = len(overlap) == 1 or (ratios.std() / ratios.mean()) < 0.05` — with exactly one overlapping bar there's no way to compute a std-dev-based consistency check, so it's hardcoded true unconditionally. Means a single corrupted/artifact bar landing in a 1-bar overlap window could itself drive a spurious whole-history rescale of the entire cached series, if its price ratio also happens to look like a clean split factor. Surfaced as a corroborating point during the split-artifact-fix paired review, not independently investigated or fixed this session — real, but a pre-existing bug in code this session didn't touch, needs its own dedicated look before changing (this is the trusted whole-history rescale path, not something to patch mid-review of unrelated work).

## [testing] Latent, noted 2026-08-15 (relayed via peer session "planner") — dormant cross-file synthetic-CSV collision risk (`tests/test_overlay_paper_trading.py`), confirmed via a real `EmptyDataError` on one full-suite run
Not currently active (no two test files share a synthetic ticker today). Full detail: `docs/deep_backlog.md`.
