# Backlog Cache

Curated, current subset of `docs/deep_backlog.md` — read in full at session start (`go`). Full detail for every item lives in `deep_backlog.md`; this is just the active/relevant pointer list. Periodically re-triage.

## Resolved, 2026-07-11 — phase emoji redesigned from single-ball to 4-bubble strip, accepted
User's reaction to the 2026-07-10 single-ball prototype: unreadable ("I didn't understand it"). Redesigned into a 4-bubble strip (Signal / Filled / Armed / Sold, grey→yellow→green) — key fix was giving Filled and Armed separate bubbles, since a position can be held without being armed. Dropped the adjacent standalone `_proximity_emoji` ball too (blurred together with the phase bubbles, and not actionable pre-bar-close anyway). Verified via rewritten `scripts/test_phase_emoji.py` and an interactive step-by-step Slack walkthrough. See `docs/design.md` for the full bubble-state mapping.

## Open, 2026-07-11 — reminder numbering still feels off, revisit next session
User flagged during the live walkthrough that the reminder `#N` numbering wasn't landing right, even after the per-phase counter-reset fix (buy placement vs buy fill now count separately). Didn't get to the bottom of exactly what still reads wrong — could be the arm/exit reminders' numbering relative to the buy-side's, or the mismatch against the `1/1r/2/2r/3/3r/4/4r` lifecycle vocabulary in `docs/design.md`. Needs fresh eyes next session, ideally walking through more live Slack examples together.

## High priority, 2026-07-10 — rerun trailing-buy backtest kernels with corrected (non-optimistic) intrabar fill logic
`_simulate_trail_buy`/`_simulate_trail_both` (backs all 11 live watchlist tickers) always assumes a bar's Low happens before its High during the trailing-buy "waiting" phase — best case for the strategy, unproven from OHLC data. Built a side analysis (`scripts/export_trades.py::simulate_trail_both_ohlc_aware`, live kernel untouched) that resolves fills as CERTAIN wherever provable (High clears prior-bar trigger, or Close — always bar-last — confirms it) and only guesses (Open/Close-direction heuristic) for genuinely ambiguous wicks — just 1/61 entries on SOXL. Result: SOXL 57-trade final equity dropped from $3.55M (7007%) to $1.85M (3591%) under the corrected logic — every live ticker's on-file alpha/return is inflated by an unquantified amount until the real kernels are patched and the full sweep is rerun. Full detail: `docs/deep_backlog.md` "High Priority" section. Not started.

## Medium priority, 2026-07-10 — same-bar arm/take-profit trigger not checked at entry
`simulate_trail_both_annotated`/`_simulate_trail_both` skip arm/TP/SL checks on the entry bar itself (the fill sets `in_trade=True` then `continue`s, so trailing-arm logic starts evaluating the *next* bar). Checked across the 6 live tickers whether the entry bar's own High already cleared the arm threshold: SOXL 0/57, EDC 0/32, KORU 0/30, AGQ 2/36, LABU 1/38, but **HIBL 20/54 (37%)** — meaning over a third of HIBL trades could arm the trailing-sell an hour earlier than the backtest credits, understating how early/high the real trailing peak would start from. Direction of the return bias (helps or hurts) not yet determined. Same-bar entry+stop-loss is not a concern — checked separately, 0/all trades across every ticker (SL is far enough below the bounce-entry trigger that one hourly bar can't span both). Explicit user call: leave the kernel as-is for now since live trading's arm logic has the same delayed-until-next-bar behavior — fixing the backtest without fixing live would create a live/backtest divergence. Tried quantifying the direction/magnitude via a same-bar-arm side sim, but the user correctly flagged it hits the identical intrabar-order problem as the entry-fill-timing item above (unprovable from hourly OHLC whether the arm-clearing High happened before or after the fill within that bar) — so any equity delta from that sim is speculative, not a real estimate. Would need the same higher-fidelity-data resolution as the fill-timing item to actually answer "does this help or hurt." Not started.

## Resolved, 2026-07-11 — 4r exit-pending reminder built; daemon restart still needed
`check_exit_reminders()` built and wired into `run_loop` (mirrors `check_trailing_reminders`'s supersede pattern, 15-min flat cadence — no plausibility gating needed since `exit_pending` is only ever set after a real confirmed price trigger). Also fixed the `notify_sell_signal` hardcoded `exit_reason='MANUAL'` bug (console fallback now passes the real reason) while touching this code. Verified end-to-end against an isolated sim DB, including a real crash found and fixed along the way (`_trailing_buy_status()` returning `(False, None)` instead of `(None, None)` when no cached bars existed — see `docs/design.md`).
**Still outstanding**: daemon is currently stopped (user turned it off for the weekend, was last running since 2026-07-11 05:03 — predates all of 2026-07-09 through 2026-07-11's changes). Needs a fresh `python active_signals.py run` before Monday's signal windows. Verify post-restart: reference report Phase column shows 4 bubbles not the old single ball; a real arm event's reminder numbering resets on order-placed; a real BUY alert's fill reminder doesn't nag until plausible.
Mobile-prose `_ticker_block` real-phone pass (P&L label dropped, `trigger`→`trig`, `brokerage`→`bro`, z-trigger/last-sale-notional) is still not a dedicated final pass — carried again.

## Backlogged, 2026-07-09 — fill-price/drift accuracy
User flagged: fills often don't land exactly at the expected trigger, and the current typed-price-entry flow (Executed button → manual price entry) doesn't do anything with that drift beyond logging it. Explicitly deferred to next session with fresh context, separate from this session's reporting/reminder rework — scope not yet defined (better fill capture? drift-vs-expected alerting? something else).

## Open question, 2026-07-09 — is the Schwab catastrophic-stop +1% buffer the right size?
The initial broker stop-loss order is placed at `(stop_loss + 1)%` below trigger (flat +1% buffer, hardcoded, `active_signals.py` `schwab_sl_pct = node['stop_loss'] + 1`) so ordinary intraday noise doesn't trip it before the real Slack SELL signal fires. User flagged 2026-07-09 that a flat +1% (e.g. 15%→16%) doesn't feel empirically grounded — if the goal is genuinely avoiding noise-driven stop-outs, the buffer should be backtested/varied (like the fixed_sl 15% vs 30% sensitivity sim run earlier that session) rather than assumed. Separate from that day's other fixes — explicitly deferred as its own backlog item.

## Open question, 2026-07-09 — trailing-buy re-entry timing after a same-day exit
If a same-day re-entry trigger hits (ticker sold, then dislocates again same day), does the live trailing-buy order actually need to be placed relative to the **9:30 open** or the **10:30 normal bar time**? Not tested yet — flagged by the user mid-session, explicitly deferred ("test that next time"). Relevant given today's same-day-buy-warning work; worth checking against `active_signals.py`'s actual signal-window/bar-labeling logic (`docs` note: hourly bars are labeled by start time) before assuming either answer.

## In progress
- **`trail_pct`→`trail_sell_pct` + `take_profit`→`arm_sell_pct` rename**: DB-side + `active_signals.py` + `run_optimization_sweep.py` done. `axis_tp` migration on `backtest_cache` (86.2M rows) confirmed complete and verified 2026-07-07 (row-for-row match vs. pre-migration backup, fresh AGQ backfill test passed once data-drift was accounted for). 4 stale duplicate tables (`open_positions`/`trade_log`/`watch_list`/`watchlists` in `trading_universe.db`, orphaned by the live/research DB split) dropped 2026-07-07, backed up first to `cache/stale_tables_backup_20260707.sql`.
  **Streamlit/script propagation — partial, still broken in places:**
  - `pages/0_Top_Pivot.py`: fixed — `load_best_nodes()`, cliff-safety query, watchlist-pivot join (was doing `b.take_profit = w.take_profit`, always `NULL=NULL` for 6 of 8 live `TrailingBoth` tickers — silently broke the "Watchlist — Alpha by Strategy" section).
  - `db_cache.py` (off-scope, not in the original file list, but shares the identical bug and runs nightly via cron): `CLIFF_GRID_SQL` and `refresh_best_nodes_cache()` both fixed (`take_profit`→`axis_tp`). **`refresh_best_nodes_cache()`'s fix is unverified** — was mid-verification-run when the session moved on; confirm it completes clean before trusting `cache/db_cache_daily.log` next run.
  - **Not yet started**: `pages/2_Node_Inspector.py`, `pages/3_Winners.py`, `pages/4_Portfolio.py`, `pages/10_Open_Positions.py`, `scripts/export_cliff_safety.py`, `scripts/verify_live_parity.py`, `scripts/fill_trail_pct_gaps.py`. Same pattern to apply: any `SELECT`/`WHERE`/`JOIN` against `backtest_cache.take_profit` → `axis_tp` (NULL for `TrailingBothZScoreBreakout` rows since the 2026-07-05 split), any `backtest_cache.trail_pct` → `trail_sell_pct` (column renamed, doesn't exist under the old name — hard `OperationalError`, not silent). **Exception**: `cache/watchlist_sweep.db` (used by "Watchlist Trade Pivot" section of `Top_Pivot.py`) is a separate, never-migrated snapshot DB — its `trail_pct`/`take_profit` columns are still correct as named; don't touch those.

## Morning reference table (2026-07-09) — built, live-report wiring unverified
- Built `active_signals.py::build_reference_table()`/`format_reference_table()`: one row per live-mode ticker — Ticker, Hold (`Nh/Mh` or blank if flat), Next Trigger $, Now, Proximity % (signed so negative always = trigger already crossed, in whichever direction fires the action), Next Action, Version, Alpha, Z, Z Trigger (`z_score_threshold`), TrailBuy%, Arm%, TrailSell%, Account. Column order and both trigger columns were adjusted mid-session per user feedback (Proximity now before Next Action; Z Trigger added next to Z).
- **Next Action wording, finalized 2026-07-09**: flat tickers show `Waiting Trigger Event` (not `Buy Trail X%` — that action only actually fires for one instant at bar-close in the signal window; showing it continuously implied an order should already be placed). Held-and-armed tickers (`trail_state.trailing=True`) show `Waiting Sell X% Fill` (order already placed at broker, waiting on the trail to fire) instead of `Sell Trail X%`. Held-not-yet-armed tickers still show `Arm X%`.
- **Primary/Secondary action split — discussed, not built.** Arm/Buy-trigger/Sell-fill is the "primary" lifecycle action; SL protection (Schwab stop, catastrophic insurance) and Max Hold (time-based forced exit) are "secondary" backstops that can fire independently of the primary state. User confirmed this framing but said skip building Secondary column(s) for now.
- **Alpha is a snapshot, not live-joined** (`watch_list.alpha`, new column) — `backtest_cache` lives in a separate DB file (`trading_universe.db`) from the live DB, and the user explicitly chose snapshot-for-now over a live cross-DB join. Rerun `scripts/backfill_watch_list_alpha.py [watchlist_id]` after any `add_node`/backfill change to a node's swept params, or alpha goes stale silently. Ran once 2026-07-09 for watchlist 9 — LEFT JOIN on ticker/strategy/version/window/`axis_tp`=COALESCE(take_profit,arm_sell_pct)/stop_loss/max_hold_hours/z_score_threshold matched 330 `backtest_cache` rows for 11 nodes (many duplicate-key reruns per node — harmless since they resolve to the same value, but means the exact matched row is nondeterministic if a duplicate ever has drifted alpha; not investigated further).
- Wired into `send_startup_report()` as a leading Slack code-block table — **not yet live-verified**, daemon restart needed to pick it up (was running throughout this session's changes; deferred per usual no-restart-mid-session precedent, verify next natural restart).
- CLI available now, works standalone: `python scripts/reference_table.py [watchlist_id]`.
- **On-demand via Slack — blocked, not started.** Needs a registered Slack slash command (e.g. `/status`) in the Slack app dashboard — user has never done this, said they'll register it 2026-07-09 night. Once registered, wire a `@bolt_app.command(...)` handler calling the same `build_reference_table`/`format_reference_table` and posting the result back.
- **Real DB gap found and fixed 2026-07-09**: AGQ was a real open position (per the user) with no row in `open_positions` at all — reference table showed it as flat, which was wrong. Logged via `open_position()` directly (600 sh @ $74.80, backdated signal/entry time to 2026-07-06 10:30 ET, matching the KORU/SOXL backdating precedent) — node id 47, v3.26, brokerage account.
- Also found (not yet fixed): `watch_list.account` had no `ensure_tables()` migration line — column existed in the live DB (added out-of-band in a prior session) but a fresh DB wouldn't get it. Added the missing migration line for `account` alongside the new `alpha` one while touching this code; the underlying gap (undocumented prior ALTER) wasn't otherwise investigated.
- **Second, separate instance of the "BUY alert while holding" bug found and fixed 2026-07-09**: `send_startup_report()`'s buy-candidate loop never filtered against `get_open_positions()` at all (a different code path from the one fixed 2026-07-08) — this morning's report told the user to buy HIBL/SOXL/KORU/EDC, all already-held positions. Root cause: no single shared helper for "is this ticker held," so the earlier fix only covered the intraday buy-check loop, not the morning report. Added `get_held_tickers()` as the shared helper and pointed `send_startup_report()` at it; the intraday loop's `(ticker, window)`-keyed set was left as-is (genuinely needs finer granularity to match specific nodes). **Not yet live-verified** — same daemon-restart caveat as above.

## Live trading behaviors — carried from 2026-07-07 night, partially resolved
- **Active watchlist is `watchlist_id=9`** ("Sweep v3 - Full", `is_active=1`), **not 7** — 7 was superseded 2026-07-07 06:26 and a prior session mistakenly wrote the new `account` column onto 7. Corrected 2026-07-08: `account` copied onto 9's rows, `CLAUDE.md` updated to point at 9. Always check `watchlists.is_active` before assuming which id is live, don't trust docs blindly.
- **Account tracking schema — resolved**: column-on-`watch_list` (not a separate table), now actually populated on watchlist 9: AGQ/GDXU/TQQQ=brokerage, EDC=sep, DPST/HIBL/KORU/NUGT/SOXL/YANG=ira, LABU=unmapped (no account assigned yet — ask user).
- **LABU — resolved, was stale**: backlog previously said "not yet backtested." False as of 2026-07-08 — `backtest_cache` has 108k rows for LABU v3.37 (`TrailingBothZScoreBreakout`), watchlist 9's LABU node draws real swept params from it. It's `live` on watchlist 9 today.
- **Live watchlist 9 mode split (2026-07-08)**: live = AGQ, EDC, HIBL, KORU, LABU, SOXL (6). research = DPST, GDXU, NUGT, TQQQ, YANG (5) — user explicitly requested this split via `scripts/set_watchlist_mode.py`.
- **IRA settlement-delay check — still not started.** Backtest's compounding math (`(df_tr['Return']+1).prod()`) assumes capital is instantly reusable for the next trade — doesn't model T+1/T+2 IRA cash settlement. Still unresolved, still blocks validating IRA position sizing.
- **P&L tracking for compounding position sizing** — still needs real capital/P&L tracking. First step taken 2026-07-08: `shares` column added to `open_positions`/`trade_log` (was completely missing — notional/P&L couldn't be computed at all before this). Backfilled for EDC (400 sh) and SOXL (300 sh) only; KORU and HIBL still NULL, need real fill share counts from the user.
- **win_twin_rate recalc / watchlist size** — still not started.
- **Slack messaging redesign** — still not started. Related new finding below (BUY-alert-while-holding bug) makes this more urgent, not less.

## Live/backtest parity gap — real, bigger than previously scoped (found 2026-07-08)
- **`TrailingBothZScoreBreakout` (100% of watchlist 9's live tickers) is deliberately excluded from `scripts/verify_live_parity.py`'s comparison** — its own docstring says the trailing-buy "wait for bounce" entry state machine has no live implementation (tracked since 2026-07-03 as "P0 #3", never closed). Live only detects "z-score crossed trigger" and hands off bounce-timing to a **broker-side trailing-buy order** — nobody has verified the broker's real trailing-buy behavior actually resembles what the backtest kernel assumed. This is the single biggest unverified assumption behind every currently-live trade.
- **Confirmed separately**: the backtest kernel itself (`_simulate_trail_both`, `backtester.py:562-600`) correctly blocks re-entry while `in_trade` is True — the kernel was never the source of the bug below. Backtested win rates/alpha are not affected by anything found this session.

## Real live-only bug found and fixed 2026-07-08: BUY alerts fired while already holding
- The buy-check loop in `active_signals.py` (`run_loop`, ~line 1635) never cross-referenced `get_open_positions()` before calling `notify_buy_signal` — existed since the loop was first written 2026-06-30 (`3ab1b8a9`), never exercised until today's broad selloff pushed already-held tickers back below trigger. Result: KORU, HIBL, and SOXL (all already open positions) each got a spurious "BUY" Slack alert off the 2026-07-08 14:30 bar.
- **Fix applied** (uncommitted → committed this session): buy loop now builds `open_position_keys` from `get_open_positions()` once per iteration and skips `notify_buy_signal` (prints `[skip]` instead) for any ticker+window already held. **Not yet live-tested** — daemon needs a restart to pick it up; verify the `[skip]` line actually appears next time a held ticker re-crosses trigger.

## WSL/Windows sleep incident, 2026-07-08 — root-caused, partially mitigated
- `active_signals.py` froze 07:54:22–13:08:17 (5h14m), missing the entire 10:25 AM signal window — confirmed via Windows event log: Modern Standby entered 07:54:41 ("AC/DC Display Burst Suppressed" — idle timeout on battery), exited 13:08:05 ("Lid" — someone opened it). Not a code bug; the whole WSL VM was suspended.
- **EDC's trigger was confirmed active during the freeze** (z=-2.36 at the 10:30 bar, well past the -1.5 threshold, via `scripts/watchlist_status.py history EDC 7` replaying the real `compute_buy_signal()` per bar) — user's manual EDC entry at ~14:55 was a valid, if very late, catch of a real signal the daemon never got to alert on. EDC and SOXL (also manually entered, $169.73×300) are now logged in `open_positions` (they weren't before — a second consequence of the freeze).
- **Mitigation done**: `active_signals.py` now writes a heartbeat timestamp (`cache/active_signals_heartbeat.txt`) every loop iteration; `scripts/check_heartbeat.py` posts a Slack alert if it goes stale (independent of the daemon's own possibly-frozen socket). **Incomplete**: the piece that makes this actually useful — a Windows Task Scheduler job (host-level, survives WSL suspension, fires on resume/unlock) invoking the checker — was never built. Without it, nothing currently calls `check_heartbeat.py` at all.
- **Windows power settings were changed without asking first** (`standby-timeout-dc`/`hibernate-timeout-dc` set to 0, disabling sleep/hibernate on battery) — this was a mistake (see `[[feedback_confirm_before_system_changes]]` in assistant memory), not reverted per explicit user instruction ("don't touch my os" — meaning leave it as-is, don't touch it further either direction). **Do not make any further OS/system-level changes without explicit approval first.** Lid-close-action setting query was in progress, inconclusive, abandoned mid-investigation — still unknown.
- **User's actual next ask**: a start-of-day report with enough info to know both entry AND exit triggers per ticker in advance, specifically so a missed 10:25/15:25 alert (freeze or otherwise) doesn't mean flying blind — not built yet. `scripts/watchlist_status.py`'s new `history` subcommand (calls real `compute_buy_signal()` per historical bar, no reimplementation) is a related building block but doesn't cover this on its own.

## Live-trading reliability gaps (real, not yet built)
- **Trailing-buy fill confirmation — built 2026-07-10** (idea originated 2026-07-07 from a TQQQ 700-share pending order). `pending_buys`' three-state flow (signal → order placed → filled) plus `_trailing_buy_status()` (approximates whether the bounce trigger's been met, from cached hourly bars) now covers this — see `docs/design.md`. Not yet daemon-verified (restart needed, see top-priority item above).
- **SMA/Std recalculated from scratch every poll** — `compute_buy_signal`'s `generate_daily_indicators()` recomputes the full rolling mean/std over the entire daily-close history on every single 5-min poll, per node, even though the value only depends on prior days and can't change intraday. The backtest kernel already does this correctly (computes `sma_arr`/`std_arr` once, looks up per bar via `daily_idx`) — live should cache per-day instead of recomputing. Proposed, not implemented.
- **Default rule**: every action-requiring state change in `active_signals.py` must have a Slack notification — current coverage (buy/sell/trailing-armed/limit-fill) is complete, but audit this against any new strategy/state added going forward.
- **"What's close" script** — partially addressed by `scripts/watchlist_status.py` (current distance-to-trigger table) and its new `history` subcommand (retroactive per-bar check), but not the "start of day, both triggers, in advance" version the user actually wants (see WSL incident section above).

## Test coverage / historical notes
- **Automated round-trip DB test** for `active_signals.py` (`add_node`→`open_position`→`check_sell_condition`) — no test coverage exists for this path currently.
- `win_twin_rate` reads `0` for any `backtest_cache` row computed before that column existed (pre-commit `252b3bf`) — not a bug, old rows never recomputed retroactively. Affects SOXL's v3.18 rows specifically.

## Backup/storage policy (2026-07-07, current)
- `trading_live.db`: hourly cron backup, keep 30 days (`cache/live_backups/`).
- `trading_universe.db` (research, regenerable): daily + weekly rotating single-file backups (`trading_universe_daily.db.bak` / `_weekly.db.bak`), 2 copies total, no accumulation.

## Deferred, lower priority
- Slack slash-command interaction (`/positions`, `/watchlist`, `/status`) — needs a design pass, `SOCKET_MODE` already wired.
- Split `active_signals.py` into modules (1680+ lines, one file) — deferred, live-trading takes priority.
