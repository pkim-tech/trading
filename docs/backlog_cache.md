# Backlog Cache

> **Open items only.** Resolved items live permanently in `docs/deep_backlog.md`; a rolling
> window of recently-resolved one-liners (for session-handoff context) lives in
> `docs/backlog_resolved_recent.md`, pruned to entries from roughly the last week — anything
> older is fully covered by `deep_backlog.md` and doesn't need re-reading here. `go` reads this
> file plus `backlog_resolved_recent.md` in full each session; keep both lean.
>
> **Maintenance**: when an item here resolves — (1) add/update the full-detail entry in
> `deep_backlog.md` (unchanged convention), (2) prepend a one-liner to
> `backlog_resolved_recent.md`, (3) drop entries from `backlog_resolved_recent.md` older than
> ~7 days (already permanent in `deep_backlog.md`), (4) remove the item from this file. When a
> genuinely new backlog item is raised, add a 1-2 line entry here directly (no full-detail
> writeup needed unless/until it resolves). **"Downgraded"/"deprioritized" is not a resting
> state** (found 2026-08-16, after two items sat as "downgraded" for a full session, resurfacing
> on every `go` read despite being fully decided) — once a real decision is reached (accept the
> residual risk, skip, defer with an explicit revisit trigger), close the item out via the same
> 4 steps above like any other resolution, don't leave it lingering here just because the
> decision was "don't fix" rather than "fixed."
>
> **Prioritization**: `docs/roadmap.md` holds the phased "where are we headed and in what
> order" vision (universe sweep for overlay-friendly nodes → portfolio construction → modest
> live capital → trend/edge-decay detection → scale up). Triage open items below against it —
> an item serving the current phase generally outranks one serving a later phase.
>
> **Deferral**: an item that's real but genuinely not worth touching yet gets a
> `(revisit ~YYYY-MM-DD)` tag appended to its header line, instead of just sitting in the list
> looking equally actionable as everything else. `go` should flag (not auto-resolve or drop)
> any deferred item whose date has already passed, so it surfaces again instead of quietly
> aging out. No separate section — stays in place, in normal open-items order.
>
> **Condition-based deferral** (2026-08-15): some items don't wait on a date, they wait on a
> real-world condition (e.g. "the user has a WFH day free to safely test against a real
> broker") that no fixed date can predict. For these, tag the header `(revisit when: <condition>)`
> instead of a date — `go` cannot mechanically check a condition-based trigger the way it checks
> a past date, so a future session reading the item should ask the user directly whether the
> condition now holds, rather than assuming a stale-looking item is dead.
>
> **Specced tag** (2026-08-20): once an item's design/approach is actually decided (not just
> discussed) but the build itself hasn't started, add `[specced]` to its tag list — lets a
> session filter to non-specced items (the ones still needing a design decision, not just an
> implementer) when that's the actual bottleneck. Add a one-line note of what was decided right
> in the header/body when tagging it, so the spec isn't just the tag alone. Remove the tag (or
> just close the item) once it's built.

## [tooling][backtest] Open, paused 2026-08-29 (v6.3) — `params_json` proven end-to-end (write + Phase4 + Phase5), not wired into production call paths yet, no urgency
Proven 2026-08-29: write-side round-trip (87/87 real `candidate_nodes` rows), Phase4/report-layer consumption (byte-identical, 2 real scopes), Phase5 second-level overlay check (byte-identical, 9/9 candidates) — zero gaps found anywhere tested. Per user decision tonight, stopping here: sweep pipeline is fully functional without wiring params_json into real production call paths (`build_candidate_report_ground_truth`, `phase5_second_level_overlay_check.py`, `candidate_summary_report.py`, `candidate_full_review.py`, etc. still use flat columns, unchanged, deliberately). Wiring it in is a valid future item, not scheduled. Live-side (`watch_list`/`open_positions`) params-as-JSON stays explicitly OUT of scope. Full detail: `deep_backlog.md`.

## [backtest][tooling] Raised 2026-08-29 (very late), low priority — `--window`'s multi-value grid isn't given a symmetric version suffix the way `--z-thresholds` now is
Follow-up from the `--z-thresholds`/multi-value `--window` paired review (round 2, rebuttal exchange): the round-1 HIGH fix added a `-z<values>` suffix to `version` when `--z-thresholds` is set, so a widened-z run is distinguishable from a standard-grid run in `sweep_run_log`'s dedup key and in `candidate_nodes`/`backtest_phase1_insurance`/`backtest_winner_trades`. `--window`'s own multi-value grid gets no equivalent suffix, even though it's the identical nargs/plain-replace pattern. Confirmed via rebuttal exchange this is NOT a functional bug blocking merge (window-aliasing under one version string is a PRE-EXISTING condition, already documented/handled in `phase4_candidate_nodes_resolver.py`'s own `window` param; `sweep_run_log`'s dedup is already safe for window via its own separate `windows` column, independent of `version`; `candidate_nodes`/`backtest_phase1_insurance` both carry real per-row `window` columns a consumer can filter on; `backtest_winner_trades` is keyed by `node_key`, which already encodes the full param tuple) — but it's a real "misleading signal" risk (a future reader sees z encoded in version and reasonably but wrongly infers version fully discriminates the grid). Fix: add a symmetric `-w<values>` suffix to `version` when `args.window is not None`, mirroring the `-z<values>` block exactly. Small, additive, no consumer parses `version` (all take it as an opaque exact-match string) so safe to add anytime. Not urgent.

## [tooling][security] Found 2026-08-29 (much later), high priority — ~16 stale worktrees hold real uncommitted diffs, several touching live-trading files, never merged
Confirmed directly (`git worktree list` + per-worktree `git status --short` loop): of 44 total worktrees under `.claude/worktrees/`, 18 have real uncommitted changes. 2 are tonight's actual in-progress dispatches (`agent-a0e275fc3bb109c0a` = massive_second_derived work in progress; `agent-af015196213acba00` = AGQ ticker-generalization in progress) — leave those alone, they're live. The other ~16 are from past, unrelated sessions and were never merged to main:
- `agent-a099e74c2380d8a79`: `signals_notify.py` + new `tests/test_report_production_incident_filter.py`
- `agent-a0f7bee415286ba23`: `scripts/evening_status.py`, `scripts/node_candidate_trace.py`, `signals_db.py` + new `tests/test_node_archive.py`
- `agent-a253add9f502cb9b2`: `schwab_client.py` + new `tests/test_schwab_client_retry_lock_scenario.py`
- `agent-a2f0f827fdba1c062`: `scripts/coverage_designated_tester.py`, `scripts/coverage_registry.py`, `scripts/coverage_ticket_table.py` + new `tests/test_coverage_designated_tester.py`
- `agent-a47aca41d08ae639d`: `pages/4_Portfolio.py`, `schwab_safety.py`, `scripts/premarket_prep.py`, `scripts/watchlist_status.py`, `signals_blocks.py`, `signals_helpers.py`, `signals_notify.py` (all staged, `M `) + new `tests/test_live_tier_display.py`
- `agent-a54ff1791e91d0981`: `docs/backlog_resolved_recent.md`, `docs/deep_backlog.md`, `schwab_safety.py`, `signals_notify.py`, 2 test files + new `scripts/preview_intraday_risk_review.py`
- `agent-a74e8f849f4082886`: `docs/backlog_cache.md`, `docs/backlog_resolved_recent.md`, `docs/deep_backlog.md`, `pages/14_Coverage.py`, `scripts/coverage_proof_matrix.py`, `scripts/coverage_registry.py`, `scripts/evening_status.py`, 4 test files
- `agent-a999502f765f7de3f`: `scripts/fake_venue_harness.py`, `signals_notify.py` + new `fake_venue/scenarios_post_fill_topup.py`
- `agent-aa4bb1f1551f5c794`: `active_signals.py`, `scripts/coverage_registry.py`, `signals_config.py`, `signals_db.py` (all staged) + new `signals_trade_control.py` (staged) + new `tests/test_trade_control_channel.py` (staged) -- looks like a genuinely complete feature (trade control channel) ready to commit
- `agent-ab9d241ce5f769cff`: `schwab_safety.py`, `schwab_stream.py`, `signals_db.py`, `signals_notify.py`, 1 test file + 2 new files (`scripts/backfill_coverage_event_source.py`, `tests/test_coverage_event_source.py`)
- `agent-afb9808b12febee3a`: `tests/test_coverage_check.py`
- `agent-afbdf905edbe72f67`: `schwab_safety.py` + new `tests/test_replacing_order_id_dup_window_exemption_scenario.py`

Several of these touch real live-trading files (`active_signals.py`, `schwab_client.py`, `schwab_safety.py`, `schwab_stream.py`, `signals_db.py`, `signals_notify.py`) with what look like completed features/fixes and paired new test files -- genuinely concerning that this much real work was never merged. High priority to triage next session: for each worktree, read the diff, determine if it's complete/tested work worth merging (with the paired-review gate applied if it touches a gated file) or abandoned/superseded work safe to discard. Don't touch the 2 tonight-in-progress ones without checking they've actually finished first.
Real future direction, user's own framing (possibly "next weekend"): today's `massive_hourly_derived`/`massive_minute_derived`/(new tonight) `massive_second_derived` are each built by independently re-fetching dividends and applying adjustment at that granularity — three separate pipelines that could theoretically drift apart from each other over time (different dividend-fetch timing, different correction passes) rather than one coherent, continuously-updated source of truth. A unified design would build/adjust once at the finest available granularity and derive the coarser ones by resampling, guaranteeing all three stay consistent by construction instead of by convention. Not scoped — no schema, no build-pipeline redesign, just the direction. Checked directly, not already in backlog_cache.md/deep_backlog.md under any phrasing found tonight — first time it's on file.

## [tooling] Raised 2026-08-29 (later still), no rush — `bench_phase1_phase2_inmemory.py`'s name undersells its real scope now
Now added to CLAUDE.md's Review-Gate Persistence Rule as a real backtest kernel module (2026-08-29, once it became the real in-memory sweep pipeline) — but the filename still only names Phase1/Phase2, even though it also runs Phase2.5-CliffBox and the final top-9 candidate-write/promotion step, and now the fixed_sl-loop packaging too. A rename (e.g. something covering "sweep through promotion") would need dispatching (touches every doc/backlog reference + how it's invoked, not just the file itself) — not done here, just flagged so a future session doesn't need to rediscover the name/scope mismatch.

## [backtest][design] Raised 2026-08-29 (later still), no rush — surface max consecutive losing-streak length in the candidate report, not as a ranking factor
User's own framing: not asking to prioritize/rank on this (stays CAGR-first per standing convention) — just wants it visible in the report so the real psychological cost of a candidate ("watching 5-6-7 trades lose in a row") is visible before picking, distinct from win-rate% (already shown via checklist check 4's 70/30 win-rate stability split) — a strategy can have a fine aggregate win rate and still string together long losing streaks; the two metrics answer different questions. Cheap to add for Phase2.5's own trades: `backtest_winner_trades` (confirmed 2026-08-29, 72,423 real rows/313 nodes) already persists Phase2.5's final top-9 trade sequences — a max-consecutive-losses computation over those needs no new backtest runs. **Correction, same night**: this does NOT extend to Phase4/Phase5 — confirmed directly (grep) that neither `candidate_summary_report.py` (Phase4) nor `phase5_second_level_overlay_check.py` (Phase5) persists trade sequences anywhere; both recompute trades on the fly (for reporting / 1m-vs-1s comparison) and discard them. If the streak metric should reflect Phase5's more-accurate 1-second-resolution trades rather than Phase2.5's original resolution, Phase5 would need to start persisting its own trade list too — a real additional scope item, not free. Not scoped: exact report placement, whether to also show avg/typical streak length, and which resolution's trades (Phase2.5's cached ones vs. a new Phase5 persistence) the metric should actually use.

## [backtest][design] Raised 2026-08-29 (very late), no rush — reconsider candidate selection to weight volatility-of-return/drawdown smoothness, not just raw CAGR
Real tension with the standing convention (`project_candidate_selection_return_first` memory: rank by CAGR first, liquidity sizes the position not disqualifies it) -- user's own framing tonight, prompted by the ETHU arm=0.25-vs-1.0 spot-check (arm=0.25's higher full-period CAGR turned out to be concentrated in one half of the window, not a stable edge, per a real walk-forward split-half check same session): maybe candidate SELECTION itself should weight smoothness/lower volatility of the return path, not just total CAGR -- "for my heart's sake," i.e. an emotionally/practically easier equity curve to actually hold through, not necessarily the mathematically highest-CAGR pick. Real raw material already exists and wouldn't need new backtest mechanism, just a different ranking layer: `max_drawdown_pct`/`current_drawdown_pct` (already computed per candidate in the full report) and the walk-forward Check 13's 5-fold chrono-split per-fold CAGRs (already flags FRAGILE folds) both already capture return-path variance, just aren't currently used as a PRIMARY selection criterion -- CAGR is. Not scoped: whether this replaces CAGR-first ranking, becomes a secondary tiebreak, or a separate "smooth" candidate list alongside the existing "best CAGR" one; no specific smoothness metric chosen (Sharpe-style, max-drawdown cap, fold-variance, something else). Ties into the "expand sweep grid scope"/N_ISLANDS items below -- a smoothness-aware ranking pass would want the wider candidate pool those items are also about, so a mid-CAGR-but-smooth config isn't discarded before it's ever considered. No rush per user's own framing tonight.

## [backtest][specced] In progress 2026-08-29 — widen N_ISLANDS (3 -> 10), actively being resolved via the AGQ campaign build
No longer an untouched idea: a `--n-islands` CLI flag was built in `scripts/bench_phase1_phase2_inmemory.py`, went through the mandatory paired independent-cold + contextual Opus review (converged on 2 CONFIRMED HIGH findings — checkpoint-path/version-string not keyed on `n_islands` — both fixed, commits `d71e1ad` and `1b60821`), and a real widened-grid campaign at `n-islands=10` (AGQ, then ETHU/OILU/GDXU/UGL/WEBL) is either about to launch or already running in the background via `scripts/run_inmemory_sweep_queue.sh`. Not yet resolved (campaign hasn't completed) — real cost check done (Phase4/5 ~48min, Phase2's own dispatch cost is the bigger driver, ~+67min estimated) — see `deep_backlog.md`

**Phase2 cost measured, real numbers (user's own catch -- Phase2/2.5's own dispatch cost scales with island count too, not just Phase4/5 downstream)**: `logs/soxl_sl6_backfill_20260828_085704.log` gives a real Phase1/Phase2/Phase2.5 breakdown -- TrailingBoth: Phase1=187.2s, Phase2=192.6s (Phase2 is actually the BIGGER of the two), Phase2.5=0.9s; TrailingExit: Phase1=34.1s, Phase2=23.2s, Phase2.5=0.5s. Phase2's cost is the island fine-mesh dispatch (`±FINE_RADIUS` cells/center) -- scales ~linearly with `N_ISLANDS`. Going 3->10 (~3.33x) adds roughly: TB Phase2 192.6s->~641s (+7.5min/combo) x8 fixed_sl ~= +60min; TE Phase2 23.2s->~77s (+0.9min/combo) x8 ~= +7min. **~+67min added to Phase2 alone**, on top of the existing ~2hr Phase1-2.5 budget/ticker -- roughly a 50% total-sweep-time increase, not the "basically free" impression the Phase4/5-only math gave. Real total tradeoff for a 10-island version: ~48min more downstream (Phase4/5, genuinely cheap) PLUS ~67min more upstream (Phase2, the actual cost driver) ~= ~2hrs added per ticker, not ~48min. Not scoped -- no decision on the actual island count given this real cost.

## [live-trading][backtest] Raised 2026-08-29 (very late), no rush — candidate rotation/rebalancing needs a real workflow
User's own framing: real active goal, but "not going to rebalance every week" -- no urgency. Related to (but distinct from) the already-decided "quarterly-resweep-and-rebalance idea shelved" item (`deep_backlog.md`, 2026-08-16) -- that was about whether periodic rebalancing beats holding a config unchanged (answered: no, at a 2yr+ lookback); this is the separate, still-open question of HOW to actually rotate/rebalance candidates once picked (mechanics: swap-on-flat sequencing already touched on in the quarterly-ticker-rotation design, `docs/design.md`) -- not scoped, no rush per user's own call.

## [backtest][specced] Raised 2026-08-29 (very late), no rush — expand sweep grid scope to find better nodes; unscoped, explicit constraint: no 0.25 increments
User's own framing: wants to widen the search (more/different parameters), doesn't know what to add yet, and explicitly does NOT want 0.25-increment granularity ("feels too much" -- real cost concern, finer grid steps multiply Phase1-Coarse's cell count directly). Tied to the "fixed_sl as swept parameter" item above (once fixed_sl folds into the grid, it's itself one more axis to reason about alongside whatever new ones get added) and to the search-algo-efficiency item below (a better search algorithm is what would make adding more axes affordable without blowing up runtime). Not scoped -- no candidate parameter list yet, no rush.

## [backtest][specced] Raised 2026-08-29 (very late), no rush — once fixed_sl is parameterized, investigate faster/smarter search algorithms so more axes become affordable
User's own framing: the better the search algorithm (vs. today's brute-force-coarse-then-island-refine), the more parameters could realistically be added without the ~2hr/ticker cost above exploding further. Explicitly sequenced AFTER the fixed_sl-as-grid-parameter item (raised same night, this file) -- not a standalone effort, a follow-on once that lands. Not scoped -- no specific algorithm proposed yet (could be adaptive/Bayesian search, smarter island-detection, something else). Tied to the still-open sweep-manager item (this file) since a smarter search that runs longer/more flexibly benefits most from real job-queueing infrastructure. No rush -- user's own framing: "we don't NEED to do more work right now... this is all streamlining work for next time."

## [backtest][design] Raised 2026-08-29 (very late) — fixed_sl should be a swept grid parameter, not an external per-run config patch requiring a separate sweep invocation per value
Confirmed directly: `campaign_config.patch_config(strategy, fixed_sl)` patches `config.json`'s `execution.fixed_stop_loss` before a sweep starts — `fixed_sl` is baked in as an external constant, not one of the axes Phase1-Coarse's own inner grid loop already sweeps (take_profit/stop_loss[=trail_buy_pct]/window/z/tpct/max_hold, all in one numba-compiled pass). This is why tonight's SOXL window=15 sweep needed 8 separate `bench_phase1_phase2_inmemory.py --fixed-sl N` invocations instead of one pass covering fixed_sl 1-8 — each pays its own data-load/worker-pool-startup overhead separately. User's call: this should become a real swept grid axis like every other parameter, not a config-patch-per-run pattern. Not scoped — touches the sweep's inner grid construction (`run_optimization_sweep.py`/`bench_phase1_phase2_inmemory.py`, both backtest-kernel-adjacent, review gate applies) and `campaign_config.py`'s `patch_config` convention; real design question of whether `fixed_sl` folds into the existing hp-grid dict shape cleanly or needs its own axis-handling given it's currently read via `config.json` rather than passed as a kwarg through the sweep call chain.

**Refined 2026-08-29 (later, Campaign B planning)**: deeper open question underneath this — today's island/cliff-safety space is (take_profit, stop_loss/trail_pct) purely by legacy accident (`ZScoreBreakout`'s original single-axis architecture generalized outward), while window/z/tpct get pooled/flattened across when picking islands, and fixed_sl sits fully external (one whole campaign per value). No one has ever checked whether window or fixed_sl behave like the cliff-prone axes (TP/SL) or the safe-to-pool ones (z/tpct) — before fixed_sl joins the grid, or before trusting today's window-pooling, this needs an empirical characterization pass (does varying window/fixed_sl at a fixed TP/SL produce smooth or discontinuous CAGR surfaces?), not a decision made without data. User's call: keep fixed_sl external as-is for now (see above), but the "should it become a swept axis" question specifically depends on this characterization landing first.

## [live-trading][testing] 4 MEDIUM/LOW findings from 2026-08-29's session-wrap paired review of the daily-close detector + freshness checks, not fixed (only HIGH/CRITICAL block completion)
Real, from the same session-wrap review that fixed 3 HIGH findings (commit `4c6fb65`). (1) First-poll-of-day latency: `_fresh_daily_cache` is keyed `(ticker, window)` and cold at every daemon start, so the first poll cycle of the day can perform up to ~86 serial `yf.download(interval="1h")` calls (one per distinct ticker/window), each with a 10s timeout ceiling -- under a real yfinance slowdown this could exceed the 15-minute signal window. Consider prefetching pre-market or keying the cache on ticker only. (2) `massive_dividend_cross_check` (signals_helpers.py) calls Massive's `/stocks/v1/dividends` with `limit=50` and no explicit sort/order param -- the only other real caller (`build_massive_hourly_derived.py`) uses `limit=1000` + pagination and never had to care about default ordering. If the endpoint defaults to ascending `ex_dividend_date`, the 50 returned records are the OLDEST 50 and a freshly-added recent dividend (exactly what this check exists to find) would never appear -- unverified empirically (no `MASSIVE_API_KEY` in the review environment). Add an explicit `order=desc`/pagination and verify against a ticker with a known recent ex-date. (3) Read-modify-write race on `daily_close_consistency_state.json`: the atomic tmp+`os.replace` write prevents a torn read but not a lost update -- two concurrent callers (poll-loop thread, Bolt listener thread, or a separate ad hoc script per finding above) can both load the same snapshot and each write back only their own ticker's addition, silently dropping the other's. Effect is a one-day-delayed baseline for the losing ticker, not corruption -- a lock file or per-ticker files would fix it. (4) The new discontinuity alert's Slack button block is gated only on `ticker in AUTOMATION_ENABLED_TICKERS`, not `cfg.INTERACTIVE` like all 11 other button-rendering sites in `signals_blocks.py`/`signals_notify.py` -- a convention break, cosmetic today (the ticker-scoped click still routes correctly) but compounds finding (1)'s risk if it ever matters.

## [backtest][tooling] Idea, raised 2026-08-29 (very late), recurred same night — sweep manager: mid-sweep CPU control + job queueing, instead of manual cancel-and-chain
Real friction, user's own words (first pass): "you're acting as a proxy but it was a struggle because i would want to queue up more jobs and the only way to do it before was to cancel a sweep, add && + sweep command." Recurred later the same night, lighter framing but same root cause: "I keep want to start sweeps but don't." Currently the only way to add work to a running campaign is to kill it and relaunch with a `&&`-chained command list -- no way to (a) adjust CPU/worker allocation to an already-running sweep mid-flight, or (b) append a new job to a queue without disrupting what's already running. Not scoped -- would need real design (a persistent queue file/table the running process polls? a separate manager process that owns worker allocation across multiple sweep invocations? how it interacts with the existing ProcessPoolExecutor-per-script pattern every sweep script uses independently today). **Confirmed later the same night**: Phase 4 (`candidate_summary_report.py`) has ZERO parallelism at all (grepped directly -- no ProcessPoolExecutor/ThreadPoolExecutor/multiprocessing anywhere in that file) -- it's fast anyway (90 candidates in 4m55s) purely because per-candidate compute is cheap on the numba kernel, not because of concurrency, so this isn't blocking anything today, but confirms the "related sibling" question from the first pass: Phase 4 genuinely has no worker-pool infrastructure to interact with a future sweep manager, unlike Phase 5 (which does, currently underutilized -- separate already-flagged bug).

## [backtest][tooling] Found 2026-08-28 (Phase5 dispatch) — `scripts/verify_fill_resolution_accuracy.py` imports `replay_five_min`/`FIVE_MIN_LOOKBACK_DAYS` from `scripts/verify_trailing_buy_resolution.py`, which no longer exist there (renamed to `replay_one_min` in commit `2a9f3d3`, "Full v6 promotion") — a hard import-time crash for every consumer, found while running Phase4 (`scripts/candidate_summary_report.py`) for SOXL. Worked around (not fixed) by moving the broken import lazy in `candidate_summary_report.py`/`candidate_5min_report.py` so `--skip-5min`/`find_candidates()`-only callers work again; the real 5-min-replay accuracy feature itself stays broken. Real fix needs a judgment call on new `FIVE_MIN_LOOKBACK_DAYS`-equivalent semantics for 1-min-granularity data (yfinance's 1m-history cap differs from the old 5m one) — not decided here.

## [live-trading][testing] Found 2026-08-27 (paired review of the yahoo->massive/PHANTOM-cross-check fix), latent/unreachable — `_cheap_signal_cross_check`'s `close_check` branch buckets to the wrong bar (off by one hour)
No live node uses `close_check` entry_timing today, so this can't fire yet, but it's real and wrong -- needs the bar-bucketing logic in `scripts/evening_status.py::_cheap_signal_cross_check` fixed before any `close_check` node goes live. 4 sibling findings from the same review fixed 2026-08-28, 1 superseded into its own item -- see `backlog_resolved_recent.md`.

User's call: fix #1 now (done), backlog the rest for review after session wrap rather than have the agent make more judgment calls on holiday-calendar choice / CHECKS-list placement / data-source reconciliation.

## [live-trading][testing] Raised 2026-08-27 (evening monitor session) — no check on HOW MANY concurrent real positions are open at once
Raised while investigating today's real evening_status.py Part 3 output. Not about positions interacting (margin/correlation) -- just a real count: how many real capital-at-stake nodes have simultaneous open positions at a given time, with no existing check/report surfacing that number. Not scoped, not investigated further this session.

## [process] Found 2026-08-26 (promoter session, daemon-not-started discussion) — rule-hardening pass: audit ignored instructions, convert real ones to hard-gate memory/CLAUDE.md entries; design daemon auto-restart supervisor
Two threads from one conversation, both deliberately deferred (not urgent, needs calm design time, not a live-pressure moment): (1) User asked me to start `active_signals.py` myself this morning after forgetting to; declined per `feedback_user_runs_daemon_lifecycle` memory, held even under "override please"/"it's calm now" pressure — user then flagged a real meta-point: they give ~400 instructions that get inconsistently followed, and want to understand why this particular rule held (hard-gate phrasing + narrow/checkable + backed by a stated incident + always-loaded) so the same pattern can be applied elsewhere. Action: go through repeatedly-ignored asks and convert the ones worth hardening into explicit hard-gate memory entries or CLAUDE.md checklist items (see global CLAUDE.md's "End-of-response checklist" as the existing template for this). (2) Real operational gap surfaced: current daemon-lifecycle rule has no recovery path for "daemon crashed mid-day" or "forgot to start it, no remote access" — proposed fix is a deterministic auto-restart supervisor (systemd user service or cron @reboot + watchdog tied into `daemon_status.py`'s staleness check) rather than loosening my discretionary launch authority, since the discretion itself (not lack of authority) was the root cause of past incidents (RETL, false-exit SL). Not scoped/designed yet — needs restart-condition design (trading-hours-only? crash-detection threshold? log rotation?) before building. Separately, smaller/already-resolved-in-discussion: confirm `feedback_dispatch_to_peer_checklist`'s "background-by-default execution" bullet actually reads as background-unless-user-explicitly-asks-otherwise-in-session (checked mid-conversation, looked already consistent with `feedback_sequential_background_dispatch`'s one-at-a-time default — just needs a final read-through to confirm no gap).

## [backtest][data] Found 2026-08-26 (promoter session, DB-check follow-up) — legacy (non-v6) `backtest_cache` hasn't been re-pruned since 2026-08-13; ~5.5M un-pruned rows added since
Verified directly: last full legacy prune (`prune_backtest_cache.py --swap`) completed 2026-08-13, shrinking the whole DB to 9.9M rows. Since then 5,556,019 more non-v6 rows landed (mostly `v5-widehold-minhold112`, `v5.1`, and several windowed-backtest variants) from ongoing sweep campaigns, with no re-prune since — non-v6 alone is now 14,979,844 rows. Likely explanation: the 2026-08-23 "v6 deprecation guard" commit (`a710c4c`) coincided with attention shifting to the new GT/v6 prune tool (`prune_backtest_cache_ground_truth.py`), and the legacy tool was never re-run after. Not urgent (no data-loss risk, just DB bloat), but worth a re-prune pass once the GT/v6 prune tooling work settles — follow the `prune-validation` skill (two independent before/after snapshots + row-count regression check) same as any other prune. Snapshot extracts already pulled for reference: `output/backtest_cache_non_v6.db` (15.0M), `output/backtest_cache_non_v6_non_phase1.db` (7.1M, excludes bulk Phase1-Coarse grid rows), `output/backtest_cache_soxl_non_v6.db` (4.5M), `output/backtest_cache_soxl_v6.db` (1.6M v6/ground_truth_v6 SOXL rows).

## [live-trading][process] Found 2026-08-26 (promoter session) — build auto-restart supervisor (systemd/cron) for `active_signals.py`
Today: user forgot to start the daemon before leaving, had no remote access to fix it, session declined to hand-start it per `feedback_user_runs_daemon_lifecycle`. Real gap: no recovery path today for either "forgot to start it" or "crashed mid-day." Proposed fix: a systemd user service (or `cron @reboot` + a watchdog loop) that auto-starts `active_signals.py` on boot and auto-restarts on crash, tied into `scripts/daemon_status.py`'s existing staleness check for health detection — removes the need for anyone (user or Claude) to manually launch it. Not scoped: restart-condition design still open (trading-hours-only restart? crash-detection threshold/backoff? log rotation for `logs/active_signals.out`? how it interacts with the "user restarts on account-flag change" caching note in CLAUDE.md). Related broader discussion in the `[process]` rule-hardening item below.

## [live-trading][tooling] Raised 2026-08-26 (planner session, real SOXL/DPST/DFEN kernel-divergence check) — minute AND second-granularity market data have no automated refresh, causing real blocked checks
Confirmed directly tonight: `get_trades_and_bars_since_ground_truth`'s minute-data staleness guard (already documented in its own docstring, "no cron entry, confirmed 2026-08-23") fired for SOXL/DPST/DFEN — cached minute data was 5 days stale (through 2026-08-21), blocking any live-vs-kernel divergence check for all 3 real open positions until manually refreshed (`scripts/fetch_massive_minute_data.py` + `scripts/build_massive_hourly_derived.py`, run ad hoc tonight). Same gap exists one level finer: `cache/research/second_data/*_1s.csv` (`scripts/fetch_massive_second_data.py`) was 2 days stale for the same 3 tickers, blocking a real intrabar-price verification until manually re-pulled tonight (scoped to single-day windows via `fetch_ticker_streaming()` called directly, not the CLI's `--years`-only granularity). This is very likely the same root cause as last night's still-open backlog item ("evening_status.py Part 3 shows NOT CHECKED (RuntimeError) for 12/14 real tickers") — not yet confirmed for every one of those 12, but the mechanism matches exactly. Fix: add both refreshes to the nightly cron (alongside the existing 2am `trading_universe.db` backup / 4:15am `db_cache.py` / 6:30am `data_collector.py` jobs) for every real capital-at-stake ticker, so this class of check never silently blocks on stale data again. Not scoped: whether second-data refresh needs to be nightly for every real ticker (higher cost, ~60x row volume vs minute data per `fetch_massive_second_data.py`'s own docstring) or only pulled on-demand when a divergence investigation needs it.

## [live-trading][tooling] Raised 2026-08-26 (planner session, real portfolio-performance check) — build a `portfolio-performance` skill, broker-verified not DB-trusted
Prompted by a real "how did the portfolio do today" question: had to hand-build the answer from `trade_log`/`open_positions` + a broker cross-check (`schwab_client.get_real_position`) + fresh `yfinance` prices, no existing script/skill does this in one shot. Real finding along the way: 3 of 6 "open" `soxl_ira` positions (ERY/YINN/ERX) were actually already closed at the broker (real sunset SELLs from 2026-08-25 evening) with the local `open_positions` rows never reconciled — confirms this can't just read `trade_log`/`open_positions` and trust it, per `automation_principles.md`'s "never trust the local DB as ground truth" (same principle `reconcile_flat_position.py` already follows). Scope: either reconcile trades/orders against Schwab directly, or read real Schwab positions/orders directly rather than the local mirror. **Only the 3 real accounts matter**: `ira`, `roth`, `brokerage` — `soxl_ira` becomes a nominal/test-only account once tonight's ERY/YINN/ERX reconciliation is committed (user's own framing, 2026-08-26), and `sep` never trades (`project_sep_account_offlimits`). Not scoped/built — no design decisions made yet (real-time vs on-demand, Slack-postable vs CLI-only, whether it reuses `reconcile_flat_position.py`'s core verification call or is read-only/no-commit).

## [live-trading][testing] Found 2026-08-26 (planner session, real SOXL/DPST/DFEN kernel-divergence trace, resolved) — `get_session_open_price`'s second same-day pinned call returns a stale duplicate value; cosmetic, no real order affected
Confirmed via `open_price_quality_log`: both DPST (2026-08-25) and DFEN (2026-08-26) show byte-identical price + `is_true_open=1` logged for BOTH the 9:30 and 14:30 pinned checks that day, even though the real 14:30 bar's own Open was genuinely different (DPST: logged $138.17 both times vs. real 14:30 Open ~$134.41; DFEN: logged $69.58 both times). Verified this is real via direct second-level Massive.com data at the actual 14:30 print — the pinned mechanism did not fetch a fresh value on its second call of the day, it returned the morning's value again. **Confirmed NOT to have affected any real order or fill** — every real BUY (`entry_price`) independently matches real market data exactly regardless of this bug; it only corrupts the `signal_price`/`open_price_quality_log` diagnostic fields. Not fixed, not root-caused at the code level (haven't read `schwab_client.get_session_open_price`'s implementation) — full trace: `docs/research_log.md`'s 2026-08-26 entries, `trading_incidents` #14 (resolved).

## [tooling] Found 2026-08-26 (planner session, DB-inventory check) — `scripts/live_sim.py` (manual-step REPL) is dormant/superseded, worth archiving
Untouched since 2026-08-07 (last commit `96cfc0f`), while `tests/fake_broker.py`/`fake_venue/` have had continuous heavy development through 2026-08-20 and this project's own backlog already reframed testing strategy around the harness (Phase 2 Category A queue closed 30/30 on 2026-08-15/18; multiple resolved-item notes say "the fake-venue harness takes over that role, not more paper nodes/manual REPL stepping"). Difference in kind, not just overlap: `live_sim.py` is a manual bar-by-bar REPL against an isolated `cache/live/trading_sim.db`; `fake_broker`/`fake_venue` is a stateful fixture that drives the *real* order-placement code paths automatically for actual regression coverage. `trading_sim.db` itself still holds stale leftover state from a past manual session (11 `watch_list` rows, 1 open position, 1 trade, 1 pending buy, last touched ~2026-08-07) and has no backup coverage. Not urgent — candidate for the existing "deep-archive legacy hourly-kernel-only scripts" cleanup pattern (`git mv` into `scripts/archive/`, keeps history, drops it from `scripts/list_scripts.py`'s active surface) once picked up, plus a decision on whether to reset/delete `trading_sim.db`'s stale leftover state.

## [live-trading] Found 2026-08-25 (dispatched terminal-not-filled session, real live-state check) — `_attempt_automated_exit_sell`'s `sl_order_id`/`exit_pending` repoint fails silently when `replace_equity_order_with_market` returns success with a non-extractable `order_id`
Real, observed live tonight on ERY/YINN/ERX (soxl_ira, confirmed benign — planned sunset market SELLs, not a rogue incident): all 3 positions' `sl_order_id` still points at the OLD now-`REPLACED` order even though a real new MARKET SELL order is genuinely resting at the broker, because `signals_notify.py:774-783`'s repoint condition requires the new `order_id` to be non-`None`, and Schwab's replace can succeed with `extract_order_id` returning `None` (same documented edge case the terminal-not-filled fix's paired review flagged for `check_gap_resize`'s analogous path). ERY/ERX's `trail_state` is `None` entirely (no `exit_pending` at all); YINN's `exit_pending.order_id` is also `None`. Net: none of the normal fill-detection paths (`check_own_sell_fills`/`check_auto_fills`) have a valid order_id to poll for these real pending exits. **Action item**: verify tomorrow morning (2026-08-26) that ERY/YINN/ERX's real fills actually got picked up — broker-share-count reconciliation (`check_live_state_reconciliation`) or a manual check, since local order-id-based tracking won't catch them automatically. Not scoped/fixed — needs either a real order-id-recovery fallback (e.g. `get_real_orders`-based lookup of the new resting order when the replace response's own id is unusable) or an explicit alert when the repoint condition is skipped.

## [live-trading][tooling] Found 2026-08-25 (paired review of the Slack invalid_blocks fix) — `_sanitize_blocks` doesn't cover every real Slack send path
Only `_post_message` (and everything routed through `_post_chunked`) gets the new length-cap backstop. `signals_handlers.py`'s `chat_update`/`views_open` call sites (~18 locations) and `signals_notify.py:3503`/`signals_config.py:126` send Slack content directly, bypassing `_sanitize_blocks` entirely. All currently static/section-only text, no dynamic-length risk today — but nothing stops a future dynamic label at one of those sites from reproducing today's `invalid_blocks` incident with zero protection. Not scoped/fixed — either route those call sites through a shared sanitizer entry point, or extend `_sanitize_blocks`'s reach explicitly.

## [backtest] Raised 2026-08-24 (planner session) (revisit ~2026-08-29) — remove/reassess the daily options-snapshot cron once the theta-decay week-check is done
`crontab`: `0 10 * * 1-5 ... scripts/collect_options_snapshot.py --tickers AGQ DFEN DPST HIBL JNUG KORU LABU NUGT SOXL UGL WEBL --option-type calls --expirations 2 >> logs/options_snapshot_daily.log`, added to check real call-option theta decay over one week across the 12 v6-candidate tickers (GDXU excluded, no options market). Low cost (~1k rows/day, one small yfinance pull), so fine to let run past EOW if nobody remembers immediately, but there's no auto-expiry — remove the crontab line (`crontab -e`) once the week's data has been reviewed, don't let it run indefinitely unattended.

## [backtest] Raised 2026-08-24 (planner session) — options-overlay idea: use option contracts instead of/alongside leveraged-ETF equity for our short-hold breakout signal
Discussion, not scoped: our TB/TE overlays are picked because they have a higher win-probability short-hold breakout signal — question raised was whether that same signal could be traded via a call/put option (on either the leveraged ticker or its unleveraged underlying) instead of buying the leveraged equity outright, and whether IV cost makes that a bad trade on our timeframe. Working take (not validated): likely net negative — our hold is hours, not days, so theta/IV crush probably dominates unless the move is unusually large/fast, and it adds a second thing that has to go right (direction+magnitude+IV all at once) versus the leveraged ETF's simple convex payoff. Also flagged: liquidity-only data need (open interest/volume on the options chain, no real-time options pricing required) is cheap to source if this is ever explored. Would need its own fill/liquidity model and full backtest validation before any live use, same bar as any other new mechanism (`docs/new_mechanism_promotion_standard.md`) — no existing kernel/backtest code paths transfer. Not scoped further, no data pulled, no code written. Parked behind current V5/V6 go-live work.

## [backtest][tooling] Found 2026-08-23 (promoter session) — GT `status`/`worst_neighbor_pct` (core cliff-safety) sourced from the live add-on re-simulation instead of a `backtest_cache` lookup
`candidate_full_review.py:1832-1834` sets `status`/`worst_neighbor_pct` from `addon_detail` (`run_addon_cliff_safety_ground_truth`'s live re-simulation output), even though this specific field is core-only (`core_safe`, not addon-mixed). Every neighbor cell's core CAGR already exists as a static `backtest_cache` row from the sweep (guaranteed `same_bar_reentry=True`, no ambiguity) — a pure SQL lookup would give the identical verdict with zero simulation cost, and zero exposure to the class of bug that just hit LABU/SOXL/HIBL (a code change to the re-sim path silently changed the cliff verdict on unchanged underlying data). Not built — scoped, bounded change (only `status`/`worst_neighbor_pct`; addon-specific fields correctly stay on the re-sim path since those need the real per-trade list `backtest_cache` never stores).

## [backtest][tooling] Found 2026-08-23 (promoter session) — no report-staleness check against the code that generated it
`output/full_review_gt_final_combined_20260823_202719.xlsx` (2026-08-23 20:37) was trusted as "the real numbers" for over an hour after `255a950` (21:17, cliff-safety `same_bar_reentry` fix) and `d9063f1` (21:59, window-date threading fix) landed — both directly change the numbers that report contains, with nothing flagging that it predated them. Real cost: a real back-and-forth chasing a "same data, different neighbor" contradiction that a timestamp check would have resolved instantly. Fix idea: stamp each generated report (xlsx/csv) with the repo's current commit hash at generation time; before trusting an existing report, check whether `git log` shows any commit touching `run_optimization_sweep.py`/`candidate_full_review.py`/`candidate_summary_report.py` since that hash, and warn if so. Not built.

## [backtest][live-trading] Raised 2026-08-23 — v6 promotion readiness checklist (consolidated)
Gaps found: `coverage_registry.py`/`coverage_check.py` have zero `kernel_version` awareness; `watchlist_candidate_checklist.md` check 9 (same-day-block) is out of scope for GT and check 10 is unresolved as a result; `evening_status.py`'s 30d/10pp divergence threshold was never recalibrated for GT's error profile; `prune_backtest_cache_ground_truth.py` wiring into the tranche runner is now MOOT, not a gap — superseded 2026-08-29 by the go-forward pipeline decision: `scripts/bench_phase1_phase2_inmemory.py` is the sole go-forward sweep pipeline and writes to `candidate_nodes`, not `backtest_cache`, so nothing actively grows `backtest_cache` anymore and there's no ongoing pruning need to wire up. Dry-run from 2026-08-24 still stands as a data point (60.1M raw GT rows -> 218,271 real keep rows, 0.36%) but at most this is a one-time historical cleanup of `backtest_cache`, not standing infrastructure worth building; parity gate has no `entry_timing='close'` coverage (low-risk, no live node uses it); wash-sale check #16 has no code enforcement for a v5->v6 sunset. (The CAGR-ranking gap and the "CLAUDE.md still names v5/v5.1 as live default" gap noted here earlier are now resolved -- all 14 real watchlist tickers promoted to v6 2026-08-24/25, see `docs/backlog_resolved_recent.md`/`deep_backlog.md`.)

## [backtest][tooling] Raised 2026-08-23, corrected 2026-08-29 (real blocker is an import-graph dependency, not v6 stability) — deep-archive legacy hourly-kernel-only scripts, two-phase
Real investigation 2026-08-29 found the original "revisit when v6 stabilizes" framing was stale/wrong: the actual blocker is a real import-graph dependency problem that exists independent of v6's stability, not a time/stability condition. None of the 4 Phase-1 "safe" candidates (`candidate_5min_report.py`, `candidate_checklist_report.py`, `train_test_split_check.py`, `walk_forward_check.py`) are actually safe to archive right now — all have real active dependents:
- `checklist_v65.py` (the tool this item's Phase 1 claims supersedes all 4) directly imports `train_test_split_check` and `v4_max_drawdown`; `v4_max_drawdown` in turn imports `candidate_checklist_report` — so `checklist_v65.py` transitively depends on 2 of the 4 "safe" candidates.
- `candidate_checklist_report.py` was actively bug-fixed 2026-08-29 (commit `f7a6ecf`) — not dead code — and is imported by `chart_v4_vs_tqqq.py`, `checklist_deep_checks.py`, `train_test_split_check.py`, `walk_forward_check.py`, `v4_max_drawdown.py`.
- `candidate_5min_report.py` and `walk_forward_check.py` are both imported by `candidate_full_review.py` and `candidate_summary_report.py` — real, currently-active hub scripts (`candidate_full_review.py` last touched 2026-08-29 22:47, commit `33cb1df`).

Real gating condition (replaces "revisit when v6 stabilizes"): before any of the 4 Phase-1 scripts can be archived, `checklist_v65.py`/`candidate_full_review.py`/`candidate_summary_report.py` need to first be refactored to drop their legacy v4 imports in favor of GT equivalents — that refactor hasn't happened, isn't scoped, and has no target date. Since this is now a dependency blocker rather than a time-based condition, a calendar revisit date doesn't apply here — actionable trigger is "the v4-import refactor lands," not a date.

Phase 2 (only once v5 is actually retired from live — the real sunset, not the earlier housekeeping archiving some watch_list rows already went through, which was NOT this): the broader hourly-kernel-only tooling that still backs real v5 live-node monitoring today (`verify_trailing_buy_resolution.py`, `verify_live_parity.py`'s hourly paths) — already correctly excluded/deferred here and remains correctly excluded: `verify_live_parity.py` is imported by `evening_status.py`/`post_sweep_report.py`; `verify_trailing_buy_resolution.py` is imported by `candidate_checklist_report.py`/`verify_fill_resolution_accuracy.py` and is still cited as a required step in `checklist_v65.py`'s own docstring. Don't touch Phase 2 until v6 is actually promoted to live and v5 nodes are gone, or this removes real live coverage prematurely.

Mechanism decided: `git mv` into `scripts/archive/`, not delete outright or leave in place with a banner — keeps history, drops it from `scripts/list_scripts.py`'s active surface, reversible.

## [backtest] Raised 2026-08-23, updated 2026-08-29 — re-test entry_timing='close' vs 'open_check' once v6/GT candidate sweeps are running
`campaign_config.py` locks both live strategies to `entry_timings: ["open_check"]` only — `close` lost the v5 experimental comparison (2026-07-20) and hasn't been resweept since. Hypothesis (user's, not yet tested): v6's minute-level continuous fill modeling removes most of the bar-close-sampling drift that likely disadvantaged `close` under the old hourly kernel, so the result may not hold under GT. Backtest-only question — no live-actionability change implied (0 live nodes use `close` today, per direct DB check same session). **Trigger condition fired 2026-08-29** (v6/GT candidate sweeps are now running) — queued for a near-term small, targeted test on a few specific nodes (not a full resweep campaign); user to review/scope tomorrow, not yet dispatched.

## [backtest][testing] Deferred 2026-08-22 (user's call, low priority) — port same_day_block sensitivity (checklist check 9) to the GT kernel, for TrailingBoth first
`_simulate_trail_ground_truth` (GT/v6 kernel) has no `same_day_block` parameter at all, for either strategy — confirmed while building the GT candidate-report (checks 1/4/8/11/13, commit `ff8372e`). Not a build-from-scratch problem for `TrailingBothZScoreBreakout`: the legacy kernel's `same_day_block` logic already exists and is used by `scripts/checklist_v65.py`'s check 9 today (`backtester.py` lines ~611/851/1094/1148/1283/1335/1350/1365) — porting that same pattern into `_simulate_trail_ground_truth` is the real scope. `TrailingExitZScoreBreakout` has never had `same_day_block` support even in the legacy kernel (`checklist_v65.py:15`'s own comment), so that half would be genuinely new logic, not a port. User's read: same_day_block has never actually produced a useful signal historically, so this is low priority — deferred, revisit later rather than build now.

## [backtest][tooling] (revisit ~2026-09-19) — check whether Massive's dividends endpoint exposes a distribution before or only after its ex-date
Real test case in hand: SPY's next scheduled ex-dividend date is 2026-09-18 (real, publicly
known — payment 2026-10-30). Checked 2026-08-22: Massive's `/stocks/v1/dividends` for SPY does
NOT yet show this entry (most recent on file is last year's 2025-09-19 equivalent) — confirms
it's not exposed in advance, at least not 4 weeks out. Massive's own docs (checked 2026-08-22)
don't explicitly state whether announced-but-not-yet-effective dividends/splits are ever
included, so this is genuinely unverified either way. Action: re-check SPY's dividends
endpoint after 2026-09-18 (and again after the 2026-10-30 pay date, in case it only appears
post-payment rather than post-ex-date) to see exactly when/whether the record appears — this
determines whether future vintage-rebuild triggers for the massive_hourly_derived pipeline
(see [[project_data_subscription_upgrade]]-adjacent 2026-08-22 pipeline work, not yet fully
landed/reviewed) can ever be proactive or must always be reactive (checking after the fact).
Status update 2026-08-29: `scripts/corp_action_canary.py` (built 2026-08-28) is now actually
scheduled — added to crontab at `*/15 * * * *` this evening, after ~1 day built-but-idle with
no scheduling anywhere. It's now really polling for real going forward, producing the data
points this item is waiting on.

## [tooling] Found 2026-08-22 — Stop hook's "session wrap ran this turn" check isn't session-scoped
The `session_cache_update.py`-ran checkpoint hook (enforces the `***** Ready to clear ****`
line) appears to detect "did a wrap run" via a repo-wide signal (likely `docs/session_cache.md`/
`docs/conversation_summary.md` mtime), not something scoped to the current session's own tool
calls — it fired in a session that never ran `session close`/`session wrap` itself, right after
a separate concurrent session (multiple Claude sessions were active on the same machine/repo at
the time) ran its own real wrap. Needs a fix so the check is scoped per-session (e.g. keyed off
something the hook can tie to the actual invoking session) rather than any-session-triggers-all.
Not investigated yet — `.claude/settings.json`/hook config not read to confirm the exact
detection mechanism, only inferred from behavior.

## [live-trading][testing] Raised 2026-08-21 (evening) — paper-vs-kernel comparison needs trade-level evidence, not count-only tolerance
`evening_status.py` Part 3 section 2's paper-vs-kernel check reports "matched" purely on trade COUNT within a ±2 tolerance (`abs(len(paper) - len(bt)) > 2`) — never checks individual trade dates, prices, or exit reasons. Confirmed live 2026-08-21: tonight's report showed "7 matched, 0 issues" under this check, but that's not real evidence paper trading actually mirrors the backtest at the trade level — it only proves the counts are close. Same underlying mechanism as the live-vs-backtest divergence already being characterized in the parallel same-bar-SL research thread (paper trading polls/simulates fills continuously, same as real live trading, vs. the kernel's two-fixed-daily-window model) — plausible paper trading has the identical structural divergence, just never verified because the existing check is too coarse to catch it. User's own framing: "paper trading tests need to be evaluated not at the count level but actual trade level" — generalizes [[feedback_prove_dont_claim_verified]] to monitoring checks, not just one-off audits. **Not built** — needs a real trade-by-trade paper-vs-kernel comparison (same style as the SOXL 5-min-mimic work: match individual trades by date/price/exit-reason, not just count). Likely belongs alongside whatever the same-bar-SL session concludes about live execution fidelity, since it's the same underlying question applied to paper instead of live. Not scoped, not dispatched.

## [live-trading][testing] Found 2026-08-21 (contextual review of the overnight-carry trade-lifecycle fix) — a 5th lookup gap remains: retroactive `--date` re-check of a multi-day trade's ENTRY day, after it has already closed
`_check_trade_lifecycle`'s 4-lookup chain (pending_buys → open_positions → closed-same-day → closed-exited-on-date, see `docs/deep_backlog.md`'s 2026-08-21 entry for the fix that added the 4th) now covers the live, real-time nightly check correctly. But `get_open_positions_for_ticker_on_date` only matches "still open," which is true at QUERY time, not "as of check_date" -- so a retroactive `coverage_check.py --date <a carry's entry day>` run, executed AFTER that carry has already closed, sees nothing in any of the 4 lookups (the position isn't open anymore, and it didn't also exit that day). Reads as false `no_activity=True`, which makes it eligible for the price-action auto-explain -- reasoning about *today's* entry signal for a day that genuinely DID enter, the wrong question. Doesn't affect the live nightly run (open_positions correctly covers it in real time); only affects backfilling/re-examining old deviations after the fact. Fix would be a matching `entered_on_date` sibling lookup (`date(entry_time) = check_date`, no exit-date requirement) -- not yet built, not yet scoped for priority.

## [live-trading][tax] Raised 2026-08-21 (evening) — codify wash-sale protection in code, not just the manual promotion checklist
`trading_incident #2` (GDXU/soxl_ira, real permanent loss disallowance) closed out this session — root cause was that the wash-sale hold was documentation-only with zero code enforcement at the time. `docs/watchlist_candidate_checklist.md`'s check #16 (added since, commit `3a88c1d`) now covers this as a **manual** step at promotion time (ticker-wide, across all accounts, taxable-loss-then-IRA-repurchase-within-30-days). Real gap: nothing stops a future promotion (or an ad hoc node add outside a formal promotion pass) from repeating the same mistake if the checklist step is skipped or forgotten — confirmed directly, 2026-08-21: no wash-sale logic exists anywhere in code (`add_node`, `signals_invariants.py`, or any promotion script). Candidate approach: a `signals_invariants.py`-style check (or a guard inside `add_node`/the live-flip path) that queries `trade_log` for a real taxable-account loss on the same ticker within 30 days before allowing a tax-advantaged-account node to go live. Not scoped, not started.

## [backtest][coverage] Raised 2026-08-20 (AGQ paper-node cleanup) — port AGQ's drought+addon stacked-overlay paper permutation to a real fake_broker scenario, now that paper is being deprioritized
Found while cleaning up AGQ's 5 duplicate paper live-track/daily-sync pairs (down to 4 genuine permutations: none/addon/drought/drought+addon — see `docs/deep_backlog.md`'s 2026-08-20 promotion-pass entry). Checked `scripts/coverage_registry.py`: individual addon/drought mechanisms already have real fake_broker coverage (`addon_entry_fill`, `addon_entry_placement`, `drought_entry_placement`, etc. — mostly `event-asserted`, several `verified-live`), but the **stacked drought+addon combination specifically** (what the paper node `v5-overlay-test-da`, wl_id=186/187, was probing) has no dedicated tracked coverage row. Given the user's stated direction (leaning away from paper trading, into harness/live testing instead), the real next step isn't "keep the paper permutation running" — it's building one new `tests/test_fake_broker_*.py` scenario for the stacked case, closing a real Grid gap rather than just preserving a paper-mode proxy for it. Not started, no rush per user's call.

## [live-trading] Deferred 2026-08-18 (from get_current_price paired review) — 2 findings recorded, not blocking, from the get_current_price stale-extended-quote fix (commit `3697829`)
(1) Whether `get_current_price`'s extended-hours-tracking capability (deliberately preferring `extended.lastPrice` when genuinely fresher, so `update_real_pending_buys_running_low` can catch a real overnight/pre-market fall ahead of `check_gap_resize`) is now effectively dead outside a narrow window is unresolved — the 2026-08-18 incident payload showed `quote.lastPrice` itself tracking after-hours prints (mitigating), but only one real payload sample exists (grep for `quoteTime`/`tradeTime` hits only this fix's diff/tests). Needs a real captured pre-market payload (~08:00 ET) to settle either way. (2) No absolute-staleness floor: if both `extended` and `quote` sides are stale (weekend/holiday/feed outage), the function silently returns a stale price with no signal to the caller — pre-existing, not introduced by this fix, but worth a backlog line since this function feeds live SL/arm-trigger/top-up sizing.

## [live-trading] Found 2026-08-17 — incident alert headers still render a falsely-reassuring `DRY-RUN` for a real position on a since-demoted node; `is_dry_run_sim` alone can't fix it
Sibling of the `incident=True` gate fix closed the same day. That fix stopped SUPPRESSING a real position's incident alerts after its node was demoted / account stopped — but the header still reads `mode_tag(account, node)`, re-derived from CURRENT state, so the alert now correctly fires and says `(roth · DRY-RUN)` for a genuinely real position. `mode_tag`'s own docstring calls a reassuring-wrong DRY-RUN "the wrong failure direction", and per the mobile-readability convention that header is line 1. **A `mode_tag_for_position(pos, node)` helper was built and then REVERTED the same session**, because the full suite empirically disproved the premise: `tests/test_schwab_automation.py::test_automated_sell_notifies_sl_price_when_trailing_sell_fails_after_sl_cancel` opens a position on a dry-run account via plain `open_position`, which defaults `is_dry_run_sim=0` — so the helper labelled a never-real test position `LIVE · NODE STOPPED`, the mirror-image error (falsely ALARMING). `is_dry_run_sim` genuinely cannot distinguish (A) a real position on a since-demoted node from (B) a never-real position on an always-dry-run account: both are 0, and `sl_order_id`/`broker_stop_price` don't separate them either (the test sets a fake `sl_order_id`, and `signals_handlers.py`'s Manual Open / `signals_notify.py`'s catch-up path both call `open_position` without stamping the flag). A correct fix needs entry-time account state PINNED onto the position row (nothing records it today) — that's the real prerequisite, so do that first rather than reaching for another heuristic. Raised by the independent-cold reviewer only; the contextual reviewer did not flag it. Display-only, no control-flow impact — the suppression half of the fix is landed and correct.

## [tooling][security] 3 of 6 fixed 2026-08-15, 1 held (needs a design/policy decision), 1 stale (resolved by a threshold change since), 1 partially fixed — findings from the full-session dual-Opus review
**Fixed**: tax-forecast character-netting (cross-character offset + baseline_remaining-never-exceeds-principal, `k1_tax.brokerage_tax_forecast`, 3 new pinned tests), tax-forecast query scoping (`evening_status.py` now scopes brokerage tax-forecast tickers to real closed trades this year, not just currently-`live` nodes), tax-forecast baseline seed-guard (new `_seed_markers` table so deleting the baseline row — the only documented correction path — doesn't get silently resurrected on the next `ensure_tables()` call; migration-safe against the real production DB's pre-existing row). **Held, need a decision before touching**: add-on-leg drift-alert bypass — `open_addon_leg`/`close_addon_leg` don't take a full `node` (only `parent_position`) and have no clean "signal_price" concept for an add-on entry the way core entries do, so wiring `check_abnormal_drift` in requires deciding what "drift" even means for an add-on leg first, not just threading a call; confirmed 2026-08-17 (`signals_db.py`, grep) that neither function calls `check_abnormal_drift` today — still real. **Confirmed stale 2026-08-17**: the brokerage-threshold drift-alert gap ("all 3 real `brokerage` nodes sit below `CAPITAL_AT_STAKE_THRESHOLD`") no longer holds — `CAPITAL_AT_STAKE_THRESHOLD` is $5,000 today (not $10k), and all 3 real `brokerage` nodes (ETHU/AGQ/JNUG) carry `starting_notional=$6,000` with `trading_enabled=True`, so they now clear the threshold and can trip the drift alert same as any other capital-at-stake node. No code change made — the gap closed itself when the threshold moved. **Not attempted (real order-placement hot path, too risky for a quick fix)**: order-retry-dedup's extra broker round-trip on every placement — the described improvement (thread the `check_order`-adjacent snapshot through instead of re-fetching) touches the already-heavily-reviewed retry/dedup logic on the live-money placement path. **Not attempted**: tax-forecast baseline year-rollover (2027 carryforward evaporating) — real tax-policy question (does the baseline literally carry forward at its remaining value?), not a code question. Full detail: `docs/deep_backlog.md`.

## [live-trading][coverage] Phase 1 BUILT 2026-08-15 — coverage_events write-attribution (`source` column, fixed enum, real-execution-first rollout, best-effort backfill)
Nullable `coverage_events.source` column + `COVERAGE_EVENT_SOURCES` taxonomy (`'daemon'`, `'fixture:<name>'`), threaded through the real order-placement/fill chokepoints (`check_order`/`approve_and_record`, `_reconcile_fill`/`drain_fill_queue`/`_place_stop_loss_for_position`, the stream activity-message handler) and `stage_check_order_guard_scenarios.py`. `schwab_client.py` needed no changes — inherits attribution automatically via `approve_and_record`'s default. New `scripts/backfill_coverage_event_source.py`, tested against a scratch copy of the latest `output/live_backups/` snapshot (not run against the real live DB — left for you): correctly tagged 42 `STAGE_GUARD_TEST` rows. 6 new pinned tests, 105/105 across the full relevant sweep. **Still open**: extending `source` to lower-priority call sites (`paper_trading.py`, `signals_compute.py`, `signals_handlers.py`) was explicitly deferred, piecemeal rollout by design — pick up when those become the next pain point, not a blanket migration. **Also still folded in here**: the previously-standalone "closed-trade reconciliation" item (audit `trade_log` against real broker order history — full design in `docs/design.md`'s 2026-08-15 (later still) entry, OPEN DECISIONS unresolved) — resolve its design questions as part of this same effort when picked up further, not standalone.




## [live-trading][coverage] Idea, raised 2026-08-15 (peer session "planner") — detect "this write has no attributable source" as a first-class queryable anomaly, applied to `schwab_auth`'s token writes specifically
Same principle as the `coverage_events` `source`-column design (already scoped/partially built this session) — turn an unattributed-write gap into something immediately flaggable instead of requiring a multi-tool-call forensic investigation every time it comes up. Directly actionable in this repo (unlike the hooks-level idea above): add a `SCHWAB_TOKEN_PATH` env override (matching the existing `SCHWAB_STATE_DIR` isolation pattern) plus write-attribution logging (pid/argv/cwd) on every `schwab_auth` token write. Not scoped, not built.



## [portfolio] Idea, raised 2026-08-14, substantially expanded 2026-08-16 — portfolio-level rebalancing across a 3-bucket structure (safe / risky / algo); real design work, not ready to build, blocked on the user's own hourly-data purchase for a real TQQQ volatility sim. Full detail: `docs/deep_backlog.md`. **Do not resurface** (or anything else genuinely blocked on that same data purchase) until the user has the data or asks directly (confirmed 2026-08-15).

## [live-trading][coverage] Deferred 2026-08-15 (user's call) — automated SLA-based coverage-gap troubleshooting, per the new 3-bucket testing philosophy
User's typed notes, extending tonight's testing-philosophy work (`docs/grid_ticker_coverage_promotion_process.md`'s new "Testing philosophy" section, and the script-based test-plan item above): reframe as a default assumption — nearly everything should be fake-venue-testable, nearly everything left over should be canary-testable, and only the genuine remainder needs a live test or an explicit accepted-risk exception (`not-prod-required`, the pattern already in use). Two phases: **Phase 1** = classify which bucket each scenario belongs to (this is what tonight's `coverage_proof_matrix.py`/`coverage_registry.py` BEST_HARNESS work already does). **Phase 2** = verification — since fake-venue tests can run anytime, coverage there is checkable on demand; canary/live coverage is not (it depends on organic or scripted triggers). New idea: an automated job that flags a real problem if a scenario doesn't get canary or live coverage within a specified SLA window — "troubleshoot at night," rerun when active development pauses. Not scoped: what the SLA window should be per scenario, where this job lives (a new script? folded into `coverage_regression_watch.py`, already built earlier today?), what "flag a problem" means (Slack alert? backlog entry?). Related, separately raised: **weekly code sweeps** — build scenarios and iterate them through the test harness on a cadence; may already partially overlap with the `weekend-cleanup` skill's existing weekly "audit the week's commits for new features with no proportional test coverage" pass (`project_weekend_cleanup_skill` memory) — check for overlap before building something new. Also raised: **quarterly node reviews/rebalance** — distinct from the existing "quarterly resweep cadence" backlog item (that's about resweeping backtest data; this may be about reviewing/rebalancing live node capital allocation) — needs disambiguation with the user before scoping. **Deferred 2026-08-15**: hold until the fake-venue harness work progresses further — this item's own Phase 1 premise leans directly on the harness/`BEST_HARNESS` classification work, not ready to scope ahead of it.


## [live-trading][tax] Realized + unrealized pieces BOTH done 2026-08-15; cash-sweep mechanics + Nov harvest decision DROPPED; 3 items still open (Section 1256 MTM question, liability lower-bound, Dec-Jan pause idea). Full detail: `deep_backlog.md`.


## [live-trading][coverage] Built 2026-08-15 — abnormal-drift liquidity-signal alert: implemented, tested, paired-reviewed; no live proof yet (organic-only, same as daemon_exception_survival); real consecutive-trades detection spec given 2026-08-15, evening-report counter not yet built
**Real detection spec, 2026-08-15 (user's own concrete framing)**: don't act on a single trade's drift as evidence — track CONSECUTIVE trades (sized as expected, not already resized) showing drift at or above a threshold, provisionally **0.5%** (leaning toward 0.5% rather than 1% specifically because it's entry AND exit sides combined — "can't lose 1% every trade"). Once 3 consecutive trades hit that, that's real evidence something's wrong, not noise. **Scope for this phase: DETECTION ONLY** — the alert mechanism above is already built; what's not built is the running counter itself: surface it in the evening report as `0/3`, `1/3`, `2/3`, `3/3` toward the consecutive threshold. This directly ties into the "trim/liquidity-capped entry sizing" item below (item 18) — notional-resizing is a separate, later follow-up once detection proves out (user doesn't expect this to fire often), not built alongside the counter. Not built yet — spec only.
Design settled 2026-08-14 evening (original full design writeup still only in this file's prior revision / git history — not yet moved to `docs/deep_backlog.md`, left for session close per this project's convention). **Built**: `signals_db.check_abnormal_drift` (`signals_db.py`), invoked from the single real chokepoint every entry/exit fill passes through — `open_position()`/`close_position()` — rather than threaded into their 15+ call sites across `signals_notify.py`/`paper_trading.py`/`signals_handlers.py`. Real (non-paper, non-`is_dry_run_sim`) fills only, gated on `signals_helpers.has_capital_at_stake` (same gate as every other routine/anomaly alert since 2026-08-08). Threshold: the 0.5% placeholder was replaced with **3.0%** (`signals_db.ABNORMAL_DRIFT_THRESHOLD_PCT`, env-overridable), calibrated off the same-day real drift-distribution audit (`docs/research_log.md`'s 2026-08-15 entry: excluding 3 known-anomalous backdated catch-up rows, real drift is mean -0.3% to -0.5%, std 1.4-1.9%, n=25-33) — explicitly flagged in-code as a first-pass calibration on a small sample, revisit as more real trades (especially SL exits) accumulate. Escalation: 2 max Slack posts/ticker/day, a 3rd+ same-day breach still logs a `coverage_events` row (`result='suppressed_daily_cap'`) rather than being silently lost. Alert-only, never automatic. New Grid row: `scripts/coverage_registry.py`'s `abnormal_drift_alert` (currently `wired-never-fired` — no live proof yet, not forceable via node config, same organic-only shape as `daemon_exception_survival`). Test coverage: `tests/test_fake_broker_abnormal_drift_scenario.py`, 6 scenarios (entry/exit breach alerts, below-threshold no-op, below-capital-at-stake no-op, dry-run-sim no-op, 2/day escalation cap), driven through the real `open_position()`/`close_position()` chokepoints. **Paired Opus review (independent-cold + contextual, both agreed, no rebuttal conflict) found one CONFIRMED MEDIUM**: `check_abnormal_drift`'s real Slack HTTP POST was originally issued *inside* `_position_lock` (the mutex serializing every concurrent real position open/close across the daemon) — fixed same session by deferring the Slack call to after the lock releases in both `open_position` and `close_position`. Three LOW findings acknowledged, not changed (deliberate/low-value): threshold is read once at import (a change needs a daemon restart, consistent with other env-derived constants here); drift is recomputed inline with the same formula rather than reading back `trade_log`'s own column (verified byte-identical, no correctness issue); the 2/day escalation counter is keyed on ticker alone, not (ticker, account) — matches the design's literal wording, a latent edge case only if a ticker ever ran two real capital-at-stake nodes in different accounts simultaneously. Full suite re-run same session to confirm no regressions (see session notes for the exact count). Not committed — left for the user's review.

## [portfolio][design] Idea, raised 2026-08-13 evening, refined 2026-08-14 (renamed "trim") — real automation need is liquidity-capped ENTRY SIZING, not skim/reserve at all (revisit when a node's real notional starts approaching the 1%-of-liquidity ceiling)
User's framing, mid-conversation about extending skim (the equity-high-triggered reserve mechanism) to canary/dry_run: the actual automation that needs to exist is unrelated to skim's reserve/redeploy design entirely. It's a liquidity-aware CAP ON ENTRY SIZING: compare a node's real (compounded) notional against this project's existing `liquidity_dollars_per_day` metric (`avg_vol_10d * last_price * 0.01`, already used for candidate screening in `scripts/candidate_full_review.py`/`candidate_summary_report.py::liquidity_dollars_per_day`) — once the node's notional exceeds that 1%-of-daily-$-volume ceiling, the NEXT buy's sizing should shrink accordingly, not stay at the full compounded notional. **Explicitly no real order placement needed** (user's words) — this is a sizing-calculation change (wherever `target_notional`/shares get computed for a real BUY, likely `signals_helpers.buy_order_sizing` or the node's compounding-driven notional growth), not a new order type, not a reserve/redeploy mechanism, not a reversal of skim's "alert-only, no real-order code" design (`docs/design.md`, 2026-08-1x) — that design stays untouched, unrelated to this. Related: `[[project_skim_liquidity_takeprofit]]` memory anticipated liquidity limits forcing regular position trimming as size grows — this is that idea, but scoped correctly now (sizing cap, not skim).
**2026-08-14 refinement (user's typed notes)**: calling this "trim," not skim — genuinely a different mechanism, confirmed distinct enough that **skimming itself has been stopped** as a consequence of this reframing. Concrete calc proposed: once the semi-arbitrary liquidity boundary is hit, the next BUY doesn't use all available cash — compute live and take `MIN(available_cash, last_sale)` (last_sale explicitly **not including add-on** capital) as the sizing basis. User's framing: "this is like a meta take profit." Also raised, not yet scoped: test position sizing specifically for the case where an add-on trade fires (does trim's cap interact correctly with the add-on doubling, or does add-on need its own separate cap check). Still not started — no node is near the liquidity ceiling today, no urgency, but the calc method is now concrete enough to implement when it's time.

## [backtest][live-trading] Investigated and closed 2026-08-21 (later still) — arm/TP check only looks at the bar's Close (never High), but live and kernel already agree — not an execution gap
Traced directly in code while re-investigating the SOXL divergence: `backtester.py`'s `_simulate_trail_both` arm check (`elif cp >= tp_price:`, ~line 806) uses only `cp` (bar Close), never High. `strategies.py`'s live-side mirror (`TrailingBothZScoreBreakout.check_exit`) explicitly gates the arm check behind `at_bar_close` (returns early if not a bar-close call, ~line 440-442) — so live only evaluates arm once/hour too, at whatever price is current at that moment, same as the kernel's Close. **Closed, not actionable**: live already faithfully executes what the kernel does here — no live-vs-backtest divergence to fix. The GDXU finding that prompted this (`docs/research_log.md`'s 2026-08-20 (late) entry: a real intrahour Close crossing the arm threshold, missed because the hourly Close-only check never saw it) is a hourly-vs-finer-resolution question, not a live-execution gap — the project's real goal is live faithfully executing the validated (hourly) backtest, not improving backtest resolution via a new sweep. No kernel change, no live change.

## [backtest] HIGH, raised 2026-08-21 (night) — v5/v5.1 candidate selection (and every overlay tuning: drought confirm_days/vol_gate, add-on) was chosen entirely by comparing KERNEL-backtested numbers against each other; ground truth shows the kernel is systematically optimistic in a way that likely varies by config — the config/ticker that "won" a sweep comparison may not be the one that would win under real minute-resolved execution. Scope is the whole selection pipeline, not one ticker — distinct from (but the root cause underlying) the SOXL/HIBL entry below. User's call: start the fix tonight, not deferred. (revisit when: GT-kernel candidate-selection replaces robust_alpha)

## [backtest][testing] Found 2026-08-22 (GT-kernel-obsolescence audit) — `docs/watchlist_candidate_checklist.md`'s checks 9/10/13 (same-day-block sensitivity, its 70/30 stability check, walk-forward N-fold) all rank on `MIN(possible,pessimistic,certain)` robust-alpha — same root cause as the HIGH item above, concretely located in the actual promotion gate document. Checks 1/6/8/11/12/14-17 (macro/trend, splits, trade-count fluke, drawdown, config-drift baseline, sponsor check, wash-sale, open-orders) are kernel-independent, not affected. Checks 2/3/7 (trailing-buy/sell 5-min resolution drift, fill-logic optimism) are themselves already partial acknowledgments that the hourly kernel is imprecise — arguably superseded outright by GT once it's trustworthy, rather than needing a redesign. Not scoped: whether checks 9/10/13 get a GT-aware variant or get dropped once GT-derived worst-neighbor-CAGR (the plan's new bar) replaces robust-alpha as the ranking metric entirely.

## [backtest][testing] Found 2026-08-22 (GT-kernel-obsolescence audit) — entire candidate-report pipeline (`candidate_5min_report.py`, `candidate_checklist_report.py`, `candidate_full_review.py`, `candidate_summary_report.py`, `checklist_v65.py`, `train_test_split_check.py`, `walk_forward_check.py`) computes headline numbers (CAGR, alpha, cliff-safety) exclusively from hourly-kernel `possible`/`pessimistic`/`certain` — zero GT-kernel path exists in any of them today. This is the literal "run the report" step of the user's real promotion workflow ("after we finish a backtest run... you run the report, i do some filtering, i provide results back"), so it's the most load-bearing gap of this whole audit. `candidate_5min_report.py::_node_from_row` already has a known, related gap (`pessimistic`/`certain` have no real trade-generation path either, only a scalar MIN() input — see the tabled 2026-08-09 item below) that GT work would need to resolve or route around, not just add a 4th resolution column to. Not scoped — needs a real design decision on whether these reports gain a `--kernel=gt` mode or get replaced once GT-derived metrics stabilize per-ticker. **Same root gap, second consumer found 2026-08-22**: `scripts/evening_status.py`'s Part 3 divergence check (`compute_divergence`, 30d trailing, 10pp threshold) also compares real trade outcomes against a kernel replay of the same period via `get_backtest_trades_in_window`/`get_trades_and_bars_since` — confirmed directly (`paper_vs_backtest_reconcile.py:117-149`) that this replay uses the hourly kernel's `possible`-style fill logic, not real fills. Not a threshold-recalibration problem — once a GT-aware replay path exists (same fix as the report pipeline above), this check should get MORE trustworthy, not just differently calibrated, since GT models real minute-level execution the way live actually experiences it (independent GT re-implementation already matched SOXL's real 2026-08-20 trades to <0.06%, per the ground-truth-kernel-rebuild plan). Route both consumers through the same GT-aware replay function once it exists, don't fix them separately.

## [backtest][testing] Found 2026-08-22 (GT-kernel-obsolescence audit) — `run_optimization_sweep.ROBUST_ALPHA_SQL` already degrades safely for GT/v6 rows (`COALESCE` on NULL pessimistic/certain, confirmed in a code comment at line ~736) — the SQL foundation for kernel-aware ranking already exists. But `scripts/top_safe_nodes.py`'s `best_safe_node()` (and everything downstream of it in the candidate-report pipeline above) hardcodes `metric="robust_alpha"` with no CLI/API path to select GT rows or filter by `kernel_version` specifically. Smaller gap than the report-pipeline item above — the query layer is ready, the report/CLI layer isn't. Not scoped.

## [live-trading][testing] Checked 2026-08-22 (GT-kernel-obsolescence audit) — canary A-F infrastructure and the Trade-Flow Accountability Grid are NOT affected by the GT-kernel work; closing the loop so this doesn't get re-litigated
Confirmed directly (`docs/grid_ticker_coverage_promotion_process.md`, `scripts/seed_scenario_expectations.py`): every canary scenario (`canary_full_lifecycle`/`canary_early_sl`/`canary_pinned_entry`/`canary_overnight_carry`/`canary_market_buy_exit`/`canary_time_exit`) uses deliberately detuned hair-trigger config (z=0.1, window=5) chosen to reliably exercise an EXECUTION code path, not a robust-alpha-ranked "best" config — they test whether the code does the right thing when a signal fires, independent of which kernel picked any real candidate's parameters. Same for `scripts/coverage_registry.py`'s Grid and the fake_broker truth tables — all execution-mechanics proof, not candidate-quality proof. None of this needs to change or wait on GT-kernel work. `docs/new_mechanism_promotion_standard.md`/`docs/overlay_parameter_robustness_process.md`/`docs/cliff_safety_query_checklist.md` also checked directly — no `possible`/`pessimistic`/`certain`/`robust_alpha` references, not in scope either.

## [live-trading][backtest] HIGH — Found 2026-08-21 (night), twice adversarially reviewed — SOXL/HIBL real-execution CAGR goes NEGATIVE once corrected for real minute-level fills; needs independent clean-room re-verification before ANY live watchlist action
Full writeup: `docs/research_log.md`'s 2026-08-21 (night, continued session) entry. Real minute-level ground truth (Massive.com 1-min data, 2yr window, checkpoint bugs fixed, live-realistic continuous SL) for all 12 real capital-at-stake tickers: SOXL **-5.1% CAGR**, HIBL **-2.9% CAGR** — both negative. DFEN +11.8% (also flagged by user as too low to be worth it, not negative). All other 9 tickers stayed strongly positive (KORU +41.3%, LABU +16.3%, both specifically guessed-at-risk by the user but fine). Root cause: SOXL/HIBL's backtested edge is mostly hourly-bar fill optimism — strict kernel shows +109% CAGR for SOXL over the same window, dropping to +25.7% once WAIT-resolution is refined with real minute data (kernel's own once-per-hour cadence), then to ~-5% once entry is modeled as genuinely continuous (matching live's real broker-side `TRAILING_STOP` order, not bar-sampled). This is a real, twice-independently-reviewed finding (2 separate Opus adversarial review rounds each caught real bugs: arm-check gating, wrong-kernel dispatch, a sparse-coverage guard that silently reverted 49% of one script's "ground truth" entries back to optimistic hourly fills, wrong checkpoint minute for 1-min-vs-5-min bars, an incoherent SL-suppression+continuous-entry hybrid) — not a stray number. **User's explicit call: do NOT touch SOXL/HIBL/DFEN's live state based on tonight's numbers** — this project's own standing principle (`feedback_live_must_match_validated_backtest`) requires proof before action, and tonight's tooling was iteratively patched in place all night, even with review rounds. **Next step, dispatched as a background job tonight**: a genuinely independent, from-scratch re-implementation of the minute-resolution ground-truth walk (not another patch on `scripts/sim_3col_sl_fillbar_compare.py`/`scripts/sim_minute_ground_truth_full.py`), cross-checked against individual real trades by hand (same technique used to hand-verify SOXL's 2026-08-20 trade this session), before any live watchlist decision. Real minute data now covers all 12 tickers (`cache/research/minute_data/*_1m.csv` — 6 tickers had 2yr already, 6 more fetched fresh tonight at 5yr, 2021-08-22 through 2026-08-21).

## [live-trading][coverage] Revisited 2026-08-16 — real miss found: CURE/TMF/ERX were never added to `SCHWAB_AUTOMATION_TICKERS`, fixed; stale pending_buys rows investigated and cleared; only the daemon restart itself remains (yours to run)
Checked real state 2026-08-16 (revisit date): CURE and TMF both had real `pending_buys` rows from 2026-08-14 09:30:00, `order_placed=0`, zero reminders fired in 2 days. Root cause: none of CURE/TMF/ERX were in `.env`'s `SCHWAB_AUTOMATION_TICKERS` — same gap-shape as the FAS-missing-from-the-list bug from the FAS/FAZ consolidation. **Fixed**: CURE/TMF/ERX added to `.env`'s `SCHWAB_AUTOMATION_TICKERS`. **Stale pending_buys question resolved 2026-08-16 (later)**: traced `_scan_buy_signals`'s `already_pending` check (`active_signals.py:508`, keys off `node['id'] in pending_wl_ids`) — a node with an existing `pending_buys` row is SKIPPED by the entry scan entirely, so nothing "picks up" a stale row and places a real order off 2-day-old price context; the actual risk was silent blocking (no fresh signal ever detected for that node), not a safety issue. Since `order_placed=0` means no order was ever sent to Schwab for either row, both were safely cleared via `signals_db.clear_pending_buy_by_wl_id(224)`/`(225)` (the sanctioned function, not raw DELETE). `signals_invariants.py` confirmed clean after. **Only remaining step: a daemon restart** to pick up the `.env` change — yours to run, no urgency. ERX has no pending buy or trade yet at all — never triggered, `time_exit_trigger_armed` (226, replaces paused SH) still waiting on its first real entry.


## [live-trading][coverage] Found 2026-08-13, decided 2026-08-19 — GLOBAL_ORDERS_PER_MINUTE capacity, dispatched
Confirmed live 2026-08-19: canary/dry_run order attempts DO consume real slots in this cap — `schwab_safety.approve_and_record` unconditionally calls `check_order` and unconditionally appends to `recent_order_timestamps` before checking dry_run status at the very end (only decides whether to actually submit to the broker, not whether to count). So ~25 hair-trigger canary nodes genuinely contend with real-money order attempts for the same 12/min budget. **Decision (user's call, 2026-08-19)**: raise `GLOBAL_ORDERS_PER_MINUTE` 12→25, AND reorder node processing to a 3-tier priority (real capital-at-stake nodes first, other live/automation-scoped nodes second, canary nodes last) so that if the cap ever does get contended, a canary attempt is more likely to be the one blocked, not a real order. Dispatched to `research`, queued behind the current batch. (The 3 other findings from this same 2026-08-13 review — `coverage_grid_summary.py --date` bug, `repoint_canary_scenario_expectations.py` hardcoded wl_ids, `add_node`'s missing dedup fields — separately dispatched as Tasks #9/#10/#8, 2026-08-19.)

## [live-trading][coverage] Revived 2026-08-14, refined from the tabled 2026-08-12 form — "snooze a ticker/scenario combo after a positive coverage signal," now keyed on CODE CHANGE not elapsed time
Original 2026-08-12 idea (existing `coverage_snoozes`/`snooze_coverage` mechanism could support this) was tabled because a time-based snooze isn't trustworthy — execution code changes daily, so "proven N days ago" doesn't mean "still true today." **2026-08-14 refinement (user's typed notes) sidesteps that exact objection**: snooze until the relevant code actually changes, not for a fixed duration. Concrete example given: `post_fill_topup` has real live proof ~3 times already (see `docs/deep_backlog.md`'s 2026-08-10 entry) — no need to keep chasing fresh evidence for it under the new SLA-troubleshooting idea (see the item above) as long as the code path it exercises hasn't been touched since. "Could be tonight" — the same-bar cooldown fix (HIGH item below) touches order-placement-adjacent code, so this snooze would need to correctly invalidate/reset for any scenario whose code path that fix actually touches, once it ships. Needs: a way to map a scenario_key to "which files/functions its proof actually depends on" (not just count days), so a snooze can be invalidated precisely when relevant code changes, not on a blanket calendar-time or blanket any-commit basis. Not scoped, ties directly into the SLA-troubleshooting idea above — likely the same build, not two separate ones.






## [backtest] Idea, raised 2026-08-09 — does this mean-reversion strategy do anything interesting on recently-IPO'd stocks (post-IPO drawdown/mean-reversion pattern)? (revisit ~2026-09-10)
Prompted by SPCL's real underlying (SpaceX Class A shares, since a June 2026 mandate change) having given back almost all its gains since IPO. Two real caveats before chasing this: (1) this system's universe is leveraged ETFs, not individual common stocks — testing this needs either fetching real individual-stock price history for a basket of recent IPOs, or checking whether any already-screened candidate tickers happen to be linked to recently-IPO'd names; (2) SPCL's own data can't be the test case (mandate-change contamination, see `deep_backlog.md`'s 2026-08-09 disqualification entries). Not started.

## [backtest] Tabled 2026-08-09 (later still) — full `backtest_cache` certain-column recompute; possible stays the working default (revisit ~2026-09-10) (revisit when: GT-kernel candidate-selection replaces robust_alpha)
**Update 2026-08-21 (very late)**: real minute data (Massive.com, `MASSIVE_API_KEY`, see `docs/research_log.md`'s 2026-08-21 (very late) entry) now confirms directly, not just theoretically, that BOTH `possible` and `pessimistic` are equally-wrong extreme assumptions — real intrahour Low-vs-High ordering sits at 49.5%-51.8% (essentially a coin flip) across all 6 real live tickers checked. This doesn't resolve the "should possible stay the default" question on its own, but it's now backed by real evidence that a single globally-consistent resolution (of any kind) can't represent what actually happens — worth folding into whatever picks this item back up. `scripts/annotate_hourly_bar_ordering.py` gives per-hourly-bar ground truth (`{ticker}_hourly_ordering.csv`) for ~2 of the ~3yr cached history, reusable directly.
User's explicit call: table `certain`/`pessimistic` tooling work for now, use `possible` as the standard resolution going forward (also the empirically stronger/more-frequently-winning resolution per the same-node possible-vs-corrected-certain check — possible wins 45/81 tickers, certain 26/81, and possible's wins are on average much larger). Kernel fix stays in place (real bug, harmless to leave fixed) but no urgency behind the recompute or new tooling below. Revisit only if a future decision specifically needs certain/pessimistic as a real execution path.
`_simulate_trail_both`'s certain branch had two real bugs (frozen-trigger case missing + Close-crediting an unreachable price), both fixed in `backtester.py`, plus a new lean `_simulate_certain_only` kernel/`run_backtest_certain_only` wrapper for a cheaper recompute (no possible/pessimistic bookkeeping needed). Every `strategy_return_certain`/`alpha_vs_spy_certain` value in the entire v5 `TrailingBothZScoreBreakout` cache (5,040,750 rows, 82 tickers) was computed with the old buggy logic. **A paired Opus session-wrap review (CONFIRMED HIGH) caught that the first-pass Close-crediting fix (crediting the bar's raw Close) was itself just a different unproven guess — since fixed a second time to credit `min(buy_trigger_prior, high)`, the genuinely tight worst-case-price bound both reviewers verified mathematically.** Two stale Python mirrors that had drifted from the kernel fix (`scripts/export_trades.py`, `scripts/verify_fill_resolution_accuracy.py` — the latter being the tool whose "possible is 94% accurate" finding justifies the possible-as-default decision below) were also fixed to match, and a new pinned test (`tests/test_certain_resolution_fix.py`, 4 tests) now enforces the frozen-trigger fill, the corrected bound, and `_simulate_certain_only`'s exact parity with the bundled kernel. **Correction: an earlier note here said "KORU flips to a real loss" — that was from a since-discarded hand-mirror missing the frozen-trigger fix; the real kernel-verified value is positive.** Per-ticker spot-check (against the final, doubly-corrected kernel) still not yet rerun end-to-end — the 2x-11x drop range recorded earlier reflected the interim (Close-raw) fix, not the final (min-bound) one; the actual per-ticker numbers will shift again and should be treated as stale until re-derived. Plan: add new columns (`strategy_return_certain_fixed`/`alpha_vs_spy_certain_fixed`, matching how pessimistic/certain themselves were added in 2026-07-20) rather than a new version tag, populate via the lean kernel over all existing rows (defeats `dispatch_parallel_grid`'s cache-skip optimization since every existing value needs overwriting, not just missing ones — same scale as a real sweep campaign). Per standing convention, the user runs this, not the agent. Full detail: `docs/research_log.md`'s 2026-08-09 (later still) entry.

## [backtest][new-strategy] Idea, raised 2026-08-21 (very late) — gap-fill continuation as its own tradeable signal, distinct from the live-matching thread it was found inside (revisit when: GT-kernel candidate-selection replaces robust_alpha)
Found while auditing the same-bar-SL fix (see the two entries below and `docs/research_log.md`'s 2026-08-21 entries): for SOXL specifically, of 39 real carried gap-fills (a trailing-buy filling via a genuine overnight/opening gap — price already crossed the trigger before the bar opened) over the full ~3yr cached history, 24 (~62%) were followed by price continuing to fall through the fixed_sl level within that SAME hour. That's a real, sharp, distinct pattern from "the strategy mean-reverts generally" — a gap-fill specifically (unlike a gradual bounce-fill) looks like a fast, one-directional move that keeps going, not a stable bounce. **Confirmed across all 6 real live TrailingBoth tickers (2026-08-21, same night), with 95% Wilson CIs**: LABU 71.4% (5/7, CI 36-92% — too small a sample to trust on its own), SOXL 53.7% (29/54, CI 41-66% — the one strong data point, reliably above baseline even at the low end), JNUG 35.5% (22/62, CI 25-48%), KORU 19.0% (4/21, CI 8-40%), DPST 16.7% (8/48, CI 9-30%), HIBL 14.3% (9/63, CI 8-25%). Rate tracks cleanly with each node's `trail_buy_pct` relative to `fixed_sl` (LABU=9%/2%, SOXL=3%/2%, vs. HIBL/DPST=2%/3%) across the 5 tickers with adequate samples (SOXL/JNUG/KORU/DPST/HIBL) — a wider required bounce to trigger the fill implies a bigger, faster prior move, plausibly explaining why those fills are more likely to keep going rather than reverse. LABU fits the pattern directionally but its own number shouldn't be cited as evidence — n=7 is too thin to distinguish from noise. **Second candidate mechanism, raised same session, not distinguished from the first**: could the continuation be overnight/pre-market thin-liquidity mispricing correcting once full-session liquidity arrives, rather than (or in addition to) genuine momentum? A partial check using regular-session 9:30-bar Volume (already cached) found continuation cases have higher volume than reversion cases (SOXL: median 28.4M vs 21.8M) — directionally consistent with the story, but NOT diagnostic, since a bigger/faster move plausibly draws more volume regardless of cause; doesn't distinguish this from the trail_buy_pct/momentum explanation above. A real test needs actual pre-market/extended-hours volume data (`yfinance` with `prepost=True`), which isn't in the current cache — a genuinely new data-fetch task, not started. Related to (but distinct from) the same-session idea of a dedicated 9:30-10:30-bar volatility strategy. **Worth prioritizing over a typical raw idea**: rates up to 71% are well above this strategy family's own baseline win rate (~15-16% per this session's earlier discussion) — if a real, validated edge exists here, it's a materially stronger signal than what the core mean-reversion strategy runs on today, not just a marginal one. **Not started, not validated** — single ticker, single config, ~3yr sample. Needs: does the ~62% rate hold across the other live tickers/configs, does it hold out-of-sample (fit-half/test-half per `docs/overlay_parameter_robustness_process.md`'s standard), and what the actual tradeable action would be (skip gap-fills entirely? tighter stop specifically for gap-fills? something else?) before this is anything more than an observation. Per this session's own standing rule (`smarter = dumb until it's proven`), do not act on this without going through the project's real validation process first.

## [backtest] Idea, raised 2026-08-21 (later still) — per-trade worst-execution-path resolution, distinct from robust_alpha's current per-simulation MIN(possible,pessimistic,certain) (revisit when: GT-kernel candidate-selection replaces robust_alpha)
Raised while resolving tonight's SOXL same-bar-SL investigation, discussing why the backtest can never fully resolve fill sequencing (see `docs/research_log.md`'s 2026-08-21 entries): today's `robust_alpha = MIN(possible, pessimistic, certain)` runs three GLOBALLY-CONSISTENT full simulations (every trade in the whole history resolved the same way) and takes the worst total return. A genuinely different, more pessimistic bound: NOT "take one of the three existing trade lists and adjust each trade's price" (an earlier, wrong framing of this same idea, corrected 2026-08-21 later still) — that's invalid for the same path-dependency reason as everything else this session: which trade even happens next (entry timing, price, whether it happens at all) depends on how the PRIOR trade resolved, so you can't know trade 2 by patching trade 1 in a pre-computed list. The real build is a fresh bar-by-bar walk-forward simulation (same style as this session's `simulate_same_bar_sl`/`simulate_gapfill_only_samebar`) where, AT EVERY live decision point (does a signal fire, does it fill and at what price, does it exit and at what price), the worst-case resolution among possible/pessimistic/certain is chosen in that same walk — producing a trade sequence that can differ completely (count, dates, everything) from all three pure resolutions, not a variation on any one of them. Per this session's own standing rule (`smarter = dumb until it's proven` — see `feedback_live_must_match_validated_backtest` memory), this is a new model, not a refinement, and needs its own validation before adoption — not something to build ad hoc. Relates to, but distinct from, the two possible/pessimistic/certain items below (both tabled 2026-08-09, revisit ~2026-09-10) — read together when either is picked back up.

## [backtest] Tabled 2026-08-09 (later still) — overlay/candidate-reporting tooling is possible-only; pessimistic/certain have no real trade-generation path, only a scalar `robust_alpha` MIN() input (revisit ~2026-09-10) (revisit when: GT-kernel candidate-selection replaces robust_alpha)
Tabled alongside the recompute item above, same user call — `possible` stays the standard, no urgency to build a certain-aware overlay path right now.
Traced directly in code: `candidate_5min_report.py::_node_from_row` returns `row['strategy_return']` (possible) unconditionally for all 4 candidate types, including "best certain" — so even a node selected by ranking on `alpha_vs_spy_certain` gets its `abs_return_pct`/CAGR/years/trades/drought/add-on overlay computed entirely from `possible`'s trade sequence via `get_trades_and_bars` (which only ever calls `simulate_trail_both_annotated`, a possible-only mirror). `pessimistic`/`certain` have never had a real trade-generation path anywhere in this pipeline — only a scalar return feeding `robust_alpha=MIN(...)`. User's framing: `possible`/`pessimistic`/`certain` (once correctly computed) are three viable real execution paths, not just a fill-ambiguity artifact to collapse to one number — `possible` is the most-likely default, but `certain` in particular deserves its own real overlay/reporting path since it requires no guessing at all. Not started — needs a real certain-trades-aware variant of `get_trades_and_bars`/`ensure_overlay_for_node`/the report's headline-number logic, using the now-verified `run_backtest_certain_only` kernel. Full detail: `docs/research_log.md`'s 2026-08-09 (later still) entry.

## [backtest] Resolved-as-practice 2026-08-10, formal decision deferred to 2027 — CAGR-first (not raw robust_alpha) is the user's real, current candidate-selection convention
Full detail: `docs/research_log.md`'s 2026-08-08 (later still, 3) entry (original annualized-excess framing) + `docs/deep_backlog.md`'s 2026-08-10 HIBL/GDXU entry (real confirmation: CAGR-sorted picks beat robust_alpha-sorted picks on CAGR, cliff-safety, AND drought-overlay validity for both tickers checked). User confirmed 2026-08-10: this is already how they pick in practice (self-applied, not delegated), only needs revisiting when a genuinely new research-candidate list comes up — infrequent enough not to need standing tooling/decision work. Deferred, not urgent: (revisit ~2027-01-01).

## [backtest] Open, broadened 2026-08-08 (later) — "possible" fill-resolution confirmed empirically more accurate than pessimistic/certain at 66 signals/17 tickers; convention change itself still not decided (revisit when: GT-kernel candidate-selection replaces robust_alpha)
Full detail: `docs/research_log.md`'s two 2026-08-08 entries. Original 25-signal/6-ticker check (possible closest 24/25, mean abs 0.107% vs 0.871%/1.374%) reproduced at 66 signals/17 tickers (possible closest 62/66=94%, mean abs 0.166% vs 0.697%/0.895%) — same ranking, same order of magnitude, fully independent ticker set aside from 6 overlaps. Bounded by yfinance's fixed 58-day 5-min window (more tickers now checked, not more elapsed time) — a rolling re-check over a future time period would be the strongest next confirmation. Real implication if the user decides to act: `robust_alpha`'s `MIN(possible,pessimistic,certain)` convention may be needlessly conservative project-wide. Not decided or actioned — changing the convention is cross-cutting (`ROBUST_ALPHA_SQL`, `top_safe_nodes.py`, prune's island selection) and would need the `backtest-change-rollout` skill's staged process, not a quick edit.

## [live-trading][security] Raised 2026-08-17 (evening) — auto-fill detection should default ON for automation-scoped tickers, not opt-in per node
Found while fixing SOXS's untracked resting order (`pending_buys.order_id` was NULL despite a real broker order existing — fixed, see `backlog_resolved_recent.md`): `schwab_safety.auto_fill_detection_enabled`/`node_auto_fill_detection_enabled` both default `False`, so every live automation-scoped node requires an explicit Slack-button (or manual `enable_auto_fill_detection`/`enable_node_auto_fill_detection`) opt-in before fills get auto-recorded — otherwise the user must tap "Filled" manually, which for an intraday resting order causes real drift for a strategy meant to run automated. Checked real state: only 2/17 live automation-scoped nodes (ERY, RETL — both staged-test nodes) had it on; the other 15 (real capital incl. DPST/SOXL $10k, AGQ/ETHU/JNUG $6k each) didn't. **State fix applied same session**: all 17 nodes flipped on directly via `schwab_safety.enable_auto_fill_detection`/`enable_node_auto_fill_detection` (not a code change, just the persisted JSON flags) — so today's real gap is closed. **Confirmed 2026-08-20 (planner, post 12-ticker promotion pass)**: every node from that pass (SOXL/DPST/KORU/AGQ/GDXU/NUGT/JNUG/DFEN/UGL/WEBL/HIBL/LABU, ids 92/197/202/203/229-236) already has both the ticker- and node-level flag set True — `add_node`'s `initialize_auto_fill_detection_for_new_node` hook worked correctly for all of them, no gap for that batch. **Still open, now dispatched**: the actual code-level default (in `schwab_safety.py`) should flip so a newly-added live automation-scoped node doesn't silently inherit this manual-confirmation drift risk again. User's call, 2026-08-17: agreed this should always have been the default. Dispatched to `coder` 2026-08-20 as its own scoped item (touches `schwab_safety.py`, review-gate applies) — note for reviewers: `node_auto_fill_detection_enabled`'s own docstring currently argues explicitly FOR a False default ("this flag *grants* extra trust... missing node identity must not silently grant it"); the user's ask is a deliberate policy reversal of that stated reasoning, not an oversight, so evaluate it as a conscious tradeoff decision rather than flagging the docstring/behavior mismatch as a regression.

## [live-trading][coverage] Raised 2026-08-17 (evening), queued to `research` session then held (revisit ~2026-08-21) — `fake_broker.force_network_error_next_order()` needed to reproduce today's real Schwab-outage automated-buy failure
User's call 2026-08-17 (later): skip for now, pick up later this week — item 2 below (order_id reconciliation) goes ahead first. **Tied to `trading_incidents` #10/#11 2026-08-20**: both stay open (real Schwab-outage events, "can't help it" — not preventable) pending this harness fix; build it ~2026-08-21 per the existing revisit date, then confirm #10/#11's fallback-and-recovery behavior actually works under the new regression test and close both ~2026-08-22 once confirmed — not before.
Real incident today (SOXS, `ira`, wl_id=206 — see `trading_incidents` #10 / `backlog_resolved_recent.md`): a genuine Schwab API outage (~9:33-10:36 ET, 503/500/timeouts) hit `_attempt_automated_buy` -> `schwab_client.place_trailing_buy` mid-placement (`The read operation timed out`), which exhausted its 3x/2s retry budget and fell back to manual with zero further auto-retry on later poll cycles — the user had to manually place the real order 2.5 hours and 6 reminders later. `tests/fake_broker.py` has no way to reproduce this today: `force_reject_next_order()` simulates a broker-level REJECTED *response* (`place_order` still returns a real order id, only the async status poll sees the rejection) — not a network-level exception raised *inside* `place_order`/`get_order` before any id is ever returned, which is what actually happened. `set_timeout(secs)` exists but is a no-op stub. Needs: a new `force_network_error_next_order(exc=None)` fixture method (raises a generic/timeout-shaped exception from the next `place_order`/`get_order` call, mirroring the real `_submit_order_with_retry` exhaustion path) plus a regression test reproducing today's exact shape (automated buy throws mid-placement -> falls back to manual -> no auto-retry fires on subsequent poll cycles since `_scan_buy_signals` skips any node with an existing `pending_buys` row). Distinct from the existing parked 2026-08-07 item (`live_sanity_check.py`-calibrated `force_reject_next_order()` for a real business rejection, e.g. insufficient buying power) — that's a different failure shape (broker says no), this is a transport-level failure (broker unreachable). Not started.

## [live-trading][design] Designed 2026-08-17 (evening), parked same evening (user's call: not dropped, just don't want to spend context on it right now) — skim v2: execution-drift-triggered notional override (80% of current, "room to breathe"), replaces the retired equity-threshold skim
Old skim (retired same session — see backlog_resolved_recent.md) was equity-curve-threshold-triggered (fires on the strategy's overall gain crossing 80%/100%), removes a flat fraction to an SPY-tracked reserve, only fires after a core SELL closes — nothing to do with per-fill execution quality. **User's call: wrong mechanism, need a very different one.**

**New design**: triggered by EXECUTION DRIFT on a specific fill exceeding ~1% (real fill price vs. expected/signal price — NOT liquidity/volume, corrected mid-design from an initial 1%-of-trade-volume framing). This is a distinct trigger from the already-built `signals_db.check_abnormal_drift` (currently alert-only, threshold 3% env-configurable, called from the `open_position()`/`close_position()` chokepoints on both BUY and SELL fills, with an existing 3-consecutive-trades-before-escalation design already specced — see the "abnormal-drift liquidity-signal alert" item this replaces/extends, backlog_resolved_recent.md). Also distinct from "trim" (liquidity-capped entry sizing, different trigger — 1% of daily $ volume ceiling — stays a separate item, not superseded by this).

**Action on trigger**: override the notional for the NEXT order down to 80% of current notional ("room to breathe" — user's framing). Real lever already exists: `signals_db.set_starting_notional_override(watch_id, value)` writes `watch_list.starting_notional_override`, which `signals_helpers._last_sale_recovery` already checks FIRST (bypasses trade-history compounding) — so "current notional" = `_last_sale_recovery(node)` at trigger time, override = that value * 0.8.

**Not yet resolved / needs scoping before build**:
1. Should this hook into `check_abnormal_drift`'s existing 1%-threshold path (lower the alert threshold for this specific action, keep 3% for the Slack alert), or is it a fully separate check with its own 1% threshold?
2. Fires on BUY-side drift, SELL-side drift, or both? (`check_abnormal_drift` already receives `side` — no new plumbing needed either way.)
3. Does the 80% override compound on repeated drift events (each trigger takes 80% of the ALREADY-reduced notional), or reset to a clean 80%-of-original after one trade, or need an explicit recovery path back up? Not specified yet.
4. "Room to breathe" implies this is meant to self-heal once fills are clean again — does anything ever clear `starting_notional_override` back to null/grow it back, or is recovery manual (matching skim's existing alert-only redeploy convention)?

**Session-note correction, 2026-08-17 (later)**: an earlier pass through this same evening briefly closed this item out as fully "dropped, don't build, fresh ask if revisited" — that was a miscommunication (a peer session's relay of "don't build it right now" got over-read as a permanent decision). Corrected: this is a PARK, not a drop. It stays here, in normal open-items order, not superseded or requiring re-litigation when picked back up — the 4 scoping questions above are still exactly where the design left off.

Not started, not currently queued to any session. Needs the standard paired-review gate once scoped (touches `signals_db.py`/`signals_notify.py`), and per the earlier design note, should be prototyped in the fake-venue harness (fake execution + notional-override scenario) before any real/paper wiring.

## [live-trading][coverage] Deferred indefinitely 2026-08-08 (weekend) — judgment-based intraday risk review layer (LLM-driven, not fixed rules)
`signals_notify.check_intraday_risk_review` (built 2026-08-08) is a cheap, deterministic first version — reviews `trading_incidents` + a narrow `coverage_events` substring match, alerts only on a fixed rule. Discussed at length whether this should instead (or additionally) be judgment-based: either `active_signals.py` calling the Claude API directly with its own key when it hits an ambiguous event, or a dedicated local background session (`/loop`, which has real local DB access unlike a cloud `RemoteTrigger` routine — that mechanism was evaluated and rejected, isolated sandbox with a 1-hour minimum interval). User's framing: a fixed rule can only catch anomalies someone already anticipated; judgment is the actual point of "review," not just another alert. Explicitly deferred ("yeah not yet") in favor of shipping the deterministic baseline first and proving it out. Also noted as a simpler interim alternative: the user can message the agent directly on demand for a state check ("i'm the trigger") rather than needing autonomous polling at all.

## [live-trading][coverage] Parked 2026-08-07 (evening), condition-based trigger set 2026-08-15 (revisit when: user has a WFH day free to safely test against real broker rejections) — study real Schwab rejection behavior via `scripts/live_sanity_check.py`, then calibrate a new `fake_broker.force_reject_next_order()` to match
Plan: user runs `scripts/live_sanity_check.py --test oversized_buy --tickers SOXL` against `soxl_ira` (human-confirmed, real order, not something to run unsupervised) to observe Schwab's actual rejection shape (exception type, HTTP-immediate vs. async order-status REJECTED, exact error payload) — `oversized_buy` is a reasonable proxy for add-on's real failure mode (buying against margin credit that doesn't exist in a limited-margin IRA looks like "insufficient buying power" to Schwab either way). Once the real shape is known, add a `force_reject_next_order()` mode to `tests/fake_broker.py` (currently `place_order` always succeeds/fills — zero reject capability exists) calibrated to match, then write a fake_broker test for the add-on-BUY-rejected code path (does the alert fire correctly, does the `addon_legs` staging row get closed out instead of stuck `'open'`) — this closes the gap without waiting on an organic live reject. Not started. **Still wanted, timing depends on the user having a real WFH day** — a future session should ask "are you WFH today?" before resurfacing this, not assume it's dead just because no fixed date passed.


## [backtest][new-strategy] Research idea, raised and parked 2026-08-07 — drought overlay as its own independent strategy (own vol/z-based entry signal + own SL/arm/trail grid), swept across all v5 tickers
Full detail: `docs/research_log.md`'s 2026-08-07 entry. A real test of the underlying premise (do drought/add-on results mostly overlap across nearby node configs, so they could be evaluated once per ticker?) REFUTED it — even a true 1-grid-step neighbor (trail_buy_pct off by 1%) only gave ~30% drought-window overlap for SQQQ, and add-on's trade count collapsed to 1-3 trades across the same neighbors (too thin to call a real edge). `scripts/drought_overlay_sweep.py` (2026-08-05/06) already covers a similar independent-grid design for the 10 real v5 tickers — entry-side vol gate real for a minority (HIBL/SOXL/UDOW), still needs out-of-sample validation before extending further. Explicitly parked, not started — user's own framing: "it would have to be very robust to do this outside of v5."



## [live-trading][coverage] Idea, raised 2026-08-11, flagged for reframing 2026-08-15 — 4-tier test-confidence reframe + nightly staged-test lifecycle manager + dry-run-only "can't-trade" ticker pool for wash-sale reduction
Full detail: `docs/deep_backlog.md`'s 2026-08-11 entry (search by this header's title). Not started — Step 1 (read-only report) is the first real build target. **Flagged 2026-08-15 (user's call)**: needs reframing with the fake-venue harness in mind before building as originally scoped — heavy overlap with the harness's own FAKE-HARNESS tier work, which has grown substantially since this item was raised. Revisit once the harness design settles more, don't build the original 4-tier scope independently of it.


## [live-trading][coverage] Open, tracking moved to the fake-venue harness Phase 2 queue 2026-08-15 — remaining addon/drought-handoff Grid rows still wired-never-fired
`drought_entry_placement` and `post_fill_topup` (both drought-side) flipped to `verified-live` 2026-08-10 via RETL's real drought entry (see `backlog_resolved_recent.md`). Still `wired-never-fired`: every `addon_*` row and `drought_handoff`/`drought_handoff_cancel`/`drought_handoff_exit_placement`. **Active tracking mechanism, 2026-08-15**: the fake-venue harness Phase 2 Category A queue (above, in this same file) already covers this ground directly — `addon_entry_placement`, `addon_exit_placement`, `addon_double_buy_exemption`, `addon_leg_reconciliation`, `drought_handoff_cancel`, `drought_handoff_exit_placement` are all named items in that queue. No separate organic-wait posture needed here anymore.

## [backtest][ops] Hold, set 2026-08-07 — keep `trading_universe.db.pre_prune_20260807_151917` (31G) as a backup until ~2026-09-07
Supersedes the earlier same-day note about `...100407` (67G, still had v4 — deleted after v4 was purged and this file was superseded). This is the pre-swap copy of the full v4-purged merged-recovery DB, left behind automatically when `--swap` ran after the fixed prune's comprehensive validation passed (11,183/11,183 real island groups matched). User's explicit call — keep it ~1 month in case another prune bug surfaces before fully trusting the current pruned live DB (2.1G). Don't delete before ~2026-09-07 without asking.

## [backtest] Deferred, raised 2026-08-07 (late) — `backtest_cache`'s overloaded columns need a real schema definition, not more ad hoc patches (revisit ~2026-09-10, may push further)
Root cause of the trail_buy_pct/take_profit bug recurring three times today wasn't three independent slips — it's that columns like `take_profit` (means different things per strategy, NULL for TrailingBoth) and `trail_buy_pct` (real for TrailingBoth, always-zero for TrailingExit) have no documented, enforced contract, so every new query has to rediscover the semantics. Real fix: an explicit schema definition (which column means what per strategy, which must always be held fixed together in a neighbor search) future code follows instead of assumes. **User's explicit call: deferred to next phase, tied to whenever a new strategy variant is actually designed** — not tonight. "Limp along" on the current ad hoc-patch approach until then. (The 2026-08-02 `prune_backtest_cache.py` instance of this same root cause is now fixed — see `backlog_resolved_recent.md`.)

**Recurred again 2026-08-22**: an ad hoc `backtest_cache` query for a TrailingBoth GT campaign's best node assumed `stop_loss` = trail_buy_pct (it's actually the constant `fixed_sl`), producing a wildly wrong reproduction (247 trades / -258.98% alpha) vs. the real cached value (101 trades / +1066.8% alpha) before the mistake was caught and corrected — nearly reported as a production bug. Same root cause exactly, still unfixed by design (deferral stands), but worth noting the cost keeps recurring in the meantime.

## [live-trading][security] Real order-execution code built + paired-reviewed 2026-08-1x — drought-entry/add-on-entry now implemented, gated off, D6 node created, awaiting staged real-order testing
Full detail: `docs/design.md`'s "Real-order execution edit plan" build-status addendum. Phases 1-9 of `docs/plans/real_order_execution_drought_addon.md` implemented (schema, `schwab_safety` `is_addon_leg` exemption, real drought entry/HANDOFF, real add-on entry/lockstep-exit, Accountability Grid rows, 22 new fake_broker tests, reconciliation). Paired independent-cold + contextual Opus review run same session: both converged on the same CRITICAL (lockstep leg-exit SELL was structurally unplaceable at all 7 real call sites — fixed via a node-scoped, not parent-position-scoped, `is_addon_leg` exemption), plus 1 more CRITICAL + 2 HIGH + 2 more HIGH from each review separately, all fixed and re-verified with real fake_broker tests reproducing the actual call-site ordering (which is what caught 2 further latent bugs — a duplicate-order-fingerprint collision in both directions — that neither review itself found). Full suite 589/590 (1 pre-existing environmental failure, confirmed unrelated). **D6 decided 2026-08-07 (later): SOXL in `soxl_ira`, node created same day (wl_id=196)** — `state='live'`, clone of the real v5 SOXL core config (id=92: window=10, fixed_sl=2.0, trail_buy_pct=3.0, trail_sell_pct=1.0, arm_sell_pct=30.0, entry_timing=open_check), `starting_notional=$500` (well under the $3,000 `soxl_ira` cap even with add-on's 2x doubling), both `drought_overlay_enabled=1` (confirm_days=3, vol_gate=0.4) and `addon_enabled=1` so Tests 1-4 can all run on this one node per the plan's own suggestion. Confirmed flat (no `open_positions`/`pending_buys` row) before creation; `staged_test_config` baseline seeded (`scripts/seed_baseline_config.py`); `signals_invariants.py` clean. **Nothing has fired yet** — node just added, waiting on an organic drought/core signal; **still nothing an agent should force** — Test 1 (drought entry) requires real checkpoint bars to elapse organically, per the plan ("forcing it would invalidate the test"). **Accountability/test-coverage review, 2026-08-07 (later)**: `scripts/coverage_registry.py`'s report shows all 14 drought/add-on Grid rows (6 paper-side + 8 real-order-placement) as `wired-never-fired` — including the paper-side rows, so even paper trading hasn't organically exercised any of these paths yet; every current proof is offline. That offline proof is solid: 66/66 tests pass across `test_fake_broker_addon_entry_scenario.py`, `test_fake_broker_addon_lockstep_exit_scenario.py`, `test_fake_broker_drought_entry_scenario.py`, `test_fake_broker_drought_handoff_scenario.py`, `test_fake_broker_overlay_truth_table_scenario.py` (32+5), and `test_overlay_paper_trading.py` (29). **Correction to the earlier "not written" note below**: `test_fake_broker_overlay_truth_table_scenario.py` (Part 9's item 5) *was* written, same session as the real-order build (`ea03095`) — verified directly, 5 tests, passes. Separately, confirmed `signals_invariants.check_addon_drought_live_nodes_have_coherent_account_type` — the startup-time guard meant to catch exactly the `soxl_ira` account_type mislabel above — passes silently for node 196, giving false confidence; same root cause as the `schwab_safety` guard, **deferred together** (user's call, 2026-08-07 later) rather than patched piecemeal now. Next: (1) put-hedge is now a deliberate skip, not just deferred — see `deep_backlog.md`'s 2026-08-1x "put-hedge skipped" entry, (2) **active tracking mechanism, 2026-08-15**: the fake-venue harness Phase 2 Category A queue (above, in this same file) covers staged real-order testing for this mechanism directly — see that queue rather than tracking node 196's Test 1 trigger as a separate organic-wait item here.



## [backtest] Open, raised 2026-08-07 — USO's drought overlay is uniformly negative at every confirm_days 1-15, unexplained (revisit ~2026-09-10)
Full detail: `docs/research_log.md`'s 2026-08-07 entry. Notable because USO's own price was UP +57.1% over the same span — rules out "no positive drift" as the explanation; the drought-specific losses are a real, distinct pattern from the ticker's overall trend that hasn't been traced to a cause.


## [portfolio][tax] Open, raised 2026-08-09 — populate the new K-1 tax forecast tool with real data
`k1_tax.py`/`scripts/k1_tax_forecast.py`/`pages/16_K1_Tax.py` (built 2026-08-09, resolving the prior "build a simulator" idea — see `backlog_resolved_recent.md`) is tested against synthetic data only. Needs: real 2026 AGQ trades logged, 2025 actual total tax liability once known, real rate config confirmed against the user's actual bracket. Separately not yet built: Schedule AI/Form 2210 annualized-income-installment modeling (relevant given this strategy's "lumpy" gains) — the current tool models the flat-quarterly 110% safe harbor only, not the annualized alternative that could reduce early-quarter cash needs.

## [live-trading] Idea, designed not built, 2026-08-07 — KORU "upswing-watch" Slack alert (revisit when: user picks it back up)
Full detail: `docs/research_log.md`'s 2026-08-07 entry. Fire when price is above both 50-day and 100-day SMA (real trend, not just a bounce) AND vol_pctile<0.3 (genuinely calm) — matches the trend-continuation mechanism found for KORU specifically. `scripts/watchlist_status.py` already prints `SMA50`/`>SMA50`/`VolPctl` columns for every node (informational only, added this session) so the state is visible at a glance; the alert itself isn't wired up. **2026-08-25: user's call — skipping the 50d/100d SMA lookback as designed, wants to combine with a different lookback instead.** Not otherwise scoped; no fixed revisit date, pick up whenever the user brings it back with the lookback they want.

## [live-trading] Found 2026-08-04 very late — broader diagnosis still open (all 3 point-fixes now resolved, see `backlog_resolved_recent.md`)
**Broader diagnosis (still open)**: (1) and (2) (now fixed, see `backlog_resolved_recent.md`) weren't coincidence -- multiple scripts (`status_check.py`, `coverage_check.py`, `watchlist_status.py`, `paper_trading_status.py`) each reimplement their own ad hoc view of overlapping state (open positions, pending buys, node config) instead of sharing one canonical "real current state" function, so the same class of bug can independently exist in each. Real fix candidates, not yet decided: (a) narrow -- point-fix the next instance when found; (b) broad -- one shared `signals_db` function (e.g. `get_real_position_state(ticker, account=None)`) that all these scripts call instead of hand-rolling SQL. Either touches `signals_db.py`/coverage logic and needs the standard Opus review gate. See `.claude/skills/daily-routine-check/SKILL.md` for the full context.

## [backtest] Deep dive 2026-08-05/06 — drought-overlay: entry-side intraday-vol gate real for a minority of tickers (unresolved overfitting risk); exit-side vol-gate REJECTED at trade-price-action level, not just aggregate stats; HANDOFF mechanism traced, concentration-risk still open
Full detail: `docs/research_log.md`'s 2026-08-05 (very late 3) and two 2026-08-06 entries. **Exit-vol-gate line of inquiry now CLOSED (2026-08-06), rejected**: the per-ticker sweep's apparent KORU "improvement" was cross-axis noise (the gated winner cell also changed confirm_days); isolating the gate's true effect and auditing the 5 changed KORU trades bar-by-bar showed 3 of 5 are real recoveries killed mid-pullback (vol spikes that were the bounce itself, or persistently-elevated non-reactive vol, not danger arriving) vs. only 1 genuine save (a real overnight-gap breakdown). Same failure mode as the original pooled rejection — confirmed at the price level, not just in aggregate stats. **Verdict: not predictive enough to adopt in any form (pooled or per-ticker)** — would need a fundamentally different signal (e.g. directional/momentum confirmation, not vol alone) to be worth revisiting. Entry-side gate is UNAFFECTED by this — still open, real for HIBL/SOXL/UDOW, still needs out-of-sample validation. Concentration-risk screen (original 2026-08-05 finding, KORU/pooled-cell dominance) still unresolved. **Still deferred to next session**: (1) out-of-sample validation of the entry-gate per-ticker choice (fit first-half/test second-half), (2) whether entry-gate-helped tickers (HIBL/SOXL/UDOW) share a real structural property vs. gate-hurt ones, (4) HANDOFF/handover mechanics redesign — transfer management into the core strategy's own state machine instead of sell-then-implicitly-rebuy at the backstop bar's Open, (5) put-hedge cost/payoff modeling (rough Black-Scholes off the same intraday realized-vol measure as an IV proxy) — discussed, not built. Put-hedge option-selection methodology itself is now moot — see `deep_backlog.md`'s 2026-08-1x "put-hedge skipped" entry.

## [backtest] Research idea, found 2026-08-03, narrowed same day — the 14:30 daily signal window fires ~3x more entry signals than the 9:30 window, across nearly every v5 watchlist ticker
Side finding from the entry-timing-seasonality check (`docs/research_log.md`'s 2026-08-03 entry). Directional intraday drift is now ruled out as the explanation (same-day follow-up entry: no real 9:30-to-14:30 price drift on any of the 10 tickers). Leading unverified explanation: cumulative opportunity — more elapsed trading time by the 6th bar than the 1st for any large-enough move (either direction) to breach the lower band, not a directional bias. Not yet tested directly (would need e.g. comparing intraday range/volatility by bar number).

## [backtest][new-strategy] Research idea, raised 2026-08-02 — FFT/wavelet-based trend-extraction as a distinct strategy paradigm (not an enhancement to the existing z-score mean-reversion family) (revisit ~2026-09-10)
Evaluated a Gemini-proposed live spectral-filtering pipeline (STFT/CWT low-pass denoising → slope/crossing signal → VWAP/TWAP execution). Confirmed this describes a fundamentally different strategy (trend-following off a denoised price line, sub-minute tick-level execution) than what this system runs (hourly-bar z-score mean reversion, 2 fixed daily signal windows, manual/bridge-automation execution) — most of the doc's cold-start/microstructure/execution-routing content doesn't apply here at all. The one applicable piece (offline FFT cycle-period detection on historical hourly data) is already captured separately — see the existing FFT cycle-detection item below. This entry is specifically the "build a new denoised-trend strategy variant" idea — unscoped, would need its own backtest kernel path distinct from `strategies.py`'s current classes. Not just a stray idea: user confirmed 2026-08-02 the system may eventually run several distinct strategy paradigms in parallel, not just mean-reversion variants — so this is a real candidate for that, not automatically low priority.

## [backtest][live-trading] Research idea, raised 2026-08-02 — ATR/volatility-scaled position sizing, evaluated against a Gemini-proposed risk-architecture doc
Real blocker, not just unscoped: `starting_notional` is already capital-constrained per account (one-account-per-ticker model, sized to available capital), not a free sizing knob — needs a design for what shrinks when vol is high (idle capital? fewer shares?) before this is even a backtest question. Two other modules from the same doc (watchdog auto-flatten, loss-streak circuit breaker) were evaluated and rejected/backlogged in the same conversation — see below and `docs/conversation_summary.md`.


## [live-trading][coverage] Investigated 2026-08-02 — the 3 suspicious wired-never-fired rows explained; 2 real open items filed; remaining 14 triaged by staging feasibility, none actionable today
Full detail: `docs/deep_backlog.md`'s two 2026-08-02 entries. Of the 14 `wired-never-fired` rows: 4 need staging during market hours (blocked while not WFH), 4 need a 2nd live node on a different account (blocked, only one real account), 1 deferred to the planned Slack rearchitecture, 1 (`market_buy_placement`) just needs DPST to hit a real signal, 4 aren't realistically stageable.

## [backtest] Open (bear-market + regime items), tagged 2026-08-16 — from the 2026-08-01 research tangent's independent Opus challenge (revisit when: bear-market intraday data acquired)
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by title: "independent Opus challenge of the 2026-08-01 research tangent"). Compounding-drag item itself is resolved. User plans real bear-market testing once that data is available, not before.





## [live-trading] Open, raised 2026-07-24 ~10:55 ET, posture changed 2026-08-15 — retry `check_gap_resize`'s cancel+replace test properly pre-market on a future day, not mid-day
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title). **Posture changed 2026-08-15 (user's call)**: route to the fake-venue harness instead of waiting to catch a real pre-market window organically — the harness's replay capability can simulate pre-market conditions on demand.


## [live-trading] Low priority (user's call, 2026-08-09), raised 2026-07-23 night — Schwab auth flow is "clunky", no cleaner unattended path designed yet
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title). Distinct from the already-fixed `interactive=True` hang bug.

## [backtest] Open, paused 2026-07-22 — idle-capital parking idea (renamed from "v6" nickname 2026-08-21 to free that tag for the ground-truth kernel rebuild's real `backtest_cache.version` — this idea never reached real cache rows, so the rename is doc-only, no data migration); inconclusive, downturn-specific follow-up queued
Tested (`docs/research_log.md`'s 2026-07-22 entry): no robust fold-consistent edge found. Follow-up idea (check specifically during real SPY drawdown episodes — dates listed in CLAUDE.md) raised but not yet run. Note: `scripts/sim_v6_spy_parking.py`/`sim_v6_parking_vehicle_sweep.py`/`sim_v6_split_check.py` still carry the old `v6` filename prefix — harmless (never became a real version tag), rename opportunistically if touched again, not urgent.

## [live-trading][tax] Open question, raised 2026-07-22 — which ticker (SOXL vs AGQ) goes to the taxable brokerage account first; wash-sale cross-account mechanic needs one more confirmation
Gated on the broader wash-sale/tax analysis item below, also still open.

## [live-trading] Low priority, idea raised 2026-07-21 — should `CASH_SAFETY_BUFFER` scale with order size instead of staying a flat $200?
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).


## [backtest] Research idea, not started, 2026-07-20 — is overnight gap frequency/magnitude asymmetric (up-gap vs down-gap)?
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading][security] Phase 4 (deferred to cloud-infrastructure planning), 2026-07-18 — move order-placement/mutating Schwab calls behind a separate proxy this session can't write to
Full detail in `docs/deep_backlog.md`.


## [live-trading][backtest][portfolio] High priority, revisit ~2026-08-22 — dividend cash isn't credited into P&L/SL/arm tracking; material for DPST specifically
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title). User's call 2026-08-08: pick this up in ~2 weeks, likely as a portfolio-level piece rather than a standalone fix.


## [backtest] High priority, 2026-07-16 — need a proper delayed-entry simulation for the same-day-re-buy constraint, not just a trade-list filter
`docs/deep_backlog.md:4601-4602` confirms still unbuilt — only the admittedly-wrong post-hoc filter method exists. `sim_delayed_sell.py` (2026-07-17) covers the exit-side mirror only, not this entry-side ask.

## [execution] Research question, 2026-07-15, deferred 2026-08-09 (revisit ~2027) — when would TWAP/VWAP order execution become worth considering
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [backtest][new-strategy] Idea, not scoped, 2026-07-18 — daily-bar strategy variant, for much longer backtest history / real bear-market regime coverage
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading] Idea, not built, 2026-08-01 — real correlation-verification logic for the new JNUG/JDST "G" canary pair
Today's fix (`docs/deep_backlog.md`'s 2026-08-01 entry) only restored their real config/labels/scenario_expectations row (`canary_bull_bear_pair`) — monitored the same simple same-day-trade-happened way as every other canary, no real correlation check built yet.

## [backtest] Idea, not scoped, re-opened 2026-08-03 — rolling/time-varying FFT spectral power as a regime signal (alongside the 2026-08-01 SPY-trend/VIX finding) (revisit ~2026-09-10)
Split off from the now-closed "FFT cycle detection to inform window selection" item — that half was tested and refuted (`docs/research_log.md`'s 2026-08-03 entry: no significant fixed dominant cycle in any v5 ticker's returns). This half — whether *time-varying* spectral structure (not a single fixed cycle) says anything about regime — was never tested and is still open.

## [data] Idea, not scoped, 2026-08-01 — start recording our own 1-minute bars now, so a future "we need historical data" request never hits an expired retention window again
Full detail: `docs/deep_backlog.md`'s bulk-migrated 2026-08-01 entry (search by this header's title).

## [live-trading][tax] Idea, raised 2026-08-04 evening — live wash-sale risk monitor (detection/alerting only, not prevention)
Distinct from the backtest-only simulator below (`sim_wash_sale_impact.py`, retrospective/aggregate) — this would run against real, current positions and flag which ones are at wash-sale risk right now (e.g. a same/substantially-identical security bought in one real account within 30 days of a loss-sale in another, the cross-account gridlock mechanism already found for SOXL). Explicitly not meant to block or auto-cancel a trade — just surface the risk so the user can decide (same "detection-only, decide policy later" pattern as `_log_pre_action_state_verification`/`record_node_streak`, 2026-07-29). **Two specific tickers named as priority scope, 2026-08-04**: SPY (will be bought/sold across multiple accounts via the skim-and-reserve overlay's skim/redeploy mechanism above — real multi-account wash-sale exposure by design, not a hypothetical) and TQQQ (already held elsewhere per the standing watchlist note; a real IRA node may get added if liquidity ever becomes a problem for the existing brokerage-held position — see the 2026-08-04 TQQQ-cleared-for-IRA item above, same conditional-risk shape). Not scoped: where it hooks in (real-time during `check_order`? a daily batch report?), what "at risk" means precisely per instrument type (standard 1099 vs AGQ's Section-1256 wash-sale-exempt status needs to be excluded from triggering false positives), and whether it needs real cost-basis/lot data from Schwab or can work off local `trade_log`/`open_positions`.

## [portfolio][tax] Idea, raised 2026-08-03 evening — real wash-sale/cost-basis-tracking + tax simulator, prototype only
`scripts/sim_wash_sale_impact.py` (real AGQ/SOXL trades) found near-total wash-sale gridlock at this trade cadence in a taxable account — SOXL's result stands (owed tax despite a net loss, standard '40-Act fund, normal wash-sale rules apply). **AGQ's result is wrong and superseded, 2026-08-04**: AGQ is a Section 1256 commodity-pool/K-1 instrument, which is exempt from the wash-sale rule entirely (overrides normal broker 1099-B wash-sale treatment even at the share level) and gets 60/40 blended-rate tax treatment in a taxable account instead of the flat ordinary rate the script assumed — the reverse of what was concluded. See the new AGQ K-1/UBTI entry below for the fuller correction (AGQ may now be *better* suited to brokerage than IRA, opposite of the original recommendation). Full rebuild (Section 1256 60/40 + no-wash-sale modeling) not done — this prototype's AGQ numbers should not be used for decisions.


## [live-trading][tax] Idea, raised 2026-08-04 — TQQQ cleared for IRA trading (no K-1, wash-sale risk conditional on brokerage leg staying unsold-at-a-loss); not yet acted on
Full detail: `docs/research_log.md`'s 2026-08-04 entry. Best v5 node identified (`TrailingBothZScoreBreakout`, fixed_sl=2, open_check, 120 trades, 20.8% win rate, +242.8% robust alpha) but no IRA `watch_list` node added yet — TQQQ's existing nodes are all stale pre-v4 versions or research-mode.


Full detail: `docs/research_log.md`'s four 2026-08-09 entries (later/later still/later still 2/3). Summary: a 30-year daily-bar sim of the 3-account structure (`scripts/sim_1m_four_bucket_portfolio.py`) found it beats plain 100% SSO in only 29% of cohort-horizon pairs, and neither T-bill cash yield nor doubling algo weight to 50/50 fixed it (`scripts/sim_portfolio_variant_compare.py`) — but this was CORE-STRATEGY-ONLY on a coarse once-a-day signal check. Replaying the REAL hourly kernel through the only real drawdown in the cached window (SPY -18.8%, 2025-02-19/06-26) and a real-ticker (non-synthetic) daily-bar test through 2020 COVID/2022 bear (`scripts/sim_real_ticker_daily_crash_stress.py`) showed AGQ's real algo strategy beating both SPY and SSO in every window checked (incl. SSO itself underperforming SPY in 2 of 3 due to real volatility decay). The -64.1% KORU number from that same 2022-bear daily-bar test is **NOT real downside risk evidence — user correction (told 3x now, see `feedback_daily_bar_understates_real_risk` memory)**: it's a daily-bar-approximation artifact, since the algo performs materially worse under the coarse once-a-day signal check than under its real hourly execution, for every ticker, not specifically KORU. Don't treat it as a real hourly-kernel finding. **Not decided**: whether/how to act on the 3-account structure, whether to build the tax-adjusted version (brokerage/AGQ account only — Roth/soxl_ira don't need it, per k1_tax.py's engine). User's call, still open — this entry exists so the thread isn't lost, not to force a decision.

## [backtest] Research thread resolved-with-caveats 2026-08-04 late — SOXL-weak/AGQ-strong 2026 YTD observation confirmed real after fixing 2 real bugs in tonight's research scripts; underlying cause still unexplained
User's original observation (AGQ strategy +68% YTD 2026 despite price -58%, SOXL strategy weak despite price +197%) was initially contradicted by tonight's 4 research passes (velocity, chop-cluster, regime regression, config cross-apply) — traced to two real bugs shared across all 5 scripts written tonight: (1) `TrailingExitZScoreBreakout` nodes (AGQ/DPST/KORU/NUGT/UDOW/YANG) store their arm-sell/take-profit value in the `take_profit` column, not `arm_sell_pct` — scripts only read the latter and silently disabled TP for these 6 tickers; (2) none of the 5 scripts passed `open_check=True` even though every real v5 node uses `entry_timing='open_check'`. A cold Opus review (blind read) independently confirmed bug 1's fix and independently found bug 2, plus 3 smaller issues (a still-open mark-to-market trade counted as completed in 3 scripts; a stale docstring; dead code) — all fixed. Corrected result: AGQ 2026 YTD +52.3%, SOXL 2026 YTD -30.8% — confirms the original observation. But the corrected pooled cross-ticker/cross-time analysis (105 periods, 10 tickers) shows strategy return generally moves *with* price trend, not against it (rho=+0.539, p<0.0001, survives ticker fixed effects) — so 2026 is a genuine **outlier year for both tickers relative to their own historical pattern**, not an example of a general rule. Cause of the 2026 anomaly itself is still open. Two committed scripts had the same bug pattern — both now resolved: `scripts/multiday_cycle_entry_test.py`'s buggy path turned out to never have been exercised (its published finding was independent of node config, confirmed unaffected on inspection); `scripts/entry_timing_seasonality.py` was fixed and its 2026-08-03 finding rerun — confirmed, if anything more solidly null (win rates now nearly identical between windows, p-values higher than before). All 5 of tonight's scripts also rerun post-fix; conclusions unchanged except the chop-cluster scan's "closest to significant" ticker flipped from SOXL to KORU (neither survives multiple-comparisons scrutiny). Full detail: `docs/research_log.md`'s 2026-08-04 (late) entries.

## [portfolio] Idea, raised 2026-08-04 evening — options-based alternatives to the reserve/insurance idea, not modeled
Four candidate uses, confirmed real account capability (Schwab, researched 2026-08-04): (1) protective puts on SPY/QQQ as literal crash insurance instead of the static skim reserve — works in any IRA, even without limited margin (Level 1). (2) covered calls on the idle reserve for extra yield — also Level 1. (3) vertical spreads for cheaper-structured insurance — needs limited margin on the IRA (already a separate open item). (4) SPX (broad-based index) options get the same Section 1256 60/40 + no-wash-sale tax treatment just confirmed for AGQ — a natural fit for brokerage specifically. None modeled yet — muni-bond-as-reserve test was also started same session (`cache/research/MUB_1h.csv` fetched) and abandoned mid-calculation (NaN bug, unfixed) when this tangent came up.
**Extended 2026-08-06**: user raised a related but distinct variant — hedging the CORE v5 strategy's own positions with puts directly (not just the reserve/insurance capital), as an alternative to skimming altogether. Scope not decided (per-position, e.g. a put on AGQ itself — directly checkable now via `scripts/collect_options_snapshot.py`'s real chain data, but hits the same liquidity/expiration gaps found for the (now-skipped, see `deep_backlog.md`) drought-overlay put-hedge idea (GDXU no options, HIBL/UDOW/USD/YANG no near-term expirations); vs. portfolio-level broad-index SPY/QQQ, the original framing above — cheaper/liquid but only covers broad-market risk, not a ticker-specific move). Not modeled yet, captured for later.

## [live-trading][risk] Idea, raised 2026-08-06 (very late), broadened 2026-08-19 — no runbooks exist for known edge-case incidents, not just flash-crash/gap (revisit ~2026-08-22)
Real gap found via tonight's put-hedge deep dive (`docs/research_log.md`'s 2026-08-06 entries): (1) no real-time crash-detection trigger exists anywhere in this system — every "crash window" tested tonight (5 known SPY drawdowns, 4 named historical crashes) used retrospectively-known dates; live, there's no signal for "start protecting now" (VIX-level, regime/trend filter, and the strategy's own drawdown were named as untested candidates; the intraday-vol-spike measure is already disqualified, tested and rejected earlier this session for the same kind of trigger). (2) No documented human runbook for what to actually do if a real gap/flash-crash is observed live — distinct from the existing kill-switch (blocks BUY/SELL automation, a rogue-algo off-switch, not a "the market is crashing, do X" procedure). (3) Synthetic bear-market stress tests (`scripts/sim_bear_market_stress.py`) showed the underlying can synthetically implode -80% to -99.6% at real historical crash bottoms (2008 GFC, 2000 dotcom) — the strategy's own tight SL discipline already avoids riding a position through the whole collapse (worst single KORU trade in 2008 GFC was only -6.1%, not -99.6%), but real fund closure/reverse-split risk (a real historical occurrence for some leveraged ETFs) is completely unmodeled and might not be cleanly protected by a put option either (settlement/operational risk during a delisting event). Not scoped: what the runbook should actually say (halt trading? check for fund-level news? manual liquidation criteria?), whether it's manual-only or ties into any automated check.

**Broadened 2026-08-19 (incident #13, SOXL/ira naked overnight)**: same underlying gap shows up for any known-but-rare edge case, not just flash-crash — tonight's incident (last-bar-of-day exit signal racing the close, no market-hours guard yet) had no runbook either; the diagnosis (check real broker order state via `schwab_client.get_real_orders`, check daemon process via `ps aux`, check price staleness via raw quote timestamps, check same-day re-entry eligibility via `schwab_safety.ACCOUNTS[acct].cash_settlement_type`) was improvised live in conversation instead of following a documented sequence. User's framing: worth having runbooks ready for known edge cases BEFORE they recur, not just fixing the code path each time. Candidate first entries once scoped: "position naked overnight, pending order not yet resolved" (tonight's exact shape), "real Schwab API outage mid-order" (2026-08-17 SOXS/RETL incidents, already has real precedent), plus the original flash-crash/gap case above. Not scoped: format/location (a new `docs/runbooks/` directory? one file per incident-shape?), whether this is a `daily-routine-check`-adjacent skill or plain docs.


## [testing] Latent, noted 2026-08-15 (relayed via peer session "planner") — dormant cross-file synthetic-CSV collision risk (`tests/test_overlay_paper_trading.py`), confirmed via a real `EmptyDataError` on one full-suite run
Not currently active (no two test files share a synthetic ticker today). Full detail: `docs/deep_backlog.md`.

## [backtest] Updated 2026-08-17 — date-windowed backtest capability: CLI wiring + persistence done, paired-reviewed, merged (`427669a`); orchestration script (peer session) and cold-start bias design still open. Full detail: `deep_backlog.md`.


## [backtest][live-trading] Decided 2026-08-16 — quarterly-resweep-and-rebalance idea shelved; hold current live SOXL config, revisit lookback question ~2027-08-16 (revisit ~2027-08-16)
Real out-of-sample walk-forward experiment (`scripts/quarterly_rebalance_walkforward.py`, built + run same session): every rebalancing scheme tested — direct jump to fresh sweep winner, 1-grid-step "hop" toward it, local neighborhood re-search (radius 2 and 4) — failed to beat SPY buy-hold out-of-sample at a 1-year lookback (-56.2%/-36.8%/-36.7%/-0.2% vs SPY's +31.4% chained over the same 7 quarters). A 2-year lookback did much better (-6.1% vs SPY's +11.4% over the same 3 quarters) but **rebalancing added zero value at 2yr** — holding the very first 2yr-fit node unchanged for all 3 quarters produced the identical -6.1%, confirming a stable-enough lookback makes the whole rebalance question moot. Neither lookback beat SPY. 3-year lookback (closer to "just use one full-history sweep, don't rebalance," i.e. closer to current live practice) can't be tested yet — cached SOXL history is only ~3.06 years (2023-07-24 start, `data_manager.py`'s yfinance 730-day hourly limit isn't the binding constraint here since the cache accumulates forward daily; the binding constraint is real calendar time). First checkable 3yr-lookback out-of-sample quarter: ~2027-01 (need cached data through 2026-12-31). **Decision (user's call): don't force a rebalance this year — hold the current live SOXL config, rely on `evening_status.py` Part 5's real-vs-kernel divergence check (30d trailing, 10pp threshold) as the actual signal to watch (model-fidelity, not raw P&L — a losing stretch that matches backtest-predicted variance, like 2025's downturns, is the strategy working as designed, not a warning sign), and revisit the lookback-length question in ~2027-08-16 once a full year of real 3yr-lookback out-of-sample quarters exists to look at.** **Note (user's observation):** the standard November end-of-year tax-planning pass (`k1_tax.py`/`scripts/k1_tax_forecast.py`, already-built safe-harbor/reserve tooling — see the tax items elsewhere in this file) is a natural earlier check-in point too, even though real 3yr-lookback out-of-sample data isn't ready until ~2027-01 — any config change considered at that checkpoint has real tax consequences (realized gains/losses, wash-sale exposure) that should be evaluated together with the lookback question, not as two separate conversations. Also fixed same session (not part of the decision, a real bug found along the way): `scripts/run_quarterly_soxl_sweep.sh` was double-suffixing its `--version` string when combined with `--start-date`/`--end-date` (run_optimization_sweep.py's own Stage 2 logic appends `window_version_suffix()` on top of an already-windowed version), silently breaking every windowed campaign's candidate-selection lookup — all 8 original 1yr candidate CSVs came back blank despite the sweep itself computing real data (306K+ rows/window). Fixed at the source + generalized the script to `--lookback-years N`. Separately fixed: `scripts/candidate_summary_report.py::annualized_excess()` computed CAGR using the ticker's FULL cached-history span regardless of the campaign's actual windowed date range, silently understating CAGR by ~3x for a 1-year window on a 3-year-history ticker (found via a real example: node 365's 530.9% strategy_return divided by 3.06y instead of 1y gave 82.6% CAGR instead of the correct ~514%). Full detail/scripts: `scripts/quarterly_rebalance_walkforward.py`, `scripts/quarterly_config_time_stability.py`, `scripts/run_quarterly_soxl_sweep.sh`.

## [backtest][live-trading] Parked 2026-08-16 (user's call) — seasonal volatility investigation (Christmas/EOY quiet period, summer slowdown, monthly return concentration): no action item, real corrections found along the way (revisit ~2026-11-15)
Raised: is there a real low-volatility "Christmas rally"/EOY-quiet period (and/or summer slowdown) across the 10 real live capital-at-stake tickers (AGQ/ETHU/JNUG/DFEN/GDXU/KORU/DPST/NUGT/SOXL/SOXS), worth manually pausing the algo for. **Real, corrected finding**: using this project's own established intraday-vol measure (`drought_overlay_sweep.get_ivol_series`'s convention — hourly log returns, 9:30 excluded, x sqrt(252*6) — not the daily-close measure the investigation started with, which prior research already flagged as misleading, 2026-08-05), **all 11 tickers checked (incl. SPY/TQQQ/AGQ) show a consistent Christmas-window (Dec 20–Jan 2) volatility drop, -5pp to -27pp vs rest of year, no exceptions.** AGQ's initial apparent exception (+23pp, opposite direction) under the daily-close measure was a real trend-vs-volatility confound: AGQ's real Dec-2025 rally (+38.5% underlying move) inflated the daily-close number by capturing trend magnitude, not actual intraday choppiness — under the correct measure AGQ is quieter at Christmas too, same as everyone else. **Despite the quiet period being real, an independent Opus review (2026-08-16) found the practical "should we pause" question already has a real, on-file answer this thread initially missed**: per `docs/research_log.md`'s 2026-08-04 (late) decomposition, volatility predicts trade FREQUENCY (coef 0.459, p<0.0001) but not trade QUALITY (coef 0.0041, p=0.554, null) — the z-score trigger already self-throttles during quiet periods, so there's little real exposure to reduce by pausing, and the drought overlay mechanism exists specifically to harvest value from exactly these low-vol stretches (SOXL's real drought config, confirm_days=3/vol_gate=0.4, is IE-verdict `REAL_SELECTION`: 11 included windows +174.8%/45.5% win rate vs. 12 excluded windows -14.6%/8.3% win rate — genuinely discriminating, already validated). A manual December pause would disable that mechanism, not reduce real risk. Separately: prior research (2026-08-04) found low-vol/drought entries can gap through their nominal stop width (KORU -33.6% on a 3% stop, AGQ -14.9% on a 2% stop) — quiet is not the same as safe. **A tangential, unresolved finding surfaced along the way**: SOXL's TrailingBoth config has a real, consistent July losing streak — 22/22 losing trades across all 3 cached Julys (0% win rate), -63% raw compounded July return — but SOXL's own buy-hold return is also -51% pooled across those same Julys, meaning most (not all) of the effect is explained by a real underlying seasonal weakness in SOXL itself, not a strategy-specific flaw; not investigated further, no other ticker showed this pattern this consistently. **Also found and fixed along the way (real bugs, not part of the seasonal question)**: `scripts/candidate_summary_report.py`'s CAGR annualization bug (see the date-windowing entry above) and confirmed the additive "delta"/percentage-point framing used throughout this thread is misleading for comparing already-compounded returns (should be expressed as an equity-ratio/multiple instead — e.g. "cost you 63% of your ending balance," not "-2178 percentage points"). **Decision: no action — hold current live configs, no manual seasonal pause.** Revisit ~2026-11-15 (ahead of the actual December window, and naturally overlapping the standard EOY tax-planning pass) only if there's a reason to reconsider; otherwise this is closed, not open work. Scripts: `scripts/seasonal_volatility_check.py` (`--ivol` flag added for the correct measure), `scripts/december_shutdown_impact_check.py`.

## [backtest] Parked 2026-08-17 (user's call) — min_hold_hours compliance-hold floor: kernel built + paired-reviewed + swept (SOXL/SPCL/QPUX), no ticker justified the real compliance friction
Real firm compliance policy: 16-trading-day minimum hold (112h), blocks ALL exits incl. SL during the window — research/backtest-only, no live-automation path ever (manual sign-off required on both entry AND exit). Kernel change (`backtester.py::_simulate_trail_both`/`run_backtest_v110`, `min_hold_hours` param, default 0 = byte-identical to prior behavior) is sound, paired-Opus-reviewed (2 real bugs found and fixed pre-sweep), and stays in the codebase — not reverted, just not being pursued further right now. Full writeup incl. the HIGH/MEDIUM bugs found, the degenerate-`max_hold_hours`-below-floor methodology bug found and corrected mid-campaign, and per-ticker results (SOXL cliff-safe but fails walk-forward/drawdown/drought; QPUX/SPCL both too data-thin and below the +200% alpha bar): `docs/research_log.md`'s 2026-08-17 entry. Still uncommitted (kernel diff in `backtester.py`/`run_optimization_sweep.py`, plus `scripts/run_soxl_minhold_sweep.sh`/`run_soxl_minhold112_widehold_sweep.sh`/`run_spcl_qpux_minhold112_sweep.sh`/`audit_min_hold_hours.py`). Revisit only if the compliance situation changes or a new candidate ticker with real upside potential + enough history comes up — see the research log entry's follow-up notes.

## [tooling] Idea, raised 2026-08-17 — periodic scripts/ directory cleanup/archival
`scripts/` has grown to 78+ files (`scripts/list_scripts.py`); no process yet for archiving one-off/research scripts that are no longer applicable (superseded tooling, dead research threads) vs. the repeatable ones worth keeping discoverable. Not scoped: archive location/convention, cadence, criteria for "no longer applicable."

## [live-trading][security] Found 2026-08-18 (2-round paired review of the check_intraday_risk_review/drought fixes) — fractional `drought_arm_pct_override` silently truncated to an int
`open_drought_overlay_position` (signals_db.py ~4630-4633) routes a resolved arm override into `overlay_node['take_profit']` for every strategy except `TrailingBothZScoreBreakout`; `open_position` then does `int(tp)` into `open_positions.take_profit`, an INTEGER column, while `watch_list.drought_arm_pct_override` is REAL. A `TrailingExitZScoreBreakout` drought node with `drought_arm_pct_override=1.5` would arm at 1%, not 1.5% (int() truncates toward zero, not rounds). The *fourth* instance of the `take_profit` column-overload family this codebase has hit (see its own ~4625-4629 comment). Latent (zero `watch_list` rows set any of the 3 override columns today) — real fix needs a schema decision (widen `take_profit` to REAL project-wide, or give drought overlay its own REAL-typed arm column) rather than a quick patch, so deferred rather than rushed. Not scoped.

## [live-trading][coverage] Found 2026-08-18 — cross-cycle burst-repeat cooldown in check_intraday_risk_review is escalation-blind
`_INTRADAY_BURST_REPEAT_COOLDOWN_SECS`-gated suppression (added 2026-08-18, closes the "one message per poll cycle" noise gap) tests only whether a `(scenario_key,ticker,result,mode,node_id)` key is NEW, never whether its magnitude changed — a key that logs 1 event (rendered, cooldown starts) then escalates to 5,000 events next cycle produces zero new Slack message for up to 15 min, and the suppressed events are permanently discarded (no cumulative-total note anywhere), not just deferred. Suggested fix: carry a per-key suppressed counter in the persisted `burst_last_alerted` state and fold it into the header/`(Nx)` note on re-alert, or re-trigger early when `_burst_count` exceeds some multiple of what was last rendered for that key. Not scoped.

## [live-trading][testing] Found 2026-08-18 — dry_run drought-overlay fill path (now correctly wired) has no truth-table/staged-test coverage
`_fill_dry_run_buy` was fixed 2026-08-18 to route through `open_position_from_pending` (previously always mis-tagged a drought pending buy as `position_source='core'`, silently defeating `check_drought_handoff`'s Case B for the 4 real `state='dry_run'`/`drought_overlay_enabled=1` nodes: 192 SOXL/brokerage, 193 AGQ/brokerage, 195 USD/brokerage, 212 FAS/soxl_ira). The fix is tested directly (`tests/test_dry_run_sim.py`), but `tests/test_fake_broker_overlay_truth_table_scenario.py`'s 5 reachable-state tests are all `_real` (live-account) scenarios — the dry_run-sim drought-handoff transition this fix newly makes reachable isn't covered there. Consider a staged live-test-node-setup pass on one of the 4 real nodes above (all currently flat, no in-flight drought positions as of 2026-08-18) to get organic dry_run-sim proof, or add a dry_run-tagged truth-table test. Not scoped.

## [live-trading] Idea, raised 2026-08-18 (RETL top-up fix, paired review) — pin `target_notional` into `pending_buys` at order-placement time instead of resolving it fresh at fill time
Structural fix for the broader "frozen vs live" sizing-basis question: a trailing-buy can rest for days before filling, and `_reconcile_fill`'s top-up target is currently resolved live (`_last_sale_recovery(node)` or a passed override) rather than pinned to what was true when the order was placed. Sound idea, not urgent, needs its own paired review. Came out of the same 2026-08-18 RETL top-up incident fix.

## [live-trading][performance] Raised 2026-08-19 (incident #13 follow-up) — poll-loop orchestrator/concurrency redesign, not scoped
Traced live: `active_signals.py`'s `while True:` loop processes 72 active nodes + all open positions sequentially, one `_guarded()` call at a time, each real Schwab call measured at 1-3s. Exact per-cycle timing is unmeasured (no instrumentation exists — see Task #3, being built now, which adds section-level timing first). Idea raised: an orchestrator that spins off a dedicated task/thread per real open position FIRST (ahead of routine entry-scanning across flat nodes), since a position already in an action state (fill pending, exit armed) is higher-priority than a cold entry scan. Real complexity: order-placement paths already have real locking (per-(account,ticker) lock, `_position_lock`) built after actual race-condition incidents (2026-07-22 oversell fix) — any concurrency redesign has to preserve those, not just parallelize blindly. Scope this only after Task #3's instrumentation gives real per-section timing data to confirm where the actual bottleneck is. Needs its own design pass before any code — bigger than a patch, not for `research` to start unprompted.

## [live-trading][testing] Raised 2026-08-19 (Task #7 build) — 3 real follow-ups from the new watch_list_overlay_link traceability check, not built this session
(1) The link is node-keyed, not parameter-keyed — `set_drought_config`/`set_addon_config` still don't touch or invalidate `watch_list_overlay_link`, so a validated node's config can be silently changed afterward with the check staying green (SOXL is currently the only linked node, so not an active exposure yet, but the mechanism has no protection against it). (2) KORU (wl_id=202, roth, $10k) was backfilled with a `NO_REAL_SELECTION` verdict citing its confirm_days=5 rejection, but its LIVE config is confirm_days=10 — a different value never itself validated one way or the other; worth confirming whether confirm_days=10 specifically was ever checked, not just that the ticker's drought mechanism broadly was rejected once at a different config. (3) Whether/how to wire `TRACEABILITY_CHECKS` into the daemon's automatic Slack alerting (currently deliberately NOT wired — see `signals_invariants.py`'s own comment) needs a real decision: an ack/exemption mechanism (mirroring `coverage_deviations`/`explain_deviation`) or a dedup-on-change-only alert, not the naive "post 21 lines 3x/day forever" shape a first draft had (caught by paired review before shipping).
