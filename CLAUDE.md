# Trading Alpha Engine

## Live-State Verification Rule (read this first)
Real incident, 2026-08-09/10: asked to diagnose a stuck RETL pending-buy order, the agent stated a chain of confident, wrong claims with zero evidence behind them — the daemon "crashed," reminders "continued after" the crash, a "manual routine" existed, timestamps "aligned" a certain way — each stated as fact, each corrected only after the user pushed back, several times in the same investigation. The root cause: having the general instruction to verify (see `feedback_adversarial_verification_exact_math` in agent memory) did not, in practice, stop the agent from answering before checking. An instruction is advisory; it doesn't mechanically block a confident-sounding sentence from being written before the evidence exists.

**The rule, stated as a hard gate, not a preference**: for ANY claim about live/current system state — daemon status, a DB row's contents, an order's fate, why something happened, whether two timestamps represent the same moment — the exact command/query that proves it must be run and its output actually inspected in the same turn, BEFORE the claim is written. Never state a live-state fact from an earlier turn's claim, from memory, or from a plausible-sounding inference without re-running the check right now. If two pieces of evidence don't line up (e.g. a timestamp that seems to contradict another), that is a stop sign — resolve it before writing another sentence, don't paper over it or pick whichever fits the narrative already in progress. Don't assume environment facts (timezone, config defaults, whether a table's timestamp column is UTC vs local) — check them directly (e.g. `date`/`timedatectl` for system timezone) rather than assuming based on what "usually" holds.

This applies with extra force under a fast back-and-forth (many short user messages in a row) — that is exactly the condition under which this incident happened, because rapid exchange pressure pushed toward answering immediately instead of pausing to check.

## Session Commands
**Research tag**: the one recurring case that actually causes cross-session confusion is a long-running, unattended Research sweep (backtest kernel/sweep/candidate work, often left running for hours) overlapping with a normal interactive session on anything else. So the only tag is `[Research]`, applied at `session close`/`session wrap` when that's what the session mostly did; everything else is untagged (no separate "Execution"/"Platform" label to maintain). Entries before 2026-08-08 predate this and are untagged too, same as any other non-Research entry.
- `go` — default: read the first ~60 lines of `docs/session_cache.md` (all entries, no filtering), the full `docs/backlog_cache.md`, and `docs/backlog_resolved_recent.md`, and summarize where things stand — the normal case when only one session is active. Also run `.venv/bin/python scripts/check_backlog_stale_dates.py` (cheap, mechanical) and surface any `(revisit ~date)` entries it flags as past due, instead of relying on catching it by eye during the read.
- `go research` — only when deliberately starting a **concurrent** session alongside an active/expected Research sweep, to scope the read to `[Research]`-tagged entries (plus untagged legacy ones) so it doesn't get confused reading notes from whatever else is happening in parallel. To keep this cheap, run `.venv/bin/python scripts/backlog_filter.py --tag backtest` (covers both `docs/backlog_cache.md` and `docs/backlog_resolved_recent.md` in one call) instead of reading either file directly — it extracts only entries carrying a `[backtest]` tag (anywhere in the tag list, e.g. `[backtest][live-trading]` counts) before the content reaches context, not just filtered by eye after a full read (found 2026-08-08: reading the full files and summarizing only the matching entries paid the same context cost as no filtering at all). This is a lossy filter (a research-relevant item could in principle be mistagged) — acceptable since the weekly cleanup pass re-triages/recategorizes backlog items anyway, so a miscategorized item won't stay invisible for long.
- **Both `go` and `go research`**: since the full (or filtered) content is already being read into context either way, present deferred items (anything carrying a `(revisit ~YYYY-MM-DD)` tag, plus other clearly parked/low-priority entries) grouped separately in the summary from actionable-now items — don't mix them into one flat list. This is a presentation-time grouping only; **never restructure `docs/backlog_cache.md` itself to achieve this** (no physical reordering, no new section header written to the file) — the file's line-by-line convention (see its own header note) stays as-is, and it's frequently being edited concurrently by other sessions, so mutating it for a purely cosmetic read-time grouping is unnecessary risk for zero benefit (decided 2026-08-10, after almost doing exactly that).
- `session close` — session end: write the session summary entry (starting with `## <date> — <title>`, or `## <date> — [Research] <title>` if the session was mostly Research-sweep work, no surrounding `---`) to a file and run `python scripts/session_cache_update.py <file>` (or pipe via stdin) — this mechanically prepends to `docs/session_cache.md` (cap 10, drop oldest, gitignored) and appends to `docs/conversation_summary.md` (permanent, uncapped, committed) in one shot, no need to read either file first. Commit only `docs/conversation_summary.md`. No tests, no skills.
- `feature wrap` — mid-session feature complete: update relevant docs, review pre-commit checklist (`docs/pre_commit_checklist.md`) manually, and commit. Does not trigger session close. **Do not invoke any skills (especially verify) during this command.**
- `session wrap` — in order: (1) update relevant docs, (2) review pre-commit checklist manually, (3) if `active_signals.py`, any `signals_*.py`/`schwab_*.py` module, or any backtest kernel module (`backtester.py`, `strategies.py`, `run_optimization_sweep.py`) changed this session: (3a) spawn TWO Opus review agents (`Agent` tool, `model: opus`) in parallel against the real diff of those changed files: one **independent/cold** (no session context, just the diff plus enough file-reading to judge it) and one **contextual** (given the session's actual intent/design decisions, so it can judge whether the implementation matches intent, not just internal consistency) — a single cold review is not sufficient, this pairing is required; once both return, do a rebuttal exchange (each agent's findings shown to the other, asked to confirm/refute) before summarizing to the user (severity + one-line description each, noting which findings survived rebuttal) and starting any fixes, then resolve any CONFIRMED findings before proceeding — a kernel bug is just as load-bearing as a live-trading bug, since paper trading/dry run/live all inherit whatever the kernel got wrong, (3b) if `active_signals.py`/`signals_*.py`/`schwab_*.py` changed, additionally run `python scripts/live_sim_harness.py` and confirm all scenarios pass, (3c) check `scripts/coverage_registry.py` (the Trade-Flow Accountability Grid) for any row whose `code_path`/`offline_coverage`/`notes` the session's changes made stale or wrong, and add a new row for any genuinely new logic branch (a new `scenario_key`/`log_coverage_event` call site) — flag a `scenario_key` collision with an existing row if the change reuses one; also check whether any truth table (`tests/test_entry_abandon_truth_table.py`, `tests/test_fake_broker_overlay_truth_table_scenario.py`, etc.) has a reachable-state/transition assumption the change invalidates, and whether canary/live-test setup (`docs/watchlist_candidate_checklist.md`, `docs/new_mechanism_promotion_standard.md`, `scripts/list_canary_nodes.py`'s A-F design) needs a new scenario for the change — state explicitly when none of this applies rather than skipping silently (found 2026-08-07: a real Grid update got left for the user to explicitly request, twice, after a live-incident fix), (4) write the session summary entry via `python scripts/session_cache_update.py` (see `session close` above) to update both `docs/session_cache.md` and `docs/conversation_summary.md`, (5) commit everything including both files. **Do not invoke any skills** (the Opus review agent above is a direct `Agent` tool call, not a skill).

## Research Log
`docs/research_log.md` (permanent, committed, append-only) is a lab notebook of real experiments run against this system — hypothesis, method, result, verdict, follow-up — distinct from `docs/backlog_cache.md` (current, actively-trimmed pointer list of **open items only**, read in full every session start), `docs/backlog_resolved_recent.md` (rolling ~7-day window of recently-resolved one-liners, for session-handoff context — also read in full every session start, but pruned of anything older since `deep_backlog.md` already has it permanently), `docs/deep_backlog.md` (permanent full-detail archive of every backlog item, resolved or not — resolved items are kept in place and marked ✅, never deleted), and `docs/conversation_summary.md` (session narrative). Append an entry to the research log directly (no capping/pruning logic, no wrapper script needed — a plain append) whenever a real experiment produces a finding, whether or not it also produces code changes or a backlog item — don't wait for session close. When a backlog item is resolved: (1) add or update its full-detail entry in `docs/deep_backlog.md` (marked ✅, matching that file's existing style) so it's never lost, (2) if the resolution is mostly research narrative (hypothesis/method/result) rather than an action record, also write a `docs/research_log.md` entry and have the `deep_backlog.md` entry point to it instead of duplicating the writeup, (3) prepend a one-line pointer to `docs/backlog_resolved_recent.md` (dropping any entries there older than ~7 days), and (4) remove the item from `docs/backlog_cache.md` entirely (not just shrink it — resolved items don't stay in the open-items file at all now).

## Project Overview
A z-score mean reversion backtesting and optimization system targeting leveraged ETFs. Runs parallel grid sweeps over take profit, stop loss, and hold time parameters to find optimal strategy configurations with positive alpha vs SPY. Now in live trading phase with manual execution via Schwab.

## Live Trading — Current State
- **Canary/dry_run infrastructure consolidated onto FAS/FAZ in `soxl_ira`, 2026-08-13** (was 11 tickers — DIA/FAZ/IVV/IWM/JDST/QID/QQQ/SDOW/SPXU/TWM/XLF — spread across `ira`, each running one detuned scenario). Found while investigating a real Grid-computation bug (`canary_time_exit` showed `wired-never-fired` despite real proof — see `docs/backlog_cache.md`, HIGH priority, not fixed) that the *config* (`z=0.1`/`window=5`), not the ticker, is what makes a canary reliable. 12 new `dry_run` nodes built directly in `soxl_ira` on FAS (Direxion 3x bull financials)/FAZ (3x bear, matched leverage, user's explicit call) — one per canary-letter scenario per ticker, same real exit-side config as the ticker each replaces. The G "bull/bear pair" scenario (was JNUG/JDST, cross-underlying) is redefined to check FAS-vs-FAZ's own inverse relationship (user-confirmed). `scenario_expectations` repointed to the new nodes; **old 11 tickers' nodes AND their scenario_expectations rows deliberately still running in parallel with the new ones in `ira`, not yet retired** — wait for organic confirmation on the new nodes first. **Same-night 4-way paired review (dual Opus + Sonnet + Fable) found and fixed 6 more real bugs**, most notably `coverage_check.py` couldn't disambiguate the 5 same-shaped FAS scenario nodes by wl_id (now fixed, threaded through) and the nightly table's trigger price was look-ahead-biased (~1.5% real error, now fixed to match production's prior-day slice). Full detail: `docs/design.md`'s 2026-08-13 entry. Verified: full suite 818/818.
- **`brokerage` (real Reg-T margin, $20k equity) flipped `trading_enabled=True` 2026-08-12**, after both real leverage-calculation gaps were fixed the same day: core-entry cash check (`get_account_balance` now reads real settled `cashBalance`) and add-on's buying-power check (`get_leveraged_buying_power`, real per-ticker leverage-aware margin requirement, clamped to Schwab's own real-time `buyingPower` field — commit `a16ede6`). 3 real `state='live'` nodes now trade for real: AGQ (id=203), ETHU (id=201), JNUG (id=205), all `addon_enabled=1`, `starting_notional=$6,000` core each. `signals_invariants.check_brokerage_not_live_with_unresolved_leverage_gap` (the human-decision gate referenced by the 2026-08-10 entry below, which is now stale on this point) was removed per its own docstring's instruction, once the user made the decision explicit ("flip it"); a narrower replacement, `check_margin_floor_zero_for_trading_enabled_accounts`, guards the one real remaining lever (`accounts.margin_floor`, a core-entry-only knob distinct from the add-on clamp — see its docstring for why nonzero would be a deliberate future design reversal, not a bug). Paired Opus review (independent-cold + contextual + rebuttal) of the removal found no issue with the removal itself, but caught 2 stale docstrings (`get_account_margin_requirement`/`get_leveraged_buying_power` in `schwab_client.py` both claimed the leverage-aware path was unwired/reverted when it's actually live) — both fixed. **Daemon caveat**: `active_signals.py` caches `schwab_safety.ACCOUNTS` once per process (no `reload_accounts()` call anywhere in the live poll loop) — this flip has zero live effect until the daemon is restarted.
- **3 more real `ira` $10k nodes added 2026-08-11 (GDXU id=208, KORU id=209, DFEN id=210), SOXL's missing drought overlay fixed (wl_id=92), `SCHWAB_AUTOMATION_TICKERS` gap closed (DFEN/ETHU/SOXS were missing, blocking real orders).** `ira` now runs 7 of the 10-ticker real candidate list (DFEN/DPST/GDXU/KORU/NUGT/SOXL/SOXS); the other 3 (AGQ/ETHU/JNUG) sit in `brokerage`, still dormant. New `signals_db.watch_list_candidate_link` table (+`set_candidate_link`/`get_candidate_links`/`set_drought_config`, `scripts/node_candidate_trace.py`) gives a real, explicit node→candidate link — built after a trust-breaking session of wrong param-matching claims (missed a config axis, compared the wrong DB column for `TrailingExitZScoreBreakout` tickers, matched against the wrong reference row). 13 real nodes now traced. HIBL remains the one open gap — no record of its real candidate. Needs a daemon restart to pick up the `.env` fix. Full detail: `docs/deep_backlog.md`'s 2026-08-11 (late) entry (top).
- **`ira` account activated for real, 2026-08-10 — entering the "modest live capital" roadmap phase.** A 9-ticker CAGR-first promotion-shortlist checklist pass (`docs/watchlist_candidate_checklist.md`) led to `ira` flipping `trading_enabled=True`/`account_type="margin"` (real, user-confirmed limited-margin IRA — same category as `soxl_ira`) with 4 real `state='live'` nodes at $10k each: **DPST** (id=197, CAGR-first pick — corrected mid-session from a wrong robust_alpha-based pick), **JNUG** (id=200), **NUGT** (id=91, flipped from an existing `paper` node), **SOXL** (id=92, same fix). `roth`/`brokerage` stay dormant (`trading_enabled=False`) — `roth` expected to fund soon (~2026-08-12), `brokerage` needs more study first (real, still-unresolved leverage-gap block, `signals_invariants.check_brokerage_not_live_with_unresolved_leverage_gap`). 6 more promotion nodes created dormant (small/share-count notional) in `roth`/`brokerage` for later activation: GDXU/roth, KORU/roth, DFEN/roth, ETHU/brokerage, AGQ/brokerage. Paired Opus review (independent-cold + contextual) of the `schwab_safety.py` flip found and fixed 3 real bugs before/just after they could bite: `add_node`'s dedup silently skipped SOXL's new-row attempt (same bug independently caught for NUGT beforehand); `ira`'s `daily_order_cap=10` was already saturating from 19 canary dry-run nodes alone (bumped to 100, matching `soxl_ira`'s precedent); a real JNUG ticker+account collision between a canary (`dry_run`) node and the new real (`live`) node made the node-level automation pause a silent no-op and could have blocked the real node's exit SELL (canary relocated to `soxl_ira`). Also found: `scripts/live_sim_harness.py` and 11 test files hardcoded `account='ira'` assuming it would stay dormant forever — all repointed to `roth` (confirmed via a full 707/707 passing suite run). GDXU's old `soxl_ira` node (the validated-drought-config + `gap_resize_and_topup` staged-test vehicle) was sunset in the same pass — **ERY (id=153, `soxl_ira`) now carries the `post_fill_topup` staged-test role**, real order staged 2026-08-10 (resting). Full detail: `docs/deep_backlog.md`'s 2026-08-10 entry.
- **Slack alerting redesigned around "capital at stake," 2026-08-08.** `signals_helpers.has_capital_at_stake(node)` = `state=='live'` AND account `trading_enabled=True` AND `starting_notional >= cfg.CAPITAL_AT_STAKE_THRESHOLD` ($10k default, env-configurable) — zero nodes cross it as of 2026-08-08, even `soxl_ira`'s 11 real live nodes ($500-$2,500, a deliberate small proving-ground tier). Below the bar: zero real-time Slack (routine and anomaly-adjacent alike, including the reminder loops) — automation is the real protection, EOD/on-demand review covers the rest. New `check_intraday_risk_review()` (in `active_signals.py`'s poll loop, every cycle, 9:15am-4:00pm ET trading days) reviews `trading_incidents` + a narrow `coverage_events` scan for anything new and alerts only if something's there. A judgment-based (LLM-driven) review layer was discussed and deliberately deferred — see `docs/backlog_cache.md`. Full detail incl. the paired-Opus-review findings (4 HIGH bugs fixed, most notably the reminder loops being ungated and the Reference Report's empty-state path silently dropping the kill-switch status/Stop Engine buttons): `docs/deep_backlog.md`'s 2026-08-08 entry.
- **"Daily-track" paper trading built 2026-08-05, not yet run against the real DB.** A second `watch_list` paper node per v5-watchlist ticker (`paper_role='daily_sync'`), alongside the existing free-running "live-track" (unmodified, live-tick pricing). Isolates whether paper trading's price source (live tick vs. the backtest's hourly-bar Close) explains why paper trading's real trade sequence diverges from a backtest replay — `compute_buy_signal`'s `daily_sync` branch always prices off the last closed hourly bar's Close, ignoring live ticks and any caller-supplied `price_override`. A new nightly EOD job, `paper_trading.reconcile_daily_track_nodes`, classifies each node's actual state against a fresh backtest replay every night and writes one full diagnostic row to `daily_track_reconciliation_log` — **pure observation, never mutates daily-track's real state** (no auto-open/close/halt). This was a deliberate mid-session reversal from an initial reconcile-and-resync design: the user's call — "reconcile as one action (how far are we) vs sync (prepare for next day)" — since auto-correcting divergence erases exactly the signal this comparison exists to produce. Both entry-miss and exit-side (SL and trailing-stop wick) divergence get a "would Close alone explain this" counterfactual for classification only. `daily_sync_halted_at`/`set_daily_sync_halted`/the gate in `start_paper_buy` are left in place, inert, as scaffolding for a possible future "sync" tool — nothing currently sets the flag. Also fixed in the same session: `scripts/export_trades.py::simulate_trail_exit_chaos` never tracked `arm_i` at all (hardcoded `None` in every trade dict), silently misrouting every genuine trailing-stop exit for `TrailingExitZScoreBreakout` nodes in any consumer that reads it. `scripts/add_daily_track_paper_nodes.py` (built, tested, not run for real) would create the 10 v5 daily-track nodes when ready. Full design history: `docs/design.md`'s "Two-track paper trading" sections (several, chronicling the design's evolution through 3 paired Opus review rounds).
- **Ticker exclusion, decided 2026-08-04: never trade USO in any IRA/Roth account.** USO (and commodity-futures ETFs generally) are typically structured as limited partnerships issuing a K-1, unlike the standard '40-Act/1099 structure of SOXL/AGQ/etc. — real UBTI/UBIT exposure if held in a tax-advantaged account. USO's earlier backlog research candidacy (see `docs/backlog_cache.md`/`docs/research_log.md`'s 2026-08-03 1x-universe screen, +105.9% alpha) is dead on this basis; don't revisit without first confirming USO's actual K-1 status and, if still a K-1 filer, restricting it to taxable brokerage only.
- **V5 strategy go-live checklist (started 2026-08-02, open)**: bar is live confirmation for every real SL/TRAIL/TIME/entry code path the two live-default strategies (`TrailingBothZScoreBreakout`, `TrailingExitZScoreBreakout`) actually use, fake-venue confirmation accepted only where explicitly flagged. Checked directly against `scripts/coverage_registry.py`/`trade_log`/`paper_trade_log`, not assumed from docs (a stale doc claim was caught and corrected in the process — see the `live_sanity_check.py`/oversell-constructibility closures in `docs/backlog_resolved_recent.md`).
  - ✅ SL exit — live + paper confirmed. Shared identical `check_exit` code between both strategies (verified by reading `strategies.py`), so one strategy's live proof covers the other's exit logic too.
  - ✅ TRAIL exit — live + paper confirmed (shared code). Caveat: SH's 2026-07-31 `trade_log` row is stored `exit_reason='TRAIL'` but was actually the hold-time-forced TIME exit that motivated the 2026-08-01 labeling fix — the fix wasn't applied retroactively, so pre-2026-08-01 `exit_reason` values aren't reliable ground truth on their own.
  - ✅ TIME exit — live confirmed (shared code; a separate, correctly-labeled live TIME row exists distinct from the SH row above).
  - N/A TP exit — doesn't apply to either live-default strategy. `take_profit`/`tp_price` in `strategies.py` only *arms* the trailing-exit state (`state['trailing']=True`) for these two classes; no code path returns `'TP'` as a terminal exit reason for them (other strategy classes do use it).
  - ✅ `post_fill_topup` — live-reconfirmed 2026-08-10, via an unplanned 3rd avenue (neither SH nor ERY's staged test): RETL's (id=143, `soxl_ira`) real drought-overlay entry fired `drought_entry_placement` then a genuine successful top-up (`_reconcile_fill`'s `top_up` `result='placed'`, 2 shares @ $9.9150, position topped up 78→80 shares) — the first-ever real `'placed'` top-up event in `coverage_events`, closing the real-money risk open since the 2026-07-24 incident. Same day, ERY's staged test (2 shares vs. $500 target) also fired `top_up`, landing in the `overspent_no_corrective_sell` branch instead (overspend=$11, notify-only by design) — traced and confirmed correct, not a bug: ERY's `_last_sale_recovery` basis came from a stale 1-share prior test trade (~$10.50), not the $500 target, so the 2-share fill exceeded that tiny basis. Both are real, on-file evidence now (`scripts/coverage_registry.py`'s `post_fill_topup` row: `verified-live`, 5x live). **Follow-up still open**: SH's node (id 135, `soxl_ira`) still has `trail_buy_pct` widened 1%→5% from the now-resolved wait — revert back to 1%.
  - ✅ `buy_fill_reconciled` (core trailing-buy entry mechanism) — live confirmed, 5x.
  - ⏳ `market_buy_placement` (`TrailingExitZScoreBreakout`'s market-buy entry, not trailing-buy) — **fake-venue confirmed** (`test_fake_broker_entry_scenario.py::test_market_buy_entry_fills_and_protects_with_a_real_stop`, fixed 2026-08-03 — the registry's proof-scanner had a structural blind spot for `scenario_expectations`-mechanism rows, see `scripts/coverage_registry.py`'s module docstring; this test's proof was real all along, just undetected). No live proof yet — only a past human-explained failure on file. **Plan updated 2026-08-10**: DPST's `soxl_ira` node was resynced off `TrailingExitZScoreBreakout` onto `TrailingBothZScoreBreakout` (moved to `ira`, id=197) as part of the 9-ticker promotion-shortlist checklist pass, so it no longer exercises this path. **YANG (id=156, `soxl_ira`, `entry_timing='open_check'`) is now the only live `TrailingExitZScoreBreakout` node at all** — currently 6.98% from its z-cross trigger (`scripts/watchlist_status.py`). Same organic-wait approach as `post_fill_topup` — no manual open, no config forcing, just wait for YANG's real signal and confirm the market-buy placement actually fires correctly when it does.
  - ❌ `buy_fill_reconciles_correct_node` — no live proof (node-disambiguation on fill when multiple nodes could match). **No near-term plan** — structurally blocked, same reason as the similar `wired-never-fired` rows flagged in the 2026-08-02 coverage triage: no two live `soxl_ira` nodes currently share a ticker, so there's no real scenario to disambiguate against without adding a second live node purely to manufacture one, which isn't planned.
  - ⏳ `pinned_entry_trigger` (`open_check` pinned-timing entry) — **fake-venue confirmed** (`test_fake_broker_pinned_entry_scenario.py::test_pinned_entry_real_signal_places_real_trailing_buy`, same scanner-blind-spot fix as `market_buy_placement` above, 2026-08-03). No live proof yet. **Correction**: this Grid row's `check_mechanism` is scoped specifically to the canary IWM/TWM nodes (`scenario_expectations` id 9/31, `scenario_key='canary_pinned_entry'` — checks whether *that specific canary trade* closes `exit_reason='TRAIL'`), not to "any node exercising `_scan_pinned_entry`." A real DPST or ERY pinned-entry fire would be genuine evidence the mechanism works (both are in `AUTOMATION_ENABLED_TICKERS`, so `_scan_pinned_entry` really does process them), but would **not** flip this specific Grid row — only IWM or TWM closing a trade does that. **Plan**: IWM (`ira`, canary, currently -0.59% from trigger — essentially armed) and TWM (3.17% away) are the real candidates to watch for this box specifically; DPST/ERY's waits above are still worth having for their own sake (`market_buy_placement`, and general real-world confidence in the pinned mechanism) but track separately.
- **Fixed 2026-08-02**: two real gaps found in a "what's left to fix" backlog pass. (1) A same-account **double-buy gap** confirmed live 2026-07-24 (two real resting `TRAILING_STOP` BUYs left `get_account_balance` completely unchanged) meant `notional_cap`/the cash check couldn't stop a 2nd real BUY once the first had already *filled* — `check_order` now blocks a BUY when `get_open_position_for_account(ticker, account)` already returns a position, unless `is_protective=True` (the sanctioned post-fill top-up). Was a latent limitation (ticker+account-keyed, not node-keyed) — **fixed 2026-08-10** via `node_id` threaded through `schwab_client`'s 8 order-placement/replace functions into `check_order`; found live for AGQ/`brokerage` before it could bite (same collision shape as the JNUG/`ira` fix earlier that day). See `docs/deep_backlog.md`'s 2026-08-10 entry. (2) **TRAIL-exit reminder spam**: a confirmed-still-resting automated exit order (completely normal, fills on its own) used to re-post "still resting, no action needed" every hourly bar close and every `EXIT_REMINDER_MINUTES` — now suppressed until `_exit_pending_blocks`' existing `reminder_num>=3` escalation (~45min) fires; the arm-time "trailing stop activated" message remains the one real notification. A cold Opus review of the first draft found+fixed a HIGH gap (`check_exit_reminders` only runs 9:00-16:00, so suppressing near/outside that window could go silent up to ~17.75h instead of the intended ~45min) via a new `_trail_alert_should_post_now` time-of-day gate, plus a LOW gap (`notify_sell_signal` reset reminder escalation progress on every bar re-fire). Full detail: `docs/deep_backlog.md`'s two 2026-08-02 entries. Full suite: 507 passed.
- **`SIM_MODE` fail-safe default flipped 2026-08-01**, after a real incident: an ad hoc test call to `signals_notify.build_eod_scenario_review` (no `SIM_MODE` set) posted a real, unprefixed message to the live Slack channel. `signals_config.SIM_MODE` now defaults **on** (`os.environ.get("SIM_MODE", "1") != "0"`) — any ad hoc script/test invocation is safe by default. The real daemon launch command is unchanged (`python active_signals.py run` / bare `python active_signals.py`) — `active_signals.py`'s own entrypoint now runs `os.environ.setdefault('SIM_MODE', '0')` before `signals_config` is even imported, so it forces real mode for itself without needing an env var. Defense in depth: `signals_invariants.check_sim_mode_off_for_real_daemon()` (deliberately **not** in `CHECKS`/`run_all()`, since that also runs standalone via the pre-commit checklist where `SIM_MODE=1` is correct and would false-positive) is called directly, once, at `run_loop()` startup — alerts loudly if `SIM_MODE` is somehow still on for a genuine daemon run (e.g. `SIM_MODE=1` already exported in the shell before launch, which `setdefault` respects and won't override).
- **Fixed 2026-08-01 (late)**: `handle_entry_price` (the manual market-buy "Executed" confirmation, used by `TrailingExitZScoreBreakout` automation-scoped tickers) never auto-placed a protective stop, unlike its trailing-buy sibling `handle_trail_buy_fill_price` — found via a fake_broker test-quality audit, not a diff review. Every real live BUY entry today still requires manual Slack confirmation regardless of automation scope, so this was a live, present-day gap, not legacy code. Fixed with the identical `AUTOMATION_ENABLED_TICKERS` gate, ordered *after* the "Executed" confirmation message (not before — `handle_entry_price` is also the manual catch-up/backdated-entry flow, where the new SL call's self-correcting forced-sell branch could otherwise alert before the user sees their fill confirmed), wrapped in a bare try/except so nothing in it can block that confirmation. Full detail: `docs/deep_backlog.md`'s 2026-08-01 (late) entry, including a new paired independent+contextual Opus review pattern (adopted this session) that caught both this ordering issue and a false-positive finding a single cold review missed.
- **Nightly review/plan cycle, added 2026-08-01** (`signals_notify.build_eod_scenario_review`/`build_tomorrow_plan`, wired into `active_signals.py`'s existing 16:05 ET EOD slot, both the live-loop trigger and the startup catch-up path — runs automatically, not a manual step): posts one Slack message covering (1) a readiness headline (`verified/total (%)` from `scripts/coverage_registry.py`'s live-computed per-branch status — the direct answer to "how close are we to trading material money" and "what critical code paths haven't been tested"), (2) the canary scenario check (reuses `coverage_check.py`), (3) real live + paper activity today (closed trades and still-open positions, diffed against the prior day's `daily_plan` row when one exists), (4) tomorrow's plan written fresh to the new `daily_plan` table (canary: static copy of `scenario_expectations`; live/paper: real open positions' actual SL/arm/trail/TIME triggers from their own config — only plans around what's already open, doesn't predict new entries). Built after 5 prior sessions of this cycle (evening review → next-day plan → morning restart+sanity-check) not sticking as a manual habit — now real code instead of a conversation pattern. Morning sanity check (daemon restart + `daemon_status.py`/`signals_invariants.py`) is still manual, done by the user.
- **Watchlist**: `watchlist_id=65` ("Live v5", `is_active=1` in the `watchlists` table) is the actual active watchlist as of 2026-07-20, superseding `watchlist_id=57` ("Live v4", now archived/inactive, same pattern as the earlier 9→57 supersession) — `get_watchlist()` with no argument resolves via `get_active_watchlist_id()`, which reads this flag. **All 10 v5 nodes in watchlist 65 are `mode='research'`** (built via `scripts/build_v5_watchlist.py`, not yet candidate-tested — see `docs/backlog_cache.md`), so nothing changes for real trading yet. They were briefly flipped to `mode='live'` 2026-07-23 for a real-account test day (`dry_run=True` throughout, no real orders resulted) and flipped back to `research` 2026-07-25 to restore continuous paper-trading coverage on the actual live tickers — canary and `soxl_test` nodes were untouched by either flip. watchlist 57's 18 v4 nodes are preserved there, untouched, just not polled. The 10 tickers (AGQ, DPST, GDXU, HIBL, KORU, NUGT, SOXL, UDOW, USD, YANG) were selected from the full 18-ticker `v5` resweep by cliff-safe robust alpha, picking `TrailingBothZScoreBreakout` vs `TrailingExitZScoreBreakout` per ticker — see `docs/deep_backlog.md`'s 2026-07-20 resolved entry and `docs/research_log.md` for the full comparison. Always check `watchlists.is_active` before assuming which watchlist_id is live — don't trust a hardcoded id in docs. `signals_db.get_watchlist_audit(limit=200)` gives a mechanical log of every watchlist/node mutation going forward (added 2026-07-18, no history before that date). **Watchlist 65 now has 16 nodes, not 10**: 6 synthetic canary proof-of-life nodes (SPY/QQQ/IWM/DIA/VOO/XLF, `version='canary'`, `mode='research'`) were added 2026-07-23 via `scripts/add_canary_nodes.py` — see `docs/deep_backlog.md`'s 2026-07-23 resolved entry for what each one exercises. They're tagged `🧪CANARY` in the Morning Report and never get a "Manually Open" button (real-trade actions are deliberately suppressed for them).
- **Morning Report was silently empty for weeks, fixed 2026-07-23**: `build_reference_table` filtered to `mode == 'live'` only — since every watchlist node has been `mode='research'` since the 2026-07-20 v5 promotion, the report was posting successfully (header/kill-switch/context) with zero candidate rows underneath the whole time, no error anywhere. Fixed: filter removed, all nodes render (tagged `Mode`/`🧪CANARY`/`(research)` so nothing reads as an actionable live trigger). This immediately exposed a second bug — 16 full rows hit Slack's 50-block-per-message limit and were rejected outright (`invalid_blocks`) — fixed by collapsing each row's up-to-3 separate `actions` blocks into one. Two related observability gaps found/fixed in the same investigation: `logs/active_signals.log` never flushed (`buffering=1` now set) and `slack_message_log` recorded intent, not delivery (now has a real `error` column, populated after the actual send attempt, with `send_reference_report`/`_post_message` returning `(channel, ts)` reliably so a real permalink can be checked next time instead of guessing). See `docs/research_log.md`'s 2026-07-23 entry for the full misdiagnosis-then-root-cause writeup.
  - **All 18 tickers currently `research` mode — no ticker is live-trading manually right now** (2026-07-18 decision: v4's tight `trail_buy_pct` is too fast to catch manually; manual live trading is paused pending the Schwab automation engine actually driving entries). 18 = the original 12-ticker watchlist minus EDC (AGQ, DPST, GDXD, GDXU, HIBL, KORU, LABU, NUGT, SOXL, TQQQ, YANG) + 7 non-watchlist candidates screened in the 2026-07-18 walk-forward check (DUST, NAIL, RETL, UDOW, USD, UVIX, ZSL). All run v4 (SL=1%) nodes. `watch_list.annotation` on each node records walk-forward-clean vs. flagged-with-a-doubt status (see `docs/backlog_cache.md` for exact tickers/detail). Don't trust a hardcoded mode list here — run `python scripts/watchlist_status.py` for the current mode/trigger-distance table straight from the DB.
  - **EDC's v3.27 node removed from `watch_list` entirely, 2026-07-19** (not just excluded from automation scope) — it has **one open live position** (423 shares @ $73.57, opened 2026-07-16, real exit params `stop_loss=15`/`arm_sell_pct=20`/`trail_sell_pct=7`/`max_hold_hours=112` already baked onto the `open_positions` row at entry time), which the user is now tracking/unwinding manually via spreadsheet. Removing the `watch_list` row only stops new BUY polling — SELL alerts for the real position keep firing normally regardless (`check_sell_condition` keys off `open_positions`, not `watch_list`).
  - `schwab_safety.AUTOMATION_ENABLED_TICKERS` — now sourced from `SCHWAB_AUTOMATION_TICKERS` in `.env` (moved off a hardcoded Python set 2026-07-19, same rationale as `SCHWAB_ACCOUNT_<NAME>`: deployment-specific, shouldn't live in shared code; `.env` is gitignored so `schwab_safety.sync_automation_scope()`, called once at `run_loop` startup, logs any scope change to `watch_list_audit` instead). **Widened 2026-07-19 to all 18 v4 research-mode tickers** (was GDXD-only), each set to `starting_notional=5000` (was the flat $50k default) — still all `research` mode, so this only widens who gets the paper-trading simulation below, not who gets a real/dry-run order attempt. A research-mode ticker in this set runs a real **paper-trading simulation** (`paper_trading.py`, built 2026-07-18): `active_signals._scan_buy_signals` routes its BUY signals to `paper_trading.start_paper_buy` instead of the silent research print, `update_paper_buys()` (called every poll, unconditionally) tracks a continuous running-low and simulates the trailing-buy bounce-fill, and `check_paper_sells()` runs the same `signals_compute.check_sell_condition` exit state machine against the simulated position. All of this writes to new `paper_positions`/`paper_trade_log`/`paper_pending_buys` tables — never `open_positions`/`trade_log`/`pending_buys`, and never calls `schwab_client`/`schwab_safety` at all (this is a pure simulation, independent of `dry_run`, which by itself still produces zero fill/P&L). Run `python scripts/paper_trading_status.py` for the current paper pending-buy/position/closed-trade state. **Known limitations**: (1) fills are sampled at `POLL_SECS` cadence, not tick-perfect against where a real broker's continuously-live `TRAILING_STOP` would have filled; (2) dedup is ticker-only (`db.get_open_position(ticker, paper=True)`), not `(ticker, window)`-aware like the real `open_position()` — fine while every automation-enabled ticker has one node, would collide if a ticker ever ran two. Separately, **SELL-side automation (`_attempt_automated_sell`) is now mode-gated too, not just ticker-gated** — fixed in the 2026-07-31 exit/arm/entry hardening session (commit `f256fc4`): `_attempt_automated_sell` (`signals_notify.py` ~line 93) looks up the position's node by `wl_id` and skips (logs `automated_sell_mode_skip`, falls back to manual) unless `node.mode == 'live'`, mirroring the BUY-side gate. Confirmed 2026-08-01 via a dedicated fake-broker test (`tests/test_fake_broker_automated_sell_mode_skip_scenario.py`) — this doc previously said "not yet fixed," which was stale.
  - All 19 use `fixed_sl=1%` (v4 SL sweep winner) **except EDC (`fixed_sl=15%`, its legacy v3.27 node, kept for the open position)**. `watch_list.entry_timing` (default `'close'`) and `watch_list.starting_notional` (default `50000`) are real per-node columns — all 18 v4 nodes use `entry_timing='open_check'` (needs `active_signals._OPEN_CHECK_WINDOWS`, an earlier 9:31–9:40/14:31–14:40 poll, alongside the normal close windows); GDXD is still the only `starting_notional=5000` node (a deliberately small $5k pilot book, not the usual $50k).
  - **Fixed 2026-07-18**: `signals_db.add_node` gained a `fixed_sl_override` parameter — previously it always computed `fixed_sl` from `config.json`'s stale global default for any `uses_fixed_sl` strategy, silently ignoring the real per-node SL value (worked around via direct SQL during the v4 promotion). Legacy callers unaffected (default `None` preserves old behavior); future SL-swept promotions should pass the override explicitly.
- **Signal windows**: 10:25–10:40 AM ET and 15:25–15:40 PM ET (matches backtest target_hours=(9,14)). Hourly bars are labeled by **start** time (e.g. the "14:30" bar spans 14:30–15:30) — the bar-close signal actually being checked in the 15:25–15:40 window is the **14:30** bar (last one fully closed by then), not the 15:30 bar. `target_hours=(9,14)` reflects this: only hours 9-14 (9:30-14:30 anchors) are in the backtested grid: the 15:30 partial bar is excluded entirely.
- **Open positions**: change constantly — run `python scripts/open_positions_status.py` for the current live list straight from `open_positions`, don't trust a hardcoded snapshot here. Note: `open_position()`'s `signal_time`/`entry_time` split lets a late/manual entry be backdated to the real signal bar, so `max_hold_hours` counts from the actual dislocation rather than the late entry — used at least once (KORU/SOXL, 2026-07-06 catch-up entries off a missed 2026-07-02 signal). **Distinct from a separate 2026-07-31 fix**: the trailing-buy bounce-fill wait (the strategy's own designed "wait for price to bounce above the running low" mechanic, not a missed/late entry) was incorrectly anchored to the original signal time too, silently eating into the position's hold-time budget — the backtest kernel tracks this wait separately (`wait_bars`) and never charges it against `held`. Fixed across all 4 real/paper/dry-run trailing-buy fill paths to anchor to the real fill moment instead; the manual catch-up backdating above is untouched, genuinely a different case.
- **Execution workflow**:
  - `TrailingExitZScoreBreakout`: Stage limit order pre-market at absurd low price → Slack fires at bar close if price confirmed below lower_band → edit order to market and submit (~5 seconds).
  - `TrailingBothZScoreBreakout`: same bar-close signal fires the alert, but place a **trailing buy order at `trail_buy_pct`%** instead of a market order — the broker tracks the bounce-above-running-low entry itself (live code does not simulate this state machine). Three-step Slack confirmation (2026-07-10): BUY alert → tap **"Trailing Buy Order Placed"** once the order is resting at the broker (no position opens yet, no price asked — fill price isn't known at this point) → tap **"Filled"** separately once it actually executes, entering the real fill price (this is what opens the position and starts arm/SL/trail triggers). Reminders nag every 15 min through both phases until Filled/Skipped resolves it.
  - 🔶 in morning report = set phone alarm for 10:28 and 15:28.
- **Stop loss**: Schwab stop order set at the algo's exact `fixed_sl` price, no padding (the old `+1%` buffer was dropped 2026-07-16, commit `cf96c56` — its premise didn't hold, the algo's own SL check is already an unconfirmed intrabar breach, mechanically identical to a real stop order). Real exit is triggered by Slack SELL signal at bar close, not the Schwab stop (which is catastrophic insurance only). **Fixed 2026-07-31**: this was only actually true for market-buy strategies — `_place_stop_loss_for_position` anchored the real order to `signal_price`, not `entry_price`, which for the trailing-buy strategies (the live default) could place the real stop *above* market whenever price kept falling before the bounce. Now anchors to `pos['entry_price']` universally, matching `strategies.py`'s own SL check exactly. See `docs/deep_backlog.md`'s 2026-07-31 entry.
- **Position sizing**: `watch_list.starting_notional` per ticker (default $50k; GDXD is a $5k pilot — see above). Share count shown in BUY Slack message is sized conservatively for trailing-buy orders (`shares = target_notional // (price × (1 + trail_buy_pct))`, fixed 2026-07-17) so the worst-case fill never exceeds the target notional. **The conservative pad does not create real compounding drag at realistic (real-money-scale, e.g. $50k) notional** — `signals_notify._reconcile_fill` tops up the shortfall same-day at the real known fill price, and its `delta > fill_price` gate fires on almost every trade at that scale, which *mathematically forces* `shares == floor(target_notional / fill_price)` — the same share count a full-fractional idealized buy would round down to. This isn't something that needed live proof; it's guaranteed by the formula as long as the top-up itself executes normally (see the still-unquantified real-broker-slippage-on-the-top-up-leg caveat below). **Separately (found 2026-08-01, re-run correctly against the actual $60-200 pilot nodes — HIBL/USD/YANG — after an initial mislabeled run against the $50k research nodes)**: at toy pilot-scale notional the same gate frequently does *not* fire (shortfall smaller than one share's price), so those 3 nodes skip 71-87% of signal-window trades outright for lack of affordable share count — real, but a coverage-order-flow concern for those specific tiny nodes, not a drag/returns finding, since real capital is never deployed at that scale. See `docs/research_log.md`'s 2026-08-01 entries and `docs/backlog_cache.md` for detail.
- **Entry price**: Real-time via `yfinance fast_info.last_price` at signal check time.
- **Fixed 2026-07-29**: TIME-while-armed bug — an armed position (trail_state.trailing=True) whose hold time expired was silently left waiting on its passive resting trailing-sell order instead of force-exiting, because `strategies.py`'s trailing branch collapses both a genuine trail-stop breach and hold-time expiry into the same WIN/LOSS reason (`signals_compute.py` further collapses to 'TRAIL'). Real incident: SH sat stuck for hours. Fixed via a new `state['exit_forced_by_hold_time']` marker (all 3 trailing-exit strategy classes) plus a check in `signals_notify._attempt_automated_exit_sell` that forces a market-replace of the resting order when that flag is set — zero backtest blast radius (state-only, kernel never reads it). Regression-tested via the new `tests/fake_broker.py` fixture (see below). **The fix didn't actually hold until 2026-07-31** — three more real preconditions of the same "stale in-memory state clobbers a just-persisted update" bug shape kept recreating the stuck-exit symptom through different doors; see the 2026-07-31 entry below for the full audit that finally closed it, plus 8 more instances of the identical bug pattern found and fixed across the exit/arm/entry lifecycle in the same session. **The "further collapses to 'TRAIL'" part above is now stale as of 2026-08-01**: that was correct for routing the live execution mechanism (still is), but meant every human-facing consumer (Slack, trade_log) had no way to tell a genuine trail breach from a timeout — a real trade (SH, 2026-07-31) reported `exit_reason='TRAIL'` for what was actually a hold-time-forced exit. `signals_compute.py` now reports `'TIME'` for this case specifically; see `docs/deep_backlog.md`'s 2026-08-01 entry for the full fix (also touched `signals_notify.py`'s order-routing checks and `scripts/verify_live_parity.py`'s parity classifier).
- **Resolved 2026-07-31**: full exit/arm/entry execution-path audit (manual walkthroughs, not diff review) — 9 real bugs found and fixed, including the SL-anchor real-money bug above and a live incident (LABD's real stop got REJECTED by Schwab after a stale fill reconciled hours late; fixed with a retry+market-sell-fallback that mirrors the backtest kernel's own gap-through-trigger logic). 13 more findings confirmed and deliberately left open. **Same-day follow-up session closed 8 of those 13**, including the [HIGHEST] one (a real live entry-abandon timeout, `signals_notify.check_entry_abandon`, mirroring the kernel's `wait_bars >= max_hours_to_hold` — a never-bouncing trailing-buy is now cancelled instead of resting forever and blocking every other BUY in that account), the oversell guard's fail-open branch (now fails closed when no local position is found — closed after a dedicated investigation confirmed the prior 14-test breakage was a test-fixture gap, not a real production exposure), `sl_order_id` staleness after any atomic replace (not just the hold-time-forced TRAIL case), `running_low` extended-hours contamination (bounded to a max 20%/poll drop), `drain_fill_queue`'s opt-in auto-fill-detection gate bypass, a missing `shares >= 1` guard on the real BUY path, and a race where the ambient 10:25-:29/15:25-:29 pre-close poll could beat the more accurate pinned 10:30/15:30 check to the punch. Confirmed as intentional design, not a bug: BUY-shaped guards (kill switch, automation pause) also blocking SELL — user's explicit call, a genuine off-switch against a rogue algo. 5 items remain open (paper sizing compounding, live bounce-fill Close-vs-Low/High, dry-run false-auto-placed circuit-breaker claim, the fixed_sl sizing research question). **A fresh, independent Opus trace review of that follow-up pass (no session context) then found 8 more real defects in it, fixed same session** — most serious a HIGH real-money one: the new entry-abandon timeout's cancel branch didn't account for the manual-placement Slack flow (which sets `order_placed=True` but never captures a broker order id), so it could clear the local tracking row and post a false "resting order cancelled" message while a real order stayed live at the broker with zero local record. Also fixed: the entry-abandon check was reading the live `watch_list` node instead of the pinned pending-buy snapshot (could retarget a cancel at the wrong account after a later edit); the ambient/pinned race fix's own exclusion removed ambient coverage as a fallback when a restart skipped the pinned check entirely (not just during the pre-empt window it was built to close); paper's abandon-vs-fill check order was inverted relative to the kernel; the paper sizing fix itself was less accurate than what it replaced (reverted, with the corrected reasoning documented); a paper alert that could never fire; a pre-existing (not introduced this session) real-trade_log leak in paper's market-buy sizing, fixed while touching the adjacent code; and `drain_fill_queue`'s new gate resolving the node via a lookup its own neighboring comment says must never gate a real action. Full suite: 386 passed. Full detail: `docs/deep_backlog.md`'s three 2026-07-31 entries (top) / `docs/backlog_cache.md`.
- **Fixed 2026-07-29**: `check_gap_resize`'s `running_low` was only ever updated for `dry_run` accounts (`update_dry_run_buys`) — a real pending trailing-buy resting for hours had it frozen at `signal_price` forever, producing a stale buy-trigger. New `signals_notify.update_real_pending_buys_running_low` (wired into the main poll loop) tracks it for real accounts too, using `schwab_client.get_current_price` (real-time), not the cached-hourly `signals_compute._current_price` `update_dry_run_buys` uses.
- **Fixed 2026-07-29**: `check_buy_reminders`/`check_trailing_reminders` nagged with meaningless "Confirm Filled/Missed It/Cancelled" reminders for `dry_run`/`is_dry_run_sim` positions that resolve entirely via automatic synthesis — both now skip those positions outright.
- **New 2026-07-29**: `tests/fake_broker.py` — a stateful fake Schwab broker fixture (patches `schwab_client._get_client()`) for driving real production order-placement code against a controlled, evolving order book in pytest, instead of one-off mocked function calls. Built directly because the old per-function-mock test style let real bugs (the resting-order self-block bug, the TIME-while-armed bug above) hide behind a fully green suite. 6 scenario tests so far (`tests/test_fake_broker_*.py`); `scripts/fake_broker_sandbox.py` gives an interactive REPL for ad hoc exploration without leaking real Slack posts.
- **New 2026-07-29**: `schwab_safety._log_pre_action_state_verification`, wired into `check_order`'s BUY/SELL branches — compares the real broker position against local DB belief at the exact moment a real order is considered, logging `coverage_events` (`pre_action_state_verification`, result `match`/`mismatch`/`fetch_failed`). Deliberately detection-only (never blocks) — phased rollout, decide on a tolerance/blocking policy once real data exists.
- **New 2026-07-29**: `schwab_safety.record_node_streak` — the node-level circuit breaker (3rd of 3 deferred 2026-07-28-night design items), monitor-only like the item above. Tracks two independent consecutive-streak counters per `watch_list` node — `order_failures` (fed from all 6 of `schwab_client.py`'s real order-placement functions, incl. `place_stop_loss`/`replace_order_with_stop_loss` — a same-day Opus session-wrap review caught the initial version wiring only 4 of the 6, missing exactly the "protective SL placement repeatedly failing" case the breaker is most worth having) and `reconciliation_mismatches` (fed from `signals_notify.check_live_state_reconciliation`) — and once a streak crosses `NODE_BREAKER_THRESHOLD` (3), logs `node_circuit_breaker_tripped` + one Slack alert, but never calls `pause_node_automation()` itself. Same-session reviews found+fixed several real bugs across 3 rounds: the reset firing before a real broker submission was even attempted (so a genuine broker-rejection streak could never accumulate), the state-file write having no exception handling despite sitting unconditionally in the real order-placement control flow, `check_live_state_reconciliation`'s `expected_shares is None` branch fabricating a clean-streak reset when nothing was actually checked, and the 2 missing SL functions above. `tests/test_node_circuit_breaker.py` exercises a real post-approval broker failure/success against `tests/fake_broker.py`. `signals_notify.build_phased_monitors_report`/`scripts/phased_monitors_report.py` (2026-07-30) is an end-of-day summary of both this and `_log_pre_action_state_verification`, wired into `active_signals.py`'s daily schedule at 16:05 ET (log-only, no Slack, an after-the-fact review artifact).
- **Daily coverage report split into readiness (7am) vs outcome (16:05 EOD), 2026-07-30** — per user's correction that a start-of-day report showing yesterday's trade outcomes doesn't answer "is this ready to go right now." 7am/startup now runs `signals_invariants.run_all()` + `print_all_live_node_state()` (config-drift check against a committed baseline, `scripts/seed_baseline_config.py`, for all `soxl_ira` + `ira` live nodes) only; the outcome check (`send_coverage_report`, checking `today` now instead of the previous trading day) moved to the 16:05 EOD slot alongside a same readiness re-check. New `expected_frequency='informational'` scenario tier (checked/printed daily, never mints a ticket) for trade-conditional canary scenarios. **Session-wrap Opus review found+fixed 1 HIGH + 4 lower issues**: the EOD block's two Slack-posting pieces needed their own pre-seeded/startup-catchup pair (split from the log-only `eod_report_alerted`, which stays un-pre-seeded) since a restart after 16:05 would otherwise re-post to Slack every time; `pages/14_Coverage.py` was rendering an `informational` miss as a false-green "met" (no deviation row is ever recorded for one, so absence isn't evidence); the weekday-only weekend gate on the new EOD outcome check was widened to a full NYSE-calendar trading-day check (a market holiday has the same false-ticket failure mode); `check_staged_config_matches_expected`/`staged_config_status` were silently skipping a field gone `None` (real drift) instead of reporting it, and a non-numeric `expected_config` value would have crashed all of `run_all()` (now isolated per-field via a new `_config_field_mismatch` helper, which also fixed a `0.01` tolerance too coarse for several real ~0.1 staged values). Full detail: `docs/design.md`'s 2026-07-30 addendum.
- **New 2026-07-29**: `scripts/list_scripts.py` — mechanical index of every `scripts/*.py` file's module docstring (78+ scripts now); check this before writing a new script. `scripts/staged_test_config` (signals_db table) + `scripts/audit_live_test_candidates.py --staged` track "what should this staged live-test node's config be" per node, diffing expected vs. real.
- **Canary A-F design restored, 2026-07-29**: the original 6 canaries (`docs/deep_backlog.md`'s 2026-07-23 entry) are labeled A-F, each testing one distinct mechanism (A=full lifecycle, B=early-SL, C=pinned/open_check entry, D=overnight carry, E=TrailingExit market-buy, F=TIME-only exit). 7 new `ira` nodes added 2026-07-29 morning (FAZ/SPXU/TWM/QID/SDOW/JNUG/JDST, inverse-pair coverage per the 2026-07-25 "watchlist skews long-only" idea) were accidentally created with one generic identical config instead of mirroring their A-F counterpart. Fixed: SPXU/QID/TWM/SDOW/FAZ now exactly mirror IVV/QQQ/IWM/DIA/XLF's real hair-trigger config; JNUG converted from generic `TrailingBothZScoreBreakout` to `TrailingExitZScoreBreakout` mirroring VOO (the missing E scenario). **JDST is now purposeless** (JNUG took the E-slot instead of pairing with it) — open, see `docs/backlog_cache.md`. `scripts/list_canary_nodes.py` shows all 13 with their pair/purpose. Also fixed: `scripts/audit_live_test_candidates.py`'s share-count MISMATCH check false-positived on every `is_dry_run_sim` position (real broker legitimately shows 0 shares for those, by design).
- **Fixed 2026-07-27 (night, 2nd session)**: `at_bar_close` bookkeeping bug caused false near-instant SL exits (paper trading, and unfixed in real `_scan_pinned_exit_arm` until a review round caught it) — a fresh position's first exit-check was misgraded against pre-entry bar history. Full detail: `docs/deep_backlog.md`'s 2026-07-27 (night, 2nd session) entry (top).
- **Fixed 2026-07-22 (CRITICAL, found by a full-stack Opus review)**: `notify_trailing_activated` was clobbering the just-armed `trail_state` with a stale pre-arm copy right after arming — the DB lost `trailing`/`peak`, so the next bar close re-armed the position and `_attempt_automated_sell` placed a **second** live trailing-sell order for the same shares (oversell risk if both filled). Fixed via a fresh re-read (`signals_db.get_position_by_id`) before merging. Fixed alongside it: SELL orders had no resting-order duplicate guard at all (only BUY did — this is what let the bug above actually stack two live orders; now `schwab_safety._has_open_sell_order` blocks it), a manual-SL-price fallback alert when `_attempt_automated_sell` cancels a resting SL but the trailing-sell placement then fails (deliberately no auto-recovery — user's call), and a `threading.Lock` around `open_position`/`close_position` (poll loop vs. Slack handler thread race). See `docs/backlog_cache.md` for full detail. Full suite: 172 passed.
- **Standing convention (2026-07-22): every Slack alert must render cleanly on mobile** — the actionable fact (ticker, account, price/quantity, what to do) goes on line 1, since Slack's mobile notification preview truncates; technical detail (the underlying exception) goes on a second, parenthetical line. Applied to all three UNPROTECTED-position alerts this session.
- **Fixed 2026-07-22**: `schwab_client._submit_order_with_retry` retries only the real broker `place_order` call (3 attempts, 2s apart) on a generic/transient exception — not `schwab_safety.approve_and_record()` itself (which must run exactly once per attempt), and not a `SafetyViolation` (a deliberate policy block, not a transient failure — retrying it can't succeed and just delays the fallback alert). Wired into all three real placement paths (equity BUY/SELL incl. top-up, trailing BUY/SELL, stop-loss).
- **Discussed, deliberately not built, backlogged 2026-07-22**: a max-cumulative-BUY-notional-per-ticker-per-day ceiling as a backstop distinct from every existing guard (none of them actually bounds same-day BUY exposure to one ticker — see `docs/backlog_cache.md` for the full walkthrough of why). User wants to think it over before picking a final multiplier.
- **Fixed 2026-07-22**: test suite was leaking real Slack messages on every run (including at `session wrap`'s pre-commit checklist) — `SOCKET_MODE` is true whenever real Slack creds are in `.env`, regardless of `pytest`, and `SIM_MODE` only prefixes message text rather than suppressing the send. `tests/conftest.py` gained an autouse fixture patching `_post_message` on every module that imports it directly by name (`paper_trading`, `schwab_client`, `schwab_stream`, `signals_notify`, `signals_compute`, `signals_handlers`, `active_signals`) — patching `signals_blocks._post_message` alone doesn't reach any of them.
- **Fixed 2026-07-22**: `signals_notify.drain_fill_queue` (the account-activity-stream fast path) no longer trusts the stream message's own price/quantity — it may represent one partial execution of a still-filling order (unverified whether Schwab's `filledQuantity` is cumulative or per-execution), and locking in a partial would both under-record real share count and let `_reconcile_fill`'s top-up logic place a real second buy for the "shortfall" while the original order is still filling. Now uses the stream event only as a wake-up signal, reconfirming via the same `get_filled_order` poll (terminal `FILLED` status, aggregated across every execution leg) the poll and sync-confirm paths already trust.
- **Resolved 2026-07-22**: `same_day_block` account-type-awareness (backlog item from 2026-07-20). `schwab_safety.AccountLimits` gained `account_type` (`'cash'` or `'margin'` — regular and IRA limited margin treated identically, since both lack the same-day cash-settlement restriction). `ACCOUNTS`: brokerage is `'margin'` (has it by default); sep/roth/ira are `'cash'`. The same-day-rebuy check now only fires for `'cash'` accounts. A blanket "real orders only in a confirmed margin account" gate was considered and explicitly rejected — the user's account model is one account per ticker (cap usage, fund a new trade in a new account rather than liquidating an existing one), so a hard margin-only gate would've locked automation out of every existing account. The new (5th) limited-margin IRA funded this session isn't in `ACCOUNTS` yet — still blocked on Schwab API token scope + compliance trading permission, both unresolved as of this session.
- **Data traceability, backlogged 2026-07-22 (medium priority, doesn't block trading)**: reconciled an open 2026-07-16 question — `yf.download()` actually defaults to `auto_adjust=True`, but that only adjusts the window being fetched *right now*, not rows already cached from a prior fetch, which is why `data_manager.py`'s split-guard rescale is still real work (not dead code) despite `auto_adjust`. Since the rescale is scale-invariant to %-based signals, past `backtest_cache` numbers should stay reproducible by re-running the same code — but there's still no archived record of the exact data that fed any past result. Design agreed (traceability over full immutable versioning, per the user's call): a `data_mutation_log` table in `trading_universe.db`, one row per split-guard rescale with a `pre_mutation_snapshot` of the old data — not yet built. See `docs/research_log.md`'s 2026-07-22 entry.
- **Watchlist mode is a genuine mix as of 2026-07-25/26, not the "all research" snapshot in older bullets above** — run `python scripts/watchlist_status.py` for the real current table; don't trust any hardcoded mode list in this doc, including this one. As of this session: 10 research-mode v5 nodes (paper trading), 6 canary nodes (`mode='live'`, account `ira`, dry_run=True), a `soxl_test` regression-test group (`mode='live'`, account `soxl_ira`, dry_run=False, small notional per node) covering LABU/ERY/GDXD/SH/SPY/LABD/ERX plus a second, differently-parameterized GDXU node, and **DPST as the one deliberate real-money live volunteer** (new node, `mode='live'`, account `soxl_ira`, `starting_notional=800`) — its original research-mode node is untouched and still paper-trades independently, a deliberate side-by-side pairing.
- **`paper_alert_verbose` column added to `watch_list` 2026-07-26** (default 0/suppressed): paper trading (`paper_trading.py`) is mostly used for troubleshooting, not routine review, so its 5 Slack `_post_message` calls (buy signal, market-buy fill, trailing-buy fill, trailing-sell-armed, sell) are now gated on this per-node flag — silent by default, opt in per ticker (`paper_alert_verbose=1`) when actually weighing a go-live decision on it. Has no effect on real/dry_run alerts (`notify_buy_signal`/`notify_sell_signal` etc.), which fire unconditionally regardless of this flag. Channel-level separation of live/dry_run/paper alerts is still backlogged as a separate, larger design item (touches every `_post_message` call site) — deliberately not bundled into this change.
- **`add_node`'s dedup gap fixed 2026-07-26**: neither the Python-level check-then-skip nor the table's own SQL `UNIQUE` constraint on `watch_list` included `account` — two genuinely distinct nodes (same ticker/strategy/params, different account) were silently treated as duplicates (Python check) or outright rejected (DB constraint), found live while adding DPST's second node above. Fixed: `add_node` gained an `account` param, included in the dedup check; `ensure_tables()` now does a one-time table-rebuild migration (detected by parsing the live `sqlite_master.sql` for `account` inside the `UNIQUE(...)` clause) adding `account` to the constraint. Applied to the real `cache/live/trading_live.db` (backed up first, daemon confirmed not running, row count verified unchanged 90→90). Same-account identical-param nodes are still correctly deduped — only cross-account pairing was the gap.
- **dry_run fill synthesis, 2026-07-26**: a `dry_run=True` account's trailing-buy/market-buy order never gets a real fill event (schwab_client short-circuits before the broker call), so it used to just sit forever — the cause of the canary/dry_run "no closed trade found" false-positive `coverage_deviations`. New `signals_notify.update_dry_run_buys`/`_fill_dry_run_buy` (bounce-fill or immediate fill, mirroring `paper_trading.py`'s simulation) and `check_dry_run_sim_sells` (immediate close on exit signal) now synthesize the fill/close against real price data, writing to the real `open_positions`/`trade_log` tables tagged `is_dry_run_sim=1` (not the paper tables — user's explicit call, so `coverage_check.py` needs no changes). Opus review found and fixed 6 real issues: most severe was `active_signals._scan_pinned_exit_arm` missing the `is_dry_run_sim` skip (would have corrupted the new function's own bar-close detection and fired real Slack SELL/arm flows on a synthetic position); also fixed a missing `open_position()` return-value check (false repeat "would have filled" alerts), a `wl_id`-less fail-open path, and `closed_today`/`_last_sale_recovery` reading simulated trades unfiltered (could block/mis-size a real order). Sim positions now tagged `🧪DRY-RUN-SIM` everywhere human-facing (Morning Report, `cmd_positions`, `open_positions_status.py`), with "Manually Close" suppressed. See `docs/deep_backlog.md`'s 2026-07-26 entry for full detail.
- **Real incident, 2026-07-26: the daemon placed a real BUY order (ERY, `soxl_ira`) on a Sunday** against stale Friday-cached data — `active_signals._in_window()` had no weekday/holiday awareness. Cleaned up same day (see `docs/deep_backlog.md`); root cause fixed later the same session: a real NYSE trading-day gate (`pandas_market_calendars`) now guards both daemon scan paths (`_in_window`, and a second independent BUY entry point, `_scan_pinned_entry`, that was initially missed) **and** `schwab_safety.check_order` itself — the real chokepoint every order-placement path (manual Slack buttons, gap-correction, automated) routes through via `approve_and_record`. A 3x/5s retry added to `_scan_pinned_entry` for a transient price-fetch failure introduced its own HIGH-severity duplicate-BUY bug (stale `open_position_keys` snapshot reused across retry attempts, letting the same-day-unlock branch see a just-filled position as still open) — caught by an Opus review round before it ever reached the daemon, fixed by re-reading `open_position_keys` fresh before every attempt and only retrying tickers that actually failed to fetch a price (never re-scanning a ticker that already succeeded). Two independent review rounds (Opus, then Opus+Sonnet in parallel to compare detection) both confirmed the fixes close cleanly. See `docs/deep_backlog.md`'s 2026-07-26 entries (top two) for full detail.

## Key Files
- **Before writing or reasoning about any test** (which covers almost everything here), check `docs/design.md`'s "Test Fixtures & Coverage-Proof Techniques" reference table — this project runs 6+ distinct, deliberately-not-unified testing/coverage systems (the Trade-Flow Accountability Grid, the canary/`scenario_expectations` system, `fake_broker`, truth tables, hypothesis property tests, mutation testing), each answering a different real question. Picking the wrong one for the situation (e.g. a per-call mock where `fake_broker` was built specifically to catch what mocks miss) has caused real bugs to hide behind green tests before.
- **Whenever you touch `active_signals.py` or any `signals_*.py` module** (the live-trading daemon), review `docs/automation_principles.md` first, and update `docs/live_test_coverage.md` if the change adds/affects a scenario that needs live validation.
- **Whenever designing a genuinely new live-trading mechanism** (a new position/order type this system has never run live before — distinct from promoting a new ticker within an already-live mechanism, which is `docs/watchlist_candidate_checklist.md`'s job) — check `docs/new_mechanism_promotion_standard.md` first. Built 2026-08-07 after a first-pass design missed two already-established patterns (paper/live-vs-backtest reconciliation, the daily-track/live-track split) that had to be caught by direct prompting instead of being applied automatically.
- **Whenever writing or trusting a cliff-safety/neighbor-search query** — check `docs/cliff_safety_query_checklist.md` first. The same missing-parameter-filter bug independently appeared twice in one session (2026-08-07): a fresh query and the pre-existing `scripts/top_safe_nodes.py` tool, both producing a confident, wrong "this config is fragile" result.
- **Whenever validating a new overlay/config parameter choice** (not a ticker's core strategy — that's `docs/watchlist_candidate_checklist.md`) — `docs/overlay_parameter_robustness_process.md` has the real, proven steps (fit-half-only search, single-trade-removal stress test, included-vs-excluded challenge). Read its framing note first: the mechanical steps confirm a finding, they don't generate one — noticing something worth a second look is what actually mattered in the session that produced this process.
- `app.py` — Streamlit UI for configuring and launching optimization sweeps
- `run_optimization_sweep.py` — main parallel optimization engine (brute force + generational refinement)
- `backtester.py` — core backtest simulation logic (`run_backtest`)
- `strategies.py` — strategy classes: `ZScoreBreakout`, `TrailingExitZScoreBreakout`, `LimitOrderZScoreBreakout`, `LimitOrderTrailingExit`, `TrailingBuyZScoreBreakout`, `TrailingBothZScoreBreakout` (live default), `LimitExitZScoreBreakout`, `TrendFilteredZScore`
- `active_signals.py` — live trading daemon: polls signal windows, manages `open_positions`/`watch_list`, fires Slack alerts. Delegates to (and re-exports, for backward compat): `signals_config.py` (config/tokens/Bolt app singleton), `signals_db.py` (DB layer), `signals_compute.py` (signal computation), `signals_charts.py` (chart PNGs), `signals_blocks.py` (Slack message posting/block builders), `signals_helpers.py` (small shared helpers), `signals_handlers.py` (Bolt interactive button/modal handlers), `signals_notify.py` (notify_*/reminder loops/reference-table/report — the core, formerly the whole Slack-facing layer, split out 2026-07-14 for length), `paper_trading.py` (paper-trading simulation for `AUTOMATION_ENABLED_TICKERS` research-mode tickers — added 2026-07-18, see Live Trading — Current State above).
- `signals_invariants.py`, added 2026-07-26 — config-invariant sanity checks (a lighter alternative to hardening every downstream assumption directly): each check documents, in its own docstring, exactly which other code relies on the invariant, and returns a list of violation strings. Run automatically at `active_signals.py` `run_loop` startup (non-blocking Slack alert, doesn't halt the daemon) and standalone as a pre-commit sanity check (`.venv/bin/python signals_invariants.py`, wired into `docs/pre_commit_checklist.md`). Built from an Opus round-6 finding (stale-pending-buys guard could silently discard a real manual fill for a live `TrailingExitZScoreBreakout` ticker outside `AUTOMATION_ENABLED_TICKERS`) — rather than hardening the fragile handler assumption itself (blocked on live-Slack testing), this guards the config state that would trigger it. Same session, 3 more checks added from known backlog gaps: a `research`-mode ticker with a real open position still in automation scope (this check itself is still useful defense-in-depth, but the underlying SELL-side ticker-only-gating gap it was tracking was fixed 2026-07-31, see the Live Trading section above), any `mode='live'` node with `account=None`, and `brokerage.dry_run` ever flipping to `False` while the `availableFunds` leverage-inclusive-cash gap is unresolved. UDOW's deliberately-seeded 2026-07-23 test position (`research` mode, real open position, automation-scoped) is safe on both the mode-gate fix and the `ira` `dry_run=True` fallback.
- `scripts/coverage_registry.py`, added 2026-07-27 — the trade-flow test accountability grid's data source: 32 real logic branches (migrated from `docs/live_test_coverage.md`), each with `compute_status()` deriving live/dry_run/paper/never-fired status from real `coverage_events`/`coverage_deviations` queries — never hand-typed, so it can't go stale the way the markdown ledger did (caught stale 2026-07-25). Built directly in response to a real limitation: Opus review (required at every session wrap, see below) had already made a wrong architectural call once this same session — unconditionally deleting `coverage_deviations` rows before a later review caught it — and never reviewed the project's first 3 weeks of code at all, only incremental diffs since review became a habit. This grid is deliberately not AI-opinion-dependent: it's evidence, queried directly, not a claim. `bad_results`/`mode_filter` fields handle two real subtleties caught while building it: a scenario_key shared across unrelated code paths (`entry_fill`/`exit_fill` logged by both `paper_trading.py` and the dry_run-fill-synthesis code), and a real event firing without succeeding (`sl_placement`/`top_up`'s only live event was `result='blocked'`, a bug, not proof the path works). Rendered as a heatmap-colored table in `pages/14_Coverage.py`'s "Trade-Flow Test Accountability Grid" section (`STATUS_COLOR`/`_heat_row`). Run directly for a plain-text report: `.venv/bin/python scripts/coverage_registry.py`. Real result at build time: 25 of 32 branches (not-instrumented + wired-never-fired + attempt-failed) have zero live proof of working. Not yet built (see `docs/backlog_cache.md`): filters, direct links to the underlying tests, row grouping by feature area. **`offline_proof_for()` added 2026-07-26** — a second, orthogonal axis to `status`: `status` means "is there LIVE proof" only (correctly `wired-never-fired` for a policy-internal branch only ever exercised in pytest's isolated DB); `offline_proof_for()` derives (greps `tests/test_*.py` fresh every run, memoized per process, never hand-typed) whether there's ANY proof — `'event-asserted'` (a test asserts the real `get_coverage_events()` call), `'behavior-only'` (scenario_key mentioned but the log call itself isn't asserted), or `'none'`. Surfaced as an "Offline proof" column in `pages/14_Coverage.py`. Deliberately excludes `tests/test_coverage_check.py` from the scan (it reuses real scenario_key strings as generic fixture data to test the coverage-infrastructure plumbing itself, not production code — an Opus review caught this making `sl_placement` render as false-proven). Full detail: `docs/deep_backlog.md`'s 2026-07-26 entry (top).
- `scripts/trading_incidents.py`, added 2026-07-26 — ticket-model log of real live-trading incidents (`signals_db.trading_incidents` table + `log_incident`/`resolve_incident`/`get_incidents`, mirrors the `coverage_deviations` pattern: never deleted, `resolved_ts` NULL means still open). Distinct from `coverage_deviations` (daily scenario-expectation misses) and `docs/backlog_cache.md` (planning/design notes) — this is specifically "something real and bad happened at the broker." Built after a real one: the daemon placed a real BUY order for ERY in `soxl_ira` on Sunday 2026-07-26 (no trading-day gate — see `docs/backlog_cache.md`'s open item) against stale cached data, then a fill-detection path recorded a phantom fill without confirming against the broker. Usage: `.venv/bin/python scripts/trading_incidents.py log|resolve|list`. Same session: every Slack alert now tags `(account · LIVE/DRY-RUN)` in its header (new shared `signals_helpers.mode_tag(account)`) instead of nothing or a bare account name — found live when the user couldn't tell at a glance whether a reminder was real risk. `mode_tag` deliberately does NOT default an unrecognized/`None` account to the reassuring `DRY-RUN` the way `signals_notify._coverage_mode` does (that helper only labels a log row; this one labels human-facing risk) — it returns `UNKNOWN` instead, found by Opus review.
- `data_collector.py` — fetches and caches hourly price data
- `data_manager.py` — data fetching and cache management
- `config.json` — runtime config: tickers, hyperparameters, strategy selection
- `pages/` — Streamlit multipage app views (Spatial Topology, Node Inspector)
- `scripts/daemon_status.py` — checks whether `active_signals.py` is running and whether its process-start time is older than the newest edit among the live-trading source files (i.e. stale vs. already restarted) — run instead of manually comparing `ps`/mtimes whenever a restart is pending.
- `scripts/run_v4_backfill_sweep.sh` — v4 kernel-correctness backfill wrapper (fill-optimism resolution bounds, `fixed_sl` sweep, Open-check entry timing — see `docs/backlog_cache.md`). `backtester._simulate_trail_both` now computes three parallel trailing-buy bounce-fill resolutions per node, since none of hourly OHLC proves the true intrabar path: **possible** (existing/unchanged, assumes Low-before-High), **pessimistic** (new, mirror-image Low-after-High assumption — always same-bar-or-later and same-or-worse price than `possible`, a real bracket partner), **certain** (new, only resolves a fill when provable regardless of ordering — can occasionally beat `possible` since deferring lets `running_low` fall further first, so it is *not* a pessimistic-price bound, just a no-guessing one). Island search and cliff-safety rank/filter on `MIN(possible, pessimistic, certain)` (`run_optimization_sweep.ROBUST_ALPHA_SQL`), not `possible` alone, so a node is only selected when it holds up under every resolution. Now loops 14 `stop_loss` values (densified 2026-07-15: `1,2,3,4,5,6,9,12,15,18,21,24,27,30`) x 2 `entry_timing` values through `run_optimization_sweep.py --version v4 --entry-timing <close|open_check> --max-phase <1|2|2.5|3>`, each a phase1→2→2.5(→3) run. Unlike `run_v3_backfill_sweep.sh`, every campaign shares one version string (`v4`) — the real `stop_loss`/`entry_timing` `backtest_cache` columns disambiguate campaigns instead, so island/cliff-safety queries filter on those columns too (`run_optimization_sweep.py::_campaign_scope_sql`). New `sl_sweep_summary` table holds one rollup row per completed campaign for slice/trend queries without re-deriving island stats from raw `backtest_cache`. `--max-phase` (added 2026-07-15) caps a run at the given phase — added after confirming Phase 3 held the best `MIN(possible,pessimistic,certain)` node in 0/30 tagged SOXL+KORU campaigns, and that island/cliff-safety selection only ever reads Phase1+2+2.5 data; `run_phase3_full` now also logs a `Phase3 best=... (pre-Phase3 best=..., IMPROVED/no improvement)` line whenever Phase 3 does run, for live confirmation. New `generation` column (nullable, `Phase2-Island` rows only) records which 1-indexed island-search generation first computed a row. `MAX_PHASE`/`STOP_LOSSES` env vars let the wrapper's single-combo mode (`./scripts/run_v4_backfill_sweep.sh <stop_loss> <close|open_check> [tickers...]`) be scripted — see `scripts/run_backfill_queue.sh` for the ordered multi-step queue (KORU catch-up → SOXL/KORU low-SL density → rest of watchlist → v3-screened non-watchlist tickers), meant to be run directly by the user in their own terminal (tees to `logs/backfill_queue_<timestamp>.log`), not launched by an agent, to avoid `config.json` races with any other in-flight run.
- `scripts/live_sim.py` — manual-step live-sim REPL: drives the real `compute_buy_signal`/`check_sell_condition`/`notify_*` functions against an isolated `cache/trading_sim.db` (via `TRADING_DB_PATH` env override) so the full BUY→placed→filled→arm→trailing-sell Slack sequence can be exercised bar-by-bar without touching the live daemon or `trading_live.db`. `SIM_MODE=1` forces plain-text/typed-input Slack messages (prefixed with a `🧪 SIM MODE`/`🧪 SIM MODE END` header/footer context block, optionally labeled via `SIM_SCENARIO` env var) instead of interactive buttons — the sim never opens its own Socket Mode connection, so real buttons would risk being delivered to the live daemon's connection instead. Real interactive buttons genuinely cannot be tested this way (Slack routes all clicks to whichever process holds the socket, i.e. the live daemon, not the sim) — button *layout* can still be previewed by manually appending a dummy-`action_id` actions block (safe no-op if tapped). `buy`/`sell` REPL commands drive signal checks directly; `pending`/`placed TICKER`/`fill TICKER PRICE`/`remind_buy [--stale]` drive the three-state buy lifecycle (signal → order placed → filled) added 2026-07-10. Interactively tested end-to-end with the user 2026-07-09/10 — found and fixed several real bugs in the process (see `docs/conversation_summary.md`).
- `scripts/sim_delayed_sell.py` (2026-07-17) — repeatable, per-ticker/per-node CLI quantifying the cost of intentionally deferring a same-day exit (SL/trailing-stop/TIME) to the next calendar day, the mirror image of the existing `same_day_block` kernel feature (which defers the entry side after a same-day exit, not the exit side of a same-day entry). Uses `scripts/export_trades.py::simulate_trail_both_deferred_sell`, a pure-Python bar-by-bar mirror of `_simulate_trail_both` (entry logic byte-identical to the existing `simulate_trail_both_annotated`) — not a reimplementation of the exit logic from scratch. Only supports `entry_timing='close'` (no `open_check` branch), so results for an `open_check` node (e.g. GDXD) are indicative, not exact. `.venv/bin/python scripts/sim_delayed_sell.py TICKER --window W --z Z --tb TB --arm ARM --sl SL --ts TS --max-hours H`.
- `scripts/sim_chaos_monkey.py` (2026-07-17) — execution-adherence ("chaos monkey") Monte Carlo: quantifies how much of a node's backtested compounded return depends on catching every entry/exit signal exactly on time, the real risk during manual-execution trading (distinct from island/robust-alpha, which both assume perfect adherence). Uses `scripts/export_trades.py::simulate_trail_both_chaos`/`_resolve_miss`, another pure-Python bar-by-bar mirror of `_simulate_trail_both` (verified byte-identical to `simulate_trail_both_annotated` at `miss_rate=0`). Entry signals are only missable at the two daily signal windows (matching the real Slack cadence); exit triggers (SL/trailing-stop/TIME) are missable every bar (matching continuous live monitoring); TP-arming is never missable (internal state change, not a clickable action). Two modes: `drop` (unbounded per-check miss — the opportunity can vanish for good if the underlying condition stops holding first) and `delay` (same coin flip, but capped at `max_delay_checks` consecutive misses of the same still-true condition before it's forced). Runs all `watchlist_id=9` nodes × both modes × {1,5,10,20}% miss rates × 1000 trials by default, writes `output/chaos_monkey_summary.csv`/`chart.png` (gitignored). `.venv/bin/python scripts/sim_chaos_monkey.py [--tickers T...] [--trials N]`. First real run (2026-07-17): 9/12 tickers lose ~15-31% of mean compounded return at a 20% miss rate (KORU worst, -31%); SOXL/DPST are outliers that hold flat or improve — unexplained, flagged in `docs/backlog_cache.md` for follow-up. `drop` vs `delay` track closely everywhere, suggesting the delay cap rarely binds.
- `scripts/sim_gap_policy.py` (2026-07-19) — decides, empirically, the live-side policy for a trailing-buy entry whose real Open gaps past the trigger price before it can be caught gradually (found while scoping the trailing-buy idle-capital re-sizing item: real gaps exceed `trail_buy_pct` on 19-44% of trading days across the v4 watchlist). Uses `scripts/export_trades.py::simulate_trail_both_gap_policy`, a pure-Python mirror of `_simulate_trail_both` carrying the same gap-through-trigger fix now in the real numba kernel (`backtester.py`, fixed this session — all three resolutions, plus `_simulate_trail_buy`, now fill at the real `Open` when it's already crossed the trigger, instead of the stale theoretical price). `skip_threshold=None` reproduces the corrected kernel's default ("always resize and enter"); a float skips the entry attempt entirely once the gap overshoots the trigger by more than that fraction, so the node waits for the next clean signal instead of chasing. Sweeps `{None, 3%, 5%, 10%, 15%}` per ticker against the real active v4 node (`watchlist_id=57`), writes `output/gap_policy_summary.csv`. `.venv/bin/python scripts/sim_gap_policy.py [--tickers T...] [--watchlist-id N]`. First real run (2026-07-19, all 18 v4 tickers): gap-through trades have consistently low win rates (8.5%-41.8%, most 10-30%) but skipping them moves total compounded return only modestly with no consistent direction per ticker — no universal threshold shows a clear edge, no ticker shows a dramatic blowup avoided by skipping. Leaning "always resize and enter" as the live default (simplest, matches the kernel), pending final confirmation before Part 3 (live order-placement infra) is built.
- **Exit-side gap-through-trigger fix, 2026-07-20** (`backtester.py`): the entry-side fix above (2026-07-19) only covered the trailing-*buy* trigger — SL and trailing-*stop* exits had the identical latent bug (both intrabar-continuous, both used the theoretical `stop_price`/`trail_stop` as the fill even when the bar's Open had already gapped past them). Fixed in `_simulate_trail` (v1.8, gained a new `opens` param) and all three resolutions of `_simulate_trail_both` (v1.10): each now checks `op <= stop_price`/`op <= trail_stop_gap` (the level confirmed through the prior bar) before falling through to the existing Low check. `scripts/export_trades.py::simulate_trail_both_annotated` (the parity-verified mirror used by `verify_trailing_buy/sell_resolution.py`) got the matching fix to keep byte-identical parity; the other export_trades mirrors (chaos monkey, delayed-sell, gap-policy) were *not* touched — still on the old exit logic, flagged in `docs/backlog_cache.md`. New `tests/test_trailing_exit_gap.py` (3 tests, synthetic-gap, mirrors the entry-side test pattern).
- **Sweep-engine correctness bugs found while resweeping with the fixed kernel, 2026-07-20** (`run_optimization_sweep.py`): (1) `_sl_axis_col` was used as a literal SQL column name at 4 sites (Phase2/2.5/full-mesh-candidates) — broke for `TrailingExitZScoreBreakout`/`LimitOrderTrailingExit` whose conceptual `trail_pct` axis is actually stored in the real `trail_sell_pct` column (never exercised before since these strategies apparently never went through Phase2+); fixed via new `_sl_axis_real_column()` helper. (2) `run_phase1_coarse`'s `if cached >= expected: skip` was a **row-count comparison, not a verification that the cached rows were computed with current code** — it silently served the stale pre-kernel-fix numbers back out of `backtest_cache` after the exit-side fix (confirmed empirically: a resweep reproduced the exact same stale alpha). Commented out for now; `dispatch_parallel_grid`'s own per-task cache lookup still correctly avoids recomputing genuinely-unchanged nodes. (3) `CREATE INDEX`/`DROP INDEX` statements moved out of `init_idempotent_db()` (ran at the start of *every* invocation, maintaining index overhead through every bulk insert) into a new `rebuild_indexes()`, called once at the very end of a run — gated by the same `--skip-cache-refresh` flag, so chaining multiple sweeps only pays this cost on the true last invocation.
- **Versioning decision, 2026-07-20**: resweeping `TrailingBothZScoreBreakout` under the same `version='v4'` (overwrite-in-place, the original plan) would have destroyed the ability to compare pre-fix vs post-fix numbers — `backtest_cache`'s PK already includes `strategy`, so there was never a collision reason to split `TrailingBoth`/`TrailingExit` across `v4`/`v5` in the first place. **New convention: `v5` = everything reswept with the 2026-07-20 corrected kernel (both strategies); `v4` stays untouched as the historical pre-fix baseline** for every ticker not yet reswept. GDXD's old `v4`/`TrailingBoth` rows were already deleted earlier this session (before landing on this convention) — not restorable, but the pre-fix numbers are preserved in `docs/conversation_summary.md`.
- New scripts (2026-07-20, all follow the config.json-patch-then-restore-via-trap pattern): `scripts/run_v5_gdxd_test.sh` (GDXD only, `TrailingExitZScoreBreakout` immediate-entry test, tagged `v5`), `scripts/run_v4_gdxd_resweep_compare.sh` (GDXD only, `TrailingBothZScoreBreakout` resweep with corrected kernel, tagged `v5` per the convention above despite the filename), `scripts/run_v4_full18_resweep.sh`/`scripts/run_v5_full18_test.sh` (same pair, scaled to all 18 active watchlist tickers pulled from `watch_list` `watchlist_id=57`). All use `--max-phase 2.5`. Chain via `--skip-cache-refresh` on all but the last invocation so only that one pays for `rebuild_indexes()` + cache refresh.
- **Immediate-entry vs trailing-buy comparison — started, not concluded, 2026-07-20**: raised as a hypothesis (does a 1% `trail_buy_pct` even earn its cost, given how often it gets skipped by overnight gaps?). GDXD's own result flipped dramatically between the stale pre-fix run (+1442.2% alpha) and the corrected-kernel rerun (-37.8%, flagged CLIFF, zero cliff-free candidates) — but the full 18-ticker resweep chain was interrupted mid-run by a WSL restart (host was CPU-pegged) before finishing, so there's no real cross-ticker answer yet. **Real motivation, clarified same session**: this isn't just an alpha question — trailing-buy is operationally hard to execute manually right now (the reason all 18 tickers are `research`-mode/paused, see Live Trading state above), so immediate-entry is also being evaluated as a practically-easier fallback regardless of what the backtest alpha says.
- **`TrailingExitZScoreBreakout` gained `open_check` entry-timing support, 2026-07-20** (`backtester.py::_simulate_trail`/`run_backtest_v18`, previously `close`-only) — needed for a fair comparison against `TrailingBothZScoreBreakout`, which runs `open_check` live for all 18 tickers. Mirrors `_simulate_trail_both`'s existing Open-vs-Close signal check pattern. `run_backtest_dispatch` was also silently dropping `entry_timing` for this strategy before this fix (never wired through). New `scripts/audit_trailing_stop_gap.py` (Stage-1-style real-data trade-by-trade audit, see below) and a new synthetic unit test (`tests/test_trailing_exit_gap.py::test_v110_trailing_stop_exit_fills_at_open_on_gap_through_trigger_all_resolutions`) close the gap where the exit-side fix had a three-resolution test for SL but not for the trailing-stop trigger. Full suite: 98 passed (was 94 pre-session).
- **Per-ticker resumable sweep tooling replaces the batch full18 scripts, 2026-07-20**: `scripts/run_v4_full18_resweep.sh`/`run_v5_full18_test.sh` batched all 18 tickers through one `run_optimization_sweep.py --tickers A B C...` call each, which forces every ticker through a shared Phase1 + cross-ticker Checkpoint1 before any of them reach Phase2+ — not inspectable per-ticker, and not the pattern actually used for the real historical v3.x/v4 backfills (`run_backfill_queue.sh` always called `run_v4_backfill_sweep.sh` with one ticker at a time). New `scripts/campaign_config.py` (per-strategy hyperparameter grid + supported `entry_timing`s) and `scripts/run_sweep_queue.sh` (the queue runner: one `(strategy, ticker, fixed_sl, entry_timing)` combo per invocation) restore that pattern for `v5`. Current scope: both strategies, `open_check` only (matches the real live config; `TrailingExitZScoreBreakout`'s `close`-only default was dropped once it gained `open_check` support), `fixed_sl ∈ {1,2,3}` (further values `{4,5,6,9}` deliberately not run yet, extend by rerunning with a wider `FIXED_SLS` list). **A campaign-level `campaign_config.py done` skip check (does a `Phase2-Island` row already exist?) was added then removed the same session** — caught live while a real queue run was in progress: it has the identical blind spot as the `run_phase1_coarse` row-count check disabled above (trusts a cache hit without confirming it reflects current code; verified once by luck that a skipped AGQ combo matched a fresh recompute exactly, but a kernel edit mid-campaign would make it silently serve stale rows). Removed; every combo is now always invoked, relying solely on `dispatch_parallel_grid`'s per-node cache lookup (the one already-trusted mechanism) for resumability — a fully-done combo still returns fast, just without the false confidence of a campaign-level shortcut. New skill `.claude/skills/backtest-change-rollout/SKILL.md` documents the full staged process (single-node audit → biased single-ticker → storage sizing check → full campaign → live/paper compliance recheck) this tooling supports, including this gotcha — **the agent must ask before loading this skill, and never launches a sweep campaign itself; the user always runs `run_sweep_queue.sh` in their own shell.** Also fixed (same session): an unnecessary blanket `DELETE` of existing `backtest_cache` rows in the old `run_v5_full18_test.sh` before resweeping — `dispatch_parallel_grid`'s own per-node cache lookup already only recomputes genuinely-missing nodes, so the delete threw away real, already-correct data for nothing. Not yet run to completion: the actual `v5` campaign under this new tooling — see `docs/backlog_cache.md`.
- **The `v5` campaign above ran to completion, and its comparison table is now a real script, 2026-07-20**: `scripts/campaign_comparison_table.py` prints the per-ticker TB-vs-TE side-by-side (best alpha, worst_neighbor/cliff status, full node params, trades, win_rate, liquidity from `avg_vol_10d*last_price*0.01`), with a `--min-best` filter — built after this exact table got hand-rebuilt piece by piece several times in conversation. `scripts/build_v5_watchlist.py` inserts the 10 tickers selected this way into a new watchlist via `signals_db.create_watchlist`/`add_node`, all `mode='research'`; see the Watchlist bullet above and `docs/deep_backlog.md` for the full selection writeup.
- **`signals_db.coverage_events` + `scripts/coverage_matrix.py`, 2026-07-22**: a queryable transaction-phase/control-coverage log — `log_coverage_event(scenario_key, mode, ticker, ...)` fires at ~18 real control sites across `schwab_safety.check_order` (cash check, same_day_block, all duplicate-order guards), `signals_notify.py` (SL placement sync/async, top-up, trailing-arm re-read, fast-path fill reconciliation, gap-resize, live-state reconciliation), and `paper_trading.py` (entry/exit fills) — `mode` is `paper`/`dry_run`/`live`. `coverage_matrix.py` pivots it (rows=scenario, columns=mode, cell=count+most-recent-date+result; `--detail` drills into raw rows, `--strategy` filters) so `docs/live_test_coverage.md`'s ledger is answerable by query, not just hand-maintained prose. See that doc's 2026-07-22 note. **`coverage_events` gained a nullable `strategy_type` 3rd axis, 2026-07-25** (piece #2 of the coverage-system reframe below) — `log_coverage_event` derives it automatically from `node_id`'s real `watch_list.strategy` when not passed explicitly, so no call site needed to change. **`pages/14_Coverage.py`, 2026-07-25** (piece #1/#4): the Streamlit "Coverage Compass" page — unexplained `coverage_deviations` at top with an inline Explain action, today's per-scenario pass/fail, the scenario x mode matrix, and per-scenario drill-down into raw events. Reads `trading_live.db` directly via `sqlite3` (not `signals_db`), matching other `pages/*.py` files, to avoid constructing a Slack Bolt `App()` inside the Streamlit process. Piece #7 (Slack-callable report) still open.
- **`db_cache.log_data_mutation`/`get_data_mutations` (data-mutation-log), 2026-07-22**: implements the traceability design agreed 2026-07-22 (see `docs/research_log.md`) — a new `data_mutation_log` table in `trading_universe.db`, one row per `data_manager.py` split-guard rescale, with a full pre-rescale CSV snapshot so the old data is recoverable, not just the fact that it changed. Wired into `data_manager.py`'s existing rescale branch.
- **`scripts/live_sanity_check.py`, 2026-07-22**: standalone real-order sanity tests (oversized BUY expecting a Schwab rejection, naked SELL expecting a Schwab rejection/no accidental short) — bypasses `active_signals.py`/`schwab_safety` entirely, resolves the account by real suffix (not nickname, since a not-yet-`ACCOUNTS`-wired account can still be tested this way), requires per-ticker typed confirmation, never loops/retries. Built for the user's planned WFH-day live-account test (see `docs/backlog_cache.md`), not yet run.
- **"v6" idle-capital-parking research, 2026-07-22 — tested, negative result**: `scripts/sim_v6_spy_parking.py` (single-ticker), `scripts/sim_v6_parking_vehicle_sweep.py` (extracts real exit→next-entry gap windows across all 10 watchlist-65 tickers — `--extract-only` caches them to `output/v6_gap_windows.csv`; `eod_only=True` reproduces the original restricted "only if the exit lands on the day's last bar" definition, 62 windows; the default "every gap" definition gives 776), and `scripts/sim_v6_split_check.py` (chronological out-of-sample split, `--split 50-50`/`70-30`/`5fold`, excludes the 10 source tickers from being scored against their own windows). See `docs/research_log.md`'s 2026-07-22 entry for the full result — no vehicle showed a robust, fold-consistent edge on the restricted 62-window sample; the broader 776-window sample showed SPY-long as sign-consistent across both halves (unlike the earlier 62-window version) but still ~50% win rate, and a follow-up idea (check specifically during real SPY drawdown episodes, already identified: 2023-09-21/2023-10-11, 2023-10-18/2023-11-06, 2024-08-02/2024-08-13, 2025-03-06/2025-05-12 (-19%), 2026-03-19/2026-04-08, among others) was raised but not yet run — see `docs/backlog_cache.md`.
- **`signals_db.scenario_expectations`/`coverage_deviations` + `scripts/coverage_check.py`/`scripts/seed_scenario_expectations.py`, 2026-07-24**: the structured "compass" layer on top of `coverage_events` above — every daily scenario gets a real `scenario_expectations` row (`expected_outcome`, `check_method`, `check_params`) instead of prose in `deep_backlog.md`/`live_test_coverage.md`, and `coverage_check.py` checks each one against real `trade_log`/`pending_buys` data for the day (not `coverage_events`, which only logs control-site firings, not entry/arm/exit for live/dry_run nodes), writing a `coverage_deviations` row — `reason` starting `NULL` — whenever the expectation isn't met. `explain_deviation()` is the only way to clear a row from `get_deviations(unexplained_only=True)`, enforcing the "no unexplained failure" contract: a deviation with no reason is itself the actionable finding. `seed_scenario_expectations.py` seeds the six canary A–F designed scenarios (`docs/deep_backlog.md`'s 2026-07-23 canary entry, now structured data instead of prose). **Both `add_scenario_expectation` and `record_deviation` normalize `ticker=None` to `''` before insert** — their `UNIQUE`/`ON CONFLICT` constraints include `ticker`, and SQLite never treats `NULL == NULL` as a conflict match, so a ticker-less (control-site, `check_method='coverage_event'`) scenario would otherwise silently duplicate on every rerun instead of upserting — same bug class as `add_node`'s `take_profit=NULL` dedup failure above, caught in review before it shipped rather than in production. Usage: `.venv/bin/python scripts/coverage_check.py [--date YYYY-MM-DD]` / `--explain ID "reason text"`. This is pieces #3 and #6 of the coverage-system reframe below (structured expected-vs-actual + the no-unexplained-failure contract) — pieces #1 (Streamlit dashboard), #2 (3rd strategy-type axis on `coverage_events`), #4 (per-node drill-down), #7 (Slack-callable report) are still open, see `docs/backlog_cache.md`.

## Runtime Artifacts (not committed)
- `cache/live/` — the real trade record: `trading_live.db` plus pre-migration `.bak` snapshots, `trading_sim.db` (live-sim testing), `active_signals_heartbeat.txt`
- `cache/research/` — regenerable research data: hourly CSV per ticker, `trading_universe.db` (+ daily/weekly `.bak` rotations), `watchlist_sweep.db`, `dismissed_tickers.json`
- `logs/` — optimization output PNGs, CSVs, text reports
- `output/` — script outputs/exports/reports (not cache): `*_trades.xlsx`, `live_backups/` (hourly `trading_live.db` snapshots), archived/legacy files
- `active_phase_grid.json` — live progress state written during sweep runs
- `current_test.json` — temp telemetry file, deleted on sweep exit

## How to Run
```bash
# Install dependencies
pip install -r requirements.txt

# Launch Streamlit UI
streamlit run app.py

# Or run optimization sweep directly
python run_optimization_sweep.py
```

## Architecture Notes
- Optimization uses `ProcessPoolExecutor` with up to 10 workers
- Results cached in SQLite to avoid re-running completed nodes
- Multi-generation refinement: macro grid scan → frontier detection → fine mesh around top performers
- `active_phase_grid.json` is written by the sweep and read by the Streamlit UI for live progress display
